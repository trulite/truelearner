[package]
name = "truelearner-workstation"
version = "0.1.0"
edition = "2021"
publish = false
description = "Development visual-touch workstation Harness for TrueLearner"

[features]
research = []

[dependencies]
bincode = "1"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
truelearner-core = { path = "../core" }
truelearner-embodiment = { path = "../embodiment" }

[dev-dependencies]
serde_json = "1"
