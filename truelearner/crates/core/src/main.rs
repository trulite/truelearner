//! Production composition root for TrueLearner.
//!
//! Physical inputs and outputs are deliberately not synthesized here. A host
//! boundary will be selected during the runtime redesign; the accepted
//! substrate remains available through the sibling library in this crate.

fn main() {}
