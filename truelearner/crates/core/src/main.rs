//! Production composition root for TrueLearner.
//!
//! Host inputs and outputs are deliberately not synthesized here. The accepted
//! bounded `BoundaryRuntime` lives in the sibling library; a future host adapter
//! may enqueue physical inputs and drain outward crossings without changing it.

fn main() {}
