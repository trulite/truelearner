[package]
name = "truelearner-arena-format"
version = "0.1.0"
edition = "2021"
publish = false
description = "Canonical durable arena representation for TrueLearner"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
