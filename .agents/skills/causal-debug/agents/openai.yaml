interface:
  display_name: "Causal Debug"
  short_description: "Debug physical arrows from retained traces"
  default_prompt: "Use $causal-debug to run the offline choice-law verifier, draw paired physical-event and commuting diagrams from the retained trace, locate the first failed arrow, and continue through the next minimal solve without broad diagnostic reruns."
