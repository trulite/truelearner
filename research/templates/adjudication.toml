schema = "research-adjudication/v1"
protocol_path = "<protocol path>"
protocol_sha256 = "<protocol digest>"
evidence_path = "<evidence path>"
evidence_sha256 = "<evidence digest>"
verdict = "inconclusive"
predicate_results = ["<predicate=result>"]
control_results = ["<control=result>"]
scientific_findings = ["<finding>"]
residual_uncertainty = ["<uncertainty>"]
sufficiency = "not-established"
integration = "not-attempted"
adoption = "not-adopted"
authority = "not-promoted"
authorized_by = "<explicit authority or none>"
