schema = "research-campaign/v1"
id = "<campaign-id>"
program = "<program-id>"
kind = "solve"
mode = "discovery"
hypothesis = "<large hypothesis>"
first_divergence = "<earliest physical divergence>"
missing_transition = "<missing physical transition>"
prediction = "<prediction stated before execution>"
falsifiers = ["<killing observation>"]
positive_reference = "<exact frozen reference>"
negative_controls = ["<exact frozen negative control>"]
arm_paths = ["<path to arm manifest>"]
max_parallel_arms = 4
max_rounds = 3
interaction_expected = false

[budget]
total_sandbox_minutes = 240
max_minutes_per_arm = 60

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
