schema = "research-convergence/v1"
campaign = "<campaign-id>"
round = 1
survived = ["<arm-id>"]
falsified = ["<arm-id>"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = ["<mechanism>"]
strongest_counterexamples = ["<counterexample with arm reference>"]
retired_families = ["<retired mechanism family>"]
next_discriminators = ["<experiment that separates surviving explanations>"]

[[new_arm]]
id = "<new-arm-id>"
kind = "hybrid"
parents = ["<parent-arm-id>", "<parent-arm-id>"]
imports = ["<parent:mechanism>"]
interaction_prediction = "<prediction unique to the composition>"
falsifiers = ["<new killing observation>"]
