# Transition-local workstation opportunity forecast

Parent: `d0e284880201a77ecf4f338e818a7993dd078bd0`. Generic Workstation
Opportunity V1 remains the ordinary exploration cause. The immutable contact
recording localized the next wall after actual depth change returns but before
same-axis continued movement.

```text
generic exploration cause G ------> bounded ordinary movement

actual returned transition T_c ---> one local opportunity in component c

G + T_c --------------------------> existing learner choice and product
```

`G + T_c` means two genuinely different physical causes are kept separate.
It does not mean two learners, a desired direction, or a named body part.

## Complete candidate

| Dependency | Role | Evidence | Status | Killing falsifier |
|---|---|---|---|---|
| Connected outcome-topology product | Keep opposing alternatives ordinary inside one component and compose distinct components | `hand-causal-topology-product-authority-v1` | established | Parent one/two/five identity changes. |
| Shared generic opportunity | Expose one bounded unguided movement from the complete workstation morphology | `workstation-shared-opportunity-incidence-authority-v1` | established | Parent digit separation changes. |
| Actual transition incidence | Distinguish a real state change from resampling | LP-049 | established | An unchanged sample opens the local opportunity. |
| Current-transition continuation | Prefer a return-bearing physical arrow over unrelated ordinary alternatives | LP-067 | constrained | A returned depth transition gets a local opportunity but does not continue its physical component. |
| Component-local opportunity | Keep the transition opportunity local while allowing genuinely separate causes to compose | LP-068 and LP-069 | established in serial articulation; predicted at workstation boundary | The local cause suppresses unrelated causes or reintroduces broad coactivation. |
| Repeated geometric approach | Chain actual local depth transitions until surface entry or a real boundary consequence changes the arrow | current contact wall | predicted | Depth does not accumulate, oscillates without approach, or requires a desired direction. |

The candidate adds no durable state: the boundary already retains exactly the
set of physical axes that changed until their transition is returned on the
next step. It emits one additional research-only local opportunity from that
same returned event, then discards it normally.

Excluded proposals remain contact reward, desired depth, named finger, fixed
direction, evaluator schedule, moved surface, changed initial pose, random
action, visual target interpretation, and longer waiting without a changed
physical arrow.

## Prediction

The complete candidate will turn at least one returned depth change into
same-component movement on its next opportunity. If the existing continuation
law preserves the active arrow, contact-relevant depth will accumulate and a
real keyboard or touchpad entry will be followed by local pressure within 120
steps. The parent will reproduce the exact surface-entry wall.

## Upward ladder

1. **Parent identity.** Generic-only mode reproduces the immutable 120-step
   contact wall and adopted digit separation.
2. **Transition truth.** Only an actual changed axis emits a local opportunity;
   stable resampling emits none.
3. **Local next arrow.** A returned palm-depth or finger-depth transition is
   followed by movement in that same physical component.
4. **Bounded continuation.** Repeated local movement remains quiescent,
   replayable, and component-local without five-finger coactivation.
5. **Geometric approach.** The minimum real surface gap falls below the parent
   value 312 and continues rather than resetting.
6. **Surface entry.** A palm or fingertip enters a real key or touchpad surface.
7. **Local contact.** The following sample contains positive pressure at the
   same site.

Stop at the first failed rung. Device pressing, clicking, holding, pointing,
and typing remain later ladders.

## Failure descent

If contact still fails, inspect: transition return, local opportunity emission,
candidate executability, same-component selection, chosen direction, repeated
continuation, x/y overlap, surface entry, and next-sample pressure. If the
local arrow exists but direction alternates, the next discriminator is
consequence-bearing persistence. If it never becomes executable, local
opportunity admission is the first break. If depth accumulates but x/y misses,
eye-hand geometry becomes the next frontier.

## Conditional downward ablation

Only after the complete candidate reaches contact: restore the frozen positive
reference, remove the transition-local opportunity, and rerun the entire
ladder. Then replace actual-transition gating with unchanged-sample gating as a
killing negative control. No positive discovery becomes authority without a
separate clean integration and frozen one-shot protocol.

## Post-convergence frontier

The first surviving physical arrow decides the next rung: local admission,
direction persistence, eye-hand placement, or repeatable contact-directed
reuse. No device skill advances directly from a first touch.
