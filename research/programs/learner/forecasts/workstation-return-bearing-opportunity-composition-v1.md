# Workstation return-bearing opportunity composition forecast

Parent: `e35b834`. The first contact-relevant direction break is localized:
the exact prior-direction candidate exists, but it has no participating path
input, receives no supplied opportunity, is non-executable, and is not a
current physical transition. The opposite direction wins ordinarily.

```text
actual movement return T -----> used physical path P
bounded opportunity G --------> output competition

current adapter:  finish(T) ; run(G)       P is no longer live in G
candidate:        compose(T, G)            P carries T into the choice
```

The candidate changes only the research workstation adapter's causal timing.
It puts the actual returned transition and the two anonymous local alternatives
inside one quiescent core run. It does not store the prior direction, name a
finger, or know which way approaches a surface. The path that produced the
movement is the only permitted witness of the arrow.

## Upward ladder

1. The previously localized prior-direction candidate has a live path input.
2. It becomes executable and is recognized as the current transition.
3. Exactly one direction is admitted; opposing effects do not cancel.
4. Contact-relevant returned movements continue their prior sign more often
   than the falsified component-local parent.
5. Real surface gap improves, then surface entry and next-sample pressure may
   be tested in the same bounded 120-step trace.

Stop at the first failed rung. Contact, clicking, holding, and device use are
not inferred from local continuation.

## Downward controls

- Generic-only production behavior remains exact.
- An unchanged sample opens no composed opportunity.
- A returned component cannot continue an unrelated component.
- Ambiguous or missing return-bearing paths fall back to ordinary choice.
- Replay, natural quiescence, semantic firewall, propagation, and bounded work
  remain mandatory.

## Category-theory lens

The return is an arrow from an output effect back to the organism. Opportunity
is an arrow that makes a later output possible. The current adapter evaluates
their codomains in separate computations, losing the factor that identifies
the used path. The solve asks whether their ordinary composition commutes when
both are present in one causal run. No new object or memory is introduced.
