# Workstation digit-separation forecast

Parent: `00ef061f34b6e8a5e14bdce25a14a39f85f787f3` with the authoritative
`RecursiveLearnerCausalTopologyProductComposition` unchanged. The frozen
development recording is
`output/workstation-run-82001/recording.tlwr@07867e0071f89247951008ef053b2d1727e4bc6470200dba9c8103aa35c54abd`.
The recording is local development observation, not authority evidence.

```text
one generic opportunity wave
             |
             v
 choose one physical component -----> one isolated movement
             |
       actual transition
             |
             v
 local path can later reuse --------> learned independent control

distinct causal waves ----------------> compose across components
```

## Observed wall

The complete real-morphology parent moved on all 48 recorded steps. Forty-six
steps changed all five finger-flexion components together, two changed none,
and zero changed exactly one. No contact or device event occurred. The first
broken physical transition is therefore not motor output or body integration:
it is bounded selection among components during generic opportunity. The world
currently presents each motor opportunity at a distinct phase and physical
origin, so the organism receives thirty independently caused opportunities per
step and the accepted topology product truthfully composes them all.

## Complete candidate

| Dependency | Role | Evidence | Status | Killing falsifier |
|---|---|---|---|---|
| Connected outcome-topology product | Preserve ordinary choice inside one physical component and independent execution across disconnected components | `hand-causal-topology-product-authority-v1` | established | One/two/five serial identity or either product ablation changes. |
| Full existing workstation trace | Preserve actual eye, proprioceptive, output, movement, outcome, work, and replay observations | `workstation-run-82001` and RM-000B | established | The proposed campaign needs a new diagnostic fact before stating its prediction. |
| Shared generic-opportunity incidence | Represent one externally created exploration occasion as one wave rather than thirty independent causes | Current harness phase/origin audit | predicted | Shared incidence alone yields isolated components under the unchanged law, or changes an independently caused control. |
| Wave-scoped component sparsity | Bound alternatives carried by one cause while retaining independent causes | LP-027 and LP-031 isolated fixtures | constrained | A shared wave still emits every component, or separate waves suppress one another. |
| Participation-bounded fresh allocation | Let later generic waves expose another physical component without semantic rotation | Existing bounded opportunity and participation ranking | predicted | One component monopolizes all fresh waves, or allocation depends on axis/digit identity. |

The synthesized candidate keeps route genesis, actual participation, physical
transition return, consequence, continuation, release, construction, and the
authoritative connected-topology product. It adds no body label, desired pose,
target, action, curriculum stage, score, or evaluator-selected movement.

Retired or incompatible proposals remain excluded: global output choice that
recreates serial suppression; output-only or opportunity-only topology
factorization; immediate-origin and causal-path-origin products that changed
one-module identity; spatial separation as semantic anatomy; round-robin digit
selection; random action IDs; and longer observation as a solve.

## Upward ladder

1. **Incidence identity.** One shared external opportunity is recorded as one
   phase/origin; distinct physical causes remain distinct.
2. **Tiny choice.** A shared wave across five disconnected neutral components
   admits one component, while two distinct waves admit both.
3. **One-axis identity.** Opposite alternatives inside one connected outcome
   component retain the exact authoritative arrow.
4. **Serial preservation.** The accepted two- and five-joint independently
   caused fixtures remain exact, including shared reversal opportunity.
5. **Real morphology.** A fresh 48-step workstation run contains isolated
   finger changes from at least two anonymous components, no broad five-finger
   coactivation from generic opportunity, exact replay, natural quiescence, and
   bounded work.
6. **Conditional contact.** Only after rung 5 passes, continue the unchanged
   world long enough to ask whether isolated movement can reach physical
   contact. Contact is not part of the digit-separation solve predicate.

Stop at the first failed rung. No downstream contact, click, or typing claim is
admissible after an earlier failure.

## Failure descent

For a failed candidate, inspect in order: shared incidence formation; candidate
path proposal; executable motor candidate; outer wave choice; inner
topology-local choice; outward motor firing; actual isolated body change;
transition return; later reuse. The existing recording already establishes
proposal-to-body movement for the broad parent, so the campaign adds no generic
diagnostic event.

## Competing arms

- **Shared incidence only:** change phase/origin composition while retaining the
  exact accepted law. Prediction: the current topology product still composes
  all disconnected components, proving truthful incidence is necessary but
  insufficient.
- **Wave sparsity fixture:** apply an outer, origin-wave-scoped bounded choice to
  a neutral shared-wave fixture. Prediction: one shared wave admits one
  component and distinct waves commute. This cannot establish morphology
  sufficiency alone.
- **Composed candidate:** shared generic incidence plus wave-scoped sparse
  component allocation outside the unchanged topology-local product.
  Prediction: real fingers move in isolated steps across at least two physical
  components while serial controls remain exact.

The strongest inherited counterexample is the topology-product ablation: global
opportunity allocation made the distal serial component lose its continued
arrow at step five. Therefore wave sparsity may apply only to candidates sharing
one actual generic opportunity cause; it must not merge independently caused
component returns.

## Conditional downward ablation

Only if the composed candidate passes every rung: freeze it, remove shared
incidence while retaining sparse choice, rerun the full ladder; restore it,
remove sparse choice while retaining shared incidence, rerun; then remove
participation-sensitive fresh allocation. No ablation may run against a failed
candidate, and no survivor becomes authority without clean integration and a
separate frozen protocol.

## Post-convergence frontier

If the composed candidate survives, the next frontier is the earliest trace
break between isolated digit motion and physical surface contact. If it fails,
retain the exact counterexample and choose between incidence, sparse selection,
or fresh-allocation failure; do not introduce contact or device semantics.
