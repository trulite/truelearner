schema = "research-protocol/v1"
id = "runtime-attached-return-composition-v1"
program = "learner-physics"
claim = "RUNTIME-ATTACHED-MULTISCALE-COMPETENCE"
status = "preregistered"
parent_authority = "runtime-attached-local-regulation-v1 convergence; Connected Outcome Product V1 commit:7be8bbc3009fe3131622a6ee21e9aa260d649aa1; development revision dfe933886d4a030d7775356f78e908e8531c2fc2"
question = "Which smallest anonymous physical composition makes an action-caused sensor return reach the path that acted: placing outcome sources at the attachment boundary or coequalizing rise and fall through one component-local threshold-change surface?"
observables = ["exact frozen parent first two choices", "first disturbance path source", "first motor physical origin", "returned incidence origin", "return origin admission", "consequence write and tick", "second-step motor candidate path inputs origins and consequence", "second output choice", "fixed cross-boundary links", "natural quiescence", "checkpoint replay", "physical work and trace"]
frozen_variables = ["positive disturbance three from initialized zero", "integer thresholds minus four through plus four", "trace lifetime eight", "attachment site position ten region zero", "motors at positions nine and eleven", "motor threshold two", "outward sinks", "accepted connected-outcome product protocol", "one opportunity input per motor", "batched world effect", "first two external steps", "same capacities and full physical tracing"]
permitted_changes = ["arm one places anonymous body outcome sources at the physical attachment boundary", "arm two adds one component-local shared change junction per threshold while retaining distant outcomes", "observer-only trace projection"]
positive_predicates = ["the frozen parent repeats motor 20000 then motor 20001 with no consequence tick", "each solve preserves the first real output and its exact returned origin", "at least one solve records consequence on the exact participating path before the second choice", "a surviving solve keeps or selects the first corrective motor at the second choice", "no solve adds a fixed component link to a resident motor or outcome", "all arms replay exactly and quiesce naturally"]
negative_controls = ["unchanged split-trace distant-outcome parent", "silent attachment", "equal resampling", "shuffled return origin", "no fixed motor link", "semantic firewall"]
stop_conditions = ["the parent trajectory changes before either solve", "neither solve changes consequence write or second choice", "a solve works only through fixed cross-boundary wiring or named motor identity", "the returned origin changes", "replay quiescence or inherited controls fail"]

[run_policy]
max_valid_runs = 1
fresh_environment = false
