schema = "research-convergence/v1"
campaign = "hand-consequence-born-return-admission-v1"
round = 1
survived = ["consequence-born-return-admission"]
falsified = ["truthful-no-consequence-release", "conditional-reflected-hand-release", "complete-composition"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "strict birth-after-opening admission produced typed rejection for pre-opening origins, prevented their consequence write, preserved genuinely later local returns, exact replay, and natural quiescence",
  "the unchanged hand rejected nine pre-opening returns and reduced no-post-movement consequence writes from twenty-four to eighteen without changing its one-motor upper-clamped trajectory",
  "from the next clamped step onward, the same physical surface origins were newly sampled after the previous motor return opened, so they were truthfully later under the timing predicate and continued to renew the incumbent",
  "the candidate retained zero propagation exhaustion and boundary-effect safety but produced no upper escape, second motor, lower travel, primary closure, or perturbation recovery",
]
strongest_counterexamples = [
  "consequence-born-return-admission: temporal truth is necessary but insufficient because a later sample can report unchanged physical state",
  "truthful-no-consequence-release: rejecting some false returns does not create release while later resampling still answers every incumbent return",
  "conditional-reflected-hand-release: local mechanism survival does not imply benchmark improvement; actual position changes remained four and final position remained four",
  "complete-composition: safe non-regression is not one-joint closure or perturbation recovery",
]
retired_families = [
  "birth-after-opening timing alone as sufficient to distinguish physical change from repeated sampling",
  "assuming existing unanswered-return replacement becomes active merely because some return origins are rejected",
  "widening temporal rejection to later origins without a physical state-change discriminator",
  "claiming hand release from reduced consequence writes or retained safety",
]
next_discriminators = [
  "construct matched traces where the same later physical origin follows an actual state transition versus unchanged resampling and identify what live participating structure differs",
  "compare stable physical boundary-member change, causal-lineage membership change, and actual effect-contingent incidence without using position, direction, contact, or evaluator movement",
  "only if one local observable separates the cases, test it as a consequence-renewal gate while preserving repeated valid continuation, delayed return, no-consequence release, reflection, replay, and quiescence",
  "retain strict consequence-born admission as a constrained component, not as adopted or sufficient hand physics",
]

[[new_arm]]
id = "physical-change-versus-resampling-localization"
kind = "diagnostic"
parents = ["consequence-born-return-admission", "truthful-no-consequence-release"]
imports = ["later valid return", "later unchanged resampling", "live participating topology", "causal lineage"]
interaction_prediction = "A benchmark-blind local observable distinguishes a later sample caused by changed physical incidence from a later sample of unchanged context before consequence write."
falsifiers = [
  "The traces are physically identical at every learner-visible boundary.",
  "The distinction requires hidden position, movement, direction, contact, or evaluator state.",
  "The diagnostic changes return admission or hand behavior.",
]

[[new_arm]]
id = "stable-member-consequence-renewal"
kind = "solve"
parents = ["consequence-born-return-admission", "truthful-no-consequence-release"]
imports = ["physical boundary-member novelty", "actual participating origin set", "bounded recent consequence"]
interaction_prediction = "If stable participating membership changes only on actual physical transition, consequence renews on that change but not on unchanged later resampling, allowing eventual release without blocking travel."
falsifiers = [
  "Ordinary repeated travel loses necessary continuation.",
  "Unchanged resampling still renews consequence.",
  "The rule adds semantic state or persistent benchmark history rather than local physical membership.",
]
