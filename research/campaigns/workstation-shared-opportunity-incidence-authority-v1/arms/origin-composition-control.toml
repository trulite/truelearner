schema = "research-arm/v1"
id = "origin-composition-control"
campaign = "workstation-shared-opportunity-incidence-authority-v1"
kind = "control"
parents = ["workstation-shared-opportunity-incidence-authority-v1:full-workstation-authority"]
imports = ["shared-origin bounded-choice fixture", "distinct-origin topology product fixture"]
mechanism = "Run neutral core fixtures with one shared opportunity origin and two distinct origins."
prediction = "One shared origin emits one output while two distinct origins emit both outputs under the unchanged accepted protocol."
falsifiers = ["One shared origin emits both components.", "Two distinct origins suppress either component."]
source_revision = "commit:d18f8700b9e09b6d9fb65dfdace640868bcb149d"
protocol_sha256 = "130ab0ea3605c7e65c389fadae1b21cd77386df8f50c29042eec521cdc84bbca"
budget_minutes = 5

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-core opportunity_origin"
full_evidence = "cargo test --workspace --locked --manifest-path truelearner/Cargo.toml"
