schema = "research-protocol/v1"
id = "hand-prefix-and-activity-work-localization-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "hand-bounded-first-use-construction-continuation-v1 convergence sha256:a2bf55a62aa9e6d06165bc118d6f1983b6d70c4c74e4b7c62a2c054fee489989"
question = "Did bounded first-use continuation directly add the failed comparison work, or did its repaired physical arrow lead the hand into a different and more expensive downstream trajectory?"
observables = ["parent and candidate per-step comparisons and scans", "per-step positions learners junctions and live links", "all output-choice resolutions and exact construction-consumption events", "shared prefix first repaired choice and divergent downstream region totals", "first output input comparison and scan divergence steps", "per-live-link normalized work", "immutable predecessor hashes replay quiescence and exhaustion"]
frozen_variables = ["RecursiveLearnerConstructionOutcomeComposition parent physics", "RecursiveLearnerBoundedConstructionContinuation candidate physics", "official batched reflected-hand adapter", "sixteen external steps", "all execution cost counters", "predecessor negative cost adjudication", "immutable predecessor evidence"]
permitted_changes = ["project already observed end-of-step junction and live-link counts into public hand evidence", "one pure parent-candidate work-localization fold", "one one-shot diagnostic successor artifact"]
positive_predicates = ["all immutable predecessor digests and both live execution summaries reproduce exactly", "per-step comparison and scan sums equal each live run total", "the shared physical prefix before step three has identical work position outputs learner counts junctions and live links", "step three retains the exact tick-twenty-three repaired target-eleven choice and consumption without comparison or scan excess", "the first comparison divergence occurs only once the next external step begins from a different physical input state", "the entire frozen comparison excess remains assigned to the divergent downstream region", "exact replay natural quiescence zero exhaustion and accepted defaults remain unchanged"]
negative_controls = ["no learner state choice ranking execution counter or force law changes", "no position hand direction answer or evaluator fact enters learner execution", "the predecessor plus-two-hundred-fifty-six cost failure is not loosened reclassified or rerun", "post-choice steps are labeled divergent trajectory rather than equivalent paired computations", "zero live-link normalization is total and cannot divide by zero"]
stop_conditions = ["an immutable predecessor digest changes", "either live run differs from its frozen totals or trajectory", "retention changes replay quiescence exhaustion or work", "work diverges before or at the exact first held-state use", "per-step work fails to sum to published totals", "the first absolute cost divergence cannot be assigned to a retained step and observed topology"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
