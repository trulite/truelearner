schema = "research-arm/v1"
id = "remove-persistent-drive"
campaign = "body-curried-component-calibration-ablation-v1"
kind = "diagnostic"
parents = ["complete-calibration-reference"]
imports = ["initial nonzero drive", "real fall return", "late sample drive removal"]
mechanism = "Permit the first nonzero sample drive, then remove every later phase-seven persistent sample while retaining threshold memory and real fall returns."
prediction = "A positive disturbance moves from three to two, closes its useful return, and then remains at two with residual one because no containing-body drive remains."
falsifiers = ["The one-shot drive reaches and holds the normal band.", "Initial drive or real fall return is also removed."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "ead3468816585ed290459bc8824f86fbc91b6e0dbfa66c02db86f691ab18552f"
budget_minutes = 4

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-embodiment --test runtime_attachment calibration_ablation_persistent_drive -- --ignored --exact"
full_evidence = "test -s research/campaigns/body-curried-component-calibration-ablation-v1/artifacts/remove-persistent-drive.log"
