schema = "research-arm/v1"
id = "remove-body-context"
campaign = "body-curried-component-calibration-ablation-v1"
kind = "diagnostic"
parents = ["complete-calibration-reference"]
imports = ["fixed centered calibration context", "shifted external body norm"]
mechanism = "Keep the external body norm shifted to the disjoint band two through three while currying the old centered minus-one-through-one context into the unchanged Normalizer."
prediction = "The organism settles in or acts relative to the old context and fails four consecutive observations inside the shifted body norm."
falsifiers = ["The fixed-context removal still holds the shifted norm.", "Any factor besides curried context changes."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "ead3468816585ed290459bc8824f86fbc91b6e0dbfa66c02db86f691ab18552f"
budget_minutes = 4

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-embodiment --test runtime_attachment calibration_ablation_fixed_context -- --ignored --exact"
full_evidence = "test -s research/campaigns/body-curried-component-calibration-ablation-v1/artifacts/remove-body-context.log"
