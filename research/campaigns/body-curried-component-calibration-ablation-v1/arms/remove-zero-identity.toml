schema = "research-arm/v1"
id = "remove-zero-identity"
campaign = "body-curried-component-calibration-ablation-v1"
kind = "diagnostic"
parents = ["complete-calibration-reference"]
imports = ["existing calibration drive port", "ordinary sample drive", "initially normal body"]
mechanism = "Add an ordinary late sample to the existing calibration drive port when residual is zero, while retaining every other complete factor."
prediction = "A body beginning at zero emits an output or leaves the centered normal band instead of remaining identity."
falsifiers = ["The driven zero remains output-free and holds normal for all observations.", "The arm changes residual thresholds or supplies a preferred output."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "ead3468816585ed290459bc8824f86fbc91b6e0dbfa66c02db86f691ab18552f"
budget_minutes = 4

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-embodiment --test runtime_attachment calibration_ablation_zero_identity -- --ignored --exact"
full_evidence = "test -s research/campaigns/body-curried-component-calibration-ablation-v1/artifacts/remove-zero-identity.log"
