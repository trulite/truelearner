schema = "research-convergence/v1"
campaign = "body-curried-component-calibration-ablation-v1"
round = 1
survived = ["complete-calibration-reference", "remove-persistent-drive", "remove-directional-change", "remove-zero-identity"]
falsified = ["remove-body-context"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "every arm retained the unchanged production calibration trace exact closure parent and connected outcome product",
  "all removals were test-only transformations of existing physical input and supplied no motor direction target or evaluator reward",
  "persistent drive directional change and zero identity removals each reached their distinct predicted first wall with exact replay and natural quiescence",
  "the body-context removal ultimately settled at the fixed context rather than the external norm but falsified its broader any-four-observations oracle during transit",
]
strongest_counterexamples = [
  "without persistent drive the positive trajectory moved 3 to 2 once and then remained at unresolved residual 1",
  "merging persistent drive with Transition falsely closed the worsening -3 to -4 residual increase",
  "driving residual zero made an initially normal body act while the complete reference remained output-free",
  "fixed context produced [3, 3, 2, 2, 1, 1, ...]; the transient prefix satisfied the frozen any-window hold check even though terminal state 1 lay outside external [2, 3]",
]
retired_families = [
  "claiming body-context necessity from an any-window residence predicate that cannot distinguish transit from final holding",
  "removing production code or introducing feature flags for downward ablation",
  "treating replay or natural quiescence alone as proof that a removal preserved regulation",
]
next_discriminators = [
  "freeze a successor that requires the final four observations to remain inside the disjoint external body norm",
  "require the same fixed-context trajectory to finish outside the external norm and inside its curried fixed norm",
  "preserve the complete candidate terminal residence replay quiescence and semantic-firewall controls",
]

[[new_arm]]
id = "terminal-body-context-residence"
kind = "diagnostic"
parents = ["complete-calibration-reference", "remove-body-context"]
imports = ["disjoint shifted body norm", "fixed centered calibration context", "terminal four-observation projection"]
interaction_prediction = "The complete candidate's final four observations remain in shifted [2, 3], while fixed centered calibration finishes all four at 1, outside the external norm and inside [-1, 1]."
falsifiers = ["The complete terminal window leaves [2, 3].", "The fixed-context terminal window remains in [2, 3].", "The fixed-context run does not finish in [-1, 1].", "Replay quiescence or production equality changes."]
