schema = "research-campaign/v1"
id = "body-curried-component-calibration-ablation-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "Body context, persistent unresolved drive, directional change factorization, and zero identity are separately necessary for the supported bounded calibrated-regulation result."
first_divergence = "The complete candidate regulates under shifted context, repeated unresolved residual, worsening returns, and zero residual; each removal should break at a different one of those exact arrows."
missing_transition = "Necessity has not been established because the complete survivor has not yet been removed one factor at a time against its unchanged positive reference."
prediction = "Fixed context fails the shifted norm, one-shot drive stalls after its first useful closure, merged change falsely closes a worsening residual, and drive at zero acts from an already-normal state."
falsifiers = ["The complete positive reference changes.", "Any removal preserves every complete regulation predicate.", "An arm fails at a different first arrow than preregistered.", "A removal requires a production branch motor direction correct action or evaluator reward.", "Replay natural quiescence or the semantic firewall fails."]
positive_reference = "body-curried-component-calibration-v1 complete supported candidate and verification receipt"
negative_controls = ["fixed context under shifted body norm", "persistent drive removed after first use", "persistent drive relabeled as transition", "drive added at zero", "frozen uncalibrated exact-closure stall"]
included_mechanisms = ["body-curried residual", "attached calibration trace", "exact natural cycle closure", "existing learner construction", "connected outcome product"]
inherited_constraints = ["no production calibration change", "no motor map", "no direction", "no evaluator reward", "exact replay", "natural quiescence", "semantic firewall"]
excluded_proposals = ["new sensor API", "new core learner law", "dynamic norm learning", "real morphology claim", "authority promotion"]
arm_paths = ["arms/complete-calibration-reference.toml", "arms/remove-body-context.toml", "arms/remove-persistent-drive.toml", "arms/remove-directional-change.toml", "arms/remove-zero-identity.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 20
max_minutes_per_arm = 4

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
