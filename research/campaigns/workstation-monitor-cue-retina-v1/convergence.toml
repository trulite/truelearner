schema = "research-convergence/v1"
campaign = "workstation-monitor-cue-retina-v1"
round = 1
survived = ["retained-parent"]
falsified = ["workstation-retina"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = ["both projections use the same retained post-typing body state and unchanged monitor cue renderer", "the research-only field preserves one foveal point and samples ordinary upper-peripheral monitor values", "neither arm changes learner law world semantics or evaluator boundary"]
strongest_counterexamples = ["at retained gazes left 863 911 and right 1023 959 both cue images differ", "the workstation field samples non-background monitor values including 203 187 186 182 and 188 in the left eye and 140 169 16 and 18 in the right eye", "despite reaching the monitor both glyphs map to the exact same twenty-four-value vector so coverage alone is falsified and the remaining wall is spatial aliasing", "the pure projection artifact is artifacts/workstation-retina-projection.json sha256 e0027fa1914cbd01e7beeca745755b10ce475b4668f6b3eda75b9aa2cc69157a"]
retired_families = ["adding isolated retinal points until one happens to hit a distinguishing glyph stroke", "calling broader field of view sufficient for glyph distinction", "moving the cue to a known receptor or injecting a gaze route"]
next_discriminators = ["preserve a local spatial patch at each retinal location rather than one scalar point", "implement the patch as a reusable sensor-field transformation independent of glyph key and workstation meaning", "use the retained state and pure projection to prove glyph distinction before another organism run"]

[[new_arm]]
id = "local-spatial-field"
kind = "solve"
parents = ["retained-parent", "workstation-retina"]
imports = ["retained cue-retina counterexample", "confirmed monitor coverage", "generic sensor DriverBank composition", "ordinary local retinal field"]
interaction_prediction = "A small local spatial patch preserves distinctions that point sampling quotients away, so the two retained glyph images first differ in sensor values without moving the cue or exposing its identity."
falsifiers = ["The local patch still aliases the retained glyph pair.", "The solve depends on glyph-specific offsets or evaluator-selected features.", "Patch composition duplicates world input loses locality or breaks identity on uniform fields."]
