schema = "research-protocol/v1"
id = "workstation-monitor-cue-retina-v1"
program = "learner-physics"
claim = "WORKSTATION-KEY-SELECTION-LADDER"
status = "preregistered"
parent_authority = "workstation-monitor-cue-reuse-v1 retained trace sha256:43bb8b86f1fc05d4c83b7450ab215915211efd6e52169e7b65eecac3b9011da8"
question = "Does adding a foveal point and ordinary upper-peripheral rows to the retinal field make the two retained post-typing monitor cues produce different retinal and learner states without changing world semantics?"
observables = ["paired cue image retinal learner candidate choice and movement divergences", "post-development eye gaze and exact sampled retinal values", "two proper printable development outcomes and release", "blank unlearned replay quiet work and firewall controls"]
frozen_variables = ["monitor cue rendering and position from workstation-monitor-cue-reuse-v1", "full ANSI keyboard hand world seed horizons protocol and opportunity law", "external cue identities and evaluator-side first-failure classifier", "one cause per admitted retinal firing and existing threshold factorization"]
permitted_changes = ["research-only retinal layout with one foveal point upper-peripheral rows intermediate rows and lower-near points", "neutral Academy session construction and restore that carry an explicit research visual composition", "read-only retinal projection for the new layout"]
positive_predicates = ["the complete candidate still develops and releases two proper printable keys", "paired cue images differ and their retinal values first differ at the same or next physical sample", "learner-only fingerprints differ after retinal divergence", "no presentation value crosses the harness boundary", "replay quiet work and inherited references remain intact"]
negative_controls = ["retained parent cue-retina failure", "default absent presentation identity", "blank cue", "unlearned glyph", "existing wide-retina and stable-fixation references", "Production default"]
stop_conditions = ["the new layout changes development before two proper key outcomes can complete", "paired cue retinal values remain identical", "retina differs but learner does not", "the solve requires cue position key identity desired gaze or evaluator state inside the organism", "replay quiet work reference or firewall integrity fails"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
