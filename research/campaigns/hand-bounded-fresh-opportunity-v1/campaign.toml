schema = "research-campaign/v1"
id = "hand-bounded-fresh-opportunity-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "One live unanswered return is a bounded local opportunity that can expose a fresh already-formed alternative before signed cancellation without adding semantic exploration state."
first_divergence = "The owner-resolved silent motor has equal nonzero opposing path drive but zero opportunity while a local executable incumbent has one unanswered return."
missing_transition = "One exact live return must supply one transient owner-compatible local opportunity before cancellation and then be consumed by existing replacement."
prediction = "The tiny fixture exposes one fresh output, all lifetime/locality/ownership controls remain negative, and the unchanged hand executes its second motor and leaves the upper boundary."
falsifiers = ["Any declared control exposes an output.", "The donor return is reused.", "Only the fixture improves.", "Replay, quiescence, or cost fails."]
positive_reference = "hand-pre-executability-opportunity-v1 balanced-drive-without-opportunity"
negative_controls = ["physical-transition parent", "answered and recent returns", "no and nonlocal alternatives", "owner mismatch", "same-return repetition", "old protocol", "reflection", "replay and natural quiescence"]
included_mechanisms = ["physical-transition return", "origin factorization", "owner-local return memory", "local output competition", "unanswered-return replacement", "bounded fresh opportunity"]
inherited_constraints = ["benchmark-blind core", "one transient unit", "no new memory or path strength", "warm runtime below ten seconds"]
excluded_proposals = ["random exploration", "strength reset", "hand reversal", "global origin release", "persistent opportunity", "semantic anatomy", "default adoption"]
arm_paths = [
  "arms/constructed-bounded-fresh-opportunity.toml",
  "arms/lifetime-locality-ownership-controls.toml",
  "arms/conditional-reflected-hand-release.toml",
  "arms/complete-composition.toml",
]
max_parallel_arms = 4
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 40
max_minutes_per_arm = 10

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
