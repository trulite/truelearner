schema = "research-arm/v1"
id = "conditional-reflected-hand"
campaign = "hand-physical-boundary-member-novelty-v1"
kind = "hybrid"
parents = ["complete-boundary-composition"]
imports = ["complete boundary survivor", "unchanged reflected hand world and perturbation"]
mechanism = "After the complete primitive gate survives, run the unchanged sixteen-step reflected hand with the boundary-novelty protocol and conditionally run perturbation only after primary closure."
prediction = "If false boundary depth was the last missing prerequisite, the hand reaches and leaves both limits and recovers from the fixed perturbation; otherwise the trace localizes its next physical break."
falsifiers = ["The primary hand does not close both limits.", "A primary survivor fails perturbation.", "The hand runs before the gate, diverges on replay, or fails to quiesce."]
source_revision = "plan:c7de5c6a6f66e12ee6f7cf53bcd29f59f50fb124f3585f7f96a929ae7e5e0895"
protocol_sha256 = "de2fea80fcce50fb6a313d16762d2025c1ff359df9435ae6d8c14a9494cf4915"
budget_minutes = 12

[gates]
tiny_fixture = "cargo test --locked --manifest-path research/experiments/hand-physical-boundary-member-novelty/Cargo.toml hand_execution_is_strictly_conditional_on_the_complete_gate"
full_evidence = "cargo run --quiet --locked --manifest-path research/experiments/hand-physical-boundary-member-novelty/Cargo.toml -- --arm conditional-reflected-hand"
