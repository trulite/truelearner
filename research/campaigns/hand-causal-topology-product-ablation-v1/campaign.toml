schema = "research-campaign/v1"
id = "hand-causal-topology-product-ablation-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "Connected-topology output choice and connected-topology fresh-opportunity allocation are separately necessary and jointly sufficient for anonymous serial articulation."
first_divergence = "Without output factorization the proximal component is suppressed at two-joint step zero; without opportunity factorization the distal component loses the shared reversal arrow at step five."
missing_transition = "None; this round removes each half of the successful complete transition to test necessity."
prediction = "Both single removals preserve exact one-module identity, fail the two-joint rung at their preregistered breakpoint, and stop five, while the unchanged complete protocol closes two and five."
falsifiers = ["Either single removal still closes two joints.", "Either removal changes one-module identity.", "Either removal fails at a different stage or step.", "The complete reference drifts or fails any inherited control.", "A failed two-joint arm executes five."]
positive_reference = "hand-causal-topology-product-output-composition-v1 convergence sha256:49cd9772df41373336a2c5bb8f76211c46727bcf9a62c6ae351e0060386be93a"
negative_controls = ["remove component-local output choice", "remove component-local fresh opportunity", "immediate-origin product", "causal-path-origin product", "parent with both factors absent"]
included_mechanisms = ["complete connected-topology product", "output-only diagnostic protocol", "opportunity-only diagnostic protocol", "permanent candidate and competition trace"]
inherited_constraints = ["one shared harness", "anonymous physical input", "no anatomy direction target score evaluator state or stable learner identity", "two before five", "development evidence only"]
excluded_proposals = ["new capability geometry", "keyboard touchpad or monitor world", "global-default promotion", "repairs after authority freeze"]
arm_paths = ["arms/complete-reference.toml", "arms/remove-output-factorization.toml", "arms/remove-opportunity-factorization.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 10
max_minutes_per_arm = 3

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.3333333333333333

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
