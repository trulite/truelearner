schema = "research-protocol/v1"
id = "runtime-attached-homeostatic-surface-v1"
program = "learner-physics"
claim = "RUNTIME-ATTACHED-MULTISCALE-COMPETENCE"
status = "preregistered"
parent_authority = "runtime-attached-factored-regulation-v1 convergence; Connected Outcome Product V1@7be8bbc3009fe3131622a6ee21e9aa260d649aa1"
question = "Can one sensor-wide physical error surface and a receptor-defined quiet band compose existing action continuity into two-sided local regulation?"
observables = ["two disturbance trajectories", "unified change firing", "motor origins", "coherent-effect choices", "central entry and four-observation residence", "origin replay quiescence and work", "split and per-threshold parent controls"]
frozen_variables = ["raw scalar bounds minus four and plus four", "quiet band minus one through plus one", "physical thresholds minus two minus one plus two plus three", "one unified component-local change surface", "trace lifetime eight", "attachment and motor geometry", "accepted protocol", "disturbances plus three and minus three", "thirty-two steps", "batched effect and fixed opportunities"]
permitted_changes = ["unified component-local change factor", "receptor threshold selection defining physical quiet band", "observer-only evidence"]
positive_predicates = ["both branches enter and remain central for four observations", "both movement signs occur", "motor origin is preserved", "parent negative controls remain exact", "replay and quiescence hold", "no motor target direction or evaluator result enters the body"]
negative_controls = ["split trace", "per-threshold factor", "silent attachment", "equal resampling", "semantic firewall"]
stop_conditions = ["either branch fails residence", "action crosses the quiet band", "a parent changes", "origin replay or quiescence fails", "the adapter supplies a motor or desired direction"]

[run_policy]
max_valid_runs = 1
fresh_environment = false
