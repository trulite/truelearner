schema = "research-campaign/v1"
id = "runtime-attached-homeostatic-surface-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "A sensor-wide physical error surface supplies continuity across threshold scale, while its receptor morphology makes the locally acceptable band naturally quiet."
first_divergence = "Per-threshold factorization preserves one corrective return but loses coherence when the scalar crosses the next threshold."
missing_transition = "Corrective action does not remain on one sensor identity across scale and has no physical quiet boundary at local regulation."
prediction = "The unified surface regulates from both sides and stops inside the quiet band without a motor map or semantic goal input."
falsifiers = ["Either branch fails central residence.", "The action overshoots the quiet band.", "A parent control changes.", "Origin replay quiescence or semantic firewall fails."]
positive_reference = "runtime-attached-factored-regulation-v1 candidate log sha256:fa5416ae243ba8dc0db1c28652282b84d3a6901ca07b037ae4f5b39bfa501673"
negative_controls = ["per-threshold factor", "split trace", "silent attachment", "equal resampling"]
included_mechanisms = ["atomic attachment", "body-native trace", "unified error surface", "physical quiet band", "existing coherent effect"]
inherited_constraints = ["no new learner law", "no motor map", "no external history", "no semantic target or evaluator input", "replay quiescence and bounded work"]
excluded_proposals = ["outcome placement", "fixed cross-boundary link", "selected action", "higher ladder rungs"]
arm_paths = ["arms/per-threshold-parent.toml", "arms/unified-homeostatic-surface.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 15
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
