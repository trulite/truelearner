schema = "research-campaign/v1"
id = "hand-origin-feedback-cycle-localization-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "The origin-first hand fails because unowned outward motor effects participate in a closed learned drive cycle that re-enters the opposite motor, while carried-origin ownership can separately disagree with actual path ownership."
first_divergence = "At hand step eight, candidate activity does not settle and motor candidate observations expand from about five to 88."
missing_transition = "A provenance-preserving separation between outward effect execution and later inward path participation."
prediction = "A read-only provenance trace localizes owner disagreement and one repeated closed physical cycle crossing outward and inward before exhaustion; the boundary reference remains quiet and unchanged."
falsifiers = [
  "No graph-contiguous closed cycle repeats before exhaustion.",
  "The cycle does not cross from organism to outward region and back inward.",
  "Path and carried-origin ownership never disagree.",
  "Instrumentation changes frozen behavior, replay, quiescence, or cost bounds.",
]
positive_reference = "hand-causal-origin-ownership-factorization-v1 bounded reflected-hand failure"
negative_controls = ["matched boundary protocol", "external and unowned provenance", "exact replay", "trace-only behavior preservation"]
included_mechanisms = ["complete boundary candidate", "frozen causal-origin candidate", "bounded diagnostic send", "drive provenance observation", "generic repeated-cycle graph fold"]
inherited_constraints = ["private memory, actual participation, no semantic anatomy, exact replay, natural-quiescence truth, and warm cost"]
excluded_proposals = ["blocking outward re-entry", "changing path formation", "changing origin propagation", "semantic motor labels", "candidate repair", "default adoption"]
arm_paths = [
  "arms/origin-path-provenance-disagreement.toml",
  "arms/step-eight-feedback-cycle.toml",
  "arms/matched-boundary-control.toml",
  "arms/complete-localization.toml",
]
max_parallel_arms = 3
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 30
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
