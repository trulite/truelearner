schema = "research-campaign/v1"
id = "hand-construction-cycle-witness-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "The completed physical consequence remains on a live participating link across learner construction, but the fresh learner-owned candidate cannot read it because owner-local projection deliberately excludes organism-level link consequence."
first_divergence = "After the unique completed choice at tick eleven and before the FreshAlternative choice at tick twenty-three, the physical witness and learner-owned consequence projections diverge."
missing_transition = "A trace-supported classification of the same consequence-bearing physical link across construction and owner-local projection."
prediction = "Existing events show whether the tick-eight consequence link is deallocated, stops participating, or remains participating while learner two has no matching private consequence."
falsifiers = ["The exact consequence link cannot be followed.", "The decisive existing trace slice is incomplete.", "Retention changes the prior frozen behavior or integrity.", "Classification needs semantic world knowledge."]
positive_reference = "hand-completed-cycle-naturality-v1 completed-cycle-first-arrow-change artifact sha256:e4c38011cc9ff198474f553f72b6e4e7b366f3201a83fc114acd7c38a20e04d3"
negative_controls = ["hand-completed-cycle-naturality-v1 exact inertness summary", "no new core trace event", "fresh child private memory", "replay", "natural quiescence", "zero propagation exhaustion"]
included_mechanisms = ["existing physical consequence writes", "existing link traversal and deallocation", "existing learner construction and private consequence events", "existing candidate and choice trace", "official batched completed-cycle hand"]
inherited_constraints = ["benchmark blind", "no semantic action identity", "no external pose or direction in learner state", "no new learner memory", "exact replay", "natural quiescence", "warm runtime below ten seconds"]
excluded_proposals = ["new core event", "parent-memory copying", "construction handoff law", "new recency window", "direction or motor memory", "adapter force change", "default adoption", "authority promotion"]
arm_paths = ["arms/existing-trace-retention.toml", "arms/construction-boundary-cycle-witness.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = false

[budget]
total_sandbox_minutes = 8
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
