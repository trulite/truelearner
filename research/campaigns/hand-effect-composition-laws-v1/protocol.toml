schema = "research-protocol/v1"
id = "hand-effect-composition-laws-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "hand-physical-transition-continuation-v1 convergence sha256:3480c7cefabfc919669804eeffbe72a51b095295fdf867d9c88edc51dbde5ec2"
question = "Does cumulative one-joint travel require quiescent-phase world composition, learner-side unresolved-effect coherence, or their composition?"
observables = ["Harness phase outputs, force groups, actual state transitions, return lifetime and coherence decisions", "both limits and escapes, perturbation recovery, replay, quiescence, propagation, and work"]
frozen_variables = ["root-fresh learner parent", "batched fixed-force Academy reference", "unchanged anonymous hand topology and schedule"]
permitted_changes = ["one counterfactual quiescent-phase adapter mode", "one opt-in recent-unanswered-output coherence law", "their explicit composition"]
positive_predicates = ["batched reference remains exact", "each arm reduces opposing cancellation or increases actual travel", "complete composition reaches and leaves both limits and recovers from perturbation"]
negative_controls = ["samples", "answered/stale/multiple/unrelated returns", "old protocols", "reflection", "exact replay", "natural quiescence", "zero propagation exhaustion"]
stop_conditions = ["evaluator state enters the learner", "batched reference changes", "a coherent output never releases", "integrity fails", "no travel improvement"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
