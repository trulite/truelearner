schema = "research-protocol/v1"
id = "workstation-runtime-attached-complete-candidate-v1"
program = "learner-physics"
claim = "WORKSTATION-KEY-SELECTION-LADDER"
status = "preregistered"
parent_authority = "workstation parent bedcbe54208bd8dc2df9d1bdde976f6c28c4ea7a; body-curried calibration authority V3 evidence sha256 3d6a8106d2176048a3bea2645dbc0886e8be656dbec23acda05216ff4e3c72ff"
question = "When the strongest established body, visual, return, attachment, and body-curried calibration mechanisms are composed without a semantic controller, how far does one organism climb from ordinary key consequences through learned monitor cue, focused seeing, path reuse, choice, gaze, keyboard search, reach, touch, intended press, visible consequence, release, and natural quiet?"
observables = ["construction state, learner, device, and physical clock", "runtime raw and calibration channel cardinality, input moments, action-caused channels, and active residuals", "per-eye image hashes and exact focused-field change hashes", "learner, candidate, choice, movement, gaze, fingertip, contact, key, consequence, release, and quiet projections", "causal ordering of every ladder arrow", "paired learned cues, blank cue, unlearned cue, exact checkpoint replay, resident bytes, and physical work"]
frozen_variables = ["complete ANSI 104-key Academy world and renderer", "five-digit hand, opposing thumb, binocular eyes, touch, pressure, collision, and hysteresis", "accepted output-specific causal return and first-choice lifetime mechanism", "accepted binocular stable-fixation and focused-field mechanisms", "atomic runtime attachment with trace lifetime eight and thresholds one two three", "body-curried local interior-versus-limit calibration", "one owned world sample and no semantic controller or supplied motor map", "evidence seed 0x5eedc0de", "development budget sixty-four and probe budget sixty-four"]
permitted_changes = ["research-only complete runtime-attached workstation constructor and checkpoint state", "simultaneous frame-moment preservation for independent focused and attached sensor facts", "observer-only exact focused-change, input-moment, ordered-ladder, cost, and replay diagnostics", "independent frozen-checkpoint branch scheduling with deterministic evidence rejoining", "Academy construction and restore plumbing for the research-only constructor"]
positive_predicates = ["construction is inert and the semantic firewall remains closed", "all declared sensor modalities reach attached tissue and a single ordinary attached frame uses one input moment", "two distinct proper printable key consequences develop and release", "paired learned monitor cues differ first in world pixels and then in exact focused-field changes", "each later rung is counted only at or after its prerequisite on both branches", "the trace names exactly the earliest absent physical arrow or reaches complete", "checkpoint replay is exact, every step naturally quiesces, resident memory stays below 512 MiB, and compact evidence stays below 1 MiB"]
negative_controls = ["construction before observation", "passive or unchanged observation cannot fabricate action origin", "interior body calibration is identity while a physical limit is nonzero", "paired learned cues begin from one exact checkpoint", "blank monitor cue", "unlearned question-mark cue", "exact checkpoint replay"]
stop_conditions = ["hidden key, glyph, coordinate, route, desired action, correctness, reward, or evaluator state enters the harness", "one simultaneous sensor frame is serialized by feature or modality order", "runtime attachment changes the organism before observation", "a passive sample fabricates action return or an action-caused sample loses its output origin", "warm twelve-transition representative fixture reaches 0.5 seconds", "resident memory reaches 512 MiB", "the projection cannot locate one earliest missing arrow", "replay or natural quiescence fails"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
