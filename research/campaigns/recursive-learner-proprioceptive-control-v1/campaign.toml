schema = "research-campaign/v1"
id = "recursive-learner-proprioceptive-control-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "A recursive learner can keep proprioception as current physical incidence while owning only bounded recent consequence memory; owner-local write and read then produce fresh control without copying or mutating another learner's preference."
first_divergence = "After fresh-memory construction, current proprioception can complete motor threshold and consequence can strengthen a physical path, but candidate preference is still read from global link history rather than the deepest causal owner."
missing_transition = "An actual accepted return writes recent consequence to the deepest owner's private path-generation record, and later same-owner current proprioception reads that record during candidate selection."
prediction = "The complete candidate admits only same-owner current proprioception, records only actual participating consequence, selects the locally recent alternative, releases after the recent window, and composes through depth three without parent or sibling cross-preference."
falsifiers = [
  "Current same-owner proprioception cannot complete an otherwise executable motor threshold.",
  "A child reads inherited global preference before its own consequence or mutates parent or sibling preference while learning.",
  "Absent, shifted, stale, disconnected, or unrelated proprioception completes an owned opportunity.",
  "A withheld, duplicate, unrelated, or nonparticipating consequence writes private preference.",
  "Local write survives but later selection does not read it, does not release, or fails under recursive composition.",
  "Any frozen reference, checkpoint, quiescence, locality, or warm-runtime invariant changes.",
]
positive_reference = "recursive-learner-fresh-memory-v1 supported candidate tree b970cd48b76c61ea6413b15510896ef639e9869dba67848ab429d95366177f3c plus sensorimotor-synthesis-ladder-v1 current proprioceptive threshold completion"
negative_controls = [
  "global-link recent consequence as the inherited-preference reference",
  "fresh child before its own consequence and withheld-consequence release",
  "absent, early, late, stale, disconnected, and unrelated proprioceptive incidence",
  "unrelated return, blocked output, duplicate return, reflected choice, and reversed training order",
  "parent-before-after and opposite-trained sibling preference",
]
arm_paths = [
  "arms/current-proprioceptive-owner-gating.toml",
  "arms/global-consequence-reference.toml",
  "arms/owner-local-recent-consequence.toml",
  "arms/recursive-proprioceptive-control-composition.toml",
]
max_parallel_arms = 4
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 160
max_minutes_per_arm = 40

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
