arm: existing-learner-coarse-graining
verdict: survived
command: cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-core --test runtime_attachment physical_cycle_composition_
result: 1 passed; 0 failed; 8 filtered out

observations:
- Two separately opened and exactly closed physical cycles emitted output origins [10, 10].
- The existing causal-closure constructor formed exactly one learner containing the participating lineage; no component wrapper or new memory type was added.
- Attaching an unrelated component before versus after the tested component did not change the outputs, learner count, or owned lineage size.
- Both attachment orders naturally quiesced.

categorical_reading:
- Repeated evidence for the same closed local cycle coarsened into the already-existing learner object.
- Independent attachment order remained observationally invariant.
