schema = "research-campaign/v1"
id = "workstation-return-bearing-opportunity-composition-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "An actual returned transition and equal local opportunity must coexist in one causal run for the existing physical path to carry transition identity into output competition."
first_divergence = "At sequence thirteen the prior ring-decrease candidate was present but had zero path inputs zero supplied opportunity and was non-executable; ring-increase won ordinarily."
missing_transition = "The actual movement return and the next bounded local opportunity do not coexist while the used path is live, so that path cannot carry transition identity into executability and choice."
prediction = "The composed mode makes that exact returned path executable and current, selects one uncanceled continuation, and increases same-direction contact-relevant continuation above 63 in one bounded trace."
falsifiers = ["The prior path stays absent or non-executable.", "The winner requires adapter direction knowledge.", "Both directions act or unrelated components receive opportunity.", "Same-direction continuation does not improve.", "Replay quiet work production identity or firewall regress."]
positive_reference = "workstation-return-bearing-choice-localization-v1:first-direction-break sha256:1fa97a9c3394038adf7610bb705e0ef1c14ef371bd4b76f7a9be8c9caa53e8d9"
negative_controls = ["GenericOnly production identity", "unchanged sample", "unrelated component", "ambiguous return-bearing path", "semantic firewall replay quiet work"]
included_mechanisms = ["actual PhysicalTransition incidence", "equal anonymous component-local opportunity", "LP-067 existing current-transition continuation", "shared generic opportunity", "choice diagnostic projection"]
inherited_constraints = ["no new core physics", "no durable adapter state", "no direction anatomy surface target reward or evaluator input", "one fresh complete run"]
excluded_proposals = ["remembered direction", "toward-surface bias", "named finger selector", "contact reward", "moved surface", "longer horizon"]
arm_paths = ["arms/return-bearing-opportunity-composition.toml", "arms/production-and-sample-controls.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = false

[budget]
total_sandbox_minutes = 12
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 1.0

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
