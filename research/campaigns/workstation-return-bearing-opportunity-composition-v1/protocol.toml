schema = "research-protocol/v1"
id = "workstation-return-bearing-opportunity-composition-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "localized workstation return-bearing choice wall at e35b834; artifact sha256 1fa97a9c3394038adf7610bb705e0ef1c14ef371bd4b76f7a9be8c9caa53e8d9"
question = "Does composing an actual returned transition and equal anonymous component-local opportunity in one causal run make the existing return-bearing path executable and preserve its exact physical arrow without direction memory?"
observables = ["prior and returned movement axis and sign", "candidate live path input opportunity drive threshold executability and current-transition status", "choice target basis admitted outputs and cancellation", "same-component and same-direction continuation", "real surface gap entry local pressure and device events", "exact replay natural quiescence work propagation and semantic firewall"]
frozen_variables = ["seed 82001 and 120-step horizon", "authoritative learner protocol and shared generic opportunity", "workstation body world geometry contact physics and initial pose", "falsified LocalAfterTransition parent", "first-break diagnostic projection"]
permitted_changes = ["research-only ComposedWithReturn adapter mode", "research experiment and observer evidence"]
positive_predicates = ["the localized exact prior-direction candidate gains live path input becomes executable and is selected as one current-transition arrow", "no opposing output cancels the localized continuation", "same-direction contact-relevant continuation exceeds the falsified parent count 63 without broad five-finger coactivation", "the complete trace replays exactly naturally quiesces remains within work 2000 and preserves the semantic firewall", "surface gap entry and contact are reported but not required to establish the local solve"]
negative_controls = ["production GenericOnly identity", "unchanged sample creates no composed opportunity", "equal treatment of both component directions", "unrelated component receives no local opportunity", "ambiguous or absent current path is not semantically resolved"]
stop_conditions = ["targeted exact-path fixture fails", "adapter must know prior direction anatomy target contact or evaluator state", "both directions are admitted or cancel", "production replay quiet propagation firewall or work regress", "the complete trace fails the local executability or same-direction predicates"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
