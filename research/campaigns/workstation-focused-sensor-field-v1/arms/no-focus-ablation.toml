schema = "research-arm/v1"
id = "no-focus-ablation"
campaign = "workstation-focused-sensor-field-v1"
kind = "control"
parents = ["focused-field"]
imports = ["complete SpatialField", "the same complete associative u8-to-u64 region transducer"]
mechanism = "Remove all focus points so the complete eye raster remains one region, while leaving the field and transducer unchanged."
prediction = "The ablation determines whether focus is necessary for this retained cue pair; if the one-region sums already differ, this experiment must not claim that these glyphs require foveation."
falsifiers = ["The ablation is not the identical field and transducer with only focus removed.", "Its result is omitted or reinterpreted to manufacture a necessity claim.", "The focused arm depends on the ablation outcome to choose geometry."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "5fb4ce170f42439d19809dc2a6bf27246c3d8909de2ae7fde8123b256f75c9c4"
budget_minutes = 5

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-embodiment --test focused_sensor_fields focused_sensor_field_is_bounded_and_reconstructs_exactly"
full_evidence = "jq 'all(.eyes[]; .no_focus_region_count == 1)' research/campaigns/workstation-focused-sensor-field-v1/artifacts/focused-field-projection.json"
