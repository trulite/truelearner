schema = "research-arm/v1"
id = "existing-lineage-discriminator"
campaign = "hand-physical-change-resampling-v1"
kind = "diagnostic"
parents = ["consequence-born-return-admission"]
imports = ["causal origin", "incidence birth", "return opening"]
mechanism = "Compare later changed and unchanged occurrences using only the parent lineage fields."
prediction = "If existing lineage already contains change, the parent rejects later unchanged sampling without a new transition fact."
falsifiers = ["The parent admits later unchanged samples and renews consequence."]
source_revision = "plan:a621cf270f0a2012601784a30208f6c33a3b47d7d9987dda5f72b64d6d3bbdc1"
protocol_sha256 = "6bbf587590a79e51869a7933aaa5bf13733ba7c837a5ae7d213180b796c249c3"
budget_minutes = 8

[gates]
tiny_fixture = "cargo test --locked --manifest-path truelearner/Cargo.toml --test harness_boundary consequence_born_return"
full_evidence = "cargo run --quiet --locked --manifest-path research/experiments/hand-physical-change-resampling/Cargo.toml -- --arm existing-lineage-discriminator"

