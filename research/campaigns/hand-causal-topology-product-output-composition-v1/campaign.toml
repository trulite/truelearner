schema = "research-campaign/v1"
id = "hand-causal-topology-product-output-composition-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "Local output choice and fresh-opportunity allocation factor over connected physical outcome-source components, preserving one-joint alternatives while composing disconnected anonymous joints."
first_divergence = "The parent suppresses one independent motor at two-joint step zero; after output-only factorization, the remaining divergence is a single global fresh opportunity at the shared upper-boundary reversal."
missing_transition = "Each disconnected causal component must retain its own output arrow and its own bounded alternative opportunity."
prediction = "Immediate and completed-path origins fail one-joint identity; connected outcome topology preserves it, closes two joints under controls, and only then closes five."
falsifiers = ["Either origin proposal preserves the exact one-joint identity.", "Topology factorization changes the one-joint arrow.", "Either two-joint component loses a sign limit escape perturbation recovery or order invariance.", "Five runs before two passes or any five-joint component fails.", "Replay quiescence exhaustion hidden-authority or activity-bounded work controls fail."]
positive_reference = "hand-activity-normalized-build-v1:complete-one-joint sha256:60245d1ab700e879f41d8011c8f4ba39a27815d301bce9d62a1163f059db4053"
negative_controls = ["immediate-origin product", "completed-path-origin product", "one-module identity", "reverse registration", "proximal-only perturbation", "conditional five-joint stop", "unchanged accepted protocol"]
included_mechanisms = ["return-bearing continuation", "existing output-to-outcome-source wiring", "live physical topology components", "component-local output choice", "component-local fresh opportunity", "permanent candidate and competition trace"]
inherited_constraints = ["anonymous physical input", "one shared harness", "fresh child memory", "no semantic anatomy direction pose target or evaluator action", "development evidence only"]
excluded_proposals = ["module labels inside learner physics", "separate learner per joint", "moving modules apart", "longer observation as a solve", "accepted-default promotion", "authority adjudication"]
arm_paths = ["arms/immediate-origin-preflight.toml", "arms/causal-path-origin-preflight.toml", "arms/one-module-identity.toml", "arms/two-joint-serial-composition.toml", "arms/registration-order-invariance.toml", "arms/proximal-change-isolation.toml", "arms/conditional-five-joint.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 15
max_minutes_per_arm = 3

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 1.0

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
