schema = "research-protocol/v1"
id = "hand-causal-topology-product-output-composition-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "developmental-hand-multi-joint-v1 convergence sha256:9a757ef9274f76dba7378ff70f63ad7ccdb0cdffdc565fb00e79e73c1d6cccb4"
question = "Can existing causal topology factor independent output and opportunity choices so two and then five anonymous serial joints retain the complete one-joint physical arrow?"
observables = ["per-step component positions and cumulative serial pose", "per-phase motor outputs", "candidate drive opportunity threshold and executability", "output competition basis and component", "immediate and completed-path origins", "physical input drive return consequence ownership and choice counts", "forward and reverse registration", "proximal perturbation", "comparisons scans work topology replay quiescence and exhaustion"]
frozen_variables = ["one shared harness", "unchanged reflected force and contact law", "sixteen-step primary run", "eight-step development and sixteen-step recovery", "two-joint prerequisite before five", "unchanged predecessor digests"]
permitted_changes = ["opt-in product protocol variants", "component-local choice using existing outcome-source topology", "lossless trace projection and protocol-selecting development CLI"]
positive_predicates = ["topology protocol exactly preserves one-module identity", "both two-joint components reach and leave both limits with both signs", "two-joint registration and perturbation controls pass", "five executes only after two passes and every component closes", "replay is exact execution naturally quiesces no propagation budget exhausts and work remains activity bounded"]
negative_controls = ["immediate-origin and completed-path-origin protocols fail the one-joint control", "accepted return-bearing protocol remains unchanged", "no semantic joint module direction pose target score or evaluator-selected input", "one harness rather than independent learners"]
stop_conditions = ["predecessor digest drift", "topology candidate changes one-joint identity", "two-joint closure control or bound fails", "five runs after a failed prerequisite", "replay quiescence exhaustion or hidden-authority control fails"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
