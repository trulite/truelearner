schema = "research-protocol/v1"
id = "workstation-binocular-stable-fixation-v1"
program = "learner-physics"
claim = "BOUNDED-BINOCULAR-STABLE-FIXATION-DEVELOPMENT"
status = "preregistered"
parent_authority = "development-only minimal binocular acquisition artifact sha256:ad39ca0ee0080d36aa0409f553ea4abd7395aef2a66b7a583cb74489f8275329 at revision dfe933886d4a030d7775356f78e908e8531c2fc2"
question = "Can each independent eye compose its physical centered retinal state with the identity motor arrow, so the binocular product holds an acquired alignment without a target coordinate, desired direction, fused depth value, or hidden evaluator stop?"
observables = ["first exact joint alignment and first following departure", "every eye-local candidate opportunity choice output movement retinal transition and returned cause around that departure", "consecutive exact-alignment steps per mirrored disparity relation", "off-center exploration and one-eye-local gating", "exact replay natural quiescence work checkpoints serialized-input firewall and Production trace identity"]
frozen_variables = ["minimal signed retinal placement times movement-caused visual return acquisition parent", "shared generic opportunity before any selected eye-local identity map", "two independent eye histories axes outcomes and body force law", "external evaluator ownership of target error alignment duration and verdict", "fixed twelve-receptor binocular horizontal layout", "Production WorkstationHarness default"]
permitted_changes = ["causally inert retention of existing choice diagnostics in the experiment trace", "research-only eye-local composition of the physical center receptor with generic motor opportunity", "neutral reusable opportunity gate in the embodiment driver library if selected by the retained trace", "external stable-fixation evidence and controls"]
positive_predicates = ["all six outward and inward far middle and near relations still acquire exact joint alignment", "after first exact alignment both eyes remain exactly aligned for at least eight consecutive unchanged-world steps", "the centered eye emits no directional movement while an off-center eye retains ordinary exploration", "the complete trace replays exactly naturally quiesces remains bounded and contains no semantic input", "Production traces and the inherited acquisition negative controls remain exact"]
negative_controls = ["identity opportunity disabled", "center receptor removed", "uniform dark field does not freeze exploration", "one centered eye cannot suppress the other eye", "unchanged-body resampling cannot create a transition", "Production WorkstationHarness remains byte-identical"]
stop_conditions = ["the retained choice trace shows that the post-alignment departure already selected an identity arrow", "stable fixation requires target disparity error desired direction or an evaluator-side stop", "a proposed gate suppresses off-center exploration or crosses from one eye into the other", "replay natural quiet work checkpoint Production identity or semantic firewall regresses"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
