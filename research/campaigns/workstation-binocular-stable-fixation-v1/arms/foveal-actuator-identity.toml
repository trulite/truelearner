schema = "research-arm/v1"
id = "foveal-actuator-identity"
campaign = "workstation-binocular-stable-fixation-v1"
kind = "solve"
parents = ["foveal-identity-opportunity", "foveal-identity-return-composition"]
imports = ["eye-local physical center receptor", "retained generic-opportunity departure", "retained learned-path departure with zero opportunity", "ordinary bounded-axis no-change feedback"]
mechanism = "At the external eye actuator boundary, compose every horizontal directional output of a physically centered eye to the identity body effect; preserve the output trace, suppress no other eye or axis, and return no movement consequence because the body did not change."
prediction = "Both generic and learned-path departures produce zero actual horizontal movement at center, while all off-center acquisition arrows and the other eye remain unchanged; every mirrored relation then holds for eight steps."
falsifiers = ["A centered eye still changes horizontal position.", "An off-center eye loses an acquisition arrow.", "One centered eye suppresses the other eye or another axis.", "The effect boundary reports a transition or outcome when the pose did not change."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "6f5f283ebe549651a85127e336e345e7620b76a0a6f720046dd50ee6e2d912e3"
budget_minutes = 15

[gates]
tiny_fixture = "cargo test --release --locked --manifest-path research/experiments/workstation-binocular-alignment/Cargo.toml --lib foveal_identity_is_eye_local"
full_evidence = "cargo test --release --locked --manifest-path research/experiments/workstation-binocular-alignment/Cargo.toml --lib stable_fixation"
