schema = "research-arm/v1"
id = "foveal-identity-return-composition"
campaign = "workstation-binocular-stable-fixation-v1"
kind = "composition"
parents = ["foveal-identity-opportunity", "centered-return-removal"]
imports = ["eye-local identity opportunity", "center transition retained as sample", "minimal binocular acquisition"]
mechanism = "Compose the identity opportunity and centered-return boundary only if the trace shows both generic opportunity and returned continuation independently produce post-alignment direction."
prediction = "If both arrows are jointly sufficient for departure, neither single arm holds fixation and the composition alone preserves acquisition plus eight exact hold steps."
falsifiers = ["Either parent alone already closes the identity square.", "The composition loses acquisition or off-center exploration.", "One eye gates the other.", "The composition requires semantic alignment knowledge."]
source_revision = "dfe933886d4a030d7775356f78e908e8531c2fc2"
protocol_sha256 = "7792a8541cf90ddf2fe5a897d11e90c3a3fcb2d73fae057526819c9dfaa25279"
budget_minutes = 15

[gates]
tiny_fixture = "cargo test --release --locked --manifest-path research/experiments/workstation-binocular-alignment/Cargo.toml --lib foveal_identity_is_eye_local"
full_evidence = "cargo test --release --locked --manifest-path research/experiments/workstation-binocular-alignment/Cargo.toml --lib stable_fixation"
