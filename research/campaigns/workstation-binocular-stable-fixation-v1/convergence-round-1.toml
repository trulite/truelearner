schema = "research-convergence/v1"
campaign = "workstation-binocular-stable-fixation-v1"
round = 1
survived = ["post-alignment-choice-localization"]
falsified = ["foveal-identity-opportunity", "foveal-identity-return-composition"]
inconclusive = ["centered-return-removal"]
infrastructure_failed = []
shared_mechanisms = [
  "all arms preserve the minimal signed-placement times movement-return binocular acquisition parent",
  "all gates read only the owning eye's ordinary physical center receptor",
  "the lossless projection retains candidates choices outputs movements retinal transitions work replay and checkpoints",
]
strongest_counterexamples = [
  "at the inward-middle first departure both horizontal choices were Ordinary with no current-transition or completed-cycle admission, selecting the generic-opportunity branch",
  "removing generic horizontal opportunity while centered held outward-far and all inward relations but failed outward-middle and outward-near immediately after exact alignment",
  "at outward-middle sequence six the left increase candidate remained executable with opportunity zero admitted drive equal to threshold and consequence tick twenty-two, proving that a learned retinal path can leave center without generic opportunity",
  "also removing center off-to-on movement return did not remove that zero-opportunity candidate and therefore did not close fixation",
]
retired_families = [
  "generic opportunity gating alone as stable fixation",
  "center-transition return removal as a sufficient complement",
  "treating every motor output source separately instead of composing at the common body-effect boundary",
]
next_discriminators = [
  "compose the owning center receptor with the horizontal actuator identity after all output sources join",
  "require no actual movement and therefore no returned movement consequence while retaining the internal output trace",
  "test dark off-center and one-eye locality before the six-relation hold ladder",
]

[[new_arm]]
id = "foveal-actuator-identity"
kind = "solve"
parents = ["foveal-identity-opportunity", "foveal-identity-return-composition"]
imports = ["eye-local center receptor", "generic-source counterexample", "zero-opportunity learned-path counterexample", "bounded body no-change feedback"]
interaction_prediction = "Placing identity after every horizontal output source makes the body square commute: either route through a centered eye ends in the same unchanged gaze, while off-center and other-eye routes retain their directional effects."
falsifiers = [
  "A centered eye changes horizontal position.",
  "An off-center acquisition arrow disappears.",
  "The identity crosses eye or axis ownership.",
  "The body returns a movement consequence without a physical change.",
]
