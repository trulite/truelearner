schema = "research-convergence/v1"
campaign = "workstation-binocular-stable-fixation-v1"
round = 2
survived = ["post-alignment-choice-localization", "foveal-actuator-identity"]
falsified = ["foveal-identity-opportunity", "centered-return-removal", "foveal-identity-return-composition"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "all arms preserve the minimal signed-placement times movement-return binocular acquisition parent and its two independent eye factors",
  "all center decisions use only the owning eye's ordinary zero-offset bright receptor and never read target error disparity duration or verdict",
  "all evidence retains candidates choices internal outputs actual movements retinal returns work replay checkpoints and external alignment projection",
  "the stable arm changes only the research profile and the common horizontal eye-effect boundary after all internal output sources have joined",
]
strongest_counterexamples = [
  "the parent acquired every mirrored relation but left the first exact inward-middle alignment on the next step through two Ordinary choices with no admitted transition continuation or completed-cycle reuse",
  "eye-local generic-opportunity identity held four relations but failed outward-middle and outward-near because an executable learned retinal path emitted a directional output with opportunity zero",
  "center-return removal alone and its composition with opportunity identity both left the same zero-opportunity learned-path departure, so neither closed the common effect square",
  "foveal actuator identity preserved internal choices and outputs but composed every centered horizontal eye effect to no physical movement; all six relations then held exactly for twenty-nine to forty-seven remaining steps",
  "a centered left eye did not suppress an off-center right eye, and a dark off-center field preserved horizontal exploration in both eyes",
]
retired_families = [
  "generic opportunity gating as a complete fixation law",
  "center-transition return suppression as a complete fixation law",
  "source-by-source suppression before internal eye-output routes join",
  "evaluator-side stopping target-error gates fused depth values and semantic fixation commands",
]
next_discriminators = [
  "treat eye-local foveal actuator identity as development evidence for bounded stable fixation only",
  "test transfer from generated vertical edges to held-out natural image features before any richer stereo claim",
  "if transfer survives test whether stable binocular gaze can compose with the palm without adding a target coordinate or stop rule",
]

[[new_arm]]
id = "natural-feature-fixation-transfer"
kind = "composition"
parents = ["foveal-actuator-identity", "post-alignment-choice-localization"]
imports = [
  "stable eye-local actuator identity",
  "independent signed retinal placement and movement-caused return",
  "held-out workstation photographic image features",
  "external-only alignment and duration projection",
]
interaction_prediction = "The same zero-offset physical identity law will hold an acquired high-contrast natural image feature without knowing feature identity or target coordinates; failure will localize correspondence or receptor coverage rather than fixation physics."
falsifiers = [
  "The transfer requires a semantic feature label or evaluator coordinate.",
  "A centered natural feature does not close the owning eye effect to identity.",
  "Off-center exploration or eye independence is lost.",
  "Production replay quiet work checkpoints or semantic firewall regress.",
]
