schema = "research-protocol/v1"
id = "workstation-binocular-stable-fixation-v2"
program = "learner-physics"
claim = "BOUNDED-BINOCULAR-FOVEAL-ACTUATOR-IDENTITY-DEVELOPMENT"
status = "preregistered"
parent_authority = "v1 discovery counterexamples retained in convergence-round-1.toml: generic opportunity identity and centered-return composition both fail because a learned path emits a directional eye output with zero opportunity"
question = "Does composing every horizontal output of an eye with the identity body effect while that eye's own ordinary center receptor is physically bright preserve acquisition and produce bounded stable fixation without semantic or cross-eye control?"
observables = ["internal eye choices and outputs before the body-effect boundary", "actual eye movement and returned movement consequence after the boundary", "first exact joint alignment and consecutive exact hold steps", "dark off-center and one-eye-local exploration controls", "exact replay natural quiescence work checkpoints firewall and Production trace identity"]
frozen_variables = ["minimal signed-placement times movement-caused-return acquisition parent", "all internal learner paths choices outputs and generic opportunities", "two independent eye histories axes and outcomes", "external evaluator ownership of target error duration and verdict", "fixed twelve-receptor binocular layout", "Production WorkstationHarness default"]
permitted_changes = ["research-only owning-eye zero-offset bright receptor composed with the horizontal actuator identity after all internal output sources join", "neutral reusable optional-event driver retained for the falsified v1 opportunity arm", "external stable-fixation projection and retained traces"]
positive_predicates = ["all six mirrored far middle and near relations acquire exact joint alignment", "each relation holds exact joint alignment for at least eight following unchanged-world steps", "internal centered-eye choices may remain visible but cause no horizontal body movement and no movement return", "a dark eye and an off-center other eye preserve ordinary exploration", "replay quiet work checkpoints firewall and Production identity remain exact"]
negative_controls = ["minimal acquisition parent without actuator identity", "v1 opportunity identity only", "v1 centered-return removal only", "v1 opportunity and return composition", "uniform dark fields", "one centered eye paired with one displaced eye", "Production WorkstationHarness"]
stop_conditions = ["a centered eye physically moves", "an off-center eye loses exploration", "one eye suppresses the other eye or another axis", "the body reports or returns movement without physical change", "target disparity error direction duration verdict or another semantic value enters the organism", "replay quiet work checkpoint firewall or Production identity regresses"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
