schema = "research-protocol/v1"
id = "hand-construction-projection-admission-retention-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "hand-construction-outcome-composition-v1 convergence sha256:7ed858f0726ad449d59106f561f1c97623e756e78d0075a0406a2b3755356083"
question = "Where is the first additional completed-cycle admission caused by same-tick construction projection, and which projected same-generation link physically completes its target?"
observables = ["immutable parent artifact SHA-256 and ordered choices", "candidate ordered choices", "construction and learner consequence events", "drive and causal-lineage link participation", "typed first-added-admission verdict and decisive slice"]
frozen_variables = ["RecursiveLearnerConstructionOutcomeComposition physics", "completed-cycle parent artifact bytes", "official batched hand adapter", "four-tick recency", "prior negative campaign artifacts"]
permitted_changes = ["one successor evidence-only Rust experiment", "one pure parent/candidate comparator", "lossless persistence of already-existing events"]
positive_predicates = ["parent source hash and twenty-three ordered choices match", "all candidate choices before the first added admission preserve ordinal tick and phase identity", "the first non-Unique to Unique transition is classified", "the preceding construction owner consequence tick and projected links are named", "at least one projected same-generation link physically completes the newly unique target"]
negative_controls = ["no core or adapter behavior change", "no parent rerun", "no new trace event", "no semantic position direction hand or expected-action fact", "exact replay natural quiescence zero propagation exhaustion and unchanged external hand summary"]
stop_conditions = ["parent hash mismatch", "candidate identity differs before the added admission", "first added admission is not ordinal seven tick forty-seven", "no projected link composes to the unique target", "organism physics or adapter behavior changes"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
