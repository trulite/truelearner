schema = "research-campaign/v1"
id = "hand-boundary-effect-reentry-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "Separating outward effect delivery from path genesis prevents effect-born re-entry; the narrow causal terminal law preserves legitimate external and same-region learning better than a blanket regional closure law."
first_divergence = "An outward motor effect receiver seeds a learned path back to the opposite internal motor, closing a six-edge unowned cycle."
missing_transition = "A local path-genesis boundary between a firing caused by cross-region effect delivery and a genuinely external or same-region participating source."
prediction = "Both candidates isolate effect-born genesis, the external-surface control selects causal terminality, and the selected law removes hand exhaustion while preserving replay and physical progress; full hand closure remains an independent gate."
falsifiers = [
  "The first outward effect is lost or the unrelated effect-born path still forms.",
  "The narrow law blocks genuinely external or same-region path formation.",
  "The hand still exhausts or no longer reaches a physical boundary.",
  "A partial safety transition is promoted as complete hand control.",
]
positive_reference = "hand-origin-feedback-cycle-localization-v1 six-edge cycle"
negative_controls = ["causal-origin reference", "external outward surface", "same-region internal surface", "exact replay", "natural quiescence"]
included_mechanisms = ["complete causal-origin candidate", "transient cross-region arrival classification", "regional closure comparison", "boundary-effect terminality", "unchanged effect execution"]
inherited_constraints = ["private memory, actual participation, semantic blindness, exact replay, natural quiescence, and warm cost"]
excluded_proposals = ["motor identity", "hand direction", "suppressing effect delivery", "removing existing paths", "default adoption", "calling one-direction motion complete"]
arm_paths = [
  "arms/effect-born-genesis-isolation.toml",
  "arms/external-surface-discriminator.toml",
  "arms/conditional-reflected-hand.toml",
  "arms/complete-composition.toml",
]
max_parallel_arms = 3
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 35
max_minutes_per_arm = 9

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
