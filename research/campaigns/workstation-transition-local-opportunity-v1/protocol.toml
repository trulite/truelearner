schema = "research-protocol/v1"
id = "workstation-transition-local-opportunity-v1"
program = "learner-physics"
claim = "DEVELOPMENTAL-HAND-LADDER"
status = "preregistered"
parent_authority = "Generic Workstation Opportunity V1; localized parent d0e284880201a77ecf4f338e818a7993dd078bd0; immutable depth localization sha256 0a83883f265cc6ca1ee25d00531dc85b6127086755360c921fbdebd8e657e752"
question = "Does one ephemeral component-local motor opportunity caused by an actual returned physical transition preserve that local arrow long enough for the ordinary workstation hand to enter a real surface and receive local pressure?"
observables = ["generic and transition-caused opportunity incidence phase origin target and physical component", "returned changed axes and next-step same-component movement", "signed movement direction depth extrema surface gap and consecutive continuation", "isolated versus coactive digit movement", "real surface entry next-sample pressure and device events", "parent identity exact replay natural quiescence work and semantic leakage"]
frozen_variables = ["authoritative connected outcome-topology product", "adopted shared generic workstation opportunity", "standard hand initial pose force law sensors and checkpoint", "standard keyboard touchpad monitor geometry and contact thresholds", "seed 82001 and 120-step horizon", "parent contact artifact and complete recording digests"]
permitted_changes = ["research-only switch that emits both alternatives of each just-returned changed axis as one additional local phase and physical origin", "research-only evidence projection for local continuation and contact"]
positive_predicates = ["generic-only parent reproduces zero surface entries minimum gap 312 and established separated digit movement", "only actual returned transitions create additional local opportunity", "at least one returned contact-relevant transition is followed by same-component movement", "minimum real surface gap improves below 312 without broad five-finger coactivation", "a real surface entry is followed by positive pressure at the same site on the next sample", "replay is exact execution naturally quiesces and maximum step work remains below a preregistered bound of 2000"]
negative_controls = ["unchanged sample creates no transition-local opportunity", "generic-only parent remains exact", "opposing alternatives inside one local component are not both executed as net cancellation", "distinct returned components may compose without semantic scheduling", "device events target depth named anatomy direction score reward and evaluator action remain outside learner input and choice"]
stop_conditions = ["parent identity or authority lineage drifts", "local opportunity appears without an actual returned transition", "same-component movement does not follow any contact-relevant return", "five-finger coactivation returns", "surface entry lacks matching next-sample local pressure", "replay quiescence work or semantic firewall fails", "the candidate requires repair rerun or adaptive direction selection"]

[run_policy]
max_valid_runs = 1
fresh_environment = true
