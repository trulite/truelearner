schema = "research-campaign/v1"
id = "learner-motor-no-consequence-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "An unanswered physical return is sufficient local evidence that a consequentially learned motor route should yield one opportunity to an unused neighbor."
first_divergence = "At a joint limit, the strengthened increase route participates on every new opportunity while the ready decrease route never participates."
missing_transition = "A locally ready unused route must participate after the reused learned route produces no physical consequence."
prediction = "Pending-return deferral exposes reverse motion, but clean credit requires replacing the superseded return before the alternative participates."
falsifiers = [
  "Neither arm exposes the alternative in the tiny learned-then-no-consequence fixture.",
  "Immediate consequence no longer produces learned reuse.",
  "The surviving mechanism requires body limits, fingers, directions, capabilities, or evaluator knowledge.",
  "No arm advances unchanged Body Discovery beyond DigitSeparation while preserving replay and quiescence.",
]
positive_reference = "86d471654497ac27af0a1beb9b52cada7d52bd7d: seed 91003 acquires three capabilities, then DigitSeparation development moves no digit and its probe has MissingExploration."
negative_controls = [
  "86d471654497ac27af0a1beb9b52cada7d52bd7d core harness boundary tests",
  "immediate consequence retains strength-based reuse",
  "far-apart outputs remain independent",
  "delayed outcome without a new competing opportunity still returns",
  "reflection, exact replay, natural quiescence, and the Academy semantic firewall remain unchanged",
]
arm_paths = [
  "arms/pending-return-deferral.toml",
  "arms/pending-return-replacement.toml",
]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 60
max_minutes_per_arm = 30

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
