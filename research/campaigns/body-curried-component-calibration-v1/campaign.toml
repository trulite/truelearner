schema = "research-campaign/v1"
id = "body-curried-component-calibration-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "A local body context can be curried into one modality-independent residual function, and attached physical trace tissue can turn that residual into valenced persistent drive that ordinary action-return learning composes across types."
first_divergence = "Exact action-return closure succeeds, but the containing relation loses both unresolved drive and whether the actual return moved toward or away from the body-supplied norm."
missing_transition = "The current body-component relation is not transformed into a persistent anonymous residual whose decrease returns outcome, increase withholds outcome, and zero becomes identity."
prediction = "The pure curried laws pass first, the unchanged transformation transfers across finger hand eye ear voice and held-out types second, and the materialized residual then regulates two-sided scalar disturbances under reflected effects and shifted body context."
falsifiers = ["Any modality requires a branch in the shared library.", "Signal origin incidence or honest availability changes.", "Residual zero acts or nonzero becomes identity.", "A larger residual receives path consequence.", "A smaller residual acts again before its return closes.", "Either ordinary reflected or shifted-context regulation fails.", "Replay quiescence work or the semantic firewall fails."]
positive_reference = "runtime-attached-natural-closure-v1 exact closure and existing learner coarse-graining plus supported composable embodiment driver and perception-action receipts"
negative_controls = ["frozen uncalibrated regulation_attachment_natural_closure counterexample", "unavailable body context", "unavailable observation", "same residual", "larger residual", "collapsed hand factorization", "reflected effects", "shifted context"]
included_mechanisms = ["Signal origin and incidence", "Availability", "Driver composition", "actual InteractionStep return", "CommandFrame identity", "physical threshold trace memory", "runtime attachment", "exact natural-cycle closure", "existing learner construction", "connected outcome product"]
inherited_constraints = ["no sensor-local invented norm", "no motor map", "no semantic anatomy", "no external previous value", "actual return only", "local factors", "exact replay", "natural quiescence", "active-frontier work"]
excluded_proposals = ["sensor taxonomy", "global body snapshot", "evaluator reward", "correct motor or direction", "fixed scalar center", "dynamic metric framework", "core learner change", "higher semantic capability claim"]
arm_paths = ["arms/curried-calibration-laws.toml", "arms/cross-modality-calibration-ladder.toml", "arms/runtime-attached-calibrated-regulation.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 30
max_minutes_per_arm = 10

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
