schema = "research-convergence/v1"
campaign = "body-curried-component-calibration-v1"
round = 1
survived = ["curried-calibration-laws", "cross-modality-calibration-ladder", "runtime-attached-calibrated-regulation"]
falsified = []
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "local body context is curried into one pure available-observation-to-residual transformation with no temporal memory",
  "all tested component types preserve physical origin incidence and honest unavailability before converging on the same residual object",
  "attached threshold tissue retains temporal change and factors persistent drive rise fall and zero identity into distinct physical arrows",
  "existing exact cycle closure and ordinary learner construction discover which anonymous output reduces residual",
  "all live rungs preserve exact replay natural quiescence local factorization and the semantic firewall",
]
strongest_counterexamples = [
  "unavailable observation and unavailable body context produced no fabricated residual or Normalizer",
  "five fingers and two eyes retained independent factors while mixed and held-out Rust types used the unchanged implementation",
  "a diagnostic projection that merged old consequence reuse with new consequence write was rejected by the retained trace",
  "copying Transition incidence onto persistent drive caused a worsening residual to close falsely; restoring the preregistered Sample arrow removed the false closure",
  "ordinary reflected effects and a shifted body context still regulated both disturbance signs without naming a motor or direction",
]
retired_families = [
  "a calibrate method or temporal memory owned by each sensor type",
  "global body snapshots or a fixed scalar center supplied by the evaluator",
  "collapsing persistent drive and action-caused change into one transition arrow",
  "advancing a device-specific calibration API instead of composing arbitrary typed relations into one residual",
]
next_discriminators = [
  "ablate body currying and require shifted-context regulation to fail",
  "ablate persistent nonzero drive and require the frozen position-two stall to return",
  "collapse rise and fall onto the outcome surface and require worsening-residual false closure to return",
  "drive at residual zero and require rest-entry action or bounce to return",
  "then attach real hand eye ear and vocal morphologies by supplying only their local body relation and ordinary effect boundary",
]

[[new_arm]]
id = "calibration-body-context-ablation"
kind = "diagnostic"
parents = ["curried-calibration-laws", "runtime-attached-calibrated-regulation"]
imports = ["fixed calibration context", "shifted body-context control"]
interaction_prediction = "Removing the curried body context will preserve the original band but fail the shifted-context case, showing that normal belongs to the attached relation rather than the sensor value."
falsifiers = ["The ablation still follows shifted context.", "The positive reference or replay changes."]

[[new_arm]]
id = "calibration-arrow-factorization-ablation"
kind = "diagnostic"
parents = ["runtime-attached-calibrated-regulation"]
imports = ["persistent drive", "rise and fall transition surfaces", "zero identity"]
interaction_prediction = "Removing held drive restores the unresolved stall; merging rise into outcome restores false worsening closure; driving at zero restores rest-entry action."
falsifiers = ["An ablation retains all regulation controls.", "The positive reference changes."]
