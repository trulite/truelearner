schema = "research-protocol/v1"
id = "body-curried-component-calibration-v1"
program = "learner-physics"
claim = "RUNTIME-ATTACHED-MULTISCALE-COMPETENCE"
status = "preregistered"
parent_authority = "Connected Outcome Product V1@7be8bbc3009fe3131622a6ee21e9aa260d649aa1; runtime-attached-natural-closure-v1 convergence sha256:f07ffd240767d9c3d2e6f5cc7228bae97ce3f4525061648ece20a4dacdaa1226"
question = "Can one body-curried residual transformation remain lawful across component observation types and, when materialized through attached trace tissue, sustain two-sided regulation without a sensor norm, motor map, fixed center, or external history?"
observables = ["body context and observation types", "residual amount", "signal origin and incidence", "availability", "identity or active effect mode", "local factor outputs", "attached drive fall and rise surfaces", "consequence writes", "scalar trajectories", "reflected motor effects", "shifted body context", "exact replay", "natural quiescence", "work"]
frozen_variables = ["existing Signal Availability Driver CommandFrame and InteractionStep laws", "existing physical trace lifetime eight", "exact natural-cycle closure parent", "connected outcome product", "two anonymous motors", "scalar bounds minus four through four", "thirty-two observations", "no core learner changes"]
permitted_changes = ["one generic body-curried pure residual driver", "one saturating residual value", "one opt-in physical trace calibration mode with a late unresolved drive and fall return", "observer-only cross-modality fixtures and retained evidence"]
positive_predicates = ["generic laws pass without modality branches", "finger hand eye ear voice mixed and held-out types reuse the same transformation", "unavailability and zero remain identity", "larger residual receives no consequence while smaller residual closes before renewed drive", "both disturbances regulate under ordinary and reflected actuator effects", "shifted context regulates without a fixed center", "all runs replay and naturally quiesce"]
negative_controls = ["unavailable body context", "unavailable observation", "already-normal observation", "repeated same residual", "larger residual", "collapsed hand factorization", "reflected actuator effects", "shifted body context", "frozen uncalibrated regulation parent"]
stop_conditions = ["the generic curried law fails", "a modality requires a library branch", "origin incidence or availability is lost", "the physical trace cannot order return before renewed drive", "larger residual receives outcome", "the first runtime regulation branch fails", "replay quiescence work or semantic firewall fails"]

[run_policy]
max_valid_runs = 1
fresh_environment = false
