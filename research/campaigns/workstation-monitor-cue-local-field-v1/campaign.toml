schema = "research-campaign/v1"
id = "workstation-monitor-cue-local-field-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "The cue-retina wall is an information-losing quotient: losslessly reassociating the complete ordered raster into regular local products preserves glyph shape without semantic feature selection."
first_divergence = "The learned cue pair produces different full binocular images at probe sequence 74 but identical point-retina values and all downstream values."
missing_transition = "Different spatial arrangements do not remain different sensory values after the current point projection."
prediction = "A fixed complete 8 by 8 local factorization makes both retained eye fields differ, reconstructs each exact image, and preserves same-cue identity without cue-specific sampling."
falsifiers = ["Either factorized eye field remains equal between the learned glyphs.", "Reassembly differs from the source raster.", "A source site is lost copied reordered padded or invented.", "The factorization uses cue identity position target or evaluator-selected offsets.", "Same-cue replay parent evidence semantic firewall or runtime references change."]
positive_reference = "retained cue-image difference and point-retina alias: source sha256 43bb8b86f1fc05d4c83b7450ab215915211efd6e52169e7b65eecac3b9011da8; point projection sha256 e0027fa1914cbd01e7beeca745755b10ce475b4668f6b3eda75b9aa2cc69157a"
negative_controls = ["retained wider-point alias", "same cue replay", "blank cue", "uniform field", "same value multiset in another arrangement", "unavailable depth site", "non-divisible local edge"]
included_mechanisms = ["retained real Academy renderer and post-development body state", "complete binocular raster", "generic availability values", "ordered finite spatial field", "consuming regular local factorization", "exact reconstruction"]
inherited_constraints = ["no core learner-law change", "no runtime retina change", "no cue key target coordinate desired action correctness reward or evaluator input", "immutable parent artifacts", "exact same-cue replay"]
excluded_proposals = ["more isolated retinal points", "glyph-specific offsets", "moving the cue", "evaluator-set gaze", "semantic glyph features", "convolution", "attention pointer", "learner wiring"]
arm_paths = ["arms/retained-point-alias.toml", "arms/lossless-local-field.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = false

[budget]
total_sandbox_minutes = 20
max_minutes_per_arm = 10

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
