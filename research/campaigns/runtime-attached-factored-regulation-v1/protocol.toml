schema = "research-protocol/v1"
id = "runtime-attached-factored-regulation-v1"
program = "learner-physics"
claim = "RUNTIME-ATTACHED-MULTISCALE-COMPETENCE"
status = "preregistered"
parent_authority = "runtime-attached-return-composition-v1 convergence; Connected Outcome Product V1@7be8bbc3009fe3131622a6ee21e9aa260d649aa1"
question = "Does the observed component-local shared threshold-change surface compose existing coherent-effect continuation into complete local regulation from both disturbance sides?"
observables = ["positive and negative scalar trajectories", "motor physical origins per external step", "rise fall and shared change firings", "coherent-effect admissions", "first lost continuation", "central-region entry and four-observation residence", "action-origin return", "replay", "quiescence", "work and resident bytes", "ordered choice trace"]
frozen_variables = ["factored physical trace with integer thresholds minus four through plus four", "trace lifetime eight", "distant outcomes", "attachment position ten", "motors nine and eleven", "accepted product protocol", "reflected bounds minus four and plus four", "central region minus one through plus one", "disturbances plus three and minus three", "thirty-two steps", "batched physical effect", "same opportunities capacities and tracing"]
permitted_changes = ["use the preregistered factored-change component builder", "observer-only retained evidence"]
positive_predicates = ["positive and negative branches enter the central region", "each remains central for four observations", "both signs occur", "every action-caused return preserves origin", "split-trace parent retains its first reversal", "replay and natural quiescence hold"]
negative_controls = ["unchanged split-trace parent", "silent attachment", "equal resampling", "semantic firewall", "fixed-active dormant replacement"]
stop_conditions = ["either branch misses or crosses through the central region without residence", "coherent continuation disappears before entry", "the factor causes a runaway", "origin replay quiescence or inherited controls fail"]

[run_policy]
max_valid_runs = 1
fresh_environment = false
