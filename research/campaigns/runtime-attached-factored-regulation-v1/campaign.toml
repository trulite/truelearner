schema = "research-campaign/v1"
id = "runtime-attached-factored-regulation-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "Coequalizing rise and fall for each threshold supplies enough physical continuity for existing coherent-effect behavior to close the bounded local regulation loop."
first_divergence = "The split trace reverses on its first motor-origin return; the factored trace preserves the same corrective motor once through coherent effect."
missing_transition = "It is unknown whether that repaired one-step continuation persists until both disturbance branches enter and remain in the central region."
prediction = "The factored trace regulates from plus three and minus three while the exact split trace retains its negative reversal."
falsifiers = ["Either factored branch misses central entry or residence.", "The shared surface causes overshoot or runaway.", "The split parent changes.", "Origin replay quiescence or work controls fail."]
positive_reference = "runtime-attached-return-composition-v1 shared-change log sha256:81fb8dbacb7c3cc7a8ce187ce824b62b905c9d031b6873727cd20d9a1333e3b0"
negative_controls = ["split trace first reversal", "silent attachment", "equal resampling", "semantic firewall"]
included_mechanisms = ["atomic attachment", "body-native trace", "shared threshold-change factor", "accepted coherent effect", "ordinary action return"]
inherited_constraints = ["no motor map", "no semantic setpoint direction or evaluator input", "no new learner law", "replay quiescence and bounded work"]
excluded_proposals = ["outcome-coordinate solve", "required consequence write", "external history", "selected motor", "dependent higher rungs"]
arm_paths = ["arms/split-parent-control.toml", "arms/factored-change-regulation.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 15
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
