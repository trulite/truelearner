schema = "research-arm/v1"
id = "publishability-firewall-and-integration"
campaign = "body-curried-component-calibration-authority-v3"
kind = "control"
parents = ["body-curried-component-calibration-authority-v2:identifier-firewall-and-integration"]
imports = ["subject-tree publishability boundary", "full truelearner regression", "separate cold bootstrap and exact warm loop", "production calibration source range", "identifier-boundary firewall controls", "test-only ablation boundary"]
mechanism = "Verify the exact publishable subject and reproduce unchanged integration and identifier-firewall controls."
prediction = "Only the compact workstation preflight failure is tracked, no subject-tree blob exceeds one hundred million bytes, inherited tests and warm work pass, no exact forbidden component or production ablation appears, and CLEAR plus truelearner remain explicit nonmatches."
falsifiers = ["A generated workstation diagnostic recording is tracked.", "A subject-tree blob exceeds one hundred million bytes.", "Any inherited regression fails.", "The exact warm loop reaches ten seconds.", "Any exact forbidden identifier component appears.", "CLEAR or truelearner is classified as ear.", "A removal enters production source."]
source_revision = "commit:9679c4e2115ce904a5bf52295f7351f6fa6e4f33"
protocol_sha256 = "e9ccf685a6cf7415383b18a5c8fcc7138f1b7afbd7862ad7642d9b4b9270ffef"
budget_minutes = 15

[gates]
tiny_fixture = "git ls-tree -r --name-only HEAD -- research/campaigns/workstation-return-bearing-opportunity-composition-v1/artifacts; git ls-tree -rl HEAD; cargo test --locked --manifest-path truelearner/Cargo.toml -p truelearner-core"
full_evidence = "cargo test --workspace --locked --manifest-path truelearner/Cargo.toml; cargo test --workspace --locked --manifest-path truelearner/Cargo.toml; ! sed -n '180,680p' truelearner/crates/embodiment/src/lib.rs | rg -ni -P '(?<![A-Za-z0-9])(finger|hand|eye|ear|voice|modality|answer|score|evaluator)(?![A-Za-z0-9])'; ! rg -n 'CalibrationAblation' truelearner/crates/embodiment/src/lib.rs"
