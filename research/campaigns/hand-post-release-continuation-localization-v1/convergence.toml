schema = "research-convergence/v1"
campaign = "hand-post-release-continuation-localization-v1"
round = 1
survived = []
falsified = ["trajectory-integrity", "first-continuation-failure"]
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "the unchanged candidate again made nine actual position changes, reached and escaped upper contact, used both motors, replayed exactly, quiesced naturally, and exhausted no propagation budget",
  "the first loss of post-release continuation occurred at world step 8 immediately after upper escape at step 7",
  "at step 8 complete paths, executable owner-scoped candidates, four owner reads, three consequential reads, two return admissions, and two consequence writes were all present",
  "motor 20001 fired during delivery of the prior physical transition, then motor 20000 fired after that transition wrote a fresh consequence; the world adapter combined both effects and the state remained at position 3",
  "the diagnostic counter named changed_steps counts attempted nonzero direction even when clamping prevents actual change; comparing it to four actual strict-parent changes falsified both preregistered arms despite behavioral reproduction",
]
strongest_counterexamples = [
  "trajectory-integrity: ReflectedHandProtocolEvidence.changed_steps was 16 for the upper-clamped strict parent although only four trajectory entries changed physical position",
  "first-continuation-failure: the causal localization was present, but the arm inherited the stale changed_steps equality and therefore remained formally falsified",
  "step 8: memory, ownership, paths, executability, and output were all present, yet opposite outputs in one external transition cycle canceled cumulative travel",
]
retired_families = [
  "more path, memory-write, ownership, or executability repair at the first post-release failure",
  "counting attempted direction at a clamp as actual physical state change",
  "patching and rerunning the frozen diagnostic after observing its stale contract",
]
next_discriminators = [
  "test whether a current owner-local PhysicalTransition on a candidate with an unanswered return can transiently preserve that causal incumbent through competition before its formal consequence write",
  "require Sample incidence, answered return, stale/nonlocal transition, reflection, exact-return lifetime, and strict-parent controls",
  "reject any continuation law that persists without actual transition, prevents boundary release, loses natural quiescence, or merely hard-codes output identity or direction",
]

[[new_arm]]
id = "physical-transition-incumbent-continuation"
kind = "solve"
parents = ["first-continuation-failure"]
imports = ["current physical-transition lineage", "unanswered return", "owner-local candidate competition"]
interaction_prediction = "A current physical transition caused by an unanswered incumbent will retain that candidate during the pre-write competition wave; unchanged samples will not retain it, so ordinary fresh replacement can still release a boundary."
falsifiers = [
  "The current transition cannot be joined to the incumbent without hidden world state.",
  "Both opposing motors still fire in one external cycle.",
  "Continuation persists on samples or prevents later release.",
  "Replay, quiescence, propagation, or controls fail.",
]
