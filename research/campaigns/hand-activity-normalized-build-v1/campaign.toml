schema = "research-campaign/v1"
id = "hand-activity-normalized-build-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "Linear causal-wave extraction plus current-transition opportunity, return-bearing continuation, and multi-origin no-change release closes the anonymous reflected joint without semantic state or adapter changes."
first_divergence = "After upper escape, a real movement-return phase fires the old motor before the current-input phase reuses the newly consequential motor, causing batched cancellation."
missing_transition = "Keep the exact physically current path executable while its outcome returns, and release an unanswered path when multi-origin unchanged boundary input supplies no transition."
prediction = "Scheduled-wave cost remains linear, the opt-in parent behavior is unchanged, opposing outputs at steps six and nine compose into one direction, and the candidate reaches and leaves both limits and recovers from perturbation."
falsifiers = ["Scheduled-wave comparisons or scans grow superlinearly.", "The opt-in parent trajectory changes.", "Either localized cancellation remains.", "The candidate fails a limit, escape, perturbation, replay, quiescence, or bounded-work predicate.", "A sample or single-origin case receives continuation opportunity."]
positive_reference = "hand-downstream-work-class-localization-v1 convergence sha256:6e97b6268a4ca45f0992454377f31ef6ac0d57f2c174ac566afe7a2013a5dc77"
negative_controls = ["exact RecursiveLearnerBoundedConstructionContinuation trajectory", "sample and unbalanced direct-opportunity controls", "single-origin no-change release control", "official batched adapter", "exact replay natural quiescence and zero exhaustion", "accepted default protocol"]
included_mechanisms = ["bounded construction first use", "completed-cycle composition", "physical-transition lineage", "owner-local consequence", "bounded local opportunity", "linear causal-wave extraction", "return-bearing current continuation", "multi-origin no-change release"]
inherited_constraints = ["anonymous physical input", "official batched force", "fresh child memory", "no held path", "no semantic direction or position", "candidate remains opt-in", "development evidence only"]
excluded_proposals = ["adapter sequencing change", "longer trial", "direction memory", "pose or contact labels", "global recency extension", "accepted-default promotion", "authority claim without clean checkpoint"]
arm_paths = ["arms/activity-normalized-scaling.toml", "arms/parent-behavior-preservation.toml", "arms/return-bearing-composition.toml", "arms/complete-one-joint.toml"]
max_parallel_arms = 4
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 12
max_minutes_per_arm = 3

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 1.0

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
