schema = "research-convergence/v1"
campaign = "hand-activity-normalized-build-v1"
round = 1
survived = ["activity-normalized-scaling", "parent-behavior-preservation", "return-bearing-composition", "complete-one-joint"]
falsified = []
inconclusive = []
infrastructure_failed = []
shared_mechanisms = [
  "the timing wheel extracts each earliest causal wave once while preserving exact serial and residual order",
  "the opt-in learner uses only current anonymous physical-transition lineage owner-local return and existing local opportunity",
  "held construction evidence keeps priority while multi-origin unchanged contact can release an unanswered path only through an already executable alternative",
  "the unchanged official batched joint reaches and leaves both limits recovers from perturbation replays exactly quiesces naturally and exhausts no propagation budget",
]
strongest_counterexamples = [
  "the exact parent retained all sixteen old position arrows, showing that scheduler scaling alone does not create the hand behavior",
  "sample-only unbalanced and single-origin fixtures remained ineligible, showing that the candidate is not a general recency or no-change rule",
  "the candidate paused at each physical contact but then escaped, showing that contact sampling is not being mistaken for another real transition",
]
retired_families = [
  "repeated minimum-key search within one already identified causal wave",
  "absolute comparison equality between executions that receive different real physical inputs",
  "semantic position direction contact hand or joint memory",
  "a held path or global recency extension for the first complete reflected joint",
]
next_discriminators = [
  "obtain an authorized clean pre-frontier checkpoint before any frozen authority or accepted-default decision for the opt-in one-joint law",
  "after that integration gate, test serial composition in one anonymous two-joint finger while preserving sibling isolation reflection perturbation replay quiescence and activity-normalized work",
  "localize the first physical transition if the two-joint finger fails; reuse this one-joint mechanism only where that trace predicts it",
]

[[new_arm]]
id = "two-joint-serial-composition"
kind = "composition"
parents = ["activity-normalized-scaling", "parent-behavior-preservation", "return-bearing-composition", "complete-one-joint"]
imports = [
  "activity-normalized-scaling: stable linear causal-wave extraction",
  "parent-behavior-preservation: exact opt-in parent control",
  "return-bearing-composition: current physical transition and bounded no-change release",
  "complete-one-joint: reflected limit closure and perturbation recovery",
]
interaction_prediction = "Two anonymous reflected joints compose in series so the distal joint develops without corrupting proximal control, and both retain reflection recovery replay quiescence and work bounds."
falsifiers = [
  "Either joint loses its already demonstrated one-joint control.",
  "One joint's evidence is credited to the unrelated sibling or adjacent learner.",
  "Serial composition requires semantic joint order direction target or anatomy inside the learner.",
  "Replay quiescence perturbation recovery or activity-normalized work fails.",
]
