schema = "research-convergence/v1"
campaign = "body-curried-component-calibration-authority-v1"
round = 1
survived = ["calibration-laws-and-transfer", "complete-regulation-and-necessity"]
falsified = []
inconclusive = ["integration-firewall-control"]
infrastructure_failed = []
shared_mechanisms = [
  "one exact clean body-curried calibration subject",
  "body context persistent nonzero Sample drive directional outcome and zero identity",
  "cross-type signal preservation exact replay returned-origin integrity and natural quiescence",
]
next_discriminators = [
  "freeze a successor authority protocol that preserves every v1 predicate and artifact while replacing substring matching with exact Rust identifier-token matching",
  "rerun the complete unchanged subject once under that successor because v1 cannot support promotion",
]

[[new_arm]]
id = "identifier-aware-firewall-successor"
kind = "diagnostic"
parents = ["integration-firewall-control"]
imports = ["integration-firewall-control:exact production source range", "integration-firewall-control:forbidden semantic identifier list"]
interaction_prediction = "Exact identifier-token matching rejects real anatomy or evaluator identifiers but does not classify TRACE_CLEAR_PHASE or truelearner_core as the modality token ear."
falsifiers = ["Any exact forbidden identifier appears in production calibration.", "The successor changes the tested subject or any non-firewall predicate."]
