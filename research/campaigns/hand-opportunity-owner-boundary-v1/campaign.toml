schema = "research-campaign/v1"
id = "hand-opportunity-owner-boundary-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "The strict-owner hand failure is the already forecast organism-to-root adjacent-recruitment boundary, not arbitrary cross-owner mixing."
first_divergence = "A non-recent organism-owned return is rejected before supplying an Owned(2) balanced local candidate."
missing_transition = "Classify the exact donor/recipient learner ancestry before considering cross-boundary recruitment."
prediction = "Every relevant hand mismatch is organism-to-root and remains distinct from synthetic sibling and unrelated controls."
falsifiers = ["Any relevant pair is non-adjacent.", "Sibling or unrelated pairs share the label.", "Diagnostics change behavior."]
positive_reference = "hand-bounded-fresh-opportunity-v1 lifetime-locality-ownership-controls"
negative_controls = ["same owner", "two roots", "siblings", "unknown owner", "parent behavior", "replay and quiescence"]
included_mechanisms = ["causal learner ancestry", "typed fresh-opportunity evaluation", "strict owner rejection"]
inherited_constraints = ["observational only", "benchmark-blind", "warm runtime below ten seconds"]
excluded_proposals = ["cross-boundary transfer", "owner erasure", "origin-only grouping", "semantic hand relation", "default adoption"]
arm_paths = ["arms/relation-classifier-controls.toml", "arms/organism-to-root-hand-boundary.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = false

[budget]
total_sandbox_minutes = 16
max_minutes_per_arm = 8

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
