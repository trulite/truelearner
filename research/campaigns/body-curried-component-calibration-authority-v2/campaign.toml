schema = "research-campaign/v1"
id = "body-curried-component-calibration-authority-v2"
program = "learner-physics"
kind = "solve"
mode = "authority"
hypothesis = "The unchanged clean body-curried calibration subject satisfies the bounded law and contains no forbidden semantic authority when the firewall examines exact identifier components rather than arbitrary substrings."
first_divergence = "Authority v1 stopped only because its raw text matcher classified ear inside CLEAR and truelearner; every scientific behavior removal and integration predicate had passed."
missing_transition = "The evidence contract must distinguish an exact semantic identifier component from coincidental letters inside an unrelated Rust identifier."
prediction = "One fresh one-shot successor batch reproduces every unchanged v1 behavior and removal, then passes the identifier-boundary firewall, cold-versus-warm integration measurement, and clean-subject controls."
falsifiers = ["Subject or v1 lineage is invalid.", "Any unchanged scientific or integration predicate fails.", "Any exact forbidden identifier component appears.", "The matcher still classifies CLEAR or truelearner as ear.", "The evidence requires repair or a second valid v2 run."]
positive_reference = "unchanged subject commit:cd03892564190d787d8fd8ca0cf0e1289289ff76 tree:74dea24d6b58280a358f1d46452e227d128a7701; v1 behavioral evidence sha256:fe437105d023c42d8e1e87bd9f624bd49541eaec954c7449acefba27aa9350bd"
negative_controls = ["unavailable body or observation", "worsening residual without complete outcome", "zero residual without complete action", "frozen any-window context oracle", "reflected effects", "TRACE_CLEAR_PHASE and truelearner_core raw-substring counterexamples", "test-only ablation boundary"]
included_mechanisms = ["body-curried Normalizer", "Residual algebra", "calibration physical trace", "persistent nonzero Sample drive", "fall-only outcome return", "rise distinction", "zero identity", "attachment-natural exact cycle closure", "Connected Outcome Product parent", "identifier-boundary evidence classifier"]
inherited_constraints = ["exact v1 subject and scientific predicates", "anonymous physical origin", "ordinary body-owned relation", "one shared harness", "exact replay", "returned-origin integrity", "natural quiescence", "cold bootstrap separate from bounded warm work", "no semantic modality or evaluator answer"]
excluded_proposals = ["production or test change", "sensor taxonomy", "sensor-owned norm", "external previous-value memory", "motor map", "fixed universal center", "real morphology competence", "repair of v1", "second v2 run"]
arm_paths = ["arms/calibration-laws-and-transfer.toml", "arms/complete-regulation-and-necessity.toml", "arms/identifier-firewall-and-integration.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 30
max_minutes_per_arm = 15

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 1.0

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
