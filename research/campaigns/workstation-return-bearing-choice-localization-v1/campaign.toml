schema = "research-campaign/v1"
id = "workstation-return-bearing-choice-localization-v1"
program = "learner-physics"
kind = "diagnostic"
mode = "discovery"
hypothesis = "The component-local candidate failed because the exact return-bearing directional path did not control the next local choice; projecting existing core diagnostics at the first direction break distinguishes absence executability ranking and cancellation without changing physics."
first_divergence = "The transition-local candidate continued the same contact-relevant component 131 times but preserved the prior direction only 63 times and reached no surface."
prediction = "At the first direction break, either the prior-direction candidate is not executable or current-transition continuation is not the admitted choice basis; selected-and-canceled remains a distinct falsifying branch."
falsifiers = ["Diagnostics change output work state replay or quiescence.", "The first break cannot be joined across returned component candidate choice crossing and movement.", "The exact prior-direction candidate is selected and executes uncanceled, contradicting the observed break.", "Classification requires semantic direction anatomy target or evaluator intervention."]
positive_reference = "transition-local candidate artifact sha256:0423e95241ede15e45a91ab72c8a830ba71a883b8b6de5d30bf5b936589186b1; trace sha256:bf1dd92a6f476793bdeaf0ba773b1e56b00f2d2fc1fa86d5b0c4ee2f2681868c"
negative_controls = ["core diagnostic purity and replay", "production schema and behavior unchanged", "same-direction return excluded from break", "observer early stop makes no capability claim", "semantic firewall"]
included_mechanisms = ["already enabled physical core trace", "transition-local candidate", "body-boundary motor-to-control projection", "observer-only first-break join"]
inherited_constraints = ["no learner physics change", "research feature only", "at most 48 focused steps", "no broad contact rerun", "natural quiescence checked per completed step"]
excluded_proposals = ["new opportunity", "direction memory", "contact reward", "target injection", "ranking change", "production diagnostics payload"]
arm_paths = ["arms/return-bearing-choice-localization.toml", "arms/diagnostic-purity-control.toml"]
max_parallel_arms = 2
max_rounds = 1
interaction_expected = false

[budget]
total_sandbox_minutes = 8
max_minutes_per_arm = 5

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 1.0

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
