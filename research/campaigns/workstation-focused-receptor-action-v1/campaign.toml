schema = "research-campaign/v1"
id = "workstation-focused-receptor-action-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "A changed focused receptor can participate in ordinary workstation action when its existing body relay is connected symmetrically to the accepted palm-horizontal motor and outcome field, preserving the physical cause without assigning a desired direction."
first_divergence = "Focused cue branches already differ in body-owned receptor transitions and learner fingerprint, but every focused relay ends before the workstation motor and outcome topology."
missing_transition = "No focused physical origin completes an executable palm candidate path, so vision cannot yet affect choice output or movement."
prediction = "The complete symmetric projection preserves different focused transition sets and makes the cue branches first differ at an executable palm candidate choice output or movement, while the isolated-relay control stops at learner divergence."
falsifiers = ["No executable palm candidate retains a focused signal origin.", "Complete cue branches remain equal through candidate choice output and movement.", "The isolated arm produces the same action divergence.", "The solve requires cue-specific wiring a selected region target coordinate or desired direction.", "One focused change is admitted more than once.", "Replay quiescence parent or semantic-firewall controls fail."]
positive_reference = "focused body integration verification sha256 c2daa7ba9ed3d138d53830f0d903b0d85c5ef5ef9b97cbd0d208842cc1bfc703"
negative_controls = ["isolated focused relays", "same-cue repeat", "reversed branch evaluation", "default sparse body", "accepted focused body", "accepted hand parent", "semantic firewall"]
included_mechanisms = ["body-owned focused field and exact receptor change state", "fixed three-valued 3,648-factor receptor product", "output-specific palm component", "first-choice physical outcome lifetime", "one fixed focused relay per factor", "symmetric relay fan-out to both palm-horizontal outcomes", "generic shared motor opportunity", "learner-only and body fingerprints", "candidate physical-origin diagnostics"]
inherited_constraints = ["one admitted input per changed factor", "actual body gaze only", "no sparse visual admission in focused mode", "no cue key coordinate target desired direction correctness reward or evaluator input", "same paired checkpoint", "natural quiescence", "exact replay", "normal body and learner law unchanged"]
excluded_proposals = ["cue-specific receptor", "selected differing region", "semantic image feature", "direction assigned by feature index", "duplicated visual input", "external action route", "hidden reach stop", "core learner-law change", "Production adoption"]
arm_paths = ["arms/focused-body-parent.toml", "arms/complete-symmetric-projection.toml", "arms/isolated-relay-control.toml"]
max_parallel_arms = 3
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 45
max_minutes_per_arm = 15

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.67

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
