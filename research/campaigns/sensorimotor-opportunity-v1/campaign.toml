schema = "research-campaign/v1"
id = "sensorimotor-opportunity-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "When the route that ordinary local choice would admit still awaits its causal-local outcome, admitting one fresh neighboring output for one opportunity and closing only the displaced return when that neighbor participates is sufficient for bidirectional one-joint control without stale credit or broad coactivation."
first_divergence = "After the strengthened positive route reaches the joint's upper limit, it fires without local change, leaves its local return unanswered, and ordinary choice admits that same route again while the ready negative neighbor never participates."
missing_transition = "The unanswered route must yield exactly one bounded opportunity to a fresh local output; if that output participates, only the displaced unanswered return must close."
prediction = "Deferral alone exposes the neighbor but eventually accumulates unanswered returns and stops; local replacement preserves exact causal credit and produces both directions plus recovery from both reflected limits; unchanged causal-local and shuffled controls preserve their frozen traces."
falsifiers = [
  "The causal-local or shuffled reference differs from sensorimotor-locality-v1.",
  "Deferral fails to expose exactly one fresh local output or changes a far output.",
  "Replacement leaves the displaced return open, closes an unrelated return, or credits the displaced path after the neighbor's consequence.",
  "Replacement preserves isolation but fails either direction or either reflected limit recovery.",
  "The candidate requires semantic identity or weakens replay, reference equality, natural quiescence, or accepted boundaries.",
]
positive_reference = "sensorimotor-locality-v1 causal-local-return: exact one-of-two causal credit followed by four positive moves, upper-limit saturation, and no negative movement in 40 steps; candidate diff abbe725a1159b77c3cc3c6b8b41c215f3f891e4a9e7889ea2311e301fb099f38"
negative_controls = [
  "sensorimotor-locality-v1 shuffled-local-return exact wrong-path credit",
  "motor-no-consequence-v1 pending-return-deferral stale-return counterexample",
  "motor-no-consequence-v1 pending-return-replacement broad-coactivation counterexample",
  "far-output, valid-delay, disconnected, replay, natural-quiescence, and semantic-firewall controls",
]
arm_paths = [
  "arms/causal-local-reference.toml",
  "arms/local-return-deferral.toml",
  "arms/local-return-replacement.toml",
  "arms/shuffled-local-reference.toml",
]
max_parallel_arms = 4
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 120
max_minutes_per_arm = 30

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.25

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
