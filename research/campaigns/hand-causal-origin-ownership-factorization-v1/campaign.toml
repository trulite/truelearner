schema = "research-campaign/v1"
id = "hand-causal-origin-ownership-factorization-v1"
program = "learner-physics"
kind = "solve"
mode = "discovery"
hypothesis = "Factoring mixed output incidence by carried physical origin before resolving ownership can separate owned and organism evidence while preserving one-effect selection."
first_divergence = "The stalled hand motor incidence contains two carried origins but only one known learner owner."
missing_transition = "A safe local separation of causal-origin factors that preserves private ownership and natural quiescence."
prediction = "The candidate transfers outside the hand, rejects same-origin mixed-owner evidence, and safely improves the unchanged hand."
falsifiers = [
  "A shared origin label combines evidence across private owners.",
  "The hand fails to improve, fails closure, or does not quiesce naturally.",
  "Replay, input-order behavior, one-effect selection, or inherited controls regress.",
]
positive_reference = "hand-owner-local-candidate-factorization-v1 unowned-origin discriminator"
negative_controls = ["same-origin mixed-owner incidence", "boundary reference", "bounded propagation exhaustion", "exact replay"]
included_mechanisms = ["complete boundary candidate", "origin-first partition", "ownership resolution inside each origin", "unique one-effect ordinary pipeline"]
inherited_constraints = ["private memory, actual participation, no benchmark identity, exact replay, natural quiescence, and warm cost"]
excluded_proposals = ["origin-ID tie breaking", "absorbing ambiguous origin groups", "semantic motor selection", "repairing a failed arm", "default adoption"]
arm_paths = [
  "arms/unrelated-origin-transfer.toml",
  "arms/same-origin-mixed-owner-control.toml",
  "arms/bounded-reflected-hand.toml",
  "arms/complete-composition.toml",
]
max_parallel_arms = 3
max_rounds = 1
interaction_expected = true

[budget]
total_sandbox_minutes = 40
max_minutes_per_arm = 10

[preflight]
tiny_fixture = true
hidden_authority_audit = true
reference_replay_equality = true
natural_quiescence = true

[convergence]
after_each_round = true
survivor_fraction = 0.5

[authority]
fresh_sandbox = true
max_valid_runs = 1
requires_frozen_protocol = true
