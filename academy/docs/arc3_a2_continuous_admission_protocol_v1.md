# ARC3 A2 continuous-admission phase diagnostic protocol v1

Status: frozen after the local-proposal tick-10 negative and before successor
implementation or execution.

## Question

Was the just-in-time proposal failure caused only by Academy inserting an
artificial idle tick between a quiescent body and the next official raster?

## Single regimen change

For the spatial ARC boundary only:

```text
old: next raster arrives at current_quiescent_tick + 1
new: next raster arrives at current_quiescent_tick
```

Babbling remains timed so its one unit coincides with the distance-1 local
proposal at the motor. No strength, pressure, eligibility, proposal, threshold,
or retained TrueLearner law changes. Dominant-palette A1 scheduling remains
unchanged.

## Primary row

Run the same `ls20`, seed 205, `[1,4,2,3]` curriculum and no-birth-time-
candidate geometry. The body begins at pressure phase 0. Require A2 four-route
development and the four-turn unaided replay to pass exactly as frozen in the
local-proposal protocol.

## Phase discriminator

Run the identical first context/action geometry after advancing an otherwise
fresh, candidate-free body by nine physical ticks:

```text
context source fires at tick 9
distance-1 proposal is due at tick 10
ordinary pressure also occurs at tick 10
```

Record whether the new proposal traverses and produces the expected motor.
This row must not be repaired or omitted if the phase-0 row passes.

## Decision

- **PASS** requires both phase-0 and phase-9 rows to create, traverse, and
  execute the first just-in-time opportunity using identical laws.
- phase-0 PASS plus phase-9 FAIL means removing the fixture gap is useful but
  not a phase-robust solution. Freeze the negative and open a substrate
  question about newborn proposal opportunity versus global pressure.
- phase-0 FAIL stops immediately at its first physical edge.

No A3-A5 evidence, physics repair, or authority claim is permitted in this
diagnostic.
