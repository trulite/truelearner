# CE1 consequence-supported efficacy protocol v1

Status: frozen before substrate or evaluator change.

Parent: RS2 v6 development-ready `542bbc0`
(`rs2-learned-inhibitory-topology-development-ready-v6`).

## Question

Can the existing accumulated-participation efficacy law mature signed physical
coupling in the direction already supplied by ordinary variation, while the
learnable inhibitory topology established by RS2 keeps executable recurrence
stable?

## Candidate law

CE0's universal support-quantum rule is retained. Qualified Modulation meeting
local participation adds that participation to `plastic_support`; each newly
completed universal support quantum changes coupling magnitude by one.

CE1 makes the update sign-preserving:

```text
gain = completed_support_quanta_after - completed_support_quanta_before

coupling > 0  -> coupling += gain
coupling < 0  -> coupling -= gain
coupling = 0  -> unchanged
```

Thus consequence can mature efficacy but cannot choose or reverse its sign.
The sign still comes only from symmetric ordinary variation. Resistance
consolidation remains the existing graded local law.

The candidate must not read a destination threshold, activation, firing,
task result, desired output, recurrence state, sign preference, path identity,
or evaluator fact. It adds no clamp, normalization, fatigue, cycle detector,
winner, or benchmark-specific target.

The change is feature-gated as `ce1`; CE0 and the accepted default runtime
remain byte-behavior-identical when it is disabled.

## Frozen matrix

Use CE0's two roots, ten clock origins, Reference and Production mechanics,
and exact replay. Retain its first nine families unchanged:

1. no consequence;
2. repeated use only;
3. prompt qualified consequence;
4. wrong and late consequence;
5. graded participation magnitude;
6. repeated supported consequence;
7. threshold family;
8. equal persistence/different efficacy;
9. fan-out locality.

The tenth family replaces only CE0's rejected unopposed reciprocal probe:

1. ordinary CV0/J0 variation generates separate `C+` and `C-` candidate
   relations from `B` toward `A`;
2. qualified consequence at `C-` selects and consolidates the negative
   relation, maturing its negative outgoing efficacy without choosing its
   sign;
3. the unsupported positive relation locally decays and its orphan junction
   disappears;
4. both ordinary reciprocal positive links receive genuine qualified
   consequence and mature from coupling `+1` to `+2`;
5. learning is frozen for a fresh probe;
6. `A` and `B` each fire exactly once, the learned negative contact
   participates, and the recurrent body naturally quiesces.

No evaluator-supplied inhibitory link may stabilize the probe. The stabilizer
must be the generated, consequence-selected relation. The retained RS2
controls remain accepted parent evidence; CE1 does not alter their worlds.

The matrix remains `200` cases and `400` mechanics rows. The observer uses
WS0 wave-level Drive/Modulatory incidence and includes signed Coupling,
Resistance, proposal, deallocation, CELL proposal/deallocation, QLP, firing,
clock, body, work, replay, and quiescence.

## Stop rule

After targeted formatting/check/strict-Clippy and a static boundary audit, the
complete matrix executes exactly once in a fresh E2B worker. Any failure
freezes CE1 negative without repair or rerun.

A complete CE1 positive alone permits FD2 v2. Authority, oracle status,
`arch.md`, and the frozen ARC curriculum remain unchanged.
