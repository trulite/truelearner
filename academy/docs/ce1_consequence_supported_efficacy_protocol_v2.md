# CE1 consequence-supported efficacy protocol v2

Status: frozen before evaluator change or physical execution.

Parent: CE1 v1 fixture negative `044edd7`.

## Classification

CE1 v1 stopped before its first recurrent-family row because its initially
orphan `A` and `B` fixture cells were correctly reclaimed by J0. No efficacy
predicate or organism law executed negatively.

## Sole correction

Add ordinary, high-resistance boundary topology before the existing recurrent
world's first admitted input:

```text
Anchor -> B -> generated signed candidate topology -> A -> Anchor
```

The anchor CELL and its two ARROWs are identical across roots, phases, and
mechanics. They are not generated by variation, do not produce the tested
action or consequence, and receive no consolidation unless they genuinely
satisfy the existing participation/locality law.

No efficacy law, candidate generation, timing, consequence path, matrix,
threshold, identity schedule, mechanics path, work limit, or acceptance
predicate may change. The first nine CE0 families and the tenth CE1 recurrent
family remain exactly v1. Only v2 output labels may also change.

## Execution and stop rule

After evaluator-scoped formatting/check/strict-Clippy and a byte/hash audit,
the complete `200`-case/`400`-row matrix executes exactly once in a fresh E2B
worker. Any failure freezes CE1 v2 negative without repair or rerun.

A complete positive alone permits FD2 v2. Authority, oracle status,
`arch.md`, and the frozen ARC curriculum remain unchanged.
