# CORE1-E13 — history composition protocol v5 amendment

## Status

Fixture-only correction before v2 definitive evidence. The physical runtime,
generic variation, learning, and consequence laws remain byte-identical.

## Preflight findings

Generic variation exposed two ordinary physical facts that the earlier
early/late fixture did not include:

1. a firing external arrival at a generated contact is itself a lawful generic
   variation opportunity, so directly exciting one contact can create the
   opposite material alternative;
2. activation relaxes between ticks, so a transient influence must meet the
   candidate traversal at the same local physical moment if it is to resolve
   that incidence.

These findings invalidate direct positive early/late excitation as the E13
composition, but do not invalidate the established SSA2-P principle.

## Sole history composition correction

After the two fixed primes and one ordinary refractory-recovery tick, clone the
same complete body as required by protocol v4. Each history then admits one
ordinary signed Drive incidence at the contact that is not to be expressed.
That `-1` incidence is simultaneous with the ordinary `+1` candidate traversal,
so the local junction receives zero and does not fire. The other physically
separate contact receives its ordinary traversal unchanged.

```text
negative-material control
    positive contact traversal + simultaneous -1  -> zero
    negative contact traversal                    -> expressed

positive-material continuation
    negative contact traversal + simultaneous -1  -> zero
    positive contact traversal                    -> expressed
```

The suppressive incidence is non-firing and therefore does not create another
variation opportunity. It is fixed before execution, contains no selected
handle or evaluator result, and uses only ordinary signed Drive physics.

The selected contact now fires one tick later than the obsolete direct-contact
fixture. For this history observation only, the existing context-trace input
and ordinary babbler are delayed by the same one tick so that the already
frozen local consequence geometry remains `context + action + returning event`.
No consequence or learning predicate changes.

## Everything else unchanged

Profiles A/B/C, contexts, action map, two primes, same-state branch comparison,
fixed positive-material developmental continuation, consequence admission,
12-tick cleanup gaps, fresh probes, Reference/replay/Production comparison,
hard prohibitions, no-ARC boundary, and stop-at-first-new-frontier rule remain
exact from protocols v1-v4.
