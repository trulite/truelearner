# PD0 old pressure interaction handoff v1

PD0 is characterization-positive and frozen. It advances no authority.

## Established baseline

```text
old eligibility covers pressure epoch
    -> pressure suppressed exactly

old eligibility absent
    -> pressure decrements exactly

continuous participation remains
    -> late local Modulation can still produce graded support
       if the route has not already died

traversal or CPC1 support
    -> zero durable resistance gain in the current candidate stage
```

All `750` physical cases and `1500` mechanics rows matched exactly. Every weak
route eventually disappeared after activity stopped. Same-path repetition
temporarily renewed live routes but did not make them permanently resistant.

## Frozen lineage

- protocol: `bde462a`, tag
  `pd0-old-pressure-interaction-protocol-v1`;
- evaluator/comparator: `a78f791`, tag
  `pd0-old-pressure-interaction-frozen-v1`;
- evidence: `ee48344`, tag
  `pd0-old-pressure-interaction-characterization-v1`.

## Status boundary

```text
PQLC1  unchanged local closure through depth 16    development positive
PD0    old pressure/participation interaction      characterized

PD1    local pressure x participation candidate    now eligible, not started
PD2    explicit eligibility deletion               blocked on PD1
ARC A2 unchanged replay                            blocked on de-supply
ARC A3-A5                                          paused
authority / oracle / arch.md                       unchanged
```

PD1 must not merely rename the current branch:

```text
if participation > 0 { skip_pressure() }
```

It must also preserve the observed distinction between temporary continued
activity and durable consequence-supported learning. A separately frozen PD1
protocol must choose candidate physical equations independently of ARC.
