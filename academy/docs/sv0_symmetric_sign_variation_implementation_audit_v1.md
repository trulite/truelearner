# SV0 symmetric sign variation implementation audit v1

Status: evidence-eligible candidate frozen before any SV0 matrix execution.

## Scope

- parent: RS2 Gate-A negative `af570f7`;
- protocol: `3d7dc13`, tag
  `sv0-symmetric-sign-variation-protocol-v1`;
- new candidate law: one feature-gated change to local proposal support;
- learning, participation, resistance, forgetting, timing, firing, scheduling,
  and mechanics changes: zero;
- CE0 efficacy plasticity: disabled;
- RS2 Gate B, FD2, ARC, authority, oracle, and `arch.md`: unchanged.

## Candidate law

Under the `sv0` feature, each already-eligible local source-to-target
opportunity iterates the constant proposal coupling slice `[1, -1]`. Both
ARROWs use the existing distance-derived delay, phase zero, resistance one,
Drive mode, and SourceFires trigger. The default non-SV0 path iterates `[1]`,
preserving the accepted runtime byte-for-byte outside the feature.

The proposal eligibility scan executes once before either alternative is
created. A later external arrival sees the live source-to-target pair and
cannot create another pair. Once both weak alternatives locally decay, the
same physical opportunity may create one fresh pair using ordinary generation
reuse.

## Selection world boundary

The evaluator never manually constructs a Drive candidate. Its only fixed
ARROWs are ordinary Modulatory consequence paths from distant consequence
CELLs to the proposal source. Both signed candidates cross the same physical
region boundary. Positive-useful and negative-useful worlds differ only in
which crossing sign the external world accepts before admitting the same
ordinary consequence; organism topology, candidate IDs/order, and the full
pre-consequence body/history are unchanged.

## Frozen candidate hashes

```text
core lib.rs       b6b7f2a47818d84ac2fd69aab466f5f917e6d3ba7cfc8f8c5db4ce91b97fbae5
core Cargo.toml   4cb6d665d738cdea61f928975fa34ddf89d62aa9150420748d94d574ed731aeb
evaluator         40617eb3deba55ff108633dc352962dc176d6e9ee99cbd00aa5384be7b78fa78
arm Cargo.toml    7a1331848974e21f45353da459b00c580bdd73c1d0a9433d6b1f153e7c52ddf0
protocol          a5a5f5286e58ad8996026e9dbe0d38d1a1b61dc9ea2c2cd562efd1b6811d96fe
```

## Frozen matrix

Six families cover symmetry/no consequence, positive usefulness, negative
usefulness, repeated use with neither useful, both useful, and bounded
reproposal. Two fresh roots, three absolute start ticks, two translated
position origins, Reference/Production, and exact same-mechanics replay yield
72 physical cases and 144 mechanics rows.

The standalone evaluator and feature-gated core compiled in reusable E2B
worker `igk8z307lfx74zbk20yeg`. The matrix has not run. No Rust or project
command ran locally.

The first strict-Clippy preflight stopped before evidence on a collapsible
match/if style warning. The nested condition was expressed as the equivalent
match guard; no consequence admission, world, predicate, or candidate law
changed.
