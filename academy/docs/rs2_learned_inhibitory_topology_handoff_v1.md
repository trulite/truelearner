# RS2 learned inhibitory topology handoff v1

Status: Gate-A static negative; stopped before learning.

## Outcome

```text
RS1
manually present ordinary negative-Drive topology
→ recurrent stability is physically expressible

RS2 Gate A
accepted ordinary variation
→ proposes only positive Drive coupling +1
→ cannot present inhibitory candidates to consequence learning
```

Gate B's permutation, consequence selection, consolidation, forgetting, and
frozen recurrent probes were never constructed. Adding negative candidates
inside that evaluator would have supplied the capability RS2 was meant to
test.

## Frozen lineage

- RS1 parent/result: `5473276`, tag
  `rs1-inhibitory-topology-sufficiency-result-v1`;
- RS2 protocol and static audit: `60043bc`, tag
  `rs2-learned-inhibitory-topology-protocol-v1`;
- fresh E2B static-audit worker: `i5y85noru41lo3sjf8d8m`;
- executable core changes: zero;
- evaluator and Rust execution: none.

## Next boundary

A successor must not be framed as an RS2 repair. It may independently ask
whether a general, sign-symmetric or locally state-dependent variation law can
produce signed causal possibilities without semantic labels, target knowledge,
oscillation scoring, or preferred inhibition.

RS2 does not resume CE0, FD2, ARC, authority, the oracle, or `arch.md`.
