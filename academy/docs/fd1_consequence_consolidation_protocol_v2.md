# FD1 consequence consolidation protocol v2

Status: frozen before v2 evaluator edits or execution.

Parent evidence:

- FD1 v1 immutable negative: `fd1-consequence-consolidation-negative-v1`;
- v1 diagnostic: `fd1-negative-v1-diagnostic-result-v1`.

## Eligibility

V1 stopped on family C3 before serializing its row. A separately frozen
diagnostic reproduced every C3 state, lifetime, work, replay, mechanics, and
quiescence requirement under disjoint identities. It did not reproduce the
composite failure.

V2 is eligible as measurement hardening. V1 remains negative and cannot be
rerun or relabeled.

## Frozen mechanism and worlds

The core candidate bytes remain exactly those frozen for v1. V2 changes no
physical law.

Run the same C0-C6 families, schedules, phases `0..9`, mechanics, replay,
predicates, expected resistance sequence, and expected death ages as v1. Use
fresh roots `4_700_000` and `4_800_000`.

## Sole measurement repair

V2 must serialize all observations and individual gate booleans before its
final acceptance assertion. The artifact must remain downloadable if any
predicate is false.

For every physical case serialize:

- Reference observation;
- Reference replay equality;
- Production observation;
- Production replay equality;
- Reference/Production equality;
- family predicate;
- natural quiescence;
- proposals and QLP counts.

No predicate may be weakened or removed. The family evaluator functions and
core physics remain byte-identical to the v1 freeze.

## Cumulative control

Only after all v2 focused rows pass, run the frozen FD0 evaluator once and
require the original exact hashes:

- matrix `5b9cfaf5e6ac93d56b07b7c0346bb95d46c1e33bc539a74897e40e40fb97dc99`;
- report `1450c5bdf8a133aed39688cb57c53586f43dd47ed4cf10243b6b305b17f38384`.

## Decision

Any focused predicate, replay, mechanics, quiescence, FD0 hash, or frozen-core
failure is an immutable FD1 v2 negative. A full pass establishes development
readiness only.

RC0, CPC/PQLC cumulative replay beyond the focused inherited behavior, ARC,
authority, oracle, and `arch.md` remain blocked.
