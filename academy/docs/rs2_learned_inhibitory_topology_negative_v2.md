# RS2 learned inhibitory topology immutable negative v2

Status: stopped before scientific matrix evidence.

Frozen evaluator: `2103b32` (`rs2-learned-inhibitory-topology-frozen-v2`).

E2B one-shot worker: `ino1dylwfmldk4t6yroa5`.

## Exact outcome

The frozen command executed once and stopped during the first training world:

```text
physical arrivals cannot precede current substrate time
```

No CSV or report was published. No RS2 family, Reference/Production
comparison, replay, learned-inhibition probe, or stability classification was
completed.

## Classification

This is an evaluator timing/fixture defect, not a scientific counterexample.

The RS2 target A was constructed at threshold 1. During initial symmetric
candidate exercise, the generated `+1` relation could therefore make A fire.
Unlike the CV0 threshold-100 target, A then traversed the v2 boundary fixture's
ordinary `A -> Anchor` link. That additional lawful delivery advanced the
quiescent body's clock from the assumed consequence age 2 to age 3. The
evaluator subsequently attempted to admit its Modulatory input at the now-past
age 2, and the substrate correctly rejected time travel.

The fixture accidentally allowed the training target to execute before the
consequence schedule. The candidate generator, J0 lifetime, signed variation,
and physical clock behaved as implemented.

## Boundary

- No organism law changed after observation.
- No result artifact was relabeled or reconstructed.
- No RS2 repair/rerun occurred.
- CE1, FD2 v2, and unchanged ARC A2 did not start.

A fresh RS2 v3 protocol may separate training from recurrent probe dynamics,
for example by using a subthreshold training target and an independently
frozen probe impulse/coupling geometry. It must preserve signed candidate
generation, consequence selection, and every RS2 hard control without moving
the consequence after observing a run.

