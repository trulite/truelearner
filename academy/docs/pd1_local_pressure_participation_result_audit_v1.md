# PD1 local pressure-participation result audit v1

Status: development-positive. No authority, oracle, PD2, or ARC claim.

Frozen candidate: `95db83abc0a2ce2f58f89e6f64bff8cd2851e057`, tagged
`pd1-local-pressure-participation-frozen-v4`.

Definitive development execution: fresh E2B worker
`ir1tvlzzyutftsaozun1c`.

## Result

- physical cases: `200/200`;
- Reference/Production rows: `400/400`;
- exact same-mechanics replay runs: `800/800`;
- exact Reference/Production observations: `200/200`;
- every one of ten frozen families: `20/20`;
- maximum PhysicalWork: `24`;
- natural quiescence: every run;
- structural proposals: zero;
- evidence matrix rows including header: `401`.

The evidence hashes are:

```text
matrix.csv ee04f3553bbd329e1c96e72a6e85a44a5fba0ab9b56960d28e2039e88dd6dfca
report.md  acacdd6d8802406e22efe71369245ba6514f37e5b890d496b638e67e2069f60b
```

## Decisive observations

At phase zero, one traversal at tick 9 left one full participation impulse.
At pressure tick 10, physical relaxation left `4026531840`; pressure absorbed
that magnitude, leaving no participation and a residual local pressure load of
`268435456`. Resistance remained one rather than receiving Boolean immunity.
After use ceased, later pressure removed the route.

The same-age magnitude control serialized pre-pressure participation:

```text
low     2562890625
medium  2938059318
high    4198185727
```

At the same pressure event the resulting local loads were strictly inverse:

```text
low     1892257336
medium  1540536686
high     359168177
```

Thus pressure response was graded by the actual local magnitude. There was no
shared protected/not-protected result.

The timely-consequence control survived pressure at resistance one with
remaining participation `3018850304`. Modulation at the same contact then
produced support `2830172160` and durable resistance `1 -> 3`.

The maintained two-contact chain reached tick 34 with both old eligibility
deadlines absent, both contacts live, and participation `5373062834` at each.
A consequence at tick 35 caused one ordinary QLP relay and strengthened both
contacts `1 -> 4`. The same wall-clock delay without maintenance left both
contacts dead, with zero support and zero QLP traversal.

Repeated use and repeated traverse/pressure stress kept routes provisionally
alive while participation continued, produced no durable support or resistance
gain, and ended in physical deallocation after use stopped. Wrong-path activity
could not affect the candidate.

Tickwise advancement and large jumps across pressure epochs produced identical
future-causal state, durable body, clock, PhysicalWork, and subsequent behavior
in all 20 time-partition cases.

## Classification

PD1 is a positive development result for one exact candidate family:

```text
pressure
    x continuously relaxing path-local participation
    -> transient local load exchange

qualified Modulation
    x remaining local participation
    -> bounded durable resistance gain
```

`eligible_until` remains stored and observed, but candidate pressure never
consults it and its expiry supplies no pressure. PD1 therefore does not yet
claim its deletion. That question belongs to PD2.

No ARC input, task timing, authority evidence, oracle change, or `arch.md`
change occurred.

## Provenance and technical stops

- reusable formatting/check/Clippy worker: `it48kiw54nkrebe6zl821`;
- sole completed matrix worker: `ir1tvlzzyutftsaozun1c`;
- pre-evidence static stops: `i9xsfarmjrom1hol3glap` and
  `iw7y93g11tvku9625z8wl`;
- reusable archive-audit stop: recorded in the implementation audit.

The two fresh stops occurred before evaluator execution. The completed matrix
executed exactly once. No Rust or project program ran locally.
