# RS2 learned inhibitory topology protocol v6

Status: frozen before evaluator change or physical execution.

Parent: RS2 post-CK0 immutable negative `b1c020b`.

## Classification carried forward

The post-CK0 matrix established exact checkpoint continuation, renaming,
replay, and Reference/Production equality. Its only failures were two stale
pre-WS0 observer assumptions:

1. continuing recurrence was required to trip an artificial observation-
   ceiling marker;
2. positive re-execution was counted through ordered `Deliver` events that
   SI0/WS0 replaced with wave-level Drive incidence.

No organism failure or new physical question was observed.

## Permitted evaluator-only repair

The runtime must remain byte-identical at
`078cf11b3082cade5640b42abfcf52496faf3b36e0c0af10abefa7a9d75992de`.

The evaluator may only:

- classify uninhibited recurrence from non-quiescence plus continuing firing
  activity at both recurrence junctions, without consulting the safety-
  ceiling marker;
- count positive re-execution through WS0 `DriveIncidence` observations at
  the target, together with the already-required selected-contact firing;
- rename its output path, report heading, assertion, and sentinel to v6.

The ceiling marker and obsolete ordered-Deliver count may remain diagnostic
but have no acceptance role.

No world, seed, root, phase, identity mapping, topology, training/probe split,
timing, learning law, mechanics configuration, work ceiling, or other
acceptance predicate may change. The matrix remains nine families, two roots,
ten clock origins, both mechanics, `180` cases, and `360` rows.

## Execution and stop rule

After one evaluator-scoped formatting/check/strict-Clippy pass, the complete
matrix executes exactly once in a fresh E2B worker. Any remaining predicate,
representation, continuation, replay, or quiescence failure freezes v6
negative without repair or rerun.

Only a complete positive permits the existing sequential prefix:

```text
RS2 v6 -> CE1 -> FD2 v2 -> unchanged frozen ARC A2
```

Authority, oracle status, `arch.md`, and the Academy curriculum remain
unchanged.
