# CORE1-E13 history composition result v2

## Outcome

Stopped negative at the first uncovered frontier. The established
history-sensitive deterministic affordance principle removes E13's simultaneous
signed-cancellation wall, but the cumulative profiles do not yet retain a
learned contextual action route.

No physical-runtime or learning-law byte changed. No chooser, RNG, winner,
preference, reward, or ARC execution was introduced.

## What the reused principle solved

From the same complete post-prime state, a fixed ordinary signed incidence
suppresses one physically separate contact while the other receives its normal
traversal. Across every completed A/B row:

- the generation primes emit no action;
- the negative-material control emits no action;
- the positive-material continuation emits exactly the intended action from
  `[1, 4, 2, 3]`;
- every completed world is naturally quiescent;
- Reference replay is exact;
- completed Reference/Production observations are exact.

E13's original `+/- expressed together -> cancellation -> no action` diagnosis
is therefore resolved by composition of already-established physics.

## First remaining breaks

### CORE1-A

All four intended actions execute and each consequence admits one Modulatory
delivery, but plasticity updates remain zero. No fresh context re-executes.

```text
action                         yes
ordinary consequence          yes
Modulatory delivery           1
plasticity updates            0
fresh re-execution            no
```

Reference, replay, and Production agree exactly on all four rows.

### CORE1-B

All four intended actions execute. Each consequence admits one Modulatory
delivery and two plasticity updates, but the generated multi-link route is no
longer present after consequence/cleanup and no fresh context re-executes.

```text
action                         yes
ordinary consequence          yes
Modulatory delivery           1
plasticity updates            2
fresh re-execution            no
```

Reference, replay, and Production agree exactly on all four rows.

This localizes the first retained-capability frontier to consolidation of the
whole generated contact route. Existing consequence reaches and changes
material, but does not preserve an executable `source -> contact -> motor`
structure.

### CORE1-C

The sole definitive command stopped before writing the first CORE1-C row.
After the two fixed primes and refractory-recovery tick, GenericActivity had no
live separated positive/negative contact alternatives at the history point:

```text
positive=false
negative=false
contacts=0
```

This is a second, later-profile structural-variation/lifetime frontier. It was
not repaired inside the spent gate.

## Evidence boundary

- protocol v5: `3aaba45`
- frozen evaluator/Academy integration: `11365e5`
- core runtime source remained unchanged; SHA-256:
  `6fd9bc810677ccfd5935e1344da9706a44b64f8a1bbf95c64679ec3a938c9b95`
- completed rows: `24` data rows (`12` CORE1-A, `12` CORE1-B)
- matrix SHA-256:
  `1c4be87f681a41b77e00c8ea33c2cf1cfc50df3148eb6b714cccf1098b798ae7`
- streamed summary SHA-256:
  `33525180743ffe6d612ef3f85d83676a4dd9a2ad105cf05b501a109d7d45b0fc`
- definitive process exit: `101`

The command was executed once. The marker
`CORE1_E13_HISTORY_COMPOSITION_V2_EVIDENCE_SPENT` was emitted before execution.
No retry, rescue, E14, or ARC run occurred.

## Scientific status

```text
CORE1 A/B/C E0-E12       retained positive
E13 action expression   composition positive
E13 route learning      stopped negative
E14 ARC                  not reached
physics authority        unchanged
```

The next question is not affordance choice. It is whether already-existing
local consequence closure can be developmentally composed across and retain
every link of topology that generic variation created. That question was not
answered or patched here.
