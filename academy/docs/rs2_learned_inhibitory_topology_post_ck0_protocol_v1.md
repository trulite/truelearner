# RS2 learned inhibitory topology post-CK0 protocol v1

Status: frozen before evaluator relabeling or physical execution.

Parent: CK0 development-ready `c0bb76e`
(`ck0-junction-checkpoint-integration-development-ready-v2`).

## Question

Can the organism generate, select, retain, and later use inhibitory topology
without the architect supplying where inhibition belongs, now that CK0 has
integrated J0 junction lifetime into checkpoint restore?

## Frozen science

This is an evidence retry of the exact consolidated RS2 science frozen by
`rs2-learned-inhibitory-topology-consolidated-protocol-v1`. Nothing in its
worlds, training/probe separation, mechanics, observations, predicates, or
limits may change.

The matrix remains:

- nine frozen families;
- two disjoint roots;
- ten pressure-independent clock origins;
- Reference and Production mechanics;
- `180` cases and `360` rows.

Its ten predicates remain exactly the consolidated predicates: consequence-
selected sign, renaming invariance, insertion-order invariance, no-consequence
negative, irrelevant-negative control, disconnected and untraversed controls,
active learned inhibition, Reference/Production wave-normalized equivalence,
and exact replay/quiescence.

## Permitted delta

The canonical runtime and RS2 evaluator logic are frozen. The evaluator may
change only:

- its result directory;
- its report heading;
- its assertion/success label.

CK0 is the sole cumulative runtime delta since the consolidated negative. Its
validated checkpoint rule makes J0 dead junction state restorable without
treating dormant CELL resistance as liveness authority. J0 and CV0/J0+SV1
already replayed exactly on this parent.

## Execution and stop rule

After one targeted formatting/check/strict-Clippy pass, the complete RS2
matrix executes exactly once in a fresh E2B worker. Any scientific,
representation, continuation, replay, or quiescence failure freezes this
result negative without repair or rerun.

A complete positive alone permits the existing sequential prefix:

```text
RS2 -> CE1 -> FD2 v2 -> unchanged frozen ARC A2
```

Authority, oracle status, `arch.md`, and the Academy curriculum remain
unchanged.
