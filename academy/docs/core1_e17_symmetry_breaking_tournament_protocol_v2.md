# CORE1-E17 — Local Symmetry-Breaking Tournament Protocol v2

## Status

Fixture-only correction to stopped-negative v1 `b0b1556`.

## Sole change

The experimental internal-continuation boundary restores the selected
context-trace input already present in the frozen E15/E16 action fixture:

```text
t       selected candidate source
t + 1   selected context trace
t + 1   nonselected material suppression
t + 2   motor assist
```

This lets the existing return relay receive its ordinary three physical
contributions: context trace, motor execution, and later returning consequence.

## Frozen remainder

Protocol v1 remains binding for:

- V/R/D/T candidate laws;
- tiny gate and scale rows;
- all contexts, identities, permutations, lifetimes, delays, thresholds, and
  action routes;
- two fixed consequence opportunities;
- PQLC and material learning;
- zero control, measurements, success predicates, ranking, prohibitions, exact
  replay, Reference/Production equality, and natural quiescence.

No candidate-law repair is permitted. In particular, route lifetime is not
extended or reset. If R/D/T still lose later alternatives, that is a valid
scientific boundary under the frozen E16 body.
