# PD1 local pressure-participation handoff v1

PD1 is development-positive and frozen.

## What has been established

Ordinary pressure no longer needs the old rectangular eligibility shield in
the PD1 candidate. The same continuously relaxing, path-local participation
already used by CPC1/PQLC can interact with pressure as a physical quantity:

```text
recent participation absorbs pressure continuously
residual pressure remains as local load
complete load quanta erode durable resistance
```

This produces graded response, provisional viability during actual use, and
eventual death after unsupported use stops. Use alone never creates durable
strength. Qualified local Modulation converts remaining participation into a
bounded durable resistance gain.

The result survived every pressure phase, two fresh identity roots, Reference
and Production mechanics, exact replay, wrong-path controls, a maintained
PQLC chain, and host time-partition changes.

## Immutable result

- `200/200` physical cases;
- `400/400` mechanics rows;
- `800` total executions in `400/400` equal original/replay pairs;
- all ten families `20/20`;
- maximum PhysicalWork `24`;
- exact mechanics equivalence and natural quiescence.

See the [result audit](pd1_local_pressure_participation_result_audit_v1.md),
[protocol](pd1_local_pressure_participation_protocol_v1.md), and
[implementation audit](pd1_local_pressure_participation_implementation_audit_v1.md).

## Boundary

PD1 does not delete `eligible_until`, replay ARC, change authority/oracle
status, or update `arch.md`.

The next scientific gate is PD2:

> With pressure, Modulation, and PQLC now all operating from continuous local
> participation, does `eligible_until` retain any future-causal role at all?

PD2 should first inventory every remaining read/write/checkpoint use, delete
only the redundant machinery, and require unchanged PD1 plus retained
CPC0/CPC1/PQLC behavior before returning to the frozen ARC A2 world.
