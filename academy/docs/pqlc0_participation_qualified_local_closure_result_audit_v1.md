# PQLC0 participation-qualified local closure result audit v1

Status: development positive. No authority or retained-law promotion.

Frozen implementation: `6b1d33c`, tagged
`pqlc0-participation-qualified-local-closure-frozen-v1`.

Immutable evidence: `5ae6aad`, tagged
`pqlc0-participation-qualified-local-closure-positive-v1`.

## Result

The sole frozen matrix completed:

```text
physical cases                              200 / 200
mechanics rows                              400 / 400
same-mechanics exact reconstructions        800 / 800
Reference/Production ordered-history match  200 / 200
world-complete signature                    10 / 10 true
maximum cycle work                         1250 / 4096
maximum cycle QLP traversals                 310
natural quiescence                            true
static prohibitions                           pass
```

Every case passed under two fresh identity roots and all ten pressure phases.
Reference and Production observations were exactly equal before predicates
were evaluated.

## Physical finding

An ordinary Modulatory arrival can be locally transduced one step through an
explicit QLP ARROW when and only when its source compartment still contains
participation on at least one live outgoing Drive contact.

The positive one-hop world produced exactly one QLP traversal. Its ordinary
Modulatory delivery reached the adjacent contact, where unchanged CPC1 local
participation produced support.

The negative worlds produced zero QLP traversals when:

- the required contact never participated;
- participation had continuously relaxed to zero;
- only a different contact or nearby structure participated;
- only unrelated activity occurred;
- the forward episode occurred without consequence; or
- ordinary Drive arrived without Modulation.

In the two-upstream world, the qualifying compartment emitted through both
explicit QLP outputs. Closure continued only at the destination whose own
contact had participated. The other destination stopped locally. This is the
frozen coarse physical claim: the signal carries no route identity, and every
local step is independently qualified by contact state.

The contact-fanout control credited both genuinely participating outgoing
Drive contacts. PQLC0 therefore does not claim finer resolution than the
ordinary CELL/ARROW topology supplies.

## Cycle finding

The deliberately cyclic delay-one QLP topology naturally quiesced without any
PQLC0 attenuation, trace consumption, TTL, refractory state, cycle detector,
or special stop rule.

Across every root and pressure phase, the cycle produced exactly `310` QLP
traversals, ended at physical tick `311`, and consumed `1250/4096` physical
work. The unmodified CPC1 participation state relaxed between successive local
interactions until neither compartment qualified another traversal.

This is positive evidence for one-hop transduction and cycle safety only. It
does not yet establish arbitrary-depth closure.

## Integrity and provenance

Evidence hashes:

```text
matrix.csv
5d9d661a872b5746b608e0a988de7aa0d29a0b9f6e30f0a2660bbb559e6116e7

report.md
a470c3366d5c2d76f89d81ddb90dfaa18fa63f444a3bc8e8f09e8366eee9571f
```

E2B:

- targeted formatting/check/Clippy/preflight:
  `ileplicyovfi2buycb8zz`;
- sole matrix execution and first static audit:
  `is6pg3hwqqfjrgmrarcg2`;
- fresh committed-artifact verification:
  `iypx5826cgiasur1dha37`.

The matrix executable ran exactly once. The later verification executed only
the frozen shell audit and checksum validation; it did not rerun Rust or any
physical world.

## Boundary

PQLC0 adds a candidate physical affordance developmentally. It changes no
pressure law, durable-learning law, old eligibility implementation, ARC,
authority, oracle, or `arch.md`. CPC2 remains an immutable stopped negative.

The next scientifically eligible gate is a fresh, separately preregistered
multi-depth repetition test using this exact same local rule. PQLC0 itself
makes no depth claim and authorizes no further change.
