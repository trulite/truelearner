# TC-DS1 continuous path participation handoff v1

Status: stopped negative; non-authoritative.

## What was established

Before the first Gate B equality failure, the frozen executable completed all
Gate A and decay assertions in process:

```text
Gate A physical cases       160
Gate A mechanics rows       320
decay physical cases        420
decay mechanics rows        840
delays characterized        0..20
```

This is useful development evidence that actual traversal can maintain an
ARROW-local, graded, continuously relaxing magnitude and that the candidate
levels agree across Reference and Production in those minimal worlds. The run
published no matrix artifacts, so this is not a completed positive gate.

## Why TC-DS1 stopped

The first Gate B case produced both preregistered stop conditions:

1. one source-local Modulatory arrival contacted and updated both A and B,
   rather than only the path whose downstream effect returned;
2. Reference and Production produced different complete physical-transition
   multisets for the feature-gated trace candidate, even though their final
   path state, body, work, clock, and quiescence agreed.

The separately frozen diagnostic confirmed condition 2 and was itself an
immutable negative.

## Preserved boundaries

- TC-DS0 remains the accepted characterization of the retained rectangular
  eligibility window.
- The `tc-ds1` candidate remains feature-gated and is absent from default
  builds.
- Default core tests passed `15/15`; default and feature strict Clippy passed.
- `eligible_until`, pressure, ARC A2-A5, authority, oracle, and `arch.md` are
  unchanged.
- TC-DS2 is not eligible from this workflow.

The next scientific question, if separately authorized later, is physical
credit-path continuity—not selection of another supplied temporal horizon.
