# RS2 learned inhibitory topology consolidated protocol v1

Status: frozen before any consolidated evaluator change or physical execution.

Parent: WS0 development-ready `a61b6eb114e83488874e062c392fd6c6594100e8`
(`ws0-complete-causal-wave-semantics-development-ready-v2`).

## Question

Can the organism generate, select, retain, and later use inhibitory topology
without the architect supplying where inhibition belongs, under the complete
Drive/Modulatory/PQLC causal-wave semantics established by WS0?

## Frozen science

This is one consolidated retry of the exact RS2 v5 science. It preserves the
nine frozen families, two roots, ten pressure-independent clock origins, both
Reference and Production mechanics, and the separate training/probe design:

1. ordinary CV0/J0 variation generates separate signed `C+` and `C-` contact
   topology;
2. an ordinary qualified consequence selects the useful signed relation;
3. the selected relation consolidates and the unsupported relation decays;
4. learning is frozen;
5. a separate executable recurrent probe tests whether learned inhibitory
   topology settles recurrence.

Training geometry, thresholds, couplings, delays, phases, consequence age,
identity namespaces, observation ceiling, and all control worlds remain
unchanged. This gate adds no organism law and does not alter the canonical
single-file runtime.

## Frozen predicates

1. Positive and negative sign selection follows physical consequence.
2. Identity permutation produces identical physics after logical renaming.
3. Insertion and observer-recording order are causally irrelevant.
4. No qualified consequence produces no consolidation.
5. An irrelevant negative candidate does not survive merely because its sign
   is negative.
6. A learned inhibitor disconnected from the recurrence cannot settle it.
7. A learned inhibitor that is present but not traversed cannot settle it.
8. An active learned inhibitor permits the intended traversal and the
   recurrent probe settles naturally.
9. Reference and Production are equal after causal-wave-normalized physical
   observation.
10. Same-mechanics replay and natural-quiescence/ceiling classification are
    exact.

## Event and checkpoint contracts

The observer uses the already-frozen WS0 constitution:

```text
same tick + phase + causal generation
    -> one simultaneous local incidence wave

Drive
    -> signed activation incidence

Modulatory
    -> local plastic/consequence incidence without CELL excitation

anything caused by that wave
    -> next causal wave
```

Numeric handles, insertion order, serial assignment, and recording order have
no causal meaning. Events are translated to logical world names before
renaming comparisons.

Raw checkpoint bytes are diagnostic, not an equivalence predicate. The exact
physical checkpoint contract is identical canonical durable body, physical
clock, canonical pending physical activity, and identical continuation under
the same future admitted inputs. Any genuine continuation difference is a
negative.

## Execution and stop rule

After one targeted formatting/check/strict-Clippy pass, the complete 180-case,
360-row matrix executes exactly once in a fresh E2B worker. The evaluator may
only rename its output path, report title, and success sentinel from the
historical v5 label to the consolidated label. Its worlds and predicates may
not change.

Any scientific, representation, continuation, replay, or quiescence failure
freezes this consolidated RS2 result negative without repair or rerun. No
further RS2 version is authorized inside this gate.

Only a complete positive permits the existing prefix:

```text
RS2 -> CE1 -> FD2 v2 -> unchanged frozen ARC A2
```

Each successor remains conditional on its immediate predecessor. Authority,
oracle status, `arch.md`, and the Academy curriculum remain unchanged here.
