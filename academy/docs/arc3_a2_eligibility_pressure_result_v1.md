# ARC3 A2 eligibility-aware pressure successor result v1

Status: immutable development negative caused by an observer/accounting defect.
The candidate physics is not accepted, and official ARC rows were not run.

## Focused substrate result

The shared focused worlds passed under both `MechanicalConfig::REFERENCE` and
`MechanicalConfig::PRODUCTION`:

- a traversed resistance-1 route survived the pressure epoch covered by live
  eligibility;
- modulation inside the window strengthened it exactly once;
- unsupported expiry removed it;
- dormant ordinary pressure still removed resistance-1 structure;
- late modulation produced no update;
- canonical body, clock, replay, quiescence, and causal counters matched.

## Paired Academy failure

The bounded four-context phase-0 regression produced identical causal
observations under reference and production mechanics:

- actions `[1,4,2,3]`;
- update sequence `[0,1,1,1,1]`;
- identical contexts, crossings, modulation, clocks, pressure phases, body
  fingerprints, and natural quiescence.

The row nevertheless failed because `Arc3SensorimotorObservation.physical_work`
was populated from legacy `Work::total()`. That aggregate includes
mechanics-dependent reference scanning work. The observed phase-0 values were:

```text
production  [196, 9408, 9474, 10118, 9347]
reference   [8736, 19407, 19495, 20152, 17039]
```

The permanent differential oracle has never treated that legacy aggregate as
physical equivalence. It compares the five causal counters separately:
Drive deliveries, Modulatory deliveries, local return updates, local
structural proposals, and physical deallocations. Those matched here.

## Classification

This is a measurement-boundary defect, not evidence for or against the
eligibility-aware pressure law. Academy labeled implementation-sensitive work
as physical work, violating the retained separation between organism history
and execution cost.

A fresh v2 may change only the Academy work observer to a causal aggregate of
the five already-authoritative counters. The candidate pressure law, physical
worlds, schedules, expected actions/updates, and paired mechanics must remain
unchanged. Phase 9 and official ARC evidence remain unspent.

## E2B evidence

- sandbox: `ijrbkyzlo011lkccwxi9s`;
- focused core: PASS;
- paired bounded Academy phase 0: FAIL on the `physical_work` field only;
- official phase 0/9: not run;
- broader PXR0/PX-C: not run.
