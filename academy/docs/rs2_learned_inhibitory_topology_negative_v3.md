# RS2 learned inhibitory topology immutable negative v3

Status: stopped before scientific matrix evidence.

Frozen evaluator: `b30dd29` (`rs2-learned-inhibitory-topology-frozen-v3`).

E2B one-shot worker: `i3dvlxo1ryb9eoimvfimn`.

## Exact outcome

The frozen command executed once and stopped with:

```text
physical cell identity must be unique
```

No CSV or report was published. No complete RS2 family or representation gate
was classified.

## Classification

This is an evaluator identity-allocation defect, not a scientific
counterexample.

The irrelevant/disconnected control adds an evaluator target using the
evaluator's `next_physical` counter. CV0 variation then creates ordinary contact
CELLs using the substrate's current maximum physical identity. Those generated
identities advance independently of the evaluator counter. When the evaluator
later adds its Modulatory source, its stale counter can reuse a physical
identity already assigned to a generated contact. The substrate correctly
rejects the duplicate.

Training/probe timing was not the failure: v3's threshold-2 training geometry
reached the later control construction before this identity invariant stopped
the run.

## Boundary

- organism source and laws remained unchanged;
- no duplicate identity was admitted;
- no rescue or v3 rerun occurred;
- CE1, FD2 v2, and ARC A2 did not run.

A fresh successor must reserve disjoint physical-identity namespaces for
evaluator fixtures and organism-generated variation, or derive every later
fixture identity from the substrate's actual current maximum before insertion.
It may not change training, selection, recurrence, or any RS2 predicate.

