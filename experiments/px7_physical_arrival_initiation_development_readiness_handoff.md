# PX7 anonymous physical arrival initiation development-readiness handoff

Status: **DEVELOPMENT READY; CLASS A; AUTHORITY ABSENT**.

No authority workflow was started or simulated. No definitive matrix was
executed. This lane creates no authoritative ancestor, advances no PX stage,
and does not modify authoritative PX0--PX2.

## Outcome classification

**A — existing PX0--PX2 physics sufficient.**

The classification is not between classes. A single mechanically unique
no-addition account passed PROBE `4/4`, MICRO `8/8`, and development GATE
`12/12`. There is no missing physical edge (B), unresolved alternative
mechanism (C), or new representation/substrate law (D).

Anonymous external arrival can already acquire and later activate an ordinary
retained arrival-to-execution arrow. Actual traversal leaves the frozen local
participation trace; ordinary in-window returned activity changes that arrow's
resistance and coupling; later arrival traverses the retained physical path;
the existing downstream path runs and the queue naturally drains.

## Frozen mechanism

There is **no new organism mechanism**. The sufficient organism law is the
authoritative PX0--PX2 physical substrate at:

- exact commit: `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- exact tag: `px2-physical-causal-direction-authoritative`;
- active PX0 source path:
  `crates/px0-physical-correspondence/src/lib.rs`;
- active PX0 source SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- retained substrate source path:
  `crates/frozen-organism-v1-physics/src/substrate.rs`;
- retained substrate SHA-256:
  `6aa28a76e1362ac8dfb1d33fb68807da40e7604dfdc8cca9efa1e314e3ce4263`.

The exact frozen artifact that constitutes the no-addition development result
is:

- result commit: `e91c10d0d7e25c9dbdab71520bd88d49487235f4`;
- result tag: `px7-physical-arrival-initiation-development-gate-v1-positive`;
- source path:
  `crates/px0-physical-correspondence/examples/px7_physical_arrival_initiation.rs`;
- complete source SHA-256:
  `95cca1e9b40a50d89dcd313d2650d6a75ef18c2b292b48989f66d12ae4368bf1`;
- compact block, inclusive markers
  `PX7_NO_NEW_MECHANISM_EXECUTION_BEGIN/END`, SHA-256
  `41d75cbd90687eaee43b8f6aa5e27d157781eb6a6b71bbe7b5a1aa248e23f57a`;
- hardened block, inclusive markers
  `PX7_MICRO_NO_NEW_MECHANISM_EXECUTION_BEGIN/END`, SHA-256
  `6c010caac4a87c8d5a7f852a5204bce197c9be52c0d121d00c08ea768db5ef9e`.

Those blocks add no law. They are the exact physical arrangements and calls
that demonstrate the already-existing law. The full source and both marked
blocks must remain immutable.

## Consumes physically

The sufficient path consumes these exact substrate facts, not conceptual
shorthand:

1. A live ordinary arrival CELL in region `0`, threshold `2`, with local
   position `0` and resistance `100`.
2. A live ordinary execution CELL in region `0`, threshold `2`, one physical
   position unit from the arrival cell.
3. A live ordinary boundary CELL in region `1`, threshold `1`.
4. One already-existing ordinary execution-to-boundary ARROW with delay `1`,
   phase `0`, coupling `1`, and resistance `100`. This is the downstream
   execution opportunity; it carries no start or answer meaning.
5. An anonymous external SPIKE at the arrival CELL with impulse `2`. When the
   CELL fires, frozen PX0 local variation proposes an ordinary
   arrival-to-execution ARROW because the cells are within radius `2`. The new
   ARROW begins with coupling `1`, resistance `1`, and physical delay equal to
   local distance.
6. Actual SPIKE traversal of that proposed ARROW. Firing the source writes the
   already-authoritative transient `eligible_until` state on that exact ARROW;
   this is the physical PX1 participation trace. No provenance object exists.
7. An ordinary returned external SPIKE with impulse `1` physically arriving at
   the same arrival CELL at offset `3`, inside the frozen local window `4`.
   Local return changes only live outgoing ARROW structure whose participation
   trace is still coincident: resistance gains `3`, coupling rises from `1` to
   at most `2`, and the trace clears.
8. Recurrence as repeated physical use and return at ticks `0/3`, `10/13`,
   `20/23`, and `30/33`; there is no recurrence counter or episode object.
9. Ordinary time, cell-state decay, refractory state, ten-tick pressure,
   unsupported-use pressure, ARROW generation/liveness, and physical
   deallocation from the frozen law.
10. A held-out anonymous external SPIKE, normally at tick `40` and in the
    post-gap control at tick `70`, physically reaching a declared CELL. It is
    not chosen by an evaluator from candidate events.
11. The ordinary pending SPIKE queue and its physical ordering by arrival time,
    phase, origin identity, target identity, and serial.

The causal direction is therefore actual traversal plus physically coincident
return acting on the existing local trace. It is not a typed causal-direction
input. Quiescence is the actual empty pending queue after propagation.

## Produces physically

Positive learning leaves:

- one live retained arrival-to-execution ARROW in the tested local topology;
- coupling `2`, sufficient to fire the threshold-`2` execution CELL;
- measured held-out resistance `6--9` across hardened cells, reflecting actual
  recurrence and pressure history;
- cleared transient eligibility after coincident return;
- no persistent task, request, start, event, role, or evaluator state;
- on held-out use, one arrival CELL firing, one execution CELL firing, one
  boundary CELL firing, and exactly one execution-to-boundary region crossing;
- cell state reset/decay and refractory updates from the ordinary law;
- an empty pending queue and natural quiescence;
- no autonomous arrival-source refiring.

Negative physical histories leave equally important frozen effects:

- unreturned or offset-6 late-return proposals remain coupling `1`, so the
  threshold-`2` execution CELL does not fire;
- ordinary pressure deallocates expired weak ARROW generations;
- fresh external activity may repropose a new weak opportunity, while dead
  generations remain in accounted storage (`528` bytes in compact unreturned
  and `896` bytes in hardened late-return cells);
- subthreshold or absent held-out activity produces no source, execution, or
  boundary firing and no outward crossing;
- activity at the fresh nearby locus can create weak local proposals but does
  not traverse the learned arrival-to-execution ARROW; later activity at the
  recurrent locus still executes;
- every tested finite history ends with an empty queue.

The next PX stage may consume only this actual retained graph, transient CELL
state, physical arrivals, timing, pressure, and outward crossings. It may not
consume a serialized PX7 result or a label saying initiation occurred.

## Assumptions and every additional environmental physical opportunity

The only authoritative assumption is PX0--PX2 commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129` with the two law hashes above.

The development environment additionally supplies, explicitly:

- fresh unique physical CELL and SPIKE identities in the preregistered
  `0x7a10`, `0x7b20`, and `0x7c30` namespace ranges;
- the three core CELL placements, regions, thresholds, and resistance listed
  above;
- the pre-existing ordinary execution-to-boundary ARROW listed above;
- the anonymous suprathreshold arrival SPIKE schedule;
- the anonymous in-window return SPIKE schedule or the declared no-return and
  offset-6 late-return alternatives;
- ordinary elapsed time to tick `40` or `70`;
- for specificity controls, one additional nearby ordinary CELL at the mirror
  position with threshold `2` and resistance `100`;
- for load controls, `4`, `8`, or `12` distant region-0 CELLs with threshold
  `3` and resistance `100`, plus same-tick subthreshold impulse-`1` SPIKE
  arrivals;
- mirrored positions, reversed CELL allocation, and reversed same-tick input
  insertion exactly where preregistered;
- a fixed physical environment schedule declared before execution;
- evaluator observation only after propagation, on returned execution records,
  with no mutation path back into the substrate.

There is no hidden fixture scheduling or typed construction. The fixture does
not identify a request, start, response time, task boundary, correct event, or
local path. It supplies only the listed physical opportunities. Different
control schedules are separate preregistered physical worlds, not semantic
branches inside the organism.

## Forbidden

The later port and every consumer forbid:

- any typed adapter;
- reconstruction of any old M schema;
- semantic labels or evaluator-selected local paths;
- `REQUEST`, `START`, respond-now, task/event boundaries, initiation roles or
  flags, and renamed equivalents;
- serializers, typed intermediate representations, or evaluator mutation in
  organism-visible execution;
- importing evidence or mechanisms from another parallel PX lane;
- anticipatory modification of PX0--PX2 or of these frozen blocks;
- treating outward crossing or queue quiescence as a retained semantic label.

The old M7 material remains only a frozen behavioral reference. No old M7
source, schema, mechanism, or evidence executes here.

## Cumulative unchanged-port rule

When the serial authoritative ancestor eventually reaches this lane:

1. Import **no new organism mechanism**, because Class A found none.
2. Port the complete frozen diagnostic source from
   `px7-physical-arrival-initiation-development-gate-v1-positive` byte-for-byte
   first, preserving complete source SHA-256 `95cca1e...4368bf1` and both
   marked-block hashes. Do not redesign it to anticipate the later parent.
3. Preserve all PROBE, MICRO, GATE, positive, and negative-control artifacts
   unchanged. Do not rerun their identities or reinterpret their verdicts.
4. Run a physical dependency/path audit against the later authoritative parent:
   resolve the exact CELL/ARROW/SPIKE API, topology, local eligibility trace,
   return update, recurrence, pressure/deallocation, crossing, and quiescence
   path used by the frozen blocks.
5. The first byte/API/physical-path incompatibility is the next cumulative
   collapse. Record that first incompatibility without adapting around it.
6. Resolve only that parent delta under a separately preregistered and frozen
   protocol. Make no anticipatory redesign and import no mechanism/evidence
   from parallel lanes.

If the byte-for-byte diagnostic source cannot compile before any physical cell
runs, that compile/API mismatch is itself the first parent delta; it is not
permission to edit the frozen source silently. If several scientifically
distinct resolutions appear, discriminate them or report genuine ambiguity.

## Frozen evidence and accounting

| stage | result commit/tag | artifacts | result |
|---|---|---|---|
| PROBE | `1ff0eb11229bc20ab43bc649f1f377a9417f98ac` / `px7-physical-arrival-initiation-probe-v1-positive` | `results/px7_physical_arrival_initiation_probe_v1.{csv,md}` | `4/4` |
| MICRO | `462dc3eb1231d23cd83b285fd571b67d359174fd` / `px7-physical-arrival-initiation-micro-v1-positive` | `results/px7_physical_arrival_initiation_micro_v1.{csv,md}` | `8/8` |
| development GATE | `e91c10d0d7e25c9dbdab71520bd88d49487235f4` / `px7-physical-arrival-initiation-development-gate-v1-positive` | `results/px7_physical_arrival_initiation_development_gate_v1.{csv,md}` | `12/12` |

GATE CSV SHA-256:
`78906a4c8f8d9df3f0ea43ba5427e8831f278ad6b9bd374133ac9042aa9b78c0`.
GATE report SHA-256:
`908d79dc941e5f70bce7d7ae6210a837d79ab0369ccdaa5d03b1ecd9acdf44bf`.
GATE audit SHA-256:
`c3a0ad8b81aa18a24fde1a0bd1fc37849b5e1cdbc1daf0d0d87ff46a4ba7de9d`.

Development work was explicitly serialized per cell. GATE work ranged from
`92` to `972` units and persistent storage from `208 -> 272` to
`832 -> 960` bytes, with the negative dead-generation costs preserved. Exact
per-cell work, storage, fingerprints, and physical firing/crossing counts are
in the frozen CSVs.

## Blockers and authority boundary

There is no scientific ambiguity and no missing mechanism blocker. The only
deliberate boundary is program authority: PX7 authority is absent, definitive
evidence is unspent and unauthorized, and PX3--PX8 are not advanced. Later
cumulative work is blocked until the serial authoritative parent reaches this
handoff and applies the unchanged-port rule above.
