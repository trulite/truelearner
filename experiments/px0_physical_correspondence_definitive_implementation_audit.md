# PX0 physical correspondence definitive implementation audit

Status: **AUTHORITY IMPLEMENTATION FROZEN; DEFINITIVE EVIDENCE UNSPENT; PX0 AUTHORITY ABSENT**.

## Frozen executable surface

| artifact | SHA-256 |
|---|---|
| frozen PX0/PX0-R active law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| authority-only runner | `58331e008aadfc64415e69e6b734a1b92fa6ce2ca0187beef4b40f35473ad295` |
| preregistered protocol | `a5c918cb15868506a333b06a6f9c70f7cf23f09707a08385d03ab164513d0739` |
| retained substrate physics | `6aa28a76e1362ac8dfb1d33fb68807da40e7604dfdc8cca9efa1e314e3ce4263` |

The active law is byte-identical to PX0-R positive PROBE, MICRO, GATE, and
development readiness. The authority implementation adds only an evaluator,
fixed physical fixtures/schedules, no-cell preflight, accounting, and atomic
write-once serialization.

## Matrix implementation

The runner implements the protocol's exact 16-cell matrix and twelve
conjunctive controls per cell. It uses all three initial route positions, every
opposite route transition, both allocation orders, direct/mirrored layouts,
8--15 tick schedules, two/three active-opportunity fields, fresh physical
namespaces, and alternating arrival orders.

Each primary fixture remains continuous through:

```text
initial acquisition
→ held-out execution
→ bounded absence and same-identity reuse
→ pressure to true physical deallocation
→ zero-activity no-resurrection observation
→ renewed generic proposal
→ opposite-world fresh-identity acquisition
→ fresh execution and historical-route silence
```

Absent-return, ambiguous-return, and exact duplicate fixtures are independent
blank controls. The evaluator observes Arrow identity, generation/liveness,
resistance, crossings, fingerprints, quiescence, work, proposal/deallocation
counts, and persistent bytes. It has no mutation API.

## Execution lock

- no argument or any argument other than `--preflight` or `--definitive`
  refuses with exit `2`;
- `--preflight` checks hashes, ancestry, source isolation, dependency surface,
  and absent final/staging paths without invoking `run_cell`;
- only `--definitive` can reach the matrix;
- failed preflight exits `2` before the evidence marker;
- the marker occurs once immediately before the literal `0..16` cell loop;
- final artifacts use create-new staging, file sync, non-replacing hard-link
  publication, staging unlink, and results-directory sync;
- complete PASS exits `0`; complete scientific FAIL exits `1` and is equally
  write-once.

## Pre-evidence validation

The following passed without executing a cell or creating a result:

- `cargo fmt --all -- --check`;
- focused compilation and package tests;
- strict Clippy over all PX0 targets;
- no-cell authority preflight;
- no-argument definitive refusal with exit `2`;
- empty normal dependency tree for the active PX0 crate;
- frozen cleanup and deterministic affordance-law conformance verification;
- exact source hashes and unchanged pre-existing results-tree digest
  `9dad0d2c8ddd4439aebd3aafc71665eb8d8e47dd2510a6a71e15ff9d0fd46920`;
- all four definitive final/staging paths absent.

No `PX0_DEFINITIVE_EVIDENCE_SPENT` marker was emitted. No seed, cell, fixture,
matrix row, or definitive result was executed or created. The next and only
evidence-spending command is the exact release command frozen in the protocol.
