# PX5 physical plasticity-allocation GATE v2 result audit

Status: **FROZEN POSITIVE; DEVELOPMENT READY; CLASSIFICATION A; AUTHORITY ABSENT**.

## Outcome

The focused GATE v2 emitted its evidence-spent marker exactly once and passed
`12/12` fresh cells and `144/144` claims at implementation commit `b213986`.
The GATE v1 `132/144` negative remains immutable and separately visible.

| frozen artifact | SHA-256 |
|---|---|
| GATE v2 protocol | `14ec9e1e4ec062ae5985295bd897e35fbbab70b22642e44fc59b95495683c4b8` |
| GATE v2 implementation | `bc9b951e2e043f274d3800edce59664482f3408f74898eb116707663f3e23a7c` |
| GATE v2 CSV | `24d8eb9f22a2c06edbc1c9b6a5a9cce31193fe0fc6cf61fd95cef89a4059ec29` |
| GATE v2 report | `51fad432d870c8a9831bda07615a2767b5effa0905bdff2d28812d760d84cf48` |
| unchanged PX0--PX2 law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |

The GATE v2 source delta from frozen v1 was confined to fresh protocol/artifact
identity and namespace, non-withheld recurrence through tick `140`, pressure to
tick `150`, and reacquisition/return at `150/152`.

## Hardened result

Across distractor loads `32, 64, 128`, useful counts `4, 8`, physical distances
`1, 2`, mirrored positions, reversed CELL allocation, and reversed insertion:

- generic initial proposals were exactly `useful + load`;
- useful physical sites received exactly `18` or `36` primary future return
  updates, while distractors received none;
- every hot and warm useful route remained live and executed `4/4` or `8/8`
  held-out crossings;
- every one-shot and repeatedly encountered return-free distractor was dead at
  resistance `0`;
- useful minimum resistance was `1`, strictly above distractor maximum `0`;
- matched return-free recurrence created six successive variations, zero
  return updates, and zero live structure;
- late return outside the four-tick eligibility window and topology outside the
  two-position local radius retained no route;
- fixed withholding physically removed the high-resistance stale useful ARROW
  while all other useful routes remained live, and fresh generic opportunity
  reacquired exactly one replacement crossing;
- evaluator-only allocation reversal was fingerprint-exact and causally inert;
- matched physical-return permutation left all original useful routes dead and
  retained exactly the actually returned `4` or `8` distractor routes.

All propagations naturally quiesced and all independent duplicates were exact.
The matrix accounted `1,067,970` operations and `338,560` bytes of persistent
storage. The experiment crate had zero dependencies and no old M path linked.

## Classification and narrow claim

Lane outcome is **A — existing PX0--PX2 physics sufficient**. No physical edge,
representation, substrate law, or organism-visible allocation policy was
added.

> The spatial and temporal allocation of future structural work emerges from
> the retained combination of generic local opportunity, actual traversed
> ARROW eligibility, timely ordinary returned SPIKE activity, recurrence,
> scalar resistance, ordinary pressure, and generic reproposal. Productive
> physical sites retain executable structure; distractors remain sparse; stale
> useful structure reacquires; evaluator-side allocation cannot substitute for
> the physical path.

## Authority boundary

This is development readiness only. No authority workflow was started or
simulated. No definitive matrix was executed. PX0--PX2 remain byte-identical;
PX3--PX8 remain unadvanced; no authoritative ancestor was created. The final
handoff must preserve every positive and negative artifact and state the
mandatory unchanged-port contract explicitly.
