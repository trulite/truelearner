# DS1 cumulative boundary-role de-supply definitive protocol

Status: **PREREGISTERED; DEFINITIVE OUTCOME UNSPENT**

Exact frozen development parent: `302168072f78161f520d531f8b5f3ab0150df8d0` /
`ds1-after-d3-cumulative-composition-development-handoff`.

## Question

> Does the exact cumulative learned interaction stack reconstruct boundary
> roles from anonymous activity and downstream physical consequences without
> supplied filler equality or semantic polarity on a fresh frozen definitive
> population?

No capability, mechanism, threshold, evidence channel, or frozen development
source may change.

## Frozen matrix

- seeds: `0..7`, none used by MICRO/GATE;
- acquisition: 64 blank-start episodes per seed;
- held-out: 32 fresh read-only episodes per seed;
- all four E0 situation signatures represented uniformly;
- ordinary downstream consequence law and reversed-world control;
- duplicate deterministic replay of every complete seed cell;
- cells single-threaded and independent.

Every seed must report:

- 64/64 E0 events with two actual frozen A1 roots;
- 64/64 physical D3 directions delivered to frozen DS1;
- 64/64 frozen DS1 updates;
- 4/4 divergent, consequence-supported mature patterns;
- 4/4 agreement with evaluator role on the main physical world;
- 32/32 held-out recovery with zero abstentions;
- reversed world: 4/4 patterns follow the reversed consequence law and 0/4
  retain the evaluator's original role;
- byte-identical duplicate replay.

The evaluator role remains post-acquisition scoring only. Any reachable
evaluator-role, correctness, reward, or semantic-polarity edge during
acquisition fails the matrix.

## Outcome

The definitive claim is conjunctive across all eight seeds. If every cell and
audit passes, M1 becomes the authoritative cumulative ancestor:

```text
M1
  learned/compiled filler correspondence
  + learned event/affordance/return/attribution/consequence stack
  + learned boundary roles
```

Any failed seed freezes a cumulative negative at its first stage; M0 remains
authoritative. No later scaffold may repair the result.

## Execution discipline

- implement a separate wrapper over the byte-frozen `3021680` source;
- development runner remains definitive-forbidden;
- local/E2B preflight may run formatting, strict Clippy, focused tests, and an
  audit using development seed 100 only;
- execute the frozen `--definitive` command exactly once on E2B;
- serialize only after the complete matrix finishes;
- create, never overwrite:
  `results/ds1_boundary_role_cumulative_definitive.csv` and
  `results/ds1_boundary_role_cumulative_definitive.md`;
- preserve all existing results byte-for-byte;
- download and retain the write-once artifacts unchanged;
- do not open cumulative DS2 before the outcome is frozen.
