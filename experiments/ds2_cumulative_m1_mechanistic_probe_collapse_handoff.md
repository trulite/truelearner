# DS2 cumulative M1 mechanistic probe collapse handoff

Status: **DEVELOPMENT COLLAPSE AT STAGE 3; M1 REMAINS AUTHORITATIVE**

## Frozen outcome

The exact authoritative M1 lineage passed stages 0–2:

- both definitive result artifacts and the frozen M1 sources match;
- the claim-ineligible M1 runtime audit reproduces event formation, two real
  A1 affordances, D3 direction, DS1 updates, learned boundary roles, held-out
  recovery, and reversed-world behavior;
- M1 acquisition has zero learner-visible causal/source/target annotation
  edges;
- the source contains one frozen DS1 choice call, one D3 call, one DS1 update
  edge, two physical A1 route executors, and one ordinary A1 proposal former.

The first collapse is:

> **3. selected DS1 affordance physically executes before downstream evidence**

Mechanically derived call-path evidence:

```text
selected-route execution edges in cumulative acquisition   0
selected-choice dependencies in the D3 consequence call    0
```

The A1 routes are real and executable, but authoritative M1's exact cumulative
`composition_probe` does not execute the route selected by frozen DS1. Its D3
downstream contrast is constructed from the physical-world affordance relation
independently of that selected choice. Therefore M1 currently contains the
components of an interaction but does not expose a route-contingent causal
history that cumulative DS2 may consume without adding a path.

Stages 4–8 are blocked. No isolated `LocalObservation`, position label,
intervention record, consequence packet, causal adapter, or direction learner
was introduced. No M2 exists and no definitive artifact was created.

## Interpretation boundary

This does not weaken the definitive M1 result: boundary roles still follow
downstream physical consequence rather than evaluator preference. It narrows
what M1 established. For cumulative DS2, the learned choice must first become
the physical cause of the observed aftermath; co-present choice and consequence
machinery is insufficient.

The smallest newly exposed dependency is selected-affordance actuation through
the already-existing A1 route. Any future enabling gate must connect the frozen
choice to that pre-existing executable route without inventing an operation,
consequence, evaluator direction, or causal label. That gate is not authorized
or implemented here.

## Frozen evidence

- authoritative M1: `16a1002b59bf0dbc23a6b6bf03572efca53b33ce`
- protocol: `3625564994bcafb0fe60f8f73366e573ef90444f`
- implementation: `44b9602415ad247b11fde1caddbaf47084ab7d72`
- mechanism SHA-256:
  `37542fdcefc8e1d8cf8012181be78aca366f1ecb320add62fd209d0f2c812683`
- runner SHA-256:
  `9c72b809c33cd14ecf37c2e28a1a7986e2cfa88a312fa417a6c78b20666d7d76`
- focused tests: `32 passed, 0 failed`
- local probe: pass as a valid ordered-collapse audit
- E2B probe: identical stage-3 collapse
- E2B sandbox: `i23skndsq77g8glo99ydk`, left running under the
  DS2-cumulative-specific state file
- worktree produced no DS2 result artifact.
