# PX6 physical consequence-credit no-new-mechanism GATE protocol

Status: **PREREGISTERED; GATE EVIDENCE UNSPENT; DEVELOPMENT ONLY; AUTHORITY ABSENT**.

This is not an authority workflow and cannot create an authoritative ancestor
or execute definitive evidence.

## Frozen basis

- authoritative PX2 parent:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- unchanged substrate SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- positive MICRO implementation SHA-256:
  `77c9108519fc5e4e61cfc5bd3436130a56b5dd0fa0800bb8a5caf168bde20cb4`;
- positive MICRO CSV SHA-256:
  `0444507fdb78e0a0d12bf08d80294aeb177087fe2c256a6f5dc67c2636d4a903`;
- positive MICRO audit SHA-256:
  `0480e5f1479ff0fd8a10ff39d69e1bacf7fd80e0920b3f1f8cb2e83be3fba574`.

Every PROBE and MICRO artifact, including PROBE v1 negative, remains immutable.

## Hardened 48-cell matrix

Run twelve physical worlds in four new strata, each from blank state twice for
exact replay: `12 * 4 = 48` cells and `96` developments. Every development has
24 presentation opportunities. Fresh namespaces begin at `0x6_4200_0000`.

| stratum | side spacing | candidate delay | presentation spacing | placement | allocation | insertion | distractor pairs |
|---|---:|---:|---:|---|---|---|---:|
| G0 | 44 | 1 | 20 | normal | normal | normal | 0 |
| G1 | 60 | 2 | 21 | mirrored | reversed | reversed | 8 |
| G2 | 72 | 1 | 22 | normal | reversed | normal | 24 |
| G3 | 88 | 2 | 23 | mirrored | normal | reversed | 48 |

Distractors are ordinary driver/sink CELL pairs connected by stable ARROWs and
activated once per presentation. They carry no path role and have no
connection to either competing structure.

## Twelve worlds

1. left traversal on all 24 opportunities;
2. right traversal on all 24 opportunities;
3. both traverse on all 24;
4. matched correlation and own-source return without either traversal;
5. left traversal with return physically crossing to the inactive right
   source;
6. left traversal with physical return disconnected;
7. left traversal on every opportunity and right on every fourth opportunity;
8. right traversal on every opportunity and left on every fourth opportunity;
9. left traversal for the first 12 opportunities, then right for 12;
10. right traversal for the first 12 opportunities, then left for 12;
11. left traversal with three right-side observational consequence/return
    occurrences per opportunity;
12. right traversal with three left-side observational consequence/return
    occurrences per opportunity.

Nonparticipant base opportunities always receive one matched independent
downstream occurrence. In observational-overload worlds, the two additional
occurrences arrive six and twelve ticks after the base occurrence, far enough
apart that inactive-source CELL state decays between returns. This full
environment schedule is evaluator fixture input; no schedule value enters the
substrate.

## Preregistered physical expectations

Expected final liveness and held-out outward crossing:

```text
left / left-overload                 true|false   1|0
right / right-overload               false|true   0|1
both                                  true|true    1|1
correlation / crossed / no-return     false|false  0|0
dominant and block-switch worlds       exact vectors in schedule clauses below
```

The schedule clauses are frozen mechanically as follows:

- dominant-versus-every-fourth: ordinary pressure must remove the sparse
  candidate before its next separated return, so only the every-opportunity
  side remains;
- block switch: the first block receives no subsequent eligible return and
  must deallocate under the second block's pressure; only the second-block
  side remains.

Thus left-dominant is `true|false`, right-dominant `false|true`,
left-then-right `false|true`, and right-then-left `true|false`.

Every cell serializes external participant firings, downstream and trace
firings, return arrivals, outward crossings, candidate resistance/liveness,
distractor firings, held-out crossings, quiescence, pressure/deallocation,
total work, persistent bytes, fingerprint, and duplicate equality. The
observationally overloaded nonparticipant must never retain or execute.

## Anti-smuggling boundary

Only unchanged CELL/ARROW/SPIKE substrate state executes. No old M source or
state, typed adapter, reconstructed schema, semantic outcome, evaluator-chosen
local learning path, hidden boundary, route attribution, or return ownership
may enter organism-visible execution. No mechanism or evidence from another
parallel lane may be imported.

## Pass and stopping rule

All `48/48` cells and every serialized clause must pass. A negative is frozen
without rescue or rerun. A positive establishes development readiness only and
requires an explicit unchanged-port contract. It does not authorize any
authority workflow or advance PX3--PX8.
