# CJ0 Arm A coincidence-threshold CELL PROBE v1 protocol

Status: **PREREGISTERED; DEVELOPMENT EVIDENCE UNSPENT; NO CANDIDATE**.

## Frozen start and exclusions

This isolated arm starts exactly at commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tagged
`px2-physical-causal-direction-authoritative`. The authoritative
`crates/px0-physical-correspondence/src/lib.rs` is SHA-256
`3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`
and may not change.

PX3 remains the frozen Class-D material negative at
`873094497ff6eb74363191dc5edc479c7d66de72`. PX3-R direct trace coupling,
anonymous shared-CELL recruitment, and downstream convergence remain the
immutable negatives ending at, respectively,
`26aa795377c47ecf6fd28232865d5404408b6df9`,
`82c0433329cf85bf3fe261661acd033011000656`, and
`5feb9b4c4755ed40d58ffc9cb8769d5523ea46f0`. This protocol does not rerun,
reinterpret, rescue, import, or modify any of them. PX4--PX8 remain parked and
PX-C remains blocked.

Only fresh arm-specific protocol, sidecar example, result, and audit paths may
be added. There is no definitive or authority execution in this arm.

## Candidate law under test

The candidate adds no substrate law or persistent variable. It uses only the
authoritative integer CELL integration, one-unit decay per elapsed tick,
ordinary ARROW propagation, four-tick eligibility, returned-activity
strengthening, coupling plasticity from `1` to at most `2`, ten-tick pressure,
generation invalidation, local proposal, and natural queue drain.

Each anonymous conjunction site contains two ordinary source-port CELLs, one
ordinary threshold-`3` convergence CELL, and one outward effect CELL. A source
port has one ordinary local coupling-`1` opportunity ARROW to the convergence
CELL. When the convergence CELL fires, ordinary return ARROWs deliver activity
to both source ports and an ordinary outward ARROW reaches the effect CELL.
The convergence CELL, not a relation record, is the only conjunction
operation.

The complete field contains all six anonymous two-port sites over four
physical contributor populations. Sites are separated beyond local proposal
radius. Every occurrence of a contributor fires all three of its physically
wired source ports. The field contains no pair identity, pair key, member
list, Event, Group, semantic selector, evaluator-selected update, or hidden
reset. Evaluator letters and site incidence are external descriptions of
fixed numeric CELL/ARROW matter only.

The threshold-`3` construction is the strongest existing-physics candidate:

- one contributor supplies mature impulse `2 < 3`;
- two fresh contributors supply `1 + 1 = 2 < 3` on their first occurrence;
- a second consecutive genuine conjunction can use ordinary transient CELL
  state to reach threshold `3`, fire, return, and retain both incident paths;
- after full deallocation, the same two ordinary occurrences can in principle
  bootstrap a changed site without a mature higher-order firing first.

Threshold `2` is algebraically dominated because a fresh crossed pair already
fires. Thresholds above `3` require at least as much recurrence. Seeded
coupling-`2` threshold constructions are excluded from execution here because
their post-deallocation coupling-`1` bootstrap failure is already preserved in
the immutable PX3-R shared-CELL arm; it is not rerun or rescued.

## Exact PROBE matter and schedules

Two fresh deterministic replicas are run from complete blank state and each
is repeated exactly for complete-state equality:

1. primary allocation, ascending physical identities, normal port placement;
2. reversed site allocation, descending physical identity blocks, mirrored
   port placement, and reversed same-tick external insertion.

Replica namespaces are `0xa310_0000_0000` and `0xa320_0000_0000`. Six site
centres are separated by `100`; source ports are at centre `-2` and `+2`, the
threshold-`3` CELL is at centre, and the region-`1` effect CELL is remote.
Every source port and structural CELL has resistance `1000`. Initial local
opportunity ARROWs have coupling `1`, resistance `1`, delay `1`; fixed return
and outward ARROWs have coupling `1`, resistance `1000`, delay `1`.

Training contains six rounds. In every round A+B occurs at consecutive ticks
`base, base+1`, C+D occurs at `base+8, base+9`, and the next base is `base+20`.
Thus A, B, C, and D each occur exactly twelve times, with equal port firing,
external activity, traversal opportunity, return opportunity, timing,
pressure, and identity frequency. Early/late allocation and insertion order
are replica-varied, not outcome-selected.

Held-out observations run on post-gap clones and never feed back. A+B and C+D
must each produce exactly one learned effect; A+D and C+B must produce zero.
Single A, B, C, and D must produce zero.

Reversal continues ordinary physical experience on the trained matter for six
matched rounds with A+D at `base, base+1` and C+B at `base+8, base+9` using the
same round spacing. It supplies no pair-change signal, invalidation call,
deletion, evaluator-selected path, or retained history. Old A+B/C+D support
must weaken or deallocate, new A+D/C+B must form and execute, and post-reversal
old use must not resurrect historically supported organization.

## Ordered claims and first-collapse rule

Each replica serializes every claim independently. The ordered candidate
claims are:

1. frozen ancestry/source/negative endpoints, parameters, namespace, and
   blank construction are exact;
2. route occurrence, external SPIKE, port firing, traversal, timing, pressure,
   and identity marginals match;
3. genuine repeated A+B/C+D participation fires and retains only those sites;
4. post-gap A+B/C+D activate learned organization while A+D/C+B and all
   one-shot singletons do not;
5. two consecutive occurrences of A alone or B alone do not organizationally
   transmit and do not preserve both incident supports as fresh conjunction
   evidence;
6. after ordinary reversal, old support weakens, new support forms from fully
   deallocated coupling-`1` proposals, new use executes, and old use does not;
7. controls and physical accounting pass;
8. complete duplicate execution is byte/fingerprint exact.

The first false ordered claim freezes the PROBE negative. No parameter retry,
timing rescue, MICRO, GATE, recursion, OR, temporal-expressivity matrix,
definitive execution, or authority claim follows a material failure. A purely
mechanical serialization or implementation defect may be corrected only
under a separately committed fresh protocol.

## Common controls

The same one-shot execution serializes:

- A alone, B alone, and B three ticks too late;
- correlation-only distractor firing without source-port traversal;
- genuine traversal with physical return ARROWs absent;
- genuine co-participation;
- repeated three-contributor ambiguity and four-contributor activity, which
  may retain every physically activated site but may not select one
  semantically;
- absent local opportunity (ports outside proposal radius);
- a weak opportunity made stale by thirty ticks of ordinary pressure;
- fresh A+D/C+B organization;
- natural quiescence, zero autonomous source refiring, exact replay, and
  finite useful recurrence without runaway.

The control schedule uses fresh identities and cannot repair the candidate.
All evaluator observations are made on clones or after the physical schedule
and have no causal path to CELL/ARROW updates.

## Serialization, execution, and disposition

The sidecar must refuse no arguments and wrong arguments with exit `2` and no
CELL execution. `--preflight` performs source/lineage/artifact/forbidden-token
checks without entering a scientific cell. After implementation is committed
and tagged, the sole evidence command is:

```text
cargo run --release -p px0-physical-correspondence \
  --example cj0_a_coincidence_threshold -- --probe
```

It emits exactly one `CJ0_A_COINCIDENCE_THRESHOLD_PROBE_V1_EVIDENCE_SPENT`
marker and atomically publishes:

```text
results/cj0_a_coincidence_threshold_probe_v1.csv
results/cj0_a_coincidence_threshold_probe_v1.md
```

Staging files use the same names prefixed by `.` and suffixed `.staging` and
must be absent before and after execution. Every independently meaningful
physical stage, work total, persistent byte count, complete fingerprint, and
claim bit is serialized. Any outcome is frozen; artifacts are never rerun or
regenerated.

A positive `2/2` result would make MICRO eligible, not authoritative. A
negative stops CJ-A and leaves recursion, OR, and the wider timing matrix not
reached. If execution reveals a need for a new persistent variable, semantic
representation, authoritative-source change, or an architectural choice, the
arm stops immediately.
