# Cumulative DS7 parent-relative dependency audit

Status: **STATIC AUDIT COMPLETE; FIRST MISSING EDGE IDENTIFIED**.

Parent: authoritative M4 at
`8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2`.

Frozen boundaries:

- DS7 target SHA-256:
  `f10f9d7b16106b6014767ff6188a6d556145ba3e5b4335e28de245c7622a7595`;
- reordered sprint policy SHA-256:
  `609dc63ab8051316703899717fc30861d7a700d0ec60f205fa6d687ad478616d`;
- authoritative M4 lifetime source SHA-256:
  `3d5659fb26ae804dee6122408f9d703ea1f226349772883075a42686ac3fd110`.

## Parts-bin audit

### P2: local structural plasticity

Frozen ancestor: `02fc1e3fe2f2ea097c307c28ee645bf6f1e5dc59` /
`p2-local-structural-plasticity`.

Reusable physical machinery:

- simultaneous activity exposes only locally adjacent endpoint pairs;
- bounded local slots open bidirectional variations;
- execution leaves an active-arrow eligibility trace;
- delayed outcome changes only arrows present in that trace;
- resource competition releases weak local allocations.

Incompatible historical machinery:

- `probation_left` is a supplied TTL;
- `consolidated` separates supplied lifetime classes;
- release branches inspect those classes directly.

Thus P2 supplies a lawful local opportunity former and eligibility path, but
its historical allocation lifecycle cannot enter cumulative M4 unchanged.

### P2.1: selective plasticity

Frozen ancestor: `85d97a90180fc290841a20a89fb6a73fcd043568` /
`p2.1-selective-structural-plasticity`.

P2.1 established that encounter-dependent gating can reduce structural work
while preserving downstream competence. Its six supplied sensory/internal /
irrelevant pair classes are forbidden by DS7 and are not reusable.

### P2.2: learned encounter representations

Frozen ancestor: `5b6ae36cbe11bed5d2491dd2e8e694e66368e9eb` /
`p2.2-learned-encounter-representations`.

Reusable learned machinery:

- recurring pre-coupling snapshots recruit encounter prototypes;
- delayed outcomes accumulate representation-relative and local-context
  evidence;
- learned value changes future admission;
- low-rate ordinary exploration keeps rejected regions revisitable;
- shuffled learned value destroys targeting while exact downstream behavior
  is preserved by the learned gate.

Incompatible historical inputs:

```text
persists_across_episodes
is_temporary_activity
has_consolidated_outgoing
has_consolidated_incoming
```

The first two are supplied lifetime meanings derived by a hard-coded branch on
`PlasticUnit`. The latter two depend on P2's supplied consolidation class.
The same hard-coded unit branch also turns sensory/internal/irrelevant kinds
into encounter features before learning. Therefore P2.2 learned its encounter
representations, but the information from which it learned them is not a
lawful cumulative DS7 input.

### Other reusable paths

- C0 (`5c8d00189593ca2f7efb47165efddf85111259f90433a016e5822b5b9578aed2`)
  supplies activity-derived eligibility without semantic path identity.
- D2 (`ac257b53e28b0bdbcfd4cbcb7ca855086d1de5812a07029f4b2405fda2a6da8f`)
  supplies differential evidence returned to an active physical trace.
- A1 (`b0a1841af3f85e725f92490b92357ddafd65289717846b5c16b85a49261e5ba1`)
  supplies local proposal competition.
- AP0 (`a33019958327b145bdb14f4386f628e2c4fd5fcca94e413736513f8b86cf78f5`)
  supplies an ordinary traversal-to-existing-proposal activation path.

These mechanisms establish reachability and delayed update. None by itself
learns where future structural work should be allocated.

## Parent-relative stage audit

```text
1 ordinary local coactivity exposes adjacent endpoints       READY (P2/AP0)
2 proposal and learned records obey M4 lifecycle             ABSENT
3 pre-proposal facts contain no supplied endpoint class      ABSENT
4 local variation leaves activity-derived eligibility        READY (P2/C0)
5 delayed outcome updates only the active variation           READY (P2/D2)
6 encounter history alters future plasticity allocation       READY IN P2.2,
                                                              NOT COMPOSED
7 cumulative M4 behavior remains exact                        UNPROBED
```

The **first missing edge is stage 2**. The historical proposal mechanism
cannot be linked onto M4 while it carries `probation_left`, `consolidated`, and
class-dependent removal. Stage 3 is the immediately following information
boundary and must be repaired in the same port before P2.2's learned gate can
lawfully execute.

## Mechanically forced port

No new cognitive representation is selected by this audit. The smallest port
is:

1. retain P2's ordinary local coactivity and bounded local opportunity former;
2. replace proposal TTL/consolidation state with the exact M4 scalar
   use/pressure lifecycle;
3. replace hard-coded endpoint-kind snapshots with facts emitted by the
   physical encounter itself: recent local activity arrival, local occupancy,
   live incoming/outgoing allocation, and scalar resistance already earned by
   history;
4. retain P2.2's encounter-prototype comparison, delayed value accumulation,
   learned admission, and exploration behavior;
5. allow the still-supplied DS8 outcome only after proposal/execution, through
   the active eligibility trace;
6. apply ordinary M4 pressure to unused proposals, prototypes, and value
   records so every new persistent DS7 allocation shares one lifecycle law.

The PROBE must test only whether this physical path exists. It cannot establish
selective-plasticity competence, cumulative readiness, or an M5 claim.

