# DS1 after C0 leak and control audit

The retry adds no organism-visible data and invokes frozen C0 read-only. All
19 frozen C0 controls pass for MICRO seed 100 and GATE seeds 100..104.

## Preserved positive chain

Per seed:

- two independently formed executable roots and two opaque handles;
- one byte-identical frozen DS1 choice;
- one selected physical route execution;
- one four-field R0 returned-evidence surface;
- one temporary C0 eligibility cell and one anonymous coupling;
- zero persistent C0 bytes and complete frozen-parent cleanup.

## Explicitly absent

- positive/negative, good/bad, correct/wrong, accepted/rejected, or reward fields;
- evaluator effect or truth filler in organism-visible state;
- stable occurrence, destination, answer, action opcode, or expected choice;
- coupling-to-update adapter or direct `apply_consequence` call;
- strength observation or held-out reconstruction after stage-8b collapse;
- economic feedback, compiler target, hidden fallback, M1, or DS2 work.

## Mechanical negative-path protection

Update, strength-observation, and held-out path inventories are derived from
separate source patterns and separate runtime counters. Mutation tests fail if
any one path is introduced while its stage is reported unavailable. Stage
8a's one coupling and zero polarity fields are counted independently from
stage 8b's zero update edges and zero runtime updates.

The definitive command exits with status 2 before harness execution. The
results tree remains byte-identical to the frozen digest.
