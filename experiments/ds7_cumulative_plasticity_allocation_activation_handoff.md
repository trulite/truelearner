# M4 -> cumulative DS7 activation handoff

Status: **CUMULATIVE DS7 DEVELOPMENT ACTIVE; NO DS7 EVIDENCE SPENT**.

Exact authoritative parent:
`8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2` /
`m4-cumulative-learned-lifetime-authoritative`.

The authoritative cumulative state is:

```text
M0  learned correspondence
M1  learned boundary roles
M2  learned causal direction
M3  learned event boundaries
M4  learned use-dependent lifetime
```

Under the frozen reordered sprint policy, the active authority line is:

```text
M4 -> DS7 -> M5 -> DS8 -> M6
   -> separately named DS4 successor -> M7 -> DS5 -> M8
```

## Active question

> Can ordinary local encounters acquire different future opportunities for
> structural change, without an evaluator-supplied plasticity target,
> proposal list, candidate class, or growth-site policy?

The intended physical path is:

```text
ordinary local coactivity
  -> local opportunity for variation
  -> active-path eligibility
  -> delayed experience from the variation
  -> learned encounter value
  -> more or less future plasticity at recurrent encounter patterns
```

All structures introduced or retained by DS7 must obey the M4 lifecycle:
ordinary use builds scalar resistance, ordinary non-use/pressure spends it,
and zero resistance removes the allocation. DS7 may not reintroduce a
probation TTL, temporary/permanent flag, consolidation lifetime class, or
special deletion operation.

Supplied request/start and finish/output conventions may operate only at the
external harness boundary. They may not identify an encounter, open a
proposal, set eligibility, rank a candidate, select a retained change, or
alter its lifecycle.

Semantic terminal correctness remains an explicit later DS8 supply. During
DS7 development it may update only a physically active delayed trace after an
ordinary proposal has already formed and executed. It may not enter the
pre-proposal snapshot, choose a site, synthesize a candidate, or determine
which local encounter is allowed to vary.

## Frozen development sequence

```text
parent-relative dependency audit
-> one-seed path-existence PROBE
-> smallest MICRO if the path exists
-> cumulative GATE if MICRO survives
-> separate definitive preregistration and authority execution
```

Mechanical collapses follow the first missing physical edge. A genuinely new
persistent representation, multiple equally supported non-semantic targeting
laws, or semantic leakage requires a scientific stop. No DS7 definitive
execution is authorized by this handoff.

