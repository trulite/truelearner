# DS4--DS8 de-supply sprint activation handoff

Status: **MANAGEMENT POLICY ACTIVE; NOT PART OF THE DS3 EVIDENTIARY CLAIM**.

M3 is now authoritative, so the separately frozen
`desupply_sprint_ds4_ds8_policy.md` enters its post-M3 operational state.

The main operational mode is the de-supply sprint:

```text
PARALLEL DEVELOPMENT
  DS4 request/start
  DS5 finish/output
  DS6 lifetime/persistence
  DS7 plasticity targeting
  DS8 semantic credit

SERIAL AUTHORITY
  M3 -> DS4 cumulative -> M4
     -> DS5 cumulative -> M5
     -> DS6 cumulative -> M6
     -> DS7 cumulative -> M7
     -> DS8 cumulative -> M8
```

Target preregistration, isolated mapping, dependency scans, PROBE, MICRO,
diagnostic arms, and GATE may run ahead in parallel. Only the cumulative
authority path is serial. Cumulative DS4 is now eligible; cumulative DS5
remains blocked until M4 is frozen, and so on.

Every lane follows the frozen autonomy instruction: resolve mechanical missing
edges without escalation, reuse frozen mechanisms, freeze each repair, and
retry. Return only for completed readiness, scientific ambiguity, a genuinely
required new representation, or forbidden semantic leakage.

This activation is a research-management decision supported by the existence
of M3. It is not an additional inference from the DS3 matrix and may not be
cited as part of the DS3 scientific claim.
