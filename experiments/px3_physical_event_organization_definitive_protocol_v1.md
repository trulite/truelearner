# PX3 physical event organization definitive protocol v1

Status: **PREREGISTERED; DEFINITIVE EVIDENCE UNSPENT; PX3 AUTHORITY ABSENT**.

## Frozen starting point

The authority workflow begins exactly at PX3 development-readiness commit
`f3db735ae76f7b6c7becdb04e0f9c3e3c081bd67`, tag
`px3-physical-event-organization-development-readiness-v1`.

| frozen artifact | SHA-256 |
|---|---|
| authoritative PX0 substrate law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| authoritative PX2 definitive CSV | `921e433e3bf358e89e3f8f288b4ab0472e9503a2a3ac25fe037a2b7f6cf6eb18` |
| PX2 definitive result audit | `7076aca03014d19040020b6bfb126e92f7d25dcac3df9cdab92de7dd7849c6fe` |
| PX2 authority handoff | `98647ab1563593e18e345cd7e5a71c4991d18b397dfe2dec71a4756106d96509` |
| frozen developed PX3 GATE source | `969042a740f92237c577d82c67399447040cb96d2c003c28100034566e30d5aa` |
| positive PX3 GATE CSV | `1c350355843ca0020a275813f1e7aa9fb36b63317375327b5bd437182d144469` |
| PX3 GATE result audit | `4e0f38d31df19fa62449340faf65c31a80b191ec0b90a0293b7fc4fe90f7f321` |
| PX3 development-readiness handoff | `92b3a557dd29e362cbefda535dc2bb0355837202d61c96afe25ac85b23203c5a` |

PX0--PX2 physics and the developed PX3 physical blocks may not change.
Authority may add only fresh fixed namespaces, four frozen evaluator worlds,
independent clause serialization, source/leak audits, refusal/preflight
surfaces and write-once publication.

All PX3 development positives and negatives remain immutable. In particular,
R2 remains the permanent oscillator boundary: a credit return may reach an
eligible source but must stay below that source's execution threshold.

## Narrow authority claim

> Reusable event organization can arise directly from overlapping physical
> participation traces and downstream physical return; mature organization
> behaves as one ordinary participant and can recursively form higher
> organization, without Event objects, contributor lists, composite flags, or
> level-specific APIs.

This claim does not assert semantic event identity, recover a member list,
require order invariance, or authorize PX4.

## Exact fresh matrix

The definitive matrix contains exactly `64` blank deterministic cells:
sixteen fresh seed namespaces crossed with four physical worlds. Seed `s` and
world `w` use namespace:

```text
0x6_5300_0000_0000 + s * 0x1000_0000 + w * 0x0100_0000
```

This base is absent from all preregistered source and evidence. No definitive
CELL, ARROW, SPIKE or namespace identity appeared in development.

Seeds rotate through the four frozen GATE strata:

```text
G0 normal allocation, forward distance-one proposal
G1 reversed allocation, forward distance-one proposal
G2 normal allocation, reflected distance-one proposal
G3 reversed allocation, reflected distance-one proposal
```

Every cell executes twice from byte/execution-identical complete state for
exact replay. Evaluator controls operate on clones and have no causal path back
into the continuing substrate.

## Four physical worlds

### W0 — full recursive organization

The physical schedule and all organism-side operations are byte-identical to
the positive GATE:

```text
AB depth-one exposures:   0, 11
XC depth-two exposures:  20, 31
YD depth-three exposures: 40, 51
final pressure boundary: 60
```

The cell begins with zero candidate ARROWs. Native proposal, completed-loop
trace attribution, unit subthreshold return, recurrence, pressure and the same
participant normalizer must yield reusable `A+B -> X`, `X+C -> Y`, `Y+D -> Z`.
It includes the frozen one-exposure-death, C-alone, D-alone, gapped-XC,
gapped-YD and context-free reuse controls.

Required final context-free execution:

```text
primitive traces A/B/C/D:          1|1|1|1
O/P/candidate/output/P trace:      1|1|1
candidate raw impulse:             2|2|2
normalizer arrivals/impulse:       2|2|2
ordinary derived traces X/Y/Z:     1|1|1
attribution/credit/proposals:      0|0|0
```

### W1 — recurrence without downstream return

AB physically overlaps twice at starts `0,4`. Uniform context permits each
weak candidate traversal to execute X, but global return is absent. The same
candidate traverses twice with total raw impulse two, receives no attribution
or credit, stays resistance/coupling one, and reaches zero under pressure.
Context-free held-out AB may traverse the still-weak candidate but must not
execute X or emit X trace. Recurrence alone is insufficient.

### W2 — return without joint participation

Two cycles broadcast the same uniform all-P background, context and global
return while no joint A+B participation occurs. No O or P fires; no candidate
is proposed or traversed; no output, derived trace, attribution or credit
occurs. Later activity alone is insufficient.

### W3 — same-path amplitude and repetition

One ordinary A path receives a strong external impulse four and later a unit
impulse in separate traversable windows. Each actual A execution normalizes to
one ordinary A participation trace. Without B, AB O/P never fires and no
candidate or X output appears. Raw amplitude or repeated use of one path cannot
manufacture distinct joint participation.

## Frozen physical mechanism

Primitive A/B/C/D outlets and derived X/Y/Z outlets use one identical PX1
normalizer:

```text
outlet -> unit local trace input
outlet -> participant-local hub -> unit trace input
trace threshold 2 -> exactly one unit participation firing
```

All three levels use one joint-stage constructor:

```text
two unit participant traces -> O(threshold 2)
O unit + uniform background unit -> P(threshold 2)
external threshold crossing at P -> native distance-one candidate proposal
candidate + uniform context -> output
P trace + output/participant trace + global return -> M(threshold 3)
M -> P impulse 1; P threshold 2
```

The output trace is both the downstream participant and the physical
completed-effect trace. No raw candidate amplitude is forwarded. The authority
executable may not add a new physical cell, arrow, coupling, threshold, delay,
plasticity rule, damping rule, trace kind or participant adapter relative to
the frozen developed blocks.

## Independently serialized claims

Every row serializes native source/trace, O/P, proposal, candidate crossing and
impulse, output, normalizer, attribution, credit, resistance, liveness,
held-out behavior, work, storage, fingerprints and replay measurements.

Ten conjunctive bits are recorded per cell:

1. `P0` frozen hashes, matrix identity, stratum and fresh namespace are exact;
2. `P1` actual primitive/derived participation traces match the physical world;
3. `P2` candidate proposal and traversal occur only after the joint P fires;
4. `P3` every output execution normalizes to exactly one ordinary unit trace;
5. `P4` attribution and unit credit require candidate and output participation
   plus ordinary return;
6. `P5` completed-loop recurrence earns persistence while one exposure does not;
7. `P6` recurrence without return and return without participation produce no
   reusable organization;
8. `P7` context-free recursion, timing gaps, strong A and repeated A preserve
   distinct-participant behavior;
9. `P8` unit credit produces zero autonomous P refiring;
10. `P9` every propagation naturally quiesces and complete-state replay is exact.

The outcome is conjunctive: `64/64` cells and `640/640` claim bits must pass.
Any false bit is an immutable definitive negative.

## Anti-cheat and isolation

Organism-visible execution contains only ordinary CELL state, ARROW
topology/coupling, SPIKE occurrences, local timing/order, thresholds,
participation traces, resistance, pressure, local plasticity and physical
return.

Forbidden active representations include Event, member, contributor,
ContributorSet, Pair, Group, composite, level, recursion depth, provenance
owner, return owner, correct/selected organization, history, episode, old M3
schema, or renamed equivalents. World names, schedule labels, expected vectors
and P0--P9 clauses are evaluator-only.

PX3 consumes the actual authoritative PX0--PX2 substrate directly. No old
M0--M3 serializer, semantic adapter, evaluator-selected update, special delete
path or execution suppression may run. The definitive arm depends only on the
authoritative substrate crate.

## Pre-evidence validation

The committed and tagged implementation must pass formatting, focused release
tests, strict Clippy, dependency/source audits, byte-identical physical-block
audit, frozen hashes, fresh-namespace audit, no-world `--preflight`, refusal
without `--definitive`, refusal for wrong arguments, and final/staging artifact
absence.

Preflight may not construct a world, propagate activity, emit the evidence
marker or write an artifact. All validation occurs in a fresh dedicated E2B
sandbox distinct from every development and prior authority sandbox.

## Sole execution and no rescue

The exact command executes once:

```text
cargo run --manifest-path arms/px3-physical-event-organization-definitive/Cargo.toml \
  --release -- --definitive
```

After validation it emits exactly one
`PX3_PHYSICAL_EVENT_ORGANIZATION_DEFINITIVE_EVIDENCE_SPENT` marker, executes
all cells literally once plus preregistered exact duplicates, and atomically
publishes PASS or scientific FAIL artifacts. No rerun, rescue, regeneration,
tuning or protocol amendment is allowed after the marker.

## Atomic artifacts

These paths are absent at preregistration:

```text
results/.px3_physical_event_organization_definitive.csv.staging
results/.px3_physical_event_organization_definitive.md.staging
results/px3_physical_event_organization_definitive.csv
results/px3_physical_event_organization_definitive.md
```

## Program consequence

PASS makes PX3 authority-ready on top of authoritative PX0, PX1 and PX2. A
separate frozen authority handoff must still record the cumulative claim and
boundaries. PASS does not authorize PX4 execution.

FAIL leaves PX3 non-authoritative and PX4 blocked. Neither outcome permits
changing authoritative PX0--PX2 or any frozen PX3 development record.
