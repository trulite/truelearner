# CJ0 Arm A coincidence-threshold CELL PROBE v2 mechanical-retry protocol

Status: **PREREGISTERED MECHANICAL RETRY; EVIDENCE UNSPENT**.

## Frozen v1 and sole correction

V1 is frozen invalid at commit `62e8774` and tag
`cj0-a-coincidence-threshold-probe-v1-invalid`. Its protocol, implementation,
single evidence marker, exit `101`, timing panic, and absent artifacts may not
be changed, rerun, or reinterpreted.

V2 changes only the mechanically impossible external schedule. Physical
matter, threshold `3`, coupling/resistance values, complete symmetric field,
two variants, authoritative law, matched worlds, ordered `P0--P7` claims,
controls, evaluator isolation, and stop rule are exactly v1.

For initial A+B/C+D training:

- first A+B weak bootstrap occurrences are at ticks `0,1`;
- first C+D weak bootstrap occurrences are at ticks `8,9`;
- later supported A+B occurrences are single uses at ticks
  `20,40,60,80,100`;
- later supported C+D occurrences are single uses at ticks
  `28,48,68,88,108`.

Every contributor therefore occurs exactly seven times and every finite queue
drains before the next insertion. A, B, C, and D retain exact frequency,
external SPIKE, source-port firing, traversal, timing, pressure, return,
effect, identity, and allocation marginals.

Reversal uses the identical relative schedule starting at tick `120`, with
A+D in the first slot and C+B in the second. The first two changed-world
occurrences remain consecutive because the fresh coupling-`1` paths do not
yet make the convergence CELL fire on the first occurrence; they are the
registered bootstrap opportunity. Every later occurrence follows natural
queue drain.

Repeated-singleton self-evidence remains exactly two consecutive occurrences
on a discarded post-gap clone. A mature singleton's first impulse is
subthreshold and drains at `t+1`, so the second insertion at `t+1` is
physically valid. All one-shot held-out and control ticks remain unchanged.

## Fresh execution and artifacts

The corrected implementation must use fresh v2 result and staging paths,
refuse invalid arguments, pass no-CELL preflight and all v1 frozen-source,
negative-endpoint, forbidden-token, formatting, compilation, strict Clippy,
and isolation audits. The sole command after implementation commit/tag is:

```text
cargo run --release -p px0-physical-correspondence \
  --example cj0_a_coincidence_threshold -- --probe-v2
```

It emits exactly one
`CJ0_A_COINCIDENCE_THRESHOLD_PROBE_V2_EVIDENCE_SPENT` marker and atomically
publishes:

```text
results/cj0_a_coincidence_threshold_probe_v2.csv
results/cj0_a_coincidence_threshold_probe_v2.md
```

Any material failed clause is a frozen CJ-A negative and stops MICRO/GATE.
Only another uniquely mechanical defect could avoid classification; there is
no scientific retry, rescue, or parameter amendment.
