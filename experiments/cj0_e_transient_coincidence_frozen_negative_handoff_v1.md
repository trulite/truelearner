# CJ0-E transient-coincidence frozen-negative handoff v1

Status: **FROZEN DEVELOPMENT NEGATIVE; MICRO FIRST-CLAUSE FAILURE; NO PX3
AUTHORITY**.

## Outcome and classification

CJ0-E stops at MICRO. The exact transient-state candidate passed PROBE and
passed the mandatory reversal and bootstrap physics at MICRO, but the frozen
MICRO result is negative because the preregistered repeated-singleton
self-evidence bit is false. GATE, recursion, derived OR, temporal-expressivity
GATE cells, definitive evidence, PX3 authority, and PX4--PX8 composition were
not executed.

The negative must be interpreted narrowly. Repeated A alone emitted **zero**
learned conjunction output, and learned output support weakened from
resistance `15` to `10`. The failing evaluator compared the substrate-wide
`local_return_updates` total and observed `33` fresh updates. Those updates
were A's next external occurrence returning to A while its three ordinary
fanout ARROWs remained eligible at the exact four-tick boundary: `11`
recurrences x `3` fanout ARROWs = `33`. The learned locus-to-output ARROW did
not traverse and did not receive support.

Thus this result does **not** establish that the learned conjunction used
itself as evidence. It establishes that the frozen MICRO control scoped
"fresh support" too broadly to distinguish ordinary contributor-route return
from learned-conjunction support. The standing discipline forbids returning
for an accounting defect. The result remains a material protocol negative,
the candidate is not claim-eligible, and this lane may not repair, rerun, or
advance it.

## Frozen lineage

| boundary | commit | annotated tag |
|---|---|---|
| authoritative frozen start | `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129` | `px2-physical-causal-direction-authoritative` |
| development protocol | `c179fd0e40ba32eb9f59e6ab67b3c162c65be09e` | `cj0-e-transient-coincidence-development-protocol-v1` |
| exact implementation | `2596ce2b9c561fd20bb4480e3258d26c0117d6eb` | `cj0-e-transient-coincidence-development-implementation-v1` |
| positive PROBE | `15ae29b042835a4baea43db6ec69bd9da1017d28` | `cj0-e-transient-coincidence-probe-v1-positive` |
| frozen-negative MICRO | `3f3163bb9d401fd469c87a8bc80bf526918ac369` | `cj0-e-transient-coincidence-micro-v1-frozen-negative` |

PX3 and PX3-R A/B/C remain immutable at their frozen branch heads. No source,
result, or mechanism from those negatives was changed, imported as authority,
or reinterpreted as positive evidence.

## Exact candidate law and hashes

The law consumes only ordinary existing transient CELL state:

```text
decay target to current tick
live_before = target.state > 0
add current physical impulse

if threshold == 1:
    use ordinary PX2 threshold/refractory firing
if threshold > 1:
    require live_before plus threshold/refractory satisfaction

if that transient-completed CELL fires:
    traverse ordinary outgoing ARROWs
    expose the unchanged local proposal operation
```

It adds no persistent variable. `live_before` exists only on the execution
stack and is derived from the already serialized scalar CELL state. No
contributor identity, relation key, member list, scenario bit, change signal,
or historical record is present.

| artifact | SHA-256 |
|---|---|
| frozen authoritative PX0--PX2 law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| frozen PX2 definitive executable | `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5` |
| CJ0-E protocol | `b10a7454ae4da2f71e53dcc9662078acc5b7a09c7c506a7d77e5159b33774a8e` |
| implementation build transform | `de69f4913e7c29379bdd13fddf8b70042d92159e59842ed0a9c2f5f9b21201b9` |
| exact generated physical source | `e64c8c915c2fbc4679d1e34ee69ecfe36e2c5ff05bdff5d7feeb5a55578bf1c1` |
| fixed construction/evaluator binary | `df8d5c3cb1bcf5d35b345268deed7f40166b2c7307250723020f5907d699c963` |
| implementation audit | `0e67c8140f1e1da6d350aadc2b28a30496b9dcd0eafc809a8500e5863d94e226` |
| PROBE CSV/report | `d2b1bc6da9f865add6eaf35a8f06adb027c491b0f20557d58c034532ed6b6a6f` / `c1b9f6c833093d4f95ad6e1748b2f59da38f7978b278090d162ccedd0a618617` |
| MICRO CSV/report | `340db47ffc46b9f07d79fc7df1069ffe3c53390eb2466b3801bc030c9753dc04` / `f5f01ebc069c2fdc4292d101a53931f6af556a9c737b853dea5329f336093143` |

The generated source diff contains only removal of an inherited crate header,
the live-state predicate, proposal permission on that same firing, and two
read-only accounting accessors. Every persistent structure and pressure,
decay, return, eligibility, traversal, generation, ordering, and fingerprint
rule is otherwise byte-derived from the frozen law.

## Physical consume/produce contract

Consumed physically:

- ordinary CELL state after existing linear tick decay;
- the current SPIKE impulse, arrival tick, phase, and target generation;
- existing threshold and refractory state;
- ordinary local incidence, ARROW delay/coupling/resistance, four-tick
  eligibility, returned activity, and ten-tick pressure;
- a complete symmetric six-site opportunity field for four anonymous
  participant routes;
- global matched output priming and return activity reaching every site
  identically.

Produced physically:

- a first arrival can leave only the already-existing scalar CELL state;
- a completing live-state arrival can fire the same CELL;
- that firing can expose an ordinary local weak output ARROW;
- returned activity can raise its resistance and coupling through unchanged
  PX2 plasticity;
- recurrence retains executable output; lack of recurrence lets ordinary
  pressure weaken and deallocate it;
- deallocated output can be generically proposed again from ordinary
  conjunction activity;
- every finite propagation naturally drains; there is no cutoff or reset.

The six-site field is complete and symmetric. Site labels and the incidence
table exist only in the external construction/evaluation binary. Organism
state contains only CELL/ARROW/SPIKE matter and numeric local state.

## PROBE result

PROBE: **PASS**, `8` rows, `17/17` clauses, `36,135` ledgered operations.

Primary and exact duplicate:

- route firing marginals: `12|12|12|12`;
- trained locus firings: A+B/C+D = `12|12`;
- mature output training firings: `11|11`;
- trained output support resistance/coupling: `15|15` / `2|2`;
- held-out A+B and C+D output: `1|1`;
- crossed A+D and C+B output: `0|0`;
- singleton output and learned-support return: zero;
- genuine crossed contemporary participation still fired its physical
  convergence locus once, without mature output;
- mirror/allocation/arrival permutation and exact replay: pass;
- every propagation: naturally quiescent.

Controls passed: too-late B, correlation without traversal, traversal without
return, absent opportunity, stale path, three-way ambiguity, four-way
co-participation, fresh identities/layout, and no runaway.

## MICRO result

MICRO: **FIRST_CLAUSE_FAILURE**, `5` rows, `14/15` clauses, `195,855`
ledgered operations.

Mandatory reversal passed in primary, exact duplicate, and mirror/permutation
cells:

- per-route firing marginals after both experiences: `52|52|52|52`;
- old A+B/C+D support: `15|15 -> 0|0`, both physically deallocated;
- new A+D/C+B support: `42|43`, coupling `2|2`, both live;
- new held-out output: `1|1`;
- old held-out output and one-use exact historical replay: `0|0`;
- no evaluator invalidation, duo-change input, or historical resurrection;
- all propagations naturally quiescent.

Bootstrap passed:

- a first unsupported weak output deallocated completely under ordinary
  pressure;
- ordinary changed conjunction proposed a fresh generation from dead matter;
- the new ARROW reached resistance `4`, coupling `2`, and executed held-out;
- no mature higher-order output fired before the fresh structure formed.

Frozen failed control:

- repeated A-alone output: `0`;
- learned A+B output support: `15 -> 10`;
- aggregate fresh local returns: `33`, all on A's three ordinary fanout ARROWs;
- preregistered aggregate-zero comparison: false.

The CSV preserves the false row and its complete/permanent fingerprints,
topology, storage, work, and quiescence. It is not corrected or reclassified
as a positive.

## Unreached stages

- GATE: refused with exit `2` because MICRO positive prerequisite is absent;
- GATE CSV/report and staging paths: absent;
- recursion `A+B->X`, `X+C->Y`, `Y+D->Z`: not executed;
- derived ordinary-ARROW OR: not executed;
- together/order/overlap/window/absence timing matrix: not executed;
- definitive and authority execution: nonexistent and unspent.

No result is inferred for those unreached questions.

## Validation, leak/source audit, work, and storage

- formatting, all-target compile, inherited test `1/1`, strict all-target
  Clippy: pass;
- no-argument and wrong-argument refusal: exit `2`;
- no-CELL preflight: pass;
- MICRO/GATE prerequisite enforcement: pass;
- atomic create-new staging and rename: pass;
- CSV shape: PROBE `8 x 19`; MICRO `5 x 19`;
- exact duplicate replay: pass at both reached stages;
- authoritative PX0--PX2 hashes after execution: exact;
- changed-path audit from the frozen start: additions only, all within the
  isolated arm/protocol/result prefixes;
- generated organism-visible forbidden whole-token scan: zero matches for
  Event, Pair, Group, member/member-list, semantic, evaluator, serializer,
  Episode, and History;
- staging paths: absent;
- total ledgered development work: `231,990` operations;
- atomic result storage: `6,118` bytes;
- all lane storage before this handoff: `64,327` bytes;
- dependencies: zero;
- broad historical suite: not run because no shared source changed.

## Blocker and port rule

The requested law is not established because the frozen MICRO conjunction is
false. The failure is an accounting-scope defect, and the discipline expressly
forbids returning for accounting. Continuing would require a new protocol or
reinterpretation of the immutable negative; neither is authorized.

No mechanism is authoritative or eligible to port as PX3. A later explicitly
authorized lane must preserve this protocol, implementation, positive PROBE,
and negative MICRO byte-for-byte; it may not silently narrow the failed metric
or cite the physically encouraging subclauses as a positive handoff. PX0--PX2
remain authoritative, PX3 remains absent, PX4--PX8 remain parked, and PX-C
remains blocked.
