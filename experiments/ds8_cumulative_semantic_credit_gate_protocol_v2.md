# Cumulative DS8 non-semantic-credit GATE v2 mechanical retry

Protocol identifier: `ds8-cumulative-semantic-credit-gate-v2`.

Status: **PREREGISTERED DEVELOPMENT RETRY; OUTCOME UNSPENT**.

Exact negative parent:
`cb9b2cede8e1078464d4b619c1687af5ca06e2d9` /
`ds8-cumulative-semantic-credit-gate-v1-negative`.

Frozen v1 result/audit SHA-256:

```text
399b725cd823d6e086f8e7cbfa098c57c73e2296798afef503fd75eabf132343
7dc966be9d9a47cabd0e6569dddac208de8b7de842061b303481e827f587f171
```

## Mechanical exposure repair

V1 gave each initially open distractor proposal two executions, producing
scalar resistance `7`. Its 28 event starts applied only six ordinary pressure
ticks before the retained-economy snapshot, leaving resistance `1`.

V2 adds exactly one `begin_event` after retained training. At completed event
count 28 this invokes the already-frozen seventh pressure tick. It adds no
encounter, execution, consequence, update, or special deletion. All removal
still occurs through M4/M5 scalar pressure at zero resistance.

## Repair-stage observability

The reacquisition mechanism remains byte-identical. V2 may report these
existing predicates separately:

```text
repair encounter admitted
repair edge executed
repair differential applied
post-age route prototype resistance
post-age route value resistance
retained proposal count
retained live route-edge count
```

No predicate may influence execution or alter the pass clause.

The same 18 cells, seeds, loads, histories, topology rotation, thresholds,
source controls, M5/DS8 mechanisms, held-out tests, shuffles, lifecycle window,
and conjunctive pass criteria remain unchanged. Run focused E2B preflight and
execute GATE v2 once in release mode. PASS permits definitive readiness; FAIL
freezes the localized first remaining edge. M5 remains authoritative.
