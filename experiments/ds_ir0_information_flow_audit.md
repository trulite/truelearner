# DS-IR0 information-flow and leak audit

## Organism-visible information

- the frozen A1 learned support map and its anonymous local templates;
- fresh E0 occurrence-relative substrate structure;
- the physical normalized effect of the currently retained route;
- the current frozen D3 normalized structural signal;
- ordinary A1 proposal, probation, installation, validation, and execution
  activity.

## Evaluator-only information

- seed and control identity;
- which control is compatible, changed, ambiguous, or layout-perturbed;
- expected counts and PASS status;
- source fingerprints and accounting summaries.

## Mechanically enforced absences

- no direction slot or source/target label reaches the learner;
- no correctness, reward, polarity, causal label, or evaluator direction;
- no direct support-count mutation or new learner/candidate type;
- no historical `observe` call after the asset freeze marker;
- no retained occurrence, opaque handle, or concrete destination;
- no reopening on compatible or absent/ambiguous structural evidence.

The one invalidation predicate is exact inequality between two already-existing
anonymous normalized physical structures. Reopening constructs an ordinary
`Learner` and uses the frozen A1 `observe`, `install`, and `execute_root`
mechanisms. The historical asset is preserved rather than edited or reacquired.

The source audit requires exactly one frozen-signal call, three existing
observation sites, one existing installation site, two existing execution
sites, one structural mismatch predicate, one local route invalidation, and one
generic reopening construction.
