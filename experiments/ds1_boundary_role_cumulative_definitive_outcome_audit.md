# DS1 cumulative boundary-role definitive outcome audit

Status: **DEFINITIVE PASS; M1 AUTHORITATIVE**

The preregistered command was executed exactly once on the frozen readiness
snapshot `94a84ae0183fb7dbf5ccf363b7dc594c2d3e878f` in persistent E2B sandbox
`i1z13kob2mux2my5zixj7`. The sandbox remains running. No local definitive run
occurred.

## Outcome

All eight claim-eligible cells passed conjunctively:

- `512/512` E0 events, two-root affordance surfaces, physical D3 directions,
  deliveries, and frozen DS1 updates;
- `32/32` main-world patterns matured to the consequence-supported role;
- `256/256` fresh held-out roles reconstructed with zero abstentions;
- `32/32` reversed-world patterns followed the reversed downstream consequence
  law and `0/32` retained the evaluator's original role;
- `8/8` complete duplicate cell replays were byte-deterministic;
- every source, lineage, matrix, and evaluator-blindness audit passed;
- every cell reported first collapse `NONE`.

The exact frozen main-world work ledger was identical per seed: D3 work
`11,456`, DS1 learner work `346`, and persistent learner state `320` bytes.

## Write-once evidence

| Artifact | SHA-256 |
|---|---|
| `results/ds1_boundary_role_cumulative_definitive.csv` | `fede145a50bc059ffcd19a26dc65763843a83b1644c89bd44a3b27e8cd7cea27` |
| `results/ds1_boundary_role_cumulative_definitive.md` | `5971c78eb2688e9fb2d31e59b8d835000ece4923b84aef57d6a1ef48f9295bea` |

Both artifacts were created remotely only after the complete matrix finished,
then downloaded unchanged. The CSV has one frozen header and eight seed rows.

## Frozen claim and lineage

> Boundary roles were reconstructed cumulatively from anonymous interaction and
> downstream physical consequences, without supplied filler equality or
> semantic polarity.

M1 is now the authoritative cumulative ancestor:

```text
M1
  learned/compiled filler correspondence
  + learned event/affordance/return/attribution/consequence stack
  + learned boundary roles
```

This advances the prefix from M0 to M1. It does not claim cumulative causal
direction de-supply; DS2 becomes eligible only as a separate preregistered port
onto this frozen ancestor.
