# PX3-R4 return-window separability implementation audit v1

Status: **FROZEN; E2B PREFLIGHT PASSED; DEVELOPMENT EVIDENCE UNSPENT; PX3 AUTHORITY NEGATIVE**.

## Frozen implementation

- source commit: `7a536d49792ef87f8814ffdeca3961ee859b3ff5`;
- manifest SHA-256:
  `175213e49046694395a7d2a781c335ce9cc93a091361ec612820cdd69a70e09d`;
- source SHA-256:
  `88425c48a141c3dea5177c2581f35506a215c23166c5b90224cd4cf506fa6986`;
- protocol SHA-256:
  `6be667083059847cbe8a7e1085b021e5b0e6909ecaf43c1d9fa293e4425894ac`;
- execution-protocol SHA-256:
  `ad8a1af9983f3f9d589c9bf552c086a3edd3de9ed1a1f0ee1b2177748bb249d7`.

The executable audits the authoritative PX0 law, the failed PX3 definitive
source/result/report/audit/handoff, the positive R3 result/audit, and both R4
protocols before either preflight or evidence execution.

## Unchanged physical mechanism

The diagnostic contains the same dormant three-stage physical topology as the
failed definitive arm and stimulates only stage one. It preserves:

- primitive and derived PX1 direct-plus-hub unit normalizers;
- trace-pair threshold-two opportunity O;
- O unit plus uniform P background unit at threshold-two P;
- native distance-one P-to-X proposal beginning absent;
- unit context input to threshold-two X;
- P trace plus X trace plus global return at threshold-three M;
- unit, delay-one M-to-P echo;
- the unmodified authoritative PX0 eligibility, return, pressure, proposal,
  refractory, decay and queue-order laws.

No candidate is inserted, selected, strengthened, deleted or suppressed by
the evaluator. There is no Event, episode marker, provenance field, return
kind, direction bit or semantic credit operation. Evaluator scenarios only
schedule external ordinary inputs and read public traces, crossings, work,
resistance, liveness and fingerprints.

The two insertion strata use fresh seeds `3501,3509`. The 14 frozen scenarios
produce 28 unique namespaces. The only evidence surfaces are `--r4`, the one
marker and the four write-once final/staging artifact paths.

## E2B validation

Persistent development sandbox `i6x9gykt9tvp6xfz5z8ra` validated exact source
commit `7a536d4`:

- Rust formatting check passed;
- 2/2 release static tests passed;
- strict release Clippy passed with warnings denied;
- no-argument refusal exited 2 without an evidence marker;
- wrong-argument refusal exited 2 without an evidence marker;
- all final and staging result paths were absent;
- `--preflight` printed
  `PX3_R4_RETURN_WINDOW_SEPARABILITY_PREFLIGHT_OK`.

Preflight constructed no world, propagated no activity, emitted no evidence
marker and wrote no artifact. No Rust command executed on the local host.

The next clean commit containing this audit is the exact tagged preflight
target. Only the frozen E2B `--r4` command may spend evidence once.
