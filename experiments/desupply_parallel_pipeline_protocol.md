# De-supply parallel research and cumulative-mainline protocol

Protocol identifier: `desupply-parallel-pipeline-v2`

Status: campaign orchestration frozen after definitive IP0 and before any DS1
cumulative implementation or definitive execution.

## Frozen starting point

- identity completion tag:
  `ip0-identity-prior-economics-execution-prior-advantage`;
- identity completion commit:
  `6a8ea42`;
- identity classification:
  `EXECUTION_PRIOR_ADVANTAGE`;
- current de-supplied cumulative prefix: DS0 filler correspondence;
- next authoritative cumulative gate: DS1 boundary roles.

## Purpose

This protocol separates engineering concurrency from scientific dependency.
Isolated reconstruction research may run ahead in parallel. Only one serial
cumulative lineage may advance the shrinking organism-visible interface.

```text
TRACK A: ISOLATED RESEARCH

DS1 isolated   DS2 isolated   DS3 isolated   ...
     |              |              |
     +--------------+--------------+
                    |
                    v
          mechanisms and controls ready

TRACK B: CUMULATIVE MAINLINE

IP0 / DS0
    -> DS1 cumulative
    -> DS2 cumulative
    -> DS3 cumulative
    -> Wave-I FFS reintegration
```

## Global invariants

### 1. Prefix-valid cumulative de-supply

The cumulative claim is the longest consecutive positive prefix:

```text
minimal physics -> DS0 -> DS1 -> DS2 -> DS3 -> ...
```

If DS2 cumulative fails after DS0+DS1, a later isolated or cumulative DS3
positive cannot repair or subsidize it. The authoritative prefix stops at
DS1.

### 2. Two explicit result labels

Every result must be labeled exactly one of:

```text
ISOLATED POSITIVE / ISOLATED NEGATIVE
```

The tested scaffold was removed while later and unrelated scaffolds remained
supplied. This may establish reconstruction, function, controls, attribution,
and mechanism readiness. It never advances the minimal-physics mainline.

```text
CUMULATIVE POSITIVE / CUMULATIVE NEGATIVE
```

The scaffold was tested after every earlier scaffold in the frozen order had
already been removed. Only this result advances or stops the shrinking-channel
lineage.

No report may abbreviate an isolated positive to simply `positive`.

### 3. No downstream pressure on upstream interfaces

An isolated downstream mechanism may depend on information that an earlier
cumulative gate later removes. The earlier gate must not be modified to
preserve that dependency.

Allowed:

```text
isolated mechanism
    -> port unchanged physics onto new cumulative parent
    -> compatible: continue
    -> incompatible: freeze dependency/compositional negative
```

Forbidden:

```text
retrofit upstream de-supply
    -> retain a renamed channel because downstream code expects it
```

### 4. Representation/policy de-supply, not physics

All lanes retain the frozen minimal physics boundary:

```text
persistent CELL / ARROW identity
fresh SPIKE occurrence
local temporal precedence
local directional propagation
local mutable / plastic state
ordinary environmental input / output / consequence
```

No lane may remove or reinterpret these merely to make its scaffold test
easier.

### 5. No semantic channel under another name

Leak audits must reject any organism-visible value equivalent to the removed
scaffold, including stable IDs, enum variants, correlation tokens, map keys,
canonical representations, evaluator labels, supplied target boundaries,
expected direction, expected grouping, answers, pass/fail, or economics.

## Track A — isolated research lanes

The following lanes may develop concurrently from the frozen IP0-positive
ancestor:

| Lane | Removed scaffold | Other scaffold status |
|---|---|---|
| DS1-isolated | boundary roles | later scaffolds supplied |
| DS2-isolated | learner-visible causal direction | boundary roles and later scaffolds supplied |
| DS3-isolated | event/container boundaries | boundary roles, causal metadata, and later scaffolds supplied |
| DS4-isolated | request/activation semantics | representation scaffolds supplied unless cumulative porting begins |
| DS5-isolated | finish/output semantics | representation/control scaffolds supplied |
| DS6-isolated | temporary/permanent lifetime classes | other scaffolds supplied |
| DS7-isolated | plasticity targeting/policy | consequence semantics supplied |
| DS8-isolated | semantic correctness/credit | ordinary consequence retained; other scaffolds supplied |

Current execution opens DS1-isolated, DS2-isolated, and DS3-isolated only.
DS4-DS8 remain planned and scientifically blocked from cumulative claims.

### Isolated deliverables

An isolated lane is ready for a cumulative port only when it freezes all four
parts of this handoff bundle:

1. **Reconstruction mechanism**
   - exact source commit and mechanism fingerprint;
   - persistent learned-state schema and fingerprint;
   - learner-visible evidence interface;
   - compilation and invalidation lifecycle, when present.
2. **Leak and negative controls**
   - exact frozen control matrix and observable contract;
   - source audit for forbidden channels;
   - dynamic perturbation and covert-token audits;
   - evidence that the removed scaffold was not restored by an adapter.
3. **Dependency manifest**
   - every still-supplied channel actually consumed by the mechanism;
   - every evaluator-only channel used solely to test it;
   - incidental implementation dependencies relevant to portability;
   - explicitly absent channels and the audits establishing their absence;
   - predicted risks when ported to the cumulative parent.
4. **Physical ledger**
   - acquisition, installation, mature runtime, maintenance/carrying, and
     storage;
   - work attribution by physical operation;
   - any preregistered compilation/amortization trigger.

The dependency manifest uses one row per dependency with the following fields:

```text
dependency name
class: organism-visible hard | evaluator-only | incidental implementation | absent
visibility and representation
source/use site
audit evidence
necessary for isolated mechanism? yes/no/unknown
expected effect if removed
cumulative-port risk
```

The manifest must enumerate dependencies even when their use is scientifically
legal in the isolated world. In particular:

- DS1 records every dependence on supplied filler correspondence or equivalent
  stable identity information;
- DS2 records every dependence on supplied boundary, source/target,
  consume/produce, input/output, or endpoint-role semantics;
- DS3 records every dependence on supplied boundary roles, learner-visible
  causal annotations, relation/container-adjacent grouping, or stable event
  positions/endpoints.

Silence is not evidence of absence. A claimed absent channel requires both a
source audit and a relevant dynamic perturbation or leak probe.

Within that four-part bundle, each isolated lane may produce:

1. a frozen structural ablation and allowed-evidence contract;
2. a generic reconstruction mechanism using existing physics;
3. a frozen capability anchor and exact observable contract;
4. fresh-identity, shuffled, false-positive, context, invalidation, and leak
   controls appropriate to that scaffold;
5. MICRO and GATE development validation;
6. physical work attribution;
7. a preregistered trigger for compilation or amortization, only if a repeated
   mature tax is physically present;
8. an implementation-ready isolated freeze with the four handoff artifacts.

An isolated lane may not:

- consume a definitive run without cumulative-lineage authorization;
- claim cumulative de-supply or minimality;
- alter an earlier scaffold interface;
- implement the next scaffold;
- feed economic results into organism behavior;
- add a semantic adapter to make an unavailable process look substrate-native.

## Track B — cumulative mainline

The cumulative branch has one owner and advances serially:

```text
M0 = identity-complete mainline at IP0

M0 + DS1 cumulative definitive -> M1
M1 + DS2 cumulative definitive -> M2
M2 + DS3 cumulative definitive -> M3
M3 + full sparse FFS reintegration -> Wave-I FFS outcome
```

Every `Mn` is a separately frozen ancestor containing exactly the consecutive
cumulative-positive prefix through DSn. Diagnostic descendants never replace
an `Mn` ancestor and cannot advance the prefix.

DS2 cumulative cannot begin until DS1 cumulative is frozen positive. DS3
cumulative cannot begin until DS2 cumulative is frozen positive. The port may
reuse an isolated mechanism and controls, but the cumulative implementation
must consume the actual immediately preceding frozen parent.

### Mechanical cumulative-port rule

The cumulative question is composition, not renewed mechanism search:

> Does the frozen isolated replacement operate on the immediately preceding
> cumulative ancestor without restoring an earlier scaffold or adding a
> semantic adapter?

The isolated mechanism must be ported unchanged as far as possible. Its source
commit, relevant source-file hashes, persistent-state schema, observable
contract, and control-matrix fingerprint are recorded before porting.

Permitted port changes are limited to:

- wiring the frozen mechanism to the evidence that actually exists in the
  cumulative parent;
- evaluator-side reporting, artifact paths, matrix enumeration, and accounting
  aggregation that cannot affect organism decisions;
- purely mechanical type/module-path changes proven behavior-neutral.

Any change to learner behavior, organism-visible evidence, inference,
persistent representation, proposal/consolidation rules, compilation,
grounding, invalidation, or fallback creates a new mechanism. It must not be
reported as the unchanged cumulative port. A failed unchanged port is frozen
as a dependency/compositional result before any new capability is considered.

The cumulative port may not:

- restore a removed channel under a new field, token, role, index, or adapter;
- alter M0/M1/M2/M3 merely to preserve a downstream isolated dependency;
- use evaluator truth to manufacture evidence missing from the parent;
- tune a mechanism after seeing cumulative economics or definitive output.

### Cumulative diagnostic matrix

Every cumulative port evaluates and reports these stages independently and in
this order:

```text
1. reconstruction
2. functional recovery
3. compilation / mature execution, when preregistered
4. recursive compatibility
5. whole-stack economics
```

The first failed stage is the cumulative collapse point. Later stages are
`BLOCKED`, not negative, unless they can be evaluated without crossing the
failed prerequisite. The result must compare this collapse point with the
isolated result and identify the exact isolated-to-cumulative dependency delta.

For DS1 specifically, the authoritative composition question is:

> Does the frozen boundary-role replacement compose with M0 learned compiled
> correspondence without restoring supplied filler equality or adding a new
> adapter?

### Cumulative compatibility report

For each port, report independently:

```text
isolated reconstruction/function result
cumulative reconstruction/function result
upstream learned structures actually consumed
removed channels still absent
work attribution change from isolated to cumulative
new dependency edges revealed
first cumulative collapse point
isolated mechanism and control fingerprints
permitted wiring changes made during port
isolated-to-cumulative dependency-manifest delta
```

If isolated passes and cumulative fails, record a compositional dependency;
do not relabel the isolated result negative and do not rescue the cumulative
prefix with a later scaffold.

## Three levels of allowed parallelism

### Across isolated scaffolds

Independent scaffold branches and worktrees may run concurrently. Each owns a
distinct persistent E2B state/sandbox and never mutates another lane.

### Across development work for later waves

Wave-II and Wave-III mechanisms may eventually be explored against a frozen
available ancestor. Their scientific status remains `ISOLATED` and their
cumulative claim remains blocked on the complete preceding wave.

### Across cells within one gate

Seeds, scale cells, controls, attribution cells, and leak probes may execute in
parallel. Each organism cell remains single-threaded and deterministic. Frozen
logical work counters, not wall time, remain authoritative.

## Definitive locks

Every isolated and cumulative gate keeps MICRO, GATE, and exactly one
write-once DEFINITIVE mode. Development output supports no scientific claim.

Only the cumulative-lineage owner may authorize consumption of an isolated or
cumulative definitive command. Parent definitive artifacts remain read-only
and are not rerun as generic regression unless shared frozen behavior source
changed.

## Dependency graph output

The campaign maintains three evaluator-side views:

1. isolated scaffold ledger: whether each scaffold is independently
   reconstructable, functional, compilable, and costly;
2. cumulative prefix: the longest consecutive scaffold sequence supported by
   definitive cumulative evidence;
3. learned dependency graph: edges exposed when an isolated mechanism fails or
   changes after porting to its cumulative parent.

An isolated positive plus cumulative negative is therefore a substantive
dependency result, not an engineering inconvenience.

## Current orchestration freeze

```text
CUMULATIVE OWNER
this mainline thread / research/desupply-cumulative-mainline

ACTIVE ISOLATED LANES
DS1 boundary roles
DS2 causal direction
DS3 event/container boundaries

NOT OPENED
DS1 cumulative implementation
DS2 cumulative implementation
DS3 cumulative implementation
Wave-I FFS reintegration
DS4-DS8 implementations
```

The next serial transition remains:

```text
DS1 isolated implementation readiness
    -> cumulative DS1 port onto IP0-positive M0
    -> one frozen cumulative DS1 definitive result
```
