# CJ0-D local-subunit PROBE v3 fixture correction protocol

Status: **PREREGISTERED DEVELOPMENT RETRY; PROBE v3 UNSPENT**.

PROBE v2 is an immutable negative at commit `a995fe5`, tag
`cj0-d-local-subunit-probe-v2-frozen-negative`. Nine of eleven rows passed.
The two failed rows expose one mechanically determined fixture/expectation
class; neither changes the candidate law or a scientific clause.

Authorized corrections are exact:

1. `absent-opportunity` has no live incoming opportunity and physical spacing
   outside proposal radius, so its expected local integrations are `0`, not
   `1`; its required outward/local-CELL silence remains unchanged.
2. `stale-opportunity` advances through full weak-input deallocation and then
   receives exactly one genuine joint use. Required output is zero, required
   local-CELL firing is zero, and required subthreshold local integration is
   one. Repeating it four times is forbidden because repeated ordinary joint
   activity is the separately tested bootstrap behavior.
3. Atomic PROBE paths and the report heading use fresh `v3`; MICRO/GATE remain
   `v1` unless later separately frozen.

The v2 timing-floor correction remains exact. Zero floors are required in all
positive, alternative, ambiguity, and four-route cells. Floors remain
permitted only in the explicitly too-late control where the first finite
propagation physically consumes the requested evaluator interval.

Candidate-law bytes, constants, field topology, marginals, all other controls,
tests, and stop rules are immutable. A further failure is frozen and stops
PROBE development unless it is a new mechanically unique defect authorized by
a later fresh protocol. This development protocol creates no surface beyond
PROBE/MICRO/GATE and cannot advance the lane past GATE.

