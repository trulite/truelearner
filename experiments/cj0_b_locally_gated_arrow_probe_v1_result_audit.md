# CJ0 ARM CJ-B locally gated ARROW PROBE v1 result audit

Status: **POSITIVE DEVELOPMENT PROBE; MICRO ELIGIBLE AND UNSPENT**.

## Frozen result

The sole committed/tagged PROBE command emitted exactly one
`CJ0_B_LOCALLY_GATED_ARROW_PROBE_V1_EVIDENCE_SPENT` marker and exited `0`.
Atomic artifacts are:

| artifact | SHA-256 |
|---|---|
| `results/cj0_b_locally_gated_arrow_probe_v1.csv` | `297086aaacc6b6ef1d099e4887adbe49823c65f972b9b7a63a1096cc8faa7611` |
| `results/cj0_b_locally_gated_arrow_probe_v1.md` | `eb90ba3002ff1e392802cbe32935fc7f1afa1e139a1739803c5558294c94cd1c` |

Both staging paths are absent. The CSV is 5 lines, 75 columns, and 2,151
bytes. The report is 1,060 bytes.

## Outcome

All four fresh layout/allocation/insertion variants passed all ten conjunctive
clauses: `4/4` rows and `40/40` claims.

- acquisition outward crossings: A+B `8`, C+D `8` in every row;
- trained held-out A+B/C+D: `1|1` in every row;
- crossed held-out A+D/C+B: `0|0` in every row;
- A/B/C/D singleton crossings: `0|0|0|0` in every row;
- trigger-only self-evidence candidate consumptions: `0` in every row;
- too-late, correlation-only, no-return reuse, absent, blocked, and stale:
  `0` crossings in every row;
- ambiguity: old supported output `1`, unsupported alternative `0`;
- exact replay, natural quiescence, and finite recurrence: pass in every row.

Each trained candidate remains one live ARROW at resistance `20`, coupling
`2`; each unsupported alternative has zero live ARROWs after the fixed gap.
Fresh weak coupling `1` plus current destination state `2` bootstrapped each
training occurrence. Mature coupling `2` plus absent state `0` remained below
threshold `3`, so learned structure did not become self-evidence.

## Consume/produce contract observed

At a successful inspection the law consumed positive current destination
CELL state and emitted one ordinary SPIKE carrying that state plus local ARROW
coupling. The destination fired, emitted one ordinary outward crossing, and
sent ordinary physical return activity. Only actual candidate traversal
became locally eligible. Suppressed inspections emitted nothing and created no
eligibility or structural support.

No gate flag or other persistent field was added. The organism-visible source
contains no contributor key, relation record, member list, semantic type,
logical operator, evaluator feedback, or old-M3 structure.

## Accounting and authority boundary

- ledgered physical work: `12,984` operations;
- primary retained structure after gap: `19` ARROW records and `1,792`
  persistent bytes per row;
- complete stage fingerprints and generations: serialized independently;
- authoritative PX0--PX2 files modified: none;
- PX3/PX3-R negative artifacts modified or rerun: none;
- reversal, bootstrap-after-full-deallocation, recursion, OR, extended timing,
  GATE, definitive, authority, PX3, and PX-C execution: none.

This positive PROBE licenses only a separately preregistered MICRO. It is not
an authority result and makes no claim beyond the frozen development cells.
