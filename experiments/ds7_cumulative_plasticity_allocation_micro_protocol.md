# Cumulative DS7 plasticity-allocation MICRO

Protocol identifier: `ds7-cumulative-plasticity-allocation-micro-v1`.

Status: **PREREGISTERED DEVELOPMENT MICRO; OUTCOME UNSPENT**.

Exact positive PROBE result commit:
`fa363fc5c0f8e4f56885f5165f3891de2279bf34`.

Authoritative M4:
`8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2`.

Frozen inputs:

- PROBE mechanism SHA-256:
  `7b04bdaca1aec34d563f728bfbcd2c28be07cd28e5bfc9778c69fce197c465ea`;
- PROBE result SHA-256:
  `93c11d30b9f3ebfbf6de68a315b3410275acc4031186eaa7e747a81f78a8ded4`;
- PROBE audit SHA-256:
  `b6d9f11fcb26d9702d95db989991558f3f86ca99dfe360b597b09a2b84d474f2`.

## Question

> Does the exact PROBE mechanism learn the same plasticity-allocation law
> across different encounter histories, reduce unnecessary structural work,
> and retarget through ordinary exploration when later physical experience
> changes—without a supplied site policy or lifetime class?

This MICRO may add measurement and histories only. It may not change the
physical encounter snapshot, scalar lifecycle, prototype learner, value rule,
eligibility path, admission rule, exploration period, or authoritative M4
anchor frozen by the PROBE.

## Frozen cells

Two explicit fresh blank-start seeds:

```text
21_000_000  blocked history: N rejections precede P support
21_500_000  interleaved history: N and P alternate before evaluation
```

Each cell begins with zero proposals, prototypes, value records, and
eligibility. Opaque endpoint identities and absolute local positions differ
between the cells. Only the same physical P and N trace relations recur.

## Frozen phases

1. **Acquisition.** Both P and N receive ordinary local variation while the
   blank learner is broad. Active delayed traces support P and reject N.
2. **Economy evaluation.** Present 64 fresh locally active encounters, 32 P
   and 32 N, without changing learned value. Compare proposal admissions with
   an always-open local-plasticity baseline over the identical encounters.
3. **Value shuffle.** Swap P/N learned values without changing physical
   histories; the learned allocation preference must reverse.
4. **History reversal.** N now receives supporting delayed experience only
   when ordinary exploration admits it. Continue the fixed P/N encounter
   stream until N becomes positively valued or 96 N opportunities have
   occurred. No evaluator may force an N proposal.
5. **Post-reversal evaluation.** Fresh N must be admitted directly without
   waiting for exploration. P must remain admitted.
6. **Lifecycle pressure.** A one-off third encounter pattern and its proposal,
   prototype, and value record must disappear under ordinary M4 pressure;
   recurrent P/N structures must remain allocated.
7. **Cumulative anchor.** The unchanged M4 cell for the same seed must pass
   with lifetime vector `[1, 3, 6, 13, 27]`.

## Conjunctive cell criteria

Each cell passes only if:

- P and N proposals both arose before their first delayed outcome;
- active-trace-only updates produced positive P and negative N value;
- learned evaluation admits all 32 P encounters and no more than one of 32 N
  encounters (the one allowance is deterministic exploration);
- learned proposal admissions are at least 45 percent lower than the
  always-open baseline;
- shuffling learned values reverses P/N admission;
- after history reversal, N reaches positive value solely through ordinary
  exploration within 96 N opportunities and is then directly admitted;
- recurrent P and N allocations survive, the one-off allocation disappears,
  and its stale edge cannot execute;
- fresh identities and changed absolute layout preserve all comparisons;
- the source/information audit and exact M4 anchor pass.

The two-cell report must be duplicate-exact.

## Interpretation

PASS establishes development readiness for a cumulative DS7 GATE. It does not
establish M5 or authorize a definitive run.

FAIL freezes the exact failed phase. Mechanical absence may be repaired at
that edge under a new protocol. A new representation, semantic site choice,
or multiple equally supported allocation laws is a scientific stop.

