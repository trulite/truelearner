# PX3-R Arm C durable downstream-convergence frozen-negative handoff

Status: **FROZEN DEVELOPMENT NEGATIVE; MICRO FIRST COLLAPSE; PX3 AUTHORITY ABSENT**.

## Classification

Arm C is a **frozen negative**, not a positive candidate and not unresolved
ambiguity. The preregistered symmetric downstream-convergence opportunity
passed the mandatory matched-marginal PROBE, but failed the mandatory
swap/relearning clause at MICRO. GATE, definitive evidence, an authority
matrix, PX3 advancement, another arm, and PX4 composition were not executed.

The result discriminates this exact opportunity mechanism. It does not claim
that every possible new substrate law for reusable co-participation is
negative.

## Exact ancestry, mechanism, commits, tags, and hashes

- frozen start:
  `873094497ff6eb74363191dc5edc479c7d66de72`, tag
  `px3-physical-event-boundaries-frozen-negative-handoff-v1`;
- authoritative PX2 ancestor:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
  `px2-physical-causal-direction-authoritative`;
- first-collapse commit:
  `caff303a5d7e7b4603a49e760fd236a70a41b0a4`, tag
  `px3-r-c-downstream-convergence-first-collapse-v1`;
- protocol commit:
  `aad528a52cdcd96b261dfb6c812f2f6526cd1bf1`, tag
  `px3-r-c-downstream-convergence-development-protocol-v1`;
- exact mechanism commit:
  `42dd2daf89019ba12042fc37ba325e81545fee70`, tag
  `px3-r-c-downstream-convergence-development-implementation-v1`;
- mechanism source:
  `crates/px0-physical-correspondence/examples/px3_r_c_downstream_convergence.rs`,
  SHA-256
  `8f8f8714af17d4de70e5c21660d8a5016a703ac872a131b564a6300f8aa47a31`;
- organism-visible block SHA-256:
  `088f8c0e20d3041272b3d1c30dc2e7f26190b8ee95df992c4f16d79d207eabc0`;
- positive PROBE result commit:
  `3ede0e57575070215eb83facbb8b2939fb34e1d8`, tag
  `px3-r-c-downstream-convergence-probe-v1-positive`;
- frozen MICRO negative commit:
  `57044c1aa3261ff86c8d49911c4738fb8250b61e`, tag
  `px3-r-c-downstream-convergence-micro-v1-frozen-negative`.

The authoritative PX0-PX2 law remains SHA-256
`3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`.
The frozen earlier PX3 no-addition source/result hashes remain exact.

## Physical inputs consumed

Every executed cell consumed only:

- four independently acquired copies of the PX2 route motif: arrival,
  correspondence, continuation, consequence, participation trace, return hub,
  outward crossing, and weak learned direction;
- actual external arrival SPIKE sets at route source thresholds and actual
  participation-driver SPIKEs;
- actual continuation/consequence traversal, participation-trace firing, and
  returned SPIKE arrival within the frozen four-tick local window;
- frozen `+3` local return strengthening, coupling change up to `2`, ordinary
  ten-tick pressure, unsupported-use pressure, CELL decay, refractory state,
  deterministic arrival order, generation checks, region crossing, and
  natural queue drain;
- four ordinary threshold-`3` downstream continuation CELLs;
- sixteen ordinary edge-local threshold-`2` approach CELLs;
- strong route-continuation-to-approach ARROWs, weak approach-to-continuation
  ARROW opportunities, downstream-driver ARROWs, continuation-to-approach
  returned ARROWs, and ordinary outward ARROWs;
- fresh namespaces, layout/allocation/arrival variation parameters, and inert
  distractor CELL activity where preregistered.

There was no autonomous source refiring and every finite propagation was
naturally quiescent.

## Explicit environmental opportunity

The sole Arm C opportunity was a complete symmetric `4 x 4` Cartesian field
of resistance-`1`, coupling-`1` weak ARROWs from edge-local approaches to four
ordinary downstream continuations. Before each acquisition occurrence, the
environment exposed the full field and replaced only missing live weak
opportunities. Existing live ARROWs were never reset or topped up.

Each route received identical opportunity. Replenishment accepted no active
route, active continuation, scenario label, trained/crossed expectation, or
evaluator result. Contemporary external activity supplied one downstream
driver to the physically participating continuation during initial and swap
experience. Held-out use supplied no downstream driver and no replenishment.

The downstream continuation CELLs existed before route acquisition. No duo
created or owned a CELL. No dedicated shared CELL and no direct trace-to-trace
coupling existed.

## Physical structures and activity produced

### Positive PROBE state

- individual correspondence resistance: `20|20|20|20`;
- individual direction resistance: `15|15|15|15`;
- opportunity resistance:
  `8,0,0,0 | 8,0,0,0 | 0,15,0,0 | 0,15,0,0`;
- measured opportunity impulses:
  `2,0,0,0 | 2,0,0,0 | 0,2,0,0 | 0,2,0,0`;
- trained A+B and C+D held-out crossings: `1|1`;
- crossed A+D and C+B held-out crossings: `0|0`;
- singleton A/B/C/D held-out crossings: `0|0|0|0`;
- correlation-only, no-return, and absent-opportunity reuse: zero;
- exact duplicate replay: pass.

Thus trained and crossed organizations differed physically while route
frequency, traversal, return, pressure, outward route effects, correspondence
strength, and learned direction strength were matched. The positive cannot be
explained by A, B, C, or D being individually stronger.

### Frozen swap state

- individual correspondence resistance: `32|32|32|32`;
- individual direction resistance: `27|27|27|27`;
- final opportunity resistance:
  `20,11,0,0 | 20,13,0,0 | 13,27,0,0 | 11,15,0,0`;
- all four routes supplied coupling-`2` input to both active continuations;
- old A+B/C+D overlap: `23 -> 35`, strengthening rather than weakening;
- new A+D/C+B overlap: `24`;
- held-out new A+D/C+B crossings: `2|2`;
- held-out old A+B/C+D crossings: `2|2`.

The old coupling-`2` edge plus a fresh crossed coupling-`1` edge reached the
threshold-`3` old continuation without its contemporary driver. Physical
return then stabilized the crossed edge. Old and new endpoints recruited one
another, violating mandatory swap weakening and specificity. The exact first
collapse is therefore `MICRO swap: old physical organization does not weaken`.

The stale `220`-tick pressure control, correlation-only, no-return, absent and
blocked opportunity, ambiguity symmetry, duplicate replay, source-refiring,
and quiescence controls passed. The stable-multiple control did not pass, but
it is blocked behind the earlier swap collapse and is not the first collapse.

## Forbidden-information audit

The organism-visible source block contains only numeric CELL/ARROW/SPIKE
construction, physical activity, resistance/live/impulse measurement, local
time, and work counters. A case-insensitive scan found zero occurrences of the
preregistered forbidden vocabulary, including Event, Episode, History, Pair,
Group, member, pair key, semantic boundary, old M3, adapter, co-occurrence
record, scenario, expected, evaluator, trained, and crossed.

Evaluator labels and comparisons occur only after the organism-visible end
marker. They schedule external physical arrivals and read discarded-clone or
post-execution measurements. They do not select an ARROW update, add a
non-symmetric opportunity, delete/reset structure, or feed an expected
organization into substrate execution.

## Unchanged-port contract onto authoritative PX2

Authoritative PX0-PX2 source and artifacts are unchanged byte-for-byte. The
Arm C implementation is a sidecar example importing the frozen public
substrate API; no shared code changed.

Any later serial port must:

1. begin from exact authoritative PX2 commit
   `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129` or a separately authorized
   descendant and verify the same substrate-law bytes;
2. preserve the prior PX3 no-addition negative, this first-collapse freeze,
   protocol, mechanism source, positive PROBE, and negative MICRO byte-for-byte;
3. port no positive Arm C mechanism as authority: the candidate failed the
   mandatory swap control;
4. diagnose only a newly authorized parent delta under a new preregistration;
5. never reinterpret this negative as permission for a selector, shared CELL,
   trace coupling, relation record, rescue topology, or PX4 composition.

## Files, validation, work, and storage

Arm-added files are limited to one sidecar example, six experiment/audit
documents including this handoff, and four result files. All paths are fresh;
all staging paths are absent. No earlier path was modified.

Validation before evidence:

- focused `cargo fmt`: pass;
- focused `cargo check`: pass;
- focused strict `cargo clippy -- -D warnings`: pass;
- no-argument and wrong-argument refusal: exit `2`, pass;
- no-CELL preflight: pass;
- frozen source/ancestry/hash audit: pass;
- organism-visible forbidden-token audit: pass;
- broad historical suite: not run because shared code did not change.

Published-row ledger totals are `165,730` operations for PROBE and `446,632`
for MICRO, or `612,362` combined. These are conservative lower bounds: the
frozen reporter did not aggregate isolated-control and all discarded held-out
clone ledgers into the row total. That post-result accounting defect is
preserved without rerun or rescue and independently prevents a positive
handoff. Published persistent substrate storage is `22,832` bytes at PROBE and
`32,304` bytes at MICRO. PROBE/MICRO result storage is `3,641` bytes.

## Stop

The exact candidate is frozen negative. Progress would require a separately
authorized and preregistered opportunity or substrate law. This arm stops with
PX3 absent and GATE unspent.
