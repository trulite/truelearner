# Unchanged DS1 after A1 interface, leak, and control audit

## One-event composition

One frozen E0 acquisition instance creates all twelve support events and the
single target event. The same target supplies:

- the unchanged DS1 `Neighborhood`, through frozen E0 serialization; and
- the A1 input, through direct field copies of pulses, propagation relations,
  members, relative temporal relations, and relative propagation relations.

Every field is compared to its source. The bridge receives only roots that
already exist after A1 installation and structural deduplication.

## Passed controls

Every MICRO/GATE seed passed:

- exact E0 export and DS1 `Neighborhood` copies;
- disjoint support/target occurrence identities;
- exact frozen A1 readiness inventory `2 / 3 / 2 / 2 / 2 / 2` for
  candidates/templates/roots/structural/effects/handles;
- one-to-one unique root-to-handle mapping;
- no route formation after bridge;
- ordinary/permuted bridge inventory equality;
- a different real selected effect under handle permutation;
- selected effects contained in the pre-choice physical effect inventory;
- one frozen DS1 choice call and one physical selected execution per branch;
- cleanup to zero episode-local routes, handles, cells, arrows, observations,
  and bindings;
- mutation-sensitive zero post-choice evidence and update paths.

## Forbidden channels absent

Persistent state and composition wiring contain no stable filler/SAME,
boundary/source/target role, action opcode, legal/expected route table,
evaluator answer, correctness bit, semantic consequence, rank, hidden handle
interpreter, future destination, effect-directed choice, or concrete occurrence
identity in learned A1 templates.

The evaluator may fingerprint physical effects to verify route identity and
permutation behavior. Those fingerprints never enter DS1 choice, A1 formation,
bridge membership, or organism-visible state.

