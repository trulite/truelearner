# PX6 physical consequence-credit GATE v2 negative audit

Status: **FROZEN `40/48` PHASE-BOUNDARY NEGATIVE; AUTHORITY ABSENT**.

## Immutable artifacts

- implementation commit: `2df666f048131cc7188e17b411a1916a49b4ef8c`;
- implementation SHA-256:
  `385190d4be77c71661399e31b650d493b2a8156c71e1010983c8333aadac1aaf`;
- CSV SHA-256:
  `1818fce395e6b496b33eb4d251b6da2363587d82cf1f6d7746ccab74c53845ff`;
- report SHA-256:
  `07347133d49c1db811aea4f8b7cf089f808215502230df054834d51fe6ddeeb1`.

## Result

All 40 nonswitch cells passed. Sparse differential persistence, threefold
observational overload, return controls, distractor loads, layouts, replay,
and quiescence were exact.

The fresh local proposal survived and produced the correct final live and
held-out vector in both reverse-allocation strata (G1/G2). It deallocated in
both normal-allocation strata (G0/G3). The discriminator is pressure phase:
the reserve-1 arrow was created three ticks before the delayed presentation,
then an ordinary pressure boundary occurred before its first physical return
in G0/G3. G1/G2 had no intervening boundary. This is the frozen PX2 finite
opportunity window operating exactly, not a distinct mechanism.

V2 also counted the two external proposal-opportunity arrivals as returned
arrivals because the aggregate source-arrival metric subtracted only ordinary
presentation inputs. The raw firings and topology are unchanged; the metric
must subtract those two explicitly serialized external SPIKEs.

## Disposition

Freeze v2 unchanged. A fresh v3 may only:

1. move the explicit local proposal opportunity from three ticks before to one
   tick before delayed participation, after the immediately preceding pressure
   step in all four frozen schedules;
2. subtract its two known external SPIKE arrivals from the return-arrival
   measurement.

All topology, laws, worlds, schedules, controls, and expectations remain exact.
