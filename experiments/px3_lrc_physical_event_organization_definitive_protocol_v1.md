# PX3 LR-C physical event organization definitive protocol v1

Status: **PREREGISTERED; DEFINITIVE EVIDENCE UNSPENT; PX3 AUTHORITY ABSENT**.

## Frozen starting point

This workflow begins at development-positive commit
`481b20b5597007189a6d199f9de64157be157dfc`, tag
`px3-lrc-fresh-integrated-development-positive-v2`.

| frozen artifact | SHA-256 |
|---|---|
| authoritative LR-C law | `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10` |
| LR-C authority-v2 report | `3ad1df774690a71ee7e6884f56a9399a098890e14d83c7a2f03231ed9aafeb3c` |
| PX3 LR-C development protocol v2 | `c249be1d74ad219896f2fe3505942166efb369e9bea4e50dfa84585f2a1d7107` |
| PX3 LR-C development result audit v2 | `44ce9f414f785a434752ccaab6a7445e430a43be5cac576d7cdec1441d85fd79` |
| positive lifecycle CSV | `0125f52fc9e5427c558caa39b8f720fe1fefea13e16f8c4e88bd0ae46904afef` |
| positive recursion CSV | `b59fa4b299d2ec22429255d78269ecb7fa56c22aeb4c122137fc21a299369724` |

LR-C physics and the developed PX3 physical blocks may not change. Authority
may add only fresh evaluator identities, fixed matrices, independent clause
serialization, refusal/preflight surfaces and write-once publication.

## Narrow authority claim

> Repeated co-participation can become one reusable physical unit when
> downstream consequence returns through qualified Modulatory transmission.
> Ordinary Drive cannot credit the organization. Unsupported organization can
> disappear and reform, and mature organization can immediately participate
> in further organization through the same ordinary interface, without Event
> objects, contributor lists, composite flags or level-specific APIs.

This does not claim semantic identity, recover member lists, authorize PX4 or
promote any earlier diagnostic attribution geometry.

## Disjoint matrix

The definitive evidence is one conjunctive workflow with two separately
published halves:

- `16` lifecycle/reversal rows; and
- `16` recursive-compression rows.

All `32` namespaces, CELL identities, ARROW identities and source origins are
fresh relative to development. Seeds are authority-only values in the ranges
`91001..91067` and `92003..92071`; no development seed is reused. Each half
contains four complete rotations through normal/reversed insertion and
forward/reflected proposal geometry.

Every row executes from fresh state twice for exact replay. Evaluator controls
run only on fresh worlds or exact clones and have no causal path into the
continuing reversal or recursion world.

## Lifecycle/reversal half

Every row must establish twelve clauses:

1. `L0` frozen inputs, authority seed, stratum and fresh namespace are exact;
2. `L1` coupling-four A crosses with impulse four but emits one unit A trace
   and no pair activity;
3. `L2` two simultaneous A entries yield one refractory traversal/trace and no
   pair activity;
4. `L3` gapped A then B yields two primitive traces and no pair activity;
5. `L4` two immediately adjacent unsupported AB episodes produce two real
   candidate traversals, zero plasticity updates and a dead candidate;
6. `L5` a completed independent effect/return path delivers one Modulatory
   crossing to P without prior candidate participation, while P does not fire,
   no candidate is proposed and plasticity updates remain zero;
7. `L6` one supported AB/CD exposure executes but dies under pressure;
8. `L7` recurrent completed AB/CD loops mature and survive bounded pressure;
9. `L8` after support changes, obsolete AB/CD deallocate under ordinary
   pressure without a special delete path;
10. `L9` recurrent AD/BC forms fresh identities and matures;
11. `L10` held-out execution follows AB/CD before reversal and AD/BC after it;
12. `L11` all propagation quiesces and complete-state replay is exact.

The decisive source-local counterexample is literal:

```text
AB episode 1 -> candidate traverses -> eligibility
AB episode 2 -> ordinary Drive reaches P -> second traversal
no Modulatory return
=> local_return_updates = 0; candidate dies
```

## Recursive-compression half

Every row must establish twelve clauses:

1. `R0` frozen inputs, authority seed, stratum and fresh namespace are exact;
2. `R1` native proposals/traversals occur only after the corresponding two
   physical participation traces overlap;
3. `R2` one exposure at AB, XC and YD dies under pressure;
4. `R3` candidate participation plus completed downstream return produces one
   lawful Modulatory delivery and strengthening at each active stage;
5. `R4` recurrence matures AB, XC and YD through the frozen resistance path;
6. `R5` candidate impulses spanning the frozen weak/mature amplitudes normalize
   to exactly one ordinary derived trace per output execution;
7. `R6` C alone and D alone cannot form or execute higher organization;
8. `R7` gapped X/C and Y/D histories cannot form or execute the next stage;
9. `R8` final context-free execution produces exactly one X, Y and Z trace;
10. `R9` context-free execution has zero return-relay firing, zero Modulatory
    crossing and zero plasticity update;
11. `R10` all three levels use identical proposal, eligibility, Drive,
    Modulatory and participant-normalization machinery with no level state;
12. `R11` all propagation quiesces and complete-state replay is exact.

## Physical mechanism and anti-cheat boundary

The only retained influences are:

```text
Drive                 -> activation/execution only
participation trace   -> transient record of actual traversal
eligibility           -> recently traversed structure is locally changeable
Modulation            -> eligible plasticity only; no activation
resistance + pressure -> persistence, coupling and deallocation
```

Primitive and derived outlets share the same PX1 normalizer. Every joint level
shares the same O/P/native-candidate/effect/return-relay/Modulatory topology.
R3/R4/R5/R6 diagnostic attribution cells, excitatory echoes and trace-readout
machinery are forbidden.

Forbidden active representations include Event, member, contributor, Pair,
Group, composite, level, recursion depth, reward, correct, return owner,
episode, history or renamed equivalents. Evaluator labels and expected vectors
may not enter organism-visible state.

## Verdict

The authority verdict is conjunctive:

```text
lifecycle rows: 16/16
lifecycle clauses: 192/192
recursion rows: 16/16
recursion clauses: 192/192
total: 32/32 rows and 384/384 clauses
```

Any false clause is an immutable definitive negative. A positive result permits
one separately frozen PX3 authority handoff. It does not authorize PX4.

## Preflight, execution and publication

Both implementations must be committed and tagged before either matrix runs.
In two distinct fresh E2B preflight sandboxes they must pass Rust formatting,
release tests, strict Clippy, dependency/source/frozen-hash audits, namespace
uniqueness, refusal without the registered argument, no-world preflight and
final/staging artifact absence. Preflight may not construct a world, propagate
activity, emit an evidence marker or write an artifact.

After freeze, both halves execute once and concurrently in two new E2B
sandboxes. Each emits one evidence-spent marker and atomically publishes its
CSV and report. No rerun, rescue, regeneration, tuning or protocol amendment is
allowed after either marker.

Registered commands:

```text
cargo run --release --manifest-path arms/px3-lrc-lifecycle/Cargo.toml -- --definitive
cargo run --release --manifest-path arms/px3-lrc-recursion/Cargo.toml -- --definitive
```

Registered artifacts:

```text
results/px3_lrc_lifecycle_definitive_v1.csv
results/px3_lrc_lifecycle_definitive_v1.md
results/px3_lrc_recursion_definitive_v1.csv
results/px3_lrc_recursion_definitive_v1.md
```
