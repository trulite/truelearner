# PX1-PT1 attributed-margin stability protocol

Status: **PREREGISTERED; DEVELOPMENT EVIDENCE UNSPENT; PX1 NON-AUTHORITATIVE**.

PT1 is a separately named successor to both immutable results:

- PX1 recurrent-stability diagnostic v2: `0/4` arms passed;
- PX1-PT0 physical participation trace: `21/21` development worlds passed.

It does not reinterpret the first result or grant authority to the second.

## Frozen inputs

- authoritative PX0 law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- recurrent-stability negative CSV SHA-256:
  `7ddf75567e4b61fd735a042ddafb949fd85be57021285465a08ca17285c61e80`;
- PT0 implementation SHA-256:
  `f0b754ed6f7b0603668319a0735da91b4c168f909d4024fd5ce5e2aea4197410`;
- PT0 MICRO CSV SHA-256:
  `f67185cd9443e5501bc19e1e967a5f5b8c1a403850cb3c2aa206ab94e94f9311`;
- PT0 readiness SHA-256:
  `f184d168331d9a7af413621415c76a78db5ba7f00f297f6cdd690525df5bd2ad`.

## Question

> Does the previously quiet margin condition learn one reusable physical
> boundary role when returned activity overlaps only the continuation branch
> that physically participated?

## Physical composition

The PX0 law remains byte-identical.

```text
anonymous source activity
→ PX0 correspondence physically forms
→ retained correspondence reaches endpoint
→ endpoint activity reaches an anonymous branch
→ physical support makes one branch participate
→ branch-local weak continuation executes
→ ordinary effect reaches one return hub
→ identical return reaches every branch
→ only recently participating branch has live eligibility
```

Source threshold and anonymous external source activity use the frozen margin
condition (`4`). The closed PX0 correspondence return remains physically
present but cannot refire the source.

Each branch-local continuation begins weak. During acquisition, ordinary
physical support makes only one branch cross threshold and its weak effect
execute. During held-out use, an identical context impulse reaches every
branch. The matured continuation alone may produce an outward effect.

No source may contain `role`, `selected`, `chosen`, `cause`, `return_for`,
correctness, ownership, or an old M0/M1 representation in organism-visible
state. Evaluator scenario names are not organism inputs.

## PROBE

One primary world must establish:

1. both PX0 correspondences acquire from anonymous activity;
2. physical training support makes only branch A participate;
3. the same return arrives at A and B;
4. only A's continuation matures;
5. held-out identical context yields effect `[1,0]`;
6. no source refires beyond external initiations;
7. a post-gap activation repeats `[1,0]` and quiesces.

## MICRO

Fresh primary and mirrored-transfer worlds add:

- supported branch swap A↔B;
- reversed allocation order;
- fresh physical identities and layout;
- no branch support → no role maturation;
- support but blocked return → no maturation;
- identical return without branch execution → no maturation;
- both branches genuinely participate → both may mature;
- weak/nonparticipating branch stays non-executable;
- exact duplicate replay.

## GATE

If MICRO passes, six fresh blank worlds vary identity, allocation, layout,
timing within the lawful window, and post-training delay. The conjunctive gate
requires:

```text
PX0 correspondence acquisition
+ differential branch participation
+ differential role maturation
+ exact supported held-out effect
+ zero unsupported effect
+ useful closed return
+ zero autonomous source refiring
+ productive post-gap recurrence
+ natural quiescence
+ fresh transfer
```

Every independently meaningful clause must be serialized separately.

## Stopping rule

Mechanical implementation defects may be repaired only before a stage is
spent. A spent negative remains immutable. Stop on any new physical ambiguity,
semantic ownership requirement, PX0 modification, or need for a typed
intermediate.

PT1 remains development-only. Even a positive GATE requires a separate
authority protocol. PX2, PX-C, the continuous organism, and Harness H1 remain
blocked.

