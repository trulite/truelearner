# PX6 physical consequence-credit no-new-mechanism GATE v2 protocol

Status: **PREREGISTERED MECHANICAL RETRY; EVIDENCE UNSPENT; AUTHORITY ABSENT**.

GATE v1 remains an immutable `32/48` negative. V2 uses fresh namespaces
beginning `0x6_6200_0000` and new write-once artifacts. All 48 physical cells,
96 developments, timings, distractor loads, base topology, schedules, and
expected final retained/held-out vectors remain unchanged except for the
explicit edge below.

## Corrected sparse-side predicate

In dominant-versus-every-fourth worlds, require the sparse source to fire six
times and require a positive measured number of downstream firings before its
reserve-3 arrow deallocates. Continued downstream execution after deallocation
is not required. The dominant side must fire downstream 24 times, remain live,
and execute held out; the sparse side must end dead and not execute held out.

## Frozen contemporary opportunity edge

Every world contains one ordinary live target CELL one position from each
participant CELL and a stable physical bridge from that target toward the
existing downstream CELL. This topology is present from blank construction and
carries no schedule or side meaning.

Only in the two block-switch worlds, three ticks before the first presentation
of the delayed side, two ordinary external SPIKEs arrive at that side's
participant CELL. Threshold firing invokes the unchanged substrate's generic
local proposal rule. Local geometry, not the evaluator, determines whether and
where an ARROW appears. Direct `add_arrow` at the switch is forbidden.

For candidate delay one the stable bridge delay is zero; for candidate delay
two it is one. Thus subsequent fresh-arrow traversal reaches downstream at the
same physical time as the existing training support. The first block's old
arrow must deallocate; a fresh locally proposed ARROW must exist only on the
delayed side, receive ordinary returned activity, remain live, and execute on
held-out activity. Raw participant firing includes the one proposal-opportunity
firing and is serialized explicitly.

All other worlds receive no proposal-opportunity SPIKEs. Their nearby targets
remain inert and must not change their frozen outcome vectors.

## Full pass rule

All `48/48` fresh cells must pass all v1 controls plus:

- exact schedule participant firings, including the single explicit switch
  opportunity;
- fresh locally proposed ARROW existence and resistance on only the delayed
  switch side;
- exact final live and held-out vectors;
- exact distractor firing;
- natural quiescence, work/storage/fingerprint accounting, and exact replay.

Failure is frozen without rescue. Success establishes development readiness
only, classified under the mandatory A--D handoff. It creates no authority and
does not authorize a definitive matrix or any other PX advance.

## Forbidden

No typed adapter, old M reconstruction, semantic outcome, evaluator-selected
local path, direct switch-time ARROW construction, hidden fixture scheduling,
return ownership, or mechanism/evidence from a parallel PX lane may execute.
