# DS6 cumulative learned-lifetime definitive protocol

Protocol identifier: `ds6-cumulative-lifetime-definitive-v1`

Status: **PREREGISTERED BEFORE DEFINITIVE IMPLEMENTATION OR EXECUTION**.

This protocol authorizes one future authority execution only after its wrapper,
hash audit, write-once schema, and exact frozen ancestry are committed and
tagged. It cannot be amended after that execution begins.

## Definitive question

> Starting blank from authoritative M3 in every cell, does the byte-frozen DS6
> scalar lifecycle eliminate supplied lifetime classes by making persistence
> and forgetting arise from recurrence/use protection under ordinary physical
> pressure, while preserving cumulative M3 behavior?

PASS supports the narrow claim:

> The cumulative organism no longer requires supplied lifetime classes;
> persistence and forgetting arise from use-dependent protection under
> ordinary physical pressure.

It does not establish DS7 plasticity allocation, DS8 semantic-credit removal,
language, planning, or general memory optimality.

## Frozen ancestry and mechanism

- authoritative M3 commit/tag:
  `ffcdfe8b36fc62348b7ebcb09aaf4797f6146ba8` /
  `m3-cumulative-event-boundary-authoritative`;
- M3 handoff SHA-256:
  `b180589c8f70330378ace19a6ec7cfed8b505cb86fdf1480f56c330e421b05ec`;
- M3 definitive CSV/Markdown SHA-256:
  `ac8c0a6c9b7badfa263ceb054ffe59c11162b1ca256c56cc6df5f0d378179401` /
  `ab77bd12b705b8620b6315260f8bb5b4df6efc961f1d20a0dd521af403e1ac5f`;
- DS6 development-readiness commit/tag:
  `d0062a1b703fd1a0e7aa2178aa609473bd6a1225` /
  `ds6-cumulative-lifetime-development-readiness`;
- DS6 development handoff SHA-256:
  `9912b1225a9fc302103a931bf1ceb6e8f1eec78af70f7dcae2a96e93c2d6285a`;
- frozen DS6 development source SHA-256:
  `3d5659fb26ae804dee6122408f9d703ea1f226349772883075a42686ac3fd110`.

The definitive wrapper may expose fresh seeds, call the frozen cell mechanics,
audit sources and information flow, serialize results, and aggregate gates.
It may not alter the scalar record or law:

```text
one anonymous signature -> i32 strength map
new recurrence                    strength = 1
existing recurrence/use           strength += 2
each fourth completed event       all strengths -= 1
strength <= 0                     allocation physically disappears
```

It may not add a temporary/permanent/TTL/tau/expiry field, retention policy,
evaluator delete, future-use signal, task-boundary cleanup, contradiction-
specific decrement, reinstatement mode, or semantic correctness input.

## Fresh blank-start matrix

The matrix contains exactly sixteen explicit base seeds. A Rust range is
forbidden:

```text
 8_000_000   8_500_000   9_000_000   9_500_000
10_000_000  10_500_000  11_000_000  11_500_000
12_000_000  12_500_000  13_000_000  13_500_000
14_000_000  14_500_000  15_000_000  15_500_000
```

Each base owns the half-open namespace `[base, base + 250_000)`. The wrapper
must fail before learner execution if the array length is not exactly sixteen,
the values differ, namespaces overlap, or a derived seed leaves its cell.

All prior development namespaces are excluded, including the accidental
5,001-cell GATE range `111_000..=116_000` and every derived occurrence used by
PROBE, MICRO, matched-history, or GATE. Every subcase starts with a fresh blank
DS6 lifecycle and fresh M3-compatible occurrences. Nothing learned in one
cell or subcase is shared with another.

## Fixed physical cells inside every base

### 1. Recurrence resistance

Recurrence counts `1,2,4,8,16` each receive four matched subsequent physical
pressure updates. Final strengths must be exactly:

```text
0, 0, 2, 9, 23
```

and nondecreasing with recurrence, with strict increase from `2 -> 4 -> 8 ->
16`. The exact `16`-recurrence value reflects four pressure updates already
encountered during its acquisition plus four subsequent updates.

### 2. Disuse/pressure

Six recurrences followed by `2,4,8,12` subsequent pressure updates must yield:

```text
8, 6, 2, 0
```

Strength strictly decreases until physical deallocation at zero.

### 3. Dynamic lifetime

Subsequent pressure updates until physical deallocation for recurrence counts
`1,2,4,8,16` must be exactly:

```text
1, 3, 6, 13, 27
```

No lifetime counter is stored; these are evaluator observations of the scalar
trajectory.

### 4. Reuse after gap

An eight-recurrence record surviving gaps of `2,4,8` pressure updates must gain
exactly `+2` when reused through the ordinary recurrence path. A two-recurrence
record exposed to sufficient pressure must physically disappear and return at
strength `1` through ordinary reacquisition, with no reinstatement branch.

### 5. Contradiction through competition

An old lawful length-three causal signature receives eight recurrences. A
changed lawful length-two signature then receives exposures `0,2,4,8,16`.
Across these matched branches:

- old strength is nonincreasing;
- changed strength is nondecreasing;
- changed activity never executes the stale old signature;
- no direct contradiction-specific decrement or delete occurs;
- returning old activity gains `+2` if still allocated and starts at `1` if
  ordinary pressure has deallocated it.

### 6. Replacement, deallocation, and stale-path controls

- changed structure becomes physically allocated and usable;
- all unsupported one-offs under loads `8,32,128` physically disappear;
- removed allocations contribute zero persistent bytes;
- no stale learned path executes after deallocation;
- generic M3 reconstruction remains available and ordinary reacquisition can
  occur afterward.

### 7. Crossed history/pressure tradeoff

The three frozen crossed comparisons must fall on opposite sides of the
survival boundary exactly as dictated by the scalar ledger:

```text
4 uses + 8 pressure   = 0    versus 2 uses + 2 pressure = 1
8 uses + 16 pressure  = 0    versus 4 uses + 4 pressure = 2
16 uses + 16 pressure = 11   versus 8 uses + 16 pressure = 0
```

### 8. Cumulative M3 and fresh rendering

For fresh occurrences, shapes, local times, consequences, allocation order,
and reversed presentation:

- allocated lawful signatures produce exact M3 spans, ordinary consequences,
  learned-use accounting, and quiescence;
- removed signatures use generic M3 reconstruction without stale execution;
- relabelling does not change recurrence/pressure trajectories;
- M0 correspondence, M1 boundary roles, M2 causal direction/execution, and M3
  event organization remain intact.

## Frozen controls

Every cell must pass:

1. keep-all retains excess one-offs;
2. no-pressure over-retains all one-offs;
3. shuffled recurrence does not build matched resistance;
4. changed external request/finish scheduling with identical organism-visible
   activity leaves lifecycle snapshots exact;
5. source audit finds no forbidden lifetime, evaluator, correctness, or
   boundary information in persistent/update paths;
6. persistent bytes equal only physically allocated records;
7. occurrence-local state returns to zero;
8. exact duplicate evaluation is byte-identical and non-plastic.

## Conjunctive PASS

The definitive result passes only if:

- all sixteen blank-start cells pass all eight physical sections and all eight
  controls;
- exact scalar vectors and dynamic lifetimes match in every cell;
- every cell preserves cumulative M3;
- frozen hashes, explicit seeds, disjoint namespaces, write-once outputs, and
  duplicate replay audits pass;
- no organism-visible forbidden information or supplied lifetime class exists.

There is no aggregate percentage or tolerance. One failed cell or control is a
definitive cumulative negative.

## Write-once authority execution

Before execution, the authority wrapper and audit must be committed/tagged and
compile in a dedicated fresh E2B sandbox without running a definitive seed.
The single command atomically creates, and must refuse to overwrite:

```text
results/ds6_cumulative_lifetime_definitive.csv
results/ds6_cumulative_lifetime_definitive.md
```

No stdout-only result is permitted. The command executes the complete matrix
once. A valid negative is immutable: no rerun, rescue, constant change, seed
replacement, selective cell execution, or reinterpretation.

PASS creates M4 as the authoritative cumulative ancestor and makes cumulative
DS7 development eligible. FAIL leaves M3 authoritative and M4 absent. Program
priority is outside this matrix's evidentiary scope.

