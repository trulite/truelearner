# CJ0 ARM CJ-B locally gated ARROW terminal GATE protocol

Status: **PREREGISTERED; GATE UNSPENT; TERMINAL DEVELOPMENT SURFACE**.

## Frozen parent and hard boundary

This GATE consumes the positive PROBE and MICRO frozen at tags
`cj0-b-locally-gated-arrow-probe-v1-positive` and
`cj0-b-locally-gated-arrow-micro-v1-positive`. The physical module remains
SHA-256
`ef0de37a9ac54b632b991f0d4647a5ee78c23810084d61497c88d6f757ec2188`.

The lane ends at GATE. No later-stage protocol, command, simulation, artifact,
or evidence marker may be created. PX3, PX-C, and parked PX4--PX8 do not
advance regardless of outcome.

## Eight hardened flat cells

Execute eight fresh numeric namespaces: the four frozen normal/mirror/
allocation/permutation variants crossed with two ordinary timing strata.

- stratum S0: initial round spacing `4`, cluster offset `2`; changed spacing
  `10`, cluster offset `4`;
- stratum S1: initial round spacing `6`, cluster offset `3`; changed spacing
  `12`, cluster offset `4`.

Each cell repeats exact matched A+B/C+D acquisition and held-out trained versus
crossed discrimination, then ordinary A+D/C+B reversal, post-change old/new
held-out discrimination, trigger-only self-evidence, additional-gap historical
non-resurrection, exact replay, pressure/proposal/deallocation accounting, and
natural quiescence. Route occurrence, external arrivals, impulse, traversal,
return opportunity, pressure class, effects, and identity occurrence are
matched between the initial and changed common worlds.

## Recursion without a level-specific law

In fresh matter, place three identical weak coupling-1/resistance-1 candidate
ARROWs in a physical chain:

```text
A + current B-state -> X
X + current C-state -> Y
Y + current D-state -> Z
```

All source and output CELLS have threshold 3. Every current contributor
supplies state 2. Repeated full-chain ordinary activity must mature all three
ARROWs to the same coupling and reusable support. Held-out tests must show:

- A+B fires X;
- learned X+C fires Y;
- learned Y+D fires Z;
- A+B+C+D reaches Z and one outward crossing;
- missing B, C, or D prevents the corresponding/downstream output;
- A, X, and Y are activated through identical strong ordinary driver ARROWs
  during held-out use; the physical propagation loop has no level branch.

Candidate resistance/coupling, firing stages, return updates, output impulse,
work, fingerprints, replay, and quiescence are serialized for all levels.
Any recursive failure that requires a topology or law choice stops the lane.

## Inclusive alternatives from ordinary convergence

Without adding a disjunction rule, use two ordinary strong ARROWs converging
on one threshold-3 CELL and its ordinary outward ARROW. A-only, B-only, and
A+B must each fire the convergent CELL once and produce one outward effect.
The joint case may emit two incoming SPIKEs but refractory state must prevent
runaway. Exact replay and quiescence are required.

## Temporal expressivity

Use one mature coupling-2 candidate and existing CELL decay/order only. Record
independent traces, output tick/impulse, final transient fingerprint, and
crossing for:

1. together: contributor state 2 and trigger at tick 0;
2. A-then-B: trigger at 0, contributor at 1;
3. overlap/while: contributor impulse 1 at 0, contributor impulse 2 and trigger
   at 1;
4. within-window: contributor state 2 at 0, trigger at 1;
5. B absent before closure: trigger with no contributor, then closure tick 1.

Preregistered signatures are: together crosses at tick 0 with candidate
impulse 4; overlap crosses at tick 1 with impulse 4 and two contributor
arrivals; within-window crosses at tick 1 with impulse 3 and one contributor
arrival; A-then-B and B-absent do not cross, while their final transient
fingerprints differ because only the former leaves contributor state. No
logical operator or new clock/state field is added.

## Cumulative controls

The frozen PROBE/MICRO controls remain part of the conjunctive evidence base:
singletons, too-late contribution, correlation without traversal, no-return,
absent/blocked/stale paths, genuine participation, ambiguity, full-deallocation
bootstrap, fresh identities/layout/allocation/insertion, exact replay,
quiescence, finite recurrence, self-evidence, reversal, and historical
non-resurrection. GATE repeats the central flat, self-evidence, reversal,
freshness, timing, recursion, convergence, replay, and quiescence controls; it
does not regenerate frozen earlier artifacts.

## GATE clauses

`G0` frozen hashes/isolation/hard boundary; `G1` matched flat acquisition and
trained-versus-crossed discrimination; `G2` self-evidence/singletons; `G3`
reversal weakening/new learning/non-resurrection; `G4` three-level recursion;
`G5` primitive/learned participant identity of law; `G6` ordinary convergent
inclusive alternatives; `G7` five temporal signatures; `G8` replay,
quiescence, finite recurrence, fresh variants; `G9` complete fingerprints,
work/storage/source-leak serialization.

All ten clauses must pass in all eight rows. Any failure is frozen and the lane
stops. A pass is a development-ready terminal result only.

## Execution discipline

Add only `src/bin/gate.rs` plus audits. Preserve the physical module and frozen
PROBE/MICRO sources/results byte-for-byte. Before the sole run, require format,
tests, strict Clippy, zero dependencies, exact hashes, semantic/source leak
audit, neutral refusal behavior, no-CELL preflight, and artifact absence.

The sole command is:

```text
cargo run --release --manifest-path arms/cj-b-locally-gated-arrow/Cargo.toml \
  --bin gate -- --gate
```

It atomically publishes one GATE CSV/report pair. It may not be rerun,
regenerated, tuned, or extended.
