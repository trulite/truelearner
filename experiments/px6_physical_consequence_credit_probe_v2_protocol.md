# PX6 physical consequence-credit no-new-mechanism PROBE v2 protocol

Status: **PREREGISTERED MEASUREMENT-ONLY RETRY; EVIDENCE UNSPENT**.

PROBE v1 remains an immutable `1/6` evaluator negative. Its physical outputs,
source, topology, and artifacts may not be changed or rerun. The v1 negative
audit SHA-256 is frozen with this protocol's implementation preflight.

V2 uses fresh namespaces beginning at `0x6_1200_0000` and fresh write-once
artifact paths. It keeps byte-identical:

- authoritative PX0--PX2 substrate source;
- all six physical world topologies;
- CELL placement, thresholds, and resistance;
- every ARROW delay, phase, coupling, and resistance;
- SPIKE schedules, presentation count, and spacing;
- expected retained-arrow and held-out-execution vectors;
- quiescence, work, storage, fingerprint, and exact-replay measurements.

## Sole predicate corrections

1. Serialize `local_return_updates` as global physical work, require it to be
   positive exactly when physical own-source return can coincide with active
   outgoing eligibility, but do not equate it to candidate-arrow changes.
   Candidate resistance and liveness remain the direct differential-persistence
   measurements.
2. In crossed-return and no-return, require the participating candidate and its
   downstream path to execute a positive measured number of times before
   ordinary pressure deallocates the unsupported reserve-3 arrow. Require the
   independent other-side downstream path to execute eight times. Continued
   execution after candidate deallocation is not required.

All other worlds still require eight traversals for each external participant,
eight downstream firings on both sides, exact physical return counts, and the
full original conjunction.

## Pass and stopping rule

All `6/6` fresh worlds and every corrected clause must pass. Failure is frozen
without rescue. Success makes a separately preregistered MICRO eligible but
does not create authority, advance any other PX capability, or authorize a
definitive matrix.
