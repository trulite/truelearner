# CJ0-NOT independent physical diagnostics handoff

Status: **TWO DEFINITIVE CLASSIFICATIONS FROZEN; AUTHORITY UNCHANGED**.

## Exact ancestry and preservation

Work began from a clean checkout whose `HEAD` exactly equaled authoritative PX2
commit `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129` (tree
`3e69c15c5a9f7259d8617aa23ffb9083064f53a1`). Every change since that commit is
an added CJ0-NOT protocol, fixture/evaluator, audit, or result path. No
pre-existing path changed.

Authoritative PX0-PX2 source/evidence hashes remain exact, no dependency was
added, and no PX3 source or result was used or reinterpreted. PX2 remains the
authority. These diagnostics do not authorize a CJ0 logical NOT primitive or
any program-stage advancement.

## NOT-1 — active inhibition

Classification: **DEFINITIVELY POSITIVE WITHIN THE TESTED ORDER-SENSITIVE
PHYSICAL BOUNDARY**.

Existing ordinary signed ARROW coupling is sufficient for timely A activity to
suppress B's integration/output path. A absent leaves B operational. A ordered
after B's threshold crossing—including later order at the same tick—does not
retroactively erase output. Blocked and ordinary-pressure-staled A paths do not
inhibit.

- PROBE: `10/10` positive;
- definitive: `112/112` positive, `112/112` exact replay and quiescence;
- definitive CSV SHA-256:
  `f9c85e70afe840b68e2610bc9e2b03101a6f258a14abc6335562cad6bafc21d1`;
- frozen result tag: `cj0-not1-active-inhibition-definitive-v1-positive`.

This is active physical inhibition, not Boolean complement.

## NOT-2 — temporal absence

Classification: **DEFINITIVELY POSITIVE WITHIN THE TESTED
TRIGGER/B/CLOSURE PHYSICAL WINDOW**.

Existing trigger-produced transient CELL state survives until physical closure
unless timely B activity changes it. B at tick `1` or ordered before closure at
tick `2` prevents the alternative path. B absent, blocked, stale, ordered after
closure, or arriving at tick `3` leaves the closure/output path already emitted
and cannot act retroactively.

- PROBE: `12/12` positive;
- definitive: `112/112` positive, `112/112` trigger-state changes,
  `112/112` exact replay, and `112/112` two-stage quiescence;
- definitive CSV SHA-256:
  `f66b5f591533a53b1ad3f17a7c9a362e5881a202ff87999d7359850655b0e414`;
- frozen result tag: `cj0-not2-temporal-absence-definitive-v1-positive`.

No absence symbol, timeout label, evaluator-selected branch, or new persistent
variable participates in the substrate.

## Closure

Both questions resolved without ambiguity and without a new-law request, so no
negative boundary is frozen. The two positive classifications remain distinct:
NOT-1 establishes suppressive causal timing through existing signed coupling;
NOT-2 establishes a temporary-state/physical-closure relation. Neither implies
the other, and neither is authority.

The consolidated audit command is:

```text
scripts/audit_cj0_not_physical_diagnostics.sh
```
