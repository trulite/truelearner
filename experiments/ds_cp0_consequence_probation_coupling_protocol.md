# DS-CP0 anonymous consequence-to-probation coupling

Status: **PREREGISTERED; DEVELOPMENT OUTCOME UNSPENT**

Exact parent: `3d45292e684ab910ea7be0e337e27d22c513a375` /
`ds2-after-ap0-mechanistic-retry-collapse-handoff`.

## Sole question

> Can the already-learned physical D3 downstream-contrast relation gate support
> of already-existing A1 probationary structure, so that a structurally
> compatible proposal gains support while its competitor does not, without a
> causal label, polarity, evaluator credit, or a new learner?

## Frozen path

```text
existing A1 probationary templates
  +
existing D3 physical direction arrow
  -> anonymous structural compatibility
  -> existing A1 Learner::observe support update on the compatible path only
```

The bridge may copy only the structural source shape already carried by the
executed D3 direction arrow and compare it with physical normalized effects of
already-existing A1 routes. It may not use affordance slot, chosen/unchosen,
correct/incorrect, source/target role, evaluator-preferred direction, or hidden
filler identity to select a probationary template.

## Development requirements

- both competing route templates receive the same initial probationary
  opportunity;
- repeated physical D3 contrast causes exactly one structurally matching
  template to reach existing A1 consolidation support;
- the incompatible template receives no further support (decrement is not
  required);
- reversing downstream history reverses which structural template matures;
- equal downstream evidence, unstable evidence, removed direction, and absent
  execution abstain without differentiating support;
- fresh occurrences, occurrence relabeling, handle permutation, and
  allocation/layout changes preserve the structural result;
- the bridge adds no persistent identity, route handle, episode occurrence,
  polarity, reward, correctness, or causal-direction field;
- all persistent support remains in the existing A1 template/support map;
- no new proposal, probation, consolidation, pruning, or causal learner type is
  introduced.

CP0 is enabling-only and claim-ineligible. It does not establish causal
direction. After it is frozen, rerun the unchanged cumulative DS2 stages and
stop at the first remaining missing physical path. M1 remains authoritative;
M2 remains absent.
