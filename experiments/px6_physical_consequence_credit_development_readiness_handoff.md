# PX6 physical consequence-credit development-readiness handoff

Status: **CLASS A DEVELOPMENT READY; NO NEW MECHANISM; AUTHORITY ABSENT**.

This lane began exactly at authoritative PX2 commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
`px2-physical-causal-direction-authoritative`. It did not start or simulate an
authority workflow, execute a definitive matrix, alter PX0--PX2, advance
PX3--PX8, or create an authoritative ancestor.

## Lane outcome

**A = existing PX0--PX2 physics sufficient.** The discriminator is resolved;
the result is not between classes. No missing PX6 edge, alternate scientific
mechanism, new representation, or substrate-law change remains.

GATE v3 passed `48/48` cells and `96/96` exact blank developments. Authority
remains absent.

## Frozen mechanism

There is **no new PX6 mechanism**. The no-addition mechanism is the unchanged
authoritative PX0--PX2 source:

- commit: `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- tag: `px2-physical-causal-direction-authoritative`;
- source path: `crates/px0-physical-correspondence/src/lib.rs`;
- whole-file SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`.

The exact frozen blocks constituting the no-addition result are:

| block | source lines | SHA-256 |
|---|---:|---|
| propagation, traversal eligibility, crossing, quiescence | 246--357 | `c857ea85910b1f2663d7dc7e41440ac14ae7383b6b3156a5d33d451fb11a69a5` |
| ordinary return at active local eligibility | 409--424 | `dde858c9064dae68045793eb4f9437819e9a3cec539516b0223a0a614abe5f1f` |
| periodic and unsupported-use pressure | 426--454 | `9af24de6e770d1f15903740a49acd11bcc92e37ef34422b47eb1a2fa8f6092c9` |
| geometry-derived generic local proposal | 456--492 | `8aa30e34e2baca922a59e07d88ddf373430a05eafc156bf230eba48f4838961d` |
| zero-resistance deallocation/generation change | 611--619 | `895b01ac40e7a853f3008cc7e8f28d2e3102b0a150653396e1821cc2a5012d29` |

The frozen development measurement is:

- implementation commit/tag:
  `da0f71ded8f6f9362328e6efd03d9437d2d768a9`,
  `px6-physical-consequence-credit-gate-v3-implementation`;
- source path:
  `crates/px0-physical-correspondence/examples/px6_physical_consequence_credit.rs`;
- SHA-256:
  `2c53f8bbb4ef8c9ace4347c01433c38929f14dc73d900a896687665deaf8e030`;
- result commit/tag:
  `15a5208d17e6435e69aa1d67259b0b57d2bc1b51`,
  `px6-physical-consequence-credit-gate-v3-positive`;
- result CSV/report SHA-256:
  `4528c7e9902203d7acc1b1e397672bb878a08f531d4483c44d5a1c3d9ed2db0e`,
  `49b2d1a989801d70d6db7e94d9235665351cc06305ded5e2f9118376b5ce8b12`.

## Consumes physically

The capability consumes these actual substrate facts, with no conceptual
shorthand standing in for them:

1. live CELLs with physical identity, position, region, threshold, decaying
   local state, refractory time, resistance, liveness, and generation;
2. live directional ARROWs with physical endpoints, delay, phase, coupling,
   resistance, generation, and the existing four-tick `eligible_until` trace;
3. fresh SPIKE occurrences with arrival tick, phase, physical origin, target
   generation, impulse, serial, and optional traversed-ARROW generation;
4. actual participant-CELL threshold firing followed by candidate-ARROW
   traversal; firing without that traversal is not substituted;
5. downstream consequence-CELL firing, two physical inputs into a threshold-2
   trace CELL, and a trace ARROW returning a SPIKE to the participating source;
6. ordinary return arrival while the traversed ARROW's local eligibility is
   still active; return to an inactive/nonparticipating source has no candidate
   effect;
7. `+3` local resistance change and coupling growth capped at `2` from the
   unchanged return block;
8. ten-tick periodic pressure, unsupported-use pressure, zero-resistance
   deallocation, generation change, and stale-spike rejection;
9. for delayed block switches only, existing generic local proposal physics:
   external threshold firing, a live CELL one position away, and geometry-based
   creation of a reserve-1 ARROW; no evaluator adds or selects that ARROW;
10. physical region change on consequence-to-outside ARROWs, producing actual
    outward crossings; and
11. queue/order-sensitive propagation until the pending SPIKE queue is empty.

## Produces physically

For supported participating structure, the developed substrate contains a live
ARROW with coupling `2` and measured resistance `15..29`; switch worlds contain
one fresh geometry-proposed live path exactly on the delayed participating
side. Its held-out traversal fires downstream and emits exactly one outward
crossing.

Unsupported, sparse, earlier-block, correlation-only, crossed-return, and
no-return candidate ARROWs end at resistance `0`, `live=false`, with generation
advanced. They emit no held-out crossing. Threefold nonparticipating return
also leaves that side deallocated.

Training trace activity is transient. Every run ends with zero pending SPIKEs
and natural quiescence. CELL residual activity decays under ordinary time.
Stable environmental fixture topology remains physical; no outcome record,
route-owner record, or task object remains. The CSV/report are observer
artifacts and are never organism input.

There is no single serialized state to hand to a next PX stage. Any cumulative
composition must pass the actual in-memory `PlasticSubstrate` CELL/ARROW/SPIKE
state directly and must preserve execution, deallocation, quiescence, and
outward-crossing behavior.

## Assumptions and explicit environmental opportunities

The sole authoritative assumption is PX0--PX2 commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129` with the whole-source hash above.

Every additional environmental physical opportunity used by the development
GATE is listed here:

- blank construction of two physical sides, each with entry, participant,
  downstream, trace, outside-region, training-driver, correlation-driver, and
  nearby proposal-target CELLs;
- stable entry-to-participant, training-support, independent downstream,
  downstream-to-trace, trace-to-source, downstream-to-outside, context, and
  proposal-target bridge ARROWs;
- two initially weak reserve-3 participant-to-downstream candidate ARROWs;
- 24 presentation opportunities per development;
- G0/G1/G2/G3 side spacing `44/60/72/88`, candidate delay `1/2/1/2`, first tick
  `2/3/4/5`, presentation spacing `20/21/22/23`, normal/mirrored placement,
  normal/reversed allocation, and normal/reversed insertion;
- explicit traversal schedules: left-all, right-all, both-all, neither,
  left-with-crossed-return, left-without-return, every-opportunity versus
  every-fourth on each side, left-then-right, right-then-left, and left/right
  traversal against threefold opposite observational occurrence;
- in overload cells, additional independent physical occurrences at offsets
  `+6` and `+12` ticks;
- `0/8/24/48` independent driver/sink distractor CELL/ARROW pairs, each fired
  once per presentation and physically disconnected from candidates;
- in block-switch cells only, two ordinary external SPIKEs one tick before the
  delayed side's first presentation; local geometry, not the evaluator,
  creates the fresh ARROW;
- held-out physical entry plus context activity after training; and
- a second blank development with identical physical identities for exact
  replay measurement.

There is no hidden fixture scheduling or typed organism construction beyond
this list. `WorldKind`, `Stratum`, expected vectors, CSV formatting, and pass
predicates exist only in the external measurement harness and never enter
`PlasticSubstrate`.

## Forbidden

- typed adapter;
- reconstruction of any old M schema;
- semantic labels or evaluator-selected local paths;
- importing evidence or mechanisms from another parallel PX lane.

Also forbidden are serializer-fed execution, typed organism intermediates,
hidden task boundaries, return-ownership metadata, direct switch-time ARROW
construction, and using observer artifacts as organism input.

## Cumulative port rule

- import the frozen mechanism byte-for-byte onto the later authoritative
  parent;
- make no anticipatory redesign;
- run a physical dependency/path audit;
- the first incompatibility is the next cumulative collapse;
- resolve only that parent delta under a separately frozen protocol.

Because this is a no-addition result, “frozen mechanism” means the exact PX2
whole source and five blocks/hashes above. Port the GATE v3 measurement source
and its explicit physical opportunities unchanged first. If the later serial
parent already contains the PX2 bytes by ancestry, verify rather than rewrite
them. Do not import a parallel lane's mechanisms or evidence. Diagnose only the
later parent's physical delta after the unchanged port is present.

## Preserved evidence and accounting

PROBE v1 (`1/6`), GATE v1 (`32/48`), and GATE v2 (`40/48`) remain immutable
negatives. PROBE v2 (`6/6`), MICRO (`24/24`), and GATE v3 (`48/48`) remain
immutable positives.

GATE v3 serialized `5,432,159` work units for primary cells and executed
`10,864,318` including exact duplicates. Primary persistent storage summed to
`259,584` bytes; per-development storage was `2,096..9,904` bytes and no new
substrate field was introduced.
