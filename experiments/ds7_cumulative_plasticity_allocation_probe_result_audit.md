# DS7 cumulative plasticity-allocation PROBE result audit

Status: **POSITIVE DEVELOPMENT PROBE; M4 REMAINS AUTHORITATIVE**.

Frozen lineage:

- authoritative M4:
  `8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2`;
- activation/dependency audit:
  `72b1935c51f78900fcc401d1dadcf20c06afe438`;
- preregistered protocol:
  `15dc6fb3affee96b9470717ca9c7b7f97452f643`;
- formatted implementation:
  `4fd86771fe49387042fca1303ca4cd64738cbcb2`;
- implementation SHA-256:
  `7b04bdaca1aec34d563f728bfbcd2c28be07cd28e5bfc9778c69fce197c465ea`;
- runner SHA-256:
  `74b2e93e439d5d07cb80a079b762dcd2cca7646bae94a70c930c0663b14b2305`.

## Execution

The complete one-seed PROBE ran once in persistent E2B development sandbox
`iyrkw7af5qpmwwfmq3bwm`. The sandbox was reused only for compilation cache and
was left running. The run used the clean immutable Git snapshot at the
formatted implementation commit.

Focused validation before execution:

```text
cargo fmt --check                                      PASS
focused release unit test                              1 / 1
release PROBE                                          PASS
```

No broad repository suite, cumulative GATE, or definitive matrix ran.

## Interpretation

The first missing edge from the static audit exists:

```text
ordinary local coactivity
  -> M4-governed local variation
  -> active physical eligibility
  -> delayed experience
  -> learned encounter value
  -> changed future allocation of plasticity
```

The result rules out four trivial explanations within this diagnostic:

- proposal sites were not supplied, because no-coactivity and outside-radius
  encounters formed no proposal;
- outcome did not choose a site, because the proposal existed before delayed
  feedback and feedback without an active trace was ignored;
- the preference was not a fixed encounter rule, because value shuffling
  reversed it;
- the port did not restore supplied lifetime classes, because unused proposal,
  prototype, and value allocations physically disappeared under scalar M4
  pressure and a stale edge could not execute.

The result does not yet show that the law generalizes over histories or that
learned allocation yields substantial plastic-work economy while preserving
the frozen cumulative behavior. Those are the MICRO questions.

## Authority state

```text
M4 authoritative
DS7 cumulative development positive at PROBE
M5 absent
DS7 definitive not preregistered or authorized
```

