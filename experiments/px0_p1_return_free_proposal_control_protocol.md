# PX0-P1 return-free proposal control protocol

Status: **DEVELOPMENT PREREGISTRATION; NO PX0-P1 EVIDENCE SPENT; NO MECHANISM CHANGE AUTHORIZED**.

PX0 definitive v1 remains permanently negative. PX0-P coupling diagnostic v2
is frozen at commit `76585524c8abc6f93b13e2fb4bfe2e964dd3ff31`, tag
`px0-p-physical-proposal-probation-coupling-diagnostic-v2-cross-return`.

## Discriminating question

> When fresh A correspondence proposals form normally but A receives genuinely
> zero returned physical activity, can A mature enough to produce an outward
> execution under the unchanged PX0 law?

This control changes only physical layout separation. It adds no variable,
threshold, semantic state, execution gate, learning rule, or evaluator
mutation.

## Frozen law

The active PX0 law must remain byte-identical at SHA-256
`3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`.

## Fresh crossed arms

Use two fresh blank namespaces not used by PX0, PX0-R, PX0 authority, or PX0-P
diagnostics:

1. two active opportunities, direct layout, spacing 8;
2. three active opportunities, mirrored layout, spacing 10.

Retain the same acquisition, survival, full-deallocation, opposite B-world
reproposal, five renewal presentations, held-out B, and later A schedules as
the PX0-P baseline.

The sole physical change is route stride 8 instead of 6. Each route keeps its
own source/probe/contender/gate offsets `0/1/2/4`. Therefore:

```text
nearest nonmatching source to A gate distance = 4
generic proposal radius                         = 2
```

Every nonmatching source is physically incapable of proposing an arrow into A
gate. A's own fresh source-to-probe and source-to-contender proposals remain
available exactly as before.

## Required trace and conjunctive controls

For both arms require:

1. old A arrows reach resistance zero and remain non-live;
2. renewed activity creates fresh A and B candidate identities;
3. fresh A source activity emits into its candidate neighborhood;
4. fresh A proposal impulses remain physically observable;
5. A gate fires zero times during B-world renewal;
6. A source receives zero delayed gate-return occurrences;
7. A fresh coupling never produces impulse greater than 1;
8. A fresh resistance receives no return-driven increase and trends toward
   zero under ordinary pressure;
9. later A activity produces zero outward crossing;
10. B receives lawful return, reaches impulse 2, remains live, and produces
    exactly one outward crossing;
11. all activity becomes naturally quiescent;
12. complete physical duplicates replay exactly.

The evaluator may label physical IDs in its report and inspect traces,
resistance, liveness, work, fingerprints, and crossings. It may not feed any
observation back into the substrate.

## Classification

- **P1-A — existing physical probation positive:** return-free A remains weak,
  never executes, and disappears while returned B matures and executes.
- **P1-B — evidence-before-execution failure:** return-free A executes despite
  zero gate firing, zero physical return, and zero return update.
- **P1-C — topology isolation blocks learning generally:** A is silent but B
  also cannot mature or execute.
- **P1-D — control invalid/ambiguous:** some nonmatching physical return remains
  or the trace cannot separate return-free activity from maturation.

P1-A does not rescue PX0 definitive v1 or create authority. It establishes
that the current PX0 scalar dynamics already implement physical probation and
redirects subsequent research toward return specificity under dense topology.
