# CJ0-C plasticity-only conjunction PROBE v1 protocol

Status: **PREREGISTERED DEVELOPMENT PROBE; NOT CLAIM ELIGIBLE**.

## Frozen start and authority boundary

- Start commit: `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tagged
  `px2-physical-causal-direction-authoritative`.
- PX0 correspondence, PX1 participation-attributed boundary roles, and PX2
  causal direction are authoritative and immutable.
- PX3 is a frozen Class-D material negative. PX3-R Arms A, B, and C are
  immutable negatives. This protocol neither ports nor rescues them.
- PX4--PX8 remain parked. PX-C remains blocked.
- The authoritative source paths and bytes are read-only. All candidate law,
  construction, execution, and evaluation code must live under the fresh
  isolated arm `arms/cj-c-plasticity-conjunction/`.
- This is development evidence only. No definitive or authority execution is
  permitted.

## Exact candidate law

Test one law and no alternatives:

1. When ordinary physical propagation makes two distinct live CELL loci at
   the same position fire at the same physical tick, expose coupling-`2`,
   resistance-`1`, delay-`1` ordinary ARROWs from both loci to the first local
   live CELL with no live incident ARROW. If both loci already have live
   ARROWs to one such CELL, expose nothing.
2. When a CELL fires, strengthen by `+3` only its live incident incoming
   ARROWs whose existing eligibility covers the current tick, and only if at
   least two distinct physical source loci satisfy that condition.
3. Ordinary pressure, unsupported-use pressure, ARROW generation
   invalidation, CELL decay/refractory state, deterministic arrival ordering,
   and all authoritative PX0--PX2 behavior remain byte-copied and unchanged
   within the isolated arm.
4. Execution has no special conjunction path: source firing scans ordinary
   outgoing ARROWs, ordinary SPIKEs arrive, and the existing CELL threshold
   decides firing. Candidate targets have threshold `4`, so each coupling-`2`
   input is individually subthreshold and two are sufficient.

The co-firing test reads existing refractory timing. The strengthening test
reads existing ARROW eligibility. The retained organization consists only of
ordinary CELL/ARROW matter and existing numeric fields. The candidate adds no
persistent substrate variable.

## Physical consume/produce contract

Each primary world consumes four threshold-`1` source CELL loci at one
physical position, a symmetric reservoir of threshold-`4` uncommitted CELL
matter at that position, ordinary external SPIKE arrivals, and the exact
authoritative pressure/timing machinery. Source loci receive identical inert
anchor structure so reservoir selection cannot treat an unfired source as
uncommitted matter. Names A/B/C/D exist only in the evaluator.

Repeated close A+B and C+D activity must physically produce two anonymous
convergent structures, each with two coupling-`2` incident ARROWs. Held-out
A+B and C+D must make their retained target CELL fire; A+D and C+B must not.
One source alone must deliver only a subthreshold target update.

## Primary matched-marginal schedule

- Acquisition: `16` rounds; one two-source cluster at `base`, the other at
  `base+4`; round spacing `12`; early/late organization alternates each round.
- Every source occurs once per round. Individual arrival frequency, external
  impulse, firing count, traversal opportunity, eligibility, ordinary
  pressure, target opportunity, identity frequency, and early/late timing
  marginals are exact.
- Held-out trained and crossed observations run on separate physical clones
  from one serialized acquisition state. Held-out worlds receive identical
  source counts, total activity, timing, pressure, and evaluation work.
- Independently meaningful serialization stages are: blank, first exposure,
  acquisition complete, each held-out world, pressure-only deallocation,
  changed-experience acquisition, and changed held-out worlds.

## Mandatory reversal and lifecycle

After A+B/C+D acquisition, ordinary physical experience changes to A+D/C+B
for `40` matched rounds under the same cadence and alternating cluster order.
No reset, evaluator invalidation, local path selection, relation-change
signal, or historical structure reactivation is allowed.

Pass requires:

- old A+B/C+D target support weakens and becomes non-executable;
- new A+D/C+B structure forms and executes;
- old dead ARROW records remain dead and no generation is resurrected;
- a separate lifecycle replica first advances until every learned incident
  ARROW is physically deallocated, then bootstraps A+D/C+B from ordinary
  activity alone without a mature higher-order firing.

## Mandatory self-evidence control

On a clone carrying mature A+B structure, fire A alone while an unrelated
source fires concurrently. A's old incident ARROW may deliver a subthreshold
SPIKE to the old target, but that target must not fire, must not become a
source of a new proposal, and must not strengthen its old incident ARROW.
Only actual current CELL firings may supply the two co-firing loci.

## Controls

The PROBE freezes and serializes:

- A alone, B alone, and each singleton;
- B at `+4` (too late for simultaneous participation);
- correlated external activity at isolated distractors without traversal;
- source traversal with the second source absent;
- genuine two-source participation;
- three-source and four-source ambiguity, requiring permutation-symmetric
  physical exposure rather than an evaluator-selected winner;
- absent uncommitted opportunity;
- stale structure after ordinary pressure and blocked/dead paths;
- fresh physical identities, mirrored identity order, reversed allocation,
  alternate deterministic arrival order, and translated positions;
- exact replay from blank matter;
- natural quiescence and zero autonomous source refiring;
- useful recurrence without an activity-limit cutoff or runaway queue.

## Evaluator isolation and forbidden representation

The organism-visible library may contain only numeric CELL/ARROW/SPIKE
physics, local time, existing eligibility, physical topology, resistance,
generations, fingerprints, and work counters. It must contain no Event,
Episode, History, Boundary, Pair, Group, member list/key, semantic role,
relation record, typed adapter, evaluator callback, expected mask, scenario
selector, serializer, pair-change signal, or renamed equivalent.

Evaluator names, schedules, comparisons, and CSV rendering must be after an
explicit organism-visible end marker and have no update path into the library
beyond raw physical SPIKE target/timing inputs and generic construction
parameters.

## PROBE pass conjunction and staging

PROBE passes only if every primary, lifecycle, self-evidence, ambiguity,
freshness, replay, quiescence, accounting, serialization, source-isolation,
and forbidden-information clause passes in every preregistered cell.

If PROBE passes, freeze its result before preregistering MICRO. MICRO must add
fresh randomized layouts/identities/timing/allocation, wider lifecycle
replication, and recursion. GATE is eligible only after a separately frozen
positive MICRO. OR and temporal expressivity are tested after the first viable
prototype, without adding an OR law or logical operator.

## First-collapse and no-rescue rules

Stop and freeze immediately at the first scientific failure. Accounting,
serialization, fresh-seed plumbing, or mechanically forced timing defects may
be corrected only under a separately frozen retry protocol. Do not rescue a
negative. Stop without another implementation if multiple scientifically
distinct laws remain, a new persistent variable is required, semantic state
is required, PX0--PX2 must change, or recursion creates an architectural
choice.

The runner must refuse no arguments and wrong arguments, refuse to overwrite
artifacts, write CSV/report through same-directory staging plus atomic rename,
and emit exact source/protocol/result hashes, fingerprints, work, storage,
changed-path, leak, and source-token audits.
