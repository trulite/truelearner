# Cumulative DS8 non-semantic-credit path-existence PROBE

Protocol identifier: `ds8-cumulative-semantic-credit-probe-v1`.

Status: **PREREGISTERED DEVELOPMENT PROBE; OUTCOME UNSPENT**.

Authoritative M5:
`9c5ba68a6a4ae37b51575ebaae414ab51a248575` /
`m5-cumulative-learned-plasticity-allocation-authoritative`.

## Sole question

> Does a physical path exist from delayed evaluator-blind downstream activity,
> through learned consequence contrast, to the active M5 value update without
> any supplied semantic polarity?

This is a one-seed path-existence diagnostic, not a MICRO, GATE, definitive
matrix, or M6 claim.

## Frozen seed and blank start

The only seed is `40_000_000`, disjoint from DS7 development and definitive
namespaces. The M5 allocator and the consequence learner both begin blank:
zero proposals, prototypes, value records, consequence records, directions,
and eligibility traces. The report must be duplicate-exact.

## Frozen physical episode

Each episode has exactly three ordered phases:

1. an ordinary local M5 encounter forms a variation and physically executes;
2. at least one local tick later, a fresh three-cell/two-arrow consequence
   propagates in the environment;
3. normalized consequence structure is attached only to the still-active M5
   eligibility trace.

Two recurring encounter histories are presented from blank state. One is
followed by a recurrent consequence structure. The other is followed by
varying consequence structures of equal total activation magnitude. The
learner sees only raw cells, arrows, spikes, and temporal order. It receives no
history class, stable/variable label, selected index, answer, target, expected
trace, correctness, reward, loss, score, or externally supplied sign.

After sufficient observations, the D3-style contrast may emit a temporary
differential relation. Compatibility with the active M5 trace then invokes the
existing M5 delayed update. No persistent credit object is added.

## Conjunctive checks

The PROBE is positive only if:

1. both encounter histories form and execute variations before consequences;
2. fresh delayed physical consequences traverse their actual arrows;
3. recurrent-versus-varying histories form one differential contrast;
4. equal consequence magnitude cannot determine the contrast;
5. the contrast updates only the physically active M5 eligibility trace;
6. absent execution, immediate activity, equal stability, shuffled
   consequences, or removed eligibility causes abstention;
7. swapping the raw consequence histories reverses which encounter receives
   differential support;
8. learned M5 allocation later admits the recurrent-consequence encounter and
   suppresses the varying-consequence encounter;
9. shuffling learned M5 values reverses or destroys that preference;
10. fresh opaque identities and a permuted layout preserve the law;
11. source/information-flow audit finds zero semantic polarity or covert
    route/timing/magnitude/omission/reset/namespace encoding;
12. the unchanged authoritative M5 gate cell passes exactly;
13. the complete report is duplicate-exact.

PASS means the missing linker exists and a MICRO is warranted; M5 remains
authoritative. FAIL freezes the first collapse. A mechanical absence is
repaired at that edge under a new preregistration. A new representation,
semantic ambiguity, or leak is a scientific stop.
