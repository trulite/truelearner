# CJ0-E transient-coincidence development protocol v1

Status: **PREREGISTERED; DEVELOPMENT EVIDENCE UNSPENT; NO AUTHORITY OR
DEFINITIVE EXECUTION AUTHORIZED**.

## Frozen start and isolation

This isolated lane starts exactly at commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
`px2-physical-causal-direction-authoritative`, with a clean worktree.
Authoritative PX0 correspondence, PX1 boundary roles, and PX2 causal direction
are premises and remain byte-frozen.

| frozen input | SHA-256 |
|---|---|
| PX0--PX2 substrate law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| PX2 definitive executable | `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5` |
| PX2 authority handoff | `98647ab1563593e18e345cd7e5a71c4991d18b397dfe2dec71a4756106d96509` |
| PX2 definitive CSV | `921e433e3bf358e89e3f8f288b4ab0472e9503a2a3ac25fe037a2b7f6cf6eb18` |
| PX2 definitive report | `eef9c336baea6aa1e5c2debde2e1286b2839759c55fd5fc008c7775fd4103cda` |

PX3 is the frozen Class-D material negative at
`873094497ff6eb74363191dc5edc479c7d66de72`. PX3-R Arms A, B, and C remain
immutable negatives at, respectively,
`26aa795377c47ecf6fd28232865d5404408b6df9`,
`82c0433329cf85bf3fe261661acd033011000656`, and
`5feb9b4c4755ed40d58ffc9cb8769d5523ea46f0`. Nothing from those arms is
reinterpreted, repaired, or made authoritative here. PX4--PX8 stay parked and
PX-C stays blocked.

All candidate code and results use fresh paths beneath
`arms/cj0-e-transient-coincidence`, `experiments/cj0_e_*`, and
`results/cj0_e_*`. No authoritative source byte may change.

## Singular candidate law

The arm tests one minimal law using only ordinary pre-existing transient CELL
state:

```text
decay target CELL to the current tick
live_before := target.state > 0
add the arriving physical impulse

threshold-1 CELL:
    use the authoritative firing rule
threshold-greater-than-1 CELL:
    fire only if live_before and accumulated state reaches threshold

on such a firing:
    transmit ordinary outgoing ARROWs
    expose the existing local proposal operation, regardless of whether the
    completing arrival was external or ARROW-carried
```

`live_before` is an execution-local observation of the already serialized
CELL state. It is not retained. The law adds no CELL field, ARROW field,
SPIKE field, identity trace, membership record, relation key, or evaluator
signal. Threshold is existing physical matter, not a logical level marker.

The consume/produce contract is:

- consume: current decayed target state, current physical impulse, threshold,
  refractory time, and ordinary incident/outgoing physical structure;
- produce: the existing state update and, only after a live-state threshold
  crossing, ordinary firing, ARROW traversal, eligibility, and local proposal;
- quiescence: no hidden reset, cutoff, evaluator deletion, or autonomous
  source refiring.

No alternate law will be tested in this lane. A need for another persistent
variable, contributor identity, semantic representation, evaluator-selected
proposal, or a second scientifically distinct rule is an immediate stop.

## Symmetric physical opportunity

The flat field contains four anonymous participant loci and the complete six-
site symmetric convergence opportunity. Every site has identical ordinary
CELL/ARROW construction. Labels A--D and site indices exist only in the
evaluator schedule and result serializer.

At a site, actual route activity reaches one ordinary thresholded CELL from
each of two independent physical approaches. The first arrival leaves ordinary
state. A completing contemporaneous arrival may fire it under the candidate
law. A nearby ordinary output target and the existing local proposal operation
permit repeated physical conjunction to leave a reusable ARROW. A global
matched return opportunity reaches every site identically; only an ARROW that
actually traversed is locally eligible for support.

The field is complete and symmetric before experience. No scenario selects a
site, installs an expected organization, supplies a duo-change signal, or
feeds an evaluator comparison back into matter.

## Common matched marginals

Initial experience alternates A+B and C+D. Reversal experience alternates A+D
and C+B. Within every compared schedule the following are exactly matched per
individual route and serialized independently:

- external arrival count and impulse;
- source and approach firing count;
- candidate traversal count;
- return opportunity and local return count;
- cluster order and timing marginal;
- pressure exposure and elapsed world time;
- outward route effects;
- physical identity frequency;
- complete/permanent fingerprints, live topology, resistance, coupling,
  storage, and work.

Held-out clones receive ordinary activity and no evaluator mutation. Initial
A+B and C+D must activate mature organization; crossed A+D and C+B must not.
After reversal, old organization must weaken, stale, or deallocate through
ordinary pressure, new organization must form and execute, no relation-change
signal may enter, and an exact replay of the old schedule must not resurrect
dead history.

## Stage discipline

Each stage has a distinct atomic CSV/report pair and immutable outcome. The
binary refuses a missing or unknown stage argument and provides a no-CELL
`--preflight`. Existing output or staging paths cause refusal. Staging files
are synced and atomically renamed.

### PROBE

One primary fresh namespace plus an exact duplicate and a mirrored/allocation-
reversed replica. Twelve alternating A+B/C+D rounds. Clauses:

1. frozen hashes and isolated-law fingerprint pass;
2. individual marginals match;
3. both trained organizations mature and execute on held-out activity;
4. both crossed organizations remain silent;
5. A, B, C, and D singleton uses remain silent;
6. a completing participant too late for residual state remains silent;
7. correlation without route traversal remains silent;
8. traversal with blocked return does not leave reusable organization;
9. genuine co-participation activates;
10. ambiguity and four-way activity follow only physical incidence;
11. absent opportunity, stale/blocked path, replay, mirror/permutation,
    quiescence, and no-runaway controls pass;
12. old mature organization reached by A alone neither emits nor receives
    fresh local support.

Any failure is frozen and ends the lane before MICRO.

### MICRO

Two fresh namespaces plus exact duplicates. It repeats positive acquisition,
then performs the mandatory reversal with matched ordinary experience. It
requires old support to fall below execution or deallocate, new support to
mature and execute, old held-out use to remain silent, exact historical replay
not to resurrect dead structure, and evaluator state to remain causally absent.

The bootstrap control first allows every weak/new output opportunity to
deallocate completely, then presents ordinary changed conjunction plus the
same global return opportunity. New output structure must be proposed,
supported, and executed without a mature higher-order structure firing first.

MICRO also tests repeated singleton use, exact replay, fresh identities,
timing offsets, allocation order, natural quiescence, and bounded useful
recurrence. Any failure is frozen and ends before GATE.

### GATE

Eight fresh seeds crossed with four strata: normal/mirrored position,
normal/reversed allocation, normal/reversed arrival insertion, cluster gaps at
the live/dead boundary, distractor loads `0/8/24/48`, and namespace/layout
permutation. Every cell runs from blank matter and exact duplicate initial
state. All PROBE and MICRO clauses must pass.

If flat GATE is positive, the same substrate code, CELL type, ARROW type,
SPIKE type, and firing/proposal block is exercised without level flags through
`A+B -> X`, `X+C -> Y`, and `Y+D -> Z`. Primitive and learned participants
use the same thresholds, outgoing physical interfaces, and timing law.

After recursion, ordinary convergent ARROW propagation is tested without an
OR addition for A->C, B->C, and A+B->C. The existing state/time behavior is
serialized for together, ordered A-then-B, physical overlap/while,
within-window, too-late, and B-absent-before-natural-closure schedules. These
are observations, not new operators.

GATE is development-only. It cannot claim PX3, spend definitive evidence,
advance PX4, or authorize a shared-source change.

## Mandatory stop conditions

Stop and report immediately if:

- two scientifically distinct candidate laws remain;
- a new persistent substrate variable appears necessary;
- contributor identity, Event/Pair/Group/member state, or renamed semantic
  representation is required;
- authoritative PX0--PX2 bytes would need to change;
- recursive failure presents an architectural choice;
- a non-mechanical scientific clause fails.

Only a mechanically unique defect may be corrected, and only under a new
committed protocol that preserves the failed artifact. No return is permitted
for accounting, serialization, fresh seed, or mechanically forced timing
issues.

## Audits and handoff

Before each execution: format, focused tests, strict Clippy, frozen-source
hashes, changed-path isolation, forbidden-token scan of organism-visible code,
no-argument/wrong-argument refusal, preflight, and artifact absence.

The final handoff records exact commits/tags/hashes, law and physical contract,
every reached stage, recursion/OR/timing results where eligible, work and
storage ledgers, fingerprints, leak/source audit, unchanged authoritative
hashes, blockers, and remote equality. The branch and annotated lane tags are
pushed to origin and the worktree finishes clean.
