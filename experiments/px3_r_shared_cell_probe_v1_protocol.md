# PX3-R Arm B anonymous shared-CELL recruitment PROBE v1 protocol

Status: **PREREGISTERED DEVELOPMENT ARM; EVIDENCE UNSPENT; PX3 AUTHORITY ABSENT**.

## Frozen start and authority

This independent mechanism-discrimination arm starts at exact PX3 Class-D
negative lineage HEAD `873094497ff6eb74363191dc5edc479c7d66de72`.
Its authoritative PX2 ancestor is
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
`px2-physical-causal-direction-authoritative`.

The PX3 frozen negative, every earlier authority artifact, and the
authoritative PX0-PX2 law and execution sources remain byte-identical. This
arm is development-only. It cannot advance PX3, execute a definitive matrix,
modify PX0-PX2, consume another PX3-R arm, or begin PX4 composition.

## Neutral question and stop rule

Can repeated physical co-participation stabilize ordinary incoming ARROWs
from multiple paths onto an otherwise anonymous local CELL, leaving reusable
convergent CELL/ARROW structure that distinguishes recurring organization
from individually learned paths?

The first admissible test uses only the retained PX0-PX2 local return,
ordinary pressure, refractory, decay, generation, and local weak-ARROW
proposal laws. CELL creation is not available. Failure of the preregistered
field freezes an Arm B negative. Any need for a new CELL, new allocation law,
direct trace-to-trace coupling, downstream-continuation convergence, semantic
record, or other representation/substrate law freezes that requirement rather
than rescuing the arm.

## Preregistered physical opportunity

Each fresh CELL contains a complete, symmetric six-site physical field. Every
unordered combination of four individually equivalent route occurrences has
one geometrically equivalent site; no expected training combination is given
special topology.

At each site the substrate contains:

- two ordinary route-local source CELLS, positions `site - 2` and `site + 2`,
  region `0`, threshold `2`, resistance `1000`;
- one otherwise anonymous local CELL at `site`, region `0`, threshold `2`,
  resistance `1000`;
- one outward CELL at `site + 10000`, region `1`, threshold `1`, resistance
  `1000`;
- one weak positive ARROW, resistance `1`, coupling `1`, delay `1`, from each
  route-local source CELL into the anonymous CELL;
- one ordinary positive return ARROW, resistance `1000`, coupling `1`, delay
  `1`, from the anonymous CELL to each of the two local source CELLS;
- one positive outward ARROW, resistance `1000`, coupling `1`, delay `0`, from
  the anonymous CELL to its outward CELL.

Sites are separated by `100` position units. A route occurrence physically
enters its three route-local source CELLS. Training entry uses two simultaneous
unit SPIKEs per source so the source fires once. Held-out use arrives through
three remote driver CELLS and fixed driver-to-source ARROWs, so observation
cannot create a weak opportunity. A returned unit SPIKE reaches a reset
threshold-`2` source, strengthens the eligible source-to-anonymous ARROW under
the retained local-return law, and does not refire the source.

The initial weak field is broad and pre-existing. If ordinary pressure removes
an unsupported weak ARROW during development, a later external source firing
may recreate only the same geometrically local weak ARROW under the already
authoritative radius-`2` proposal law. No CELL is created. No source is within
radius `2` of another source, so this opportunity cannot create direct
source-to-source coupling.

Each fresh CELL also contains remote driver CELLS and isolated distractor
CELLS. They have no local target within proposal radius. Scenario labels,
expected recurring combinations, and result clauses are evaluator-only and
do not construct, label, select, or update an anonymous CELL or ARROW.

## Main matched-marginal discriminator

Routes `A`, `B`, `C`, and `D` are evaluator names for four equivalent external
occurrence schedules only. The organism contains no route, combination, or
scenario identity.

- acquisition: `16` rounds;
- round spacing: `10` ticks;
- within-combination spacing: `0`;
- between-combination gap: `4` ticks;
- the `A+B` and `C+D` clusters alternate early/late every round;
- every route receives exactly one occurrence per round, three source-CELL
  firings, six external training SPIKEs, equal return opportunity, equal
  traversal count, and equal temporal/order marginals;
- one isolated distractor fires at both cluster times in every round;
- after the last cluster, `10` ticks of ordinary pressure and natural decay
  occur before state serialization and non-proposing held-out use.

The evaluator must separately serialize, for every route, the sorted three
incoming-ARROW resistance/live values and their sum. It must separately
serialize all six anonymous-CELL incident incoming resistance/live values,
anonymous-CELL firings, outward crossings, complete/permanent fingerprints,
ARROW count, persistent bytes, and the full work ledger.

Both held-out trained combinations `A+B` and `C+D` must fire their anonymous
CELL and produce one outward crossing. Both crossed combinations `A+D` and
`C+B` must produce neither. All four individual-route strength multisets and
sums must be exact equals. A positive is forbidden if any route is
individually stronger.

## Swap and contemporary relearning

The same physical matter then receives `20` matched rounds of `A+D` and
`C+B`, with the same cadence, alternating early/late order, per-route counts,
distractors, pressure, and post-training gap. Before swap, old trained use and
crossed use are recorded on non-proposing clones. After swap:

- both old structures must weaken below physical executability under ordinary
  pressure plus their unsupported ordinary use;
- both new structures must be present and executable;
- held-out `A+D` and `C+B` must each fire one anonymous CELL and cross outward;
- held-out `A+B` and `C+D` must not;
- all route-local strength multisets/sums must remain exact equals.

No evaluator deletion, reset, lifecycle flag, or stale-state selection is
permitted.

## Hard controls

All controls use fresh namespaces and atomically published rows.

1. Exact duplicate complete-state replay must match every serialized state,
   activity, fingerprint, and work value.
2. Mirrored layout, reversed CELL allocation order, reversed simultaneous
   arrival order, fresh physical identities, and a route-identity permutation
   must preserve the isomorphic result.
3. A spacing replica uses between-combination gap `6`, round spacing `14`, and
   otherwise matched marginals; it must preserve the discriminator.
4. A fresh stable-alternative control trains `A+D` and `C+B`; those structures
   must emerge and `A+B`/`C+D` must not.
5. Correlated distractor activity without route-source participation must not
   strengthen any weak incoming ARROW or fire an anonymous CELL.
6. Full route participation with anonymous-CELL return ARROWs physically
   blocked must show training-time coincidence but no reusable held-out
   structure after pressure.
7. An absent-opportunity control contains the anonymous CELLS but no weak
   incoming ARROWs and uses only non-proposing driver arrivals; it must remain
   absent.
8. A stale control lets the complete weak field experience `30` ticks of
   ordinary pressure before non-proposing use; stale ARROWs must be physically
   deallocated and non-executable.
9. Ambiguous three-route co-participation must stabilize all three physically
   activated anonymous sites, not select one expected organization.
10. Genuine simultaneous four-route co-participation must permit all six
    anonymous structures to persist.
11. Every finite propagation must drain naturally, every source firing must
    equal its explicit occurrence count, and autonomous source refiring must
    remain zero.

## Interpretation

`POSITIVE_CANDIDATE_SHARED_CELL_RECRUITMENT` requires every main, swap, source,
matched-marginal, replay, layout, spacing, ambiguity, multiple-structure,
blocked-return, absent-opportunity, stale, quiescence, and forbidden-information
clause conjunctively.

`FROZEN_NEGATIVE_SHARED_CELL_RECRUITMENT` is published if the exact retained
physics and preregistered opportunity execute correctly but the main or swap
discriminator fails. `FIRST_CLAUSE_FAILURE` is published for a source,
integrity, marginal, replay, or control failure. No outcome authorizes a
fixture amendment or rescue. A positive is only a development candidate and
does not advance PX3.

## Fresh namespaces, artifacts, and evidence spending

Namespace bases are in the unused `0x9_B1..0x9_BD` range, one base per primary,
replica, duplicate, and negative control. Exact bases are frozen in the
implementation audit before evidence is spent.

The sole evidence command, after the implementation is committed, tagged,
and validated without entering a CELL, is:

```text
cargo run --release -p px0-physical-correspondence \
  --example px3_r_shared_cell_probe_v1 -- --probe
```

It emits exactly one
`PX3_R_SHARED_CELL_PROBE_V1_EVIDENCE_SPENT` marker and atomically creates:

```text
results/px3_r_shared_cell_probe_v1.csv
results/px3_r_shared_cell_probe_v1.md
```

Staging paths use the same basenames prefixed by `.` and suffixed
`.staging`. Any final or staging path pre-existence refuses execution. Any
outcome is frozen without rerun.

## Validation and audits

Before evidence: exact frozen hashes and ancestry; protocol/source hashes;
focused format, compile, and strict Clippy; no-argument/wrong-argument refusal;
no-CELL `--preflight`; final/staging absence; organism-visible forbidden-token
scan; exact namespace inventory; and proof that the frozen HEAD differs only
by fresh Arm B files.

After evidence: artifact shape and hashes; matched marginals; swap; complete
duplicate replay; source/refiring/quiescence; work/storage accounting;
forbidden-information scan including `Event`, `Episode`, `History`, `Pair`,
`Group`, member lists, pair keys, semantic flags, evaluator-selected pairing,
old M3 schema, typed adapters, serialized co-occurrence, and renamed
equivalents; exact unchanged-port hashes for PX0-PX2; frozen-negative artifact
hashes; clean worktree; isolated branch/tag; and pushed remote equality.

No broad historical suite is authorized because shared code must not change.
No authority or definitive matrix may be run or simulated.
