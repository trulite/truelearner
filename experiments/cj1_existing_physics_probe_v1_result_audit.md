# CJ1 unchanged-physics PROBE v1 result audit

Status: **INTERPRETABLE NEGATIVE; FIRST COLLAPSE FROZEN; ONE LATER FIXTURE INVALID**.

## Sole execution and artifacts

The frozen command executed once from implementation commit
`2d17af33db2970f6116251dfbb5c1f7bb8f0db3c` and emitted exactly one
`CJ1_EXISTING_PHYSICS_PROBE_EVIDENCE` marker.

| artifact | bytes | SHA-256 |
|---|---:|---|
| PROBE CSV | 1,739 | `82840f4e16063da3301710b2524299b6092f34695850a7499667f38cd88b481e` |
| PROBE report | 635 | `506e18f9443ddd722ca17d68117e5da555e323c8921bf5b0fe69270962934b31` |

There are 13 unique rows, 20 columns, exact duplicate replay in every row,
natural quiescence in every row, no staging remnant, 571 native operations
and 7,072 summed independently reconstructed persistent bytes.

## Interpretable first collapse

The first ten ordered rows are valid. Nine pass. The first physical collapse
is `mature-one-path`:

- one source CELL fires once;
- exactly one incoming physical ARROW traverses;
- its mature coupling carries impulse 2 to the threshold-2 local CELL;
- the local CELL fires once and produces one effect, although zero is required;
- held-out execution repeats the same false effect;
- the matched fresh coupling-1 different-path row fires exactly once and
  passes.

Thus exact PX0 physics has burst/refractory saturation for repeated unit
activity but does not prevent one mature path's amplitude from replacing
different-path participation. This freezes the unchanged-physics answer as
negative without changing PX0--PX2.

## Frozen evaluator defect

The final `two-origins-shared-path` row is invalid. World construction always
inserted A->locus before its scenario branch, then additionally inserted
A->shared, B->shared and shared->locus. The recorded two locus arrivals and
one effect therefore came from two actual incoming paths, not the intended
single shared path. This is a mechanically unique topology-construction defect
visible directly in the frozen source and row counters.

The defect occurs after the already-frozen mature-path collapse and cannot
rescue or reinterpret it. The CSV/report are immutable. A fresh protocol may
correct only this control with the authoritative law and all expectations
unchanged before the candidate ladder proceeds.

## Decision

Classification A is rejected. The sole preregistered path-local transient
candidate is eligible only after the shared-path correction is preregistered,
implemented and frozen. No other scientific law, definitive/authority work or
PX3 restart is authorized.
