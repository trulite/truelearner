# CJ0 ARM CJ-B locally gated ARROW development readiness

Status: **DEVELOPMENT-READY AT GATE; LANE CLOSED**.

## Classification

Fresh GATE v2 passes `8/8` rows and `88/88` clauses after the separately
preregistered, mechanically unique, uniform timing correction. GATE v1 remains
an immutable frozen terminal development negative and is not reclassified.

This readiness finding is limited to development comparison. It does not
advance PX3, PX-C, or parked PX4--PX8 and creates no later execution surface.

## Minimal physical law

For each live outgoing ARROW when its source CELL fires:

```text
decay(destination.state, current_tick)
available = destination.state + arrow.coupling
if available < destination.threshold:
    suppress without eligibility
else:
    destination.state = 0
    emit one ordinary generation-bound SPIKE carrying available
    make only the traversed ARROW locally return-eligible
```

An ordinary timely return increases existing ARROW resistance and raises its
coupling only up to 2. Ordinary pressure weakens and may deallocate ARROWs;
generic local reproposal uses existing position, liveness, generation,
coupling, and resistance fields. No persistent physical field, participant
list, logical operator, level marker, or relation-change signal was added.

## Satisfied development properties

The frozen evidence supports genuine current co-participation, singleton
rejection, reusable learned organization, forgetting and changed-world
relearning, self-evidence rejection, bootstrap after full deallocation,
recursive reuse through three levels, ordinary convergent inclusive
alternatives, temporal discrimination from existing transient state/order,
matched-marginal discrimination, replay, finite recurrence, and natural
quiescence.

The timing-boundary control additionally fixes the scope: a coupling-1,
resistance-1 proposal is not guaranteed to survive until delivery when an
ordinary pressure boundary intervenes. Its generation-bound queued SPIKE must
be discarded, and recovery proceeds only through fresh ordinary reproposal.

## Frozen evidence chain

- PROBE: `4/4` rows, `40/40` claims;
- MICRO: `4/4` rows, `40/40` claims;
- GATE v1: frozen negative, `4/8` rows and `76/80` claims;
- GATE v2: positive terminal development gate, `8/8` rows and `88/88` claims.

Candidate physical source SHA-256:
`ef0de37a9ac54b632b991f0d4647a5ee78c23810084d61497c88d6f757ec2188`.
