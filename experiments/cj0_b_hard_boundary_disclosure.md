# CJ0 ARM CJ-B hard-boundary disclosure

The lane is development-only and ends at GATE.

Before the explicit hard-boundary message arrived, the PROBE binary was
invoked once with the rejected argument `--definitive` as a refusal check. It
exited `2`, entered zero physical cells, emitted no evidence marker, and wrote
no staging or final artifact. No protocol, scientific simulation, result, or
execution surface for that stage was created.

After the boundary message, refusal tests use only the neutral argument
`--invalid`. No later-stage protocol, command, artifact, simulation, or marker
will be created. The terminal scientific surface in this lane is GATE.
