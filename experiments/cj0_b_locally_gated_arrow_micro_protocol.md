# CJ0 ARM CJ-B locally gated ARROW MICRO protocol

Status: **PREREGISTERED; MICRO UNSPENT; GATE/DEFINITIVE UNAUTHORIZED**.

## Frozen parent

This MICRO consumes the positive PROBE frozen at commit `6b94574`, tag
`cj0-b-locally-gated-arrow-probe-v1-positive`. The physical module is frozen
at SHA-256
`ef0de37a9ac54b632b991f0d4647a5ee78c23810084d61497c88d6f757ec2188`.
It may not change. PX0--PX2 and every PX3/PX3-R negative remain immutable.

## Mandatory reversal

For each of the four frozen fresh layout/allocation/insertion variants:

1. acquire A+B and C+D for the exact eight-round PROBE schedule;
2. use the fixed physical gap and verify held-out old organization once;
3. change only ordinary experience to A+D and C+B for 20 matched rounds;
4. apply no evaluator deletion, invalidation, endpoint selection, relation
   signal, change signal, or historical lookup;
5. allow only ordinary ten-tick pressure, unsupported eligibility pressure,
   generation invalidation, generic local proposal, actual traversal, and
   returned activity;
6. apply a fixed post-change gap that removes fresh unsupported proposals;
7. verify held-out A+D and C+B each cross once, while A+B and C+D each cross
   zero times.

The old supported candidate ARROWs must weaken to zero live resistance or a
physically appropriate stale/deallocated equivalent. New candidates must form
from generic coupling-1 proposals and mature by actual changed
co-participation. Old held-out use after the gap must not recreate or resurrect
history because held-out source firing arrives through an ordinary strong
driver rather than as an external proposal-triggering source arrival.

Initial and changed phases exactly match route occurrence frequency, total
external arrivals, total impulse, source firing opportunity, contributor
fanout, traversal, return opportunity, pressure schedule class, outward
effect opportunity, and identity occurrence frequency. Cluster order
alternates. Layout mirror, cell/ARROW allocation order, physical identity
order, and arrival insertion order vary by row.

## Full-deallocation bootstrap control

In fresh matter, expose the complete weak numeric field and apply 30 ticks of
ordinary pressure before any coactivity. Confirm all four weak candidates are
dead. Then present only ordinary A+D/C+B physical activity. The changed
organization must be proposed, transmit from coupling 1 plus current state 2,
receive physical return, mature, and execute held-out without any mature
higher-order structure firing first.

An otherwise identical no-return bootstrap control may exhibit training-time
transmission but must leave zero held-out reuse after pressure.

## Additional MICRO controls

- trigger-alone and contributor-alone after reversal: zero output and zero
  candidate state consumption;
- old held-out use before change: positive, proving the old structure existed;
- old held-out use after change and after an additional quiescent gap: zero;
- changed held-out use after the same gaps: positive;
- no evaluator invalidation or selective pressure call exists in source;
- exact replay from byte-identical post-change state;
- fresh namespaces and the four frozen physical variants;
- natural queue drain and finite returned recurrence without runaway;
- all candidate records, live counts, maxima, generations, proposals,
  deallocations, stage fingerprints, work, and persistent storage serialized.

## Conjunctive clauses

`M0` frozen ancestry/source/isolation; `M1` initial organization exists and
old held-out executes; `M2` experience marginals are exact; `M3` old support
weakens/deallocates without evaluator action; `M4` changed support forms and
executes; `M5` old support does not execute or resurrect; `M6` full-deallocation
bootstrap succeeds and no-return bootstrap does not persist; `M7` self-evidence
and singleton controls; `M8` exact replay/quiescence/no runaway; `M9` complete
serialization/accounting.

All ten bits must pass in all four rows. The first failure freezes MICRO as a
negative and makes GATE ineligible. A positive MICRO licenses only a separately
frozen GATE protocol for recursion, OR, and temporal expressivity.

## Evidence discipline

Add only a fresh evaluator binary `src/bin/micro.rs` and audit documents. The
physical module and frozen PROBE evaluator/results remain byte-identical.
Require formatting, focused tests, strict Clippy, zero dependencies, frozen
hashes, source/leak audit, no-CELL preflight, refusal behavior, and result-path
absence.

The sole MICRO command is:

```text
cargo run --release --manifest-path arms/cj-b-locally-gated-arrow/Cargo.toml \
  --bin micro -- --micro
```

It atomically publishes one CSV/report pair and may not be rerun or rescued.
