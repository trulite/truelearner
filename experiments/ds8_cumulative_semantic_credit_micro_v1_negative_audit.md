# DS8 cumulative non-semantic-credit MICRO v1 negative audit

Outcome: **VALID DEVELOPMENT NEGATIVE; M5 REMAINS AUTHORITATIVE**.

The exact implementation
`0573448d0a160d595f3432360e06cd019fdf06ba` passed E2B formatting and focused
library compilation before MICRO executed once in sandbox
`ie2usmlbtjokk80bkr6zr`. It exited `1` with a duplicate-exact two-cell
negative. No cell was rerun or rescued.

Both histories passed blank acquisition, exact physical consequence
accounting, equal-magnitude contrast, learned allocation `8/1`, raw-history
swap reversal `1/8`, occurrence/identity/layout transfer, and authoritative
M5 preservation.

The blocked cell's first functional failure was the shuffled-value control.
Its varying-consequence history occurred entirely before the recurrent
history. Once recurrent contrast formed, only the active recurrent encounter
received updates, producing `9/0`. The frozen M5 helper `swap_values` requires
two existing value records, so it returned false and left allocation `8/1`.
The interleaved cell had two value records (`9/2`) and its same helper reversed
allocation to `1/8`.

This is a control-construction mismatch, not retroactive evidence for PASS.
Under the preregistered “reverses or destroys” clause, a mechanical retry may
move the single learned value association from one already-recognized
encounter to the other when only one exists. It may not change acquisition,
the consequence learner, M5 update, histories, thresholds, or admissions.

Both cells also reported a source-audit failure isolated to the generated
organism-fragment digest. All positive PROBE source predicates remained exact;
the fragment extraction's newline convention was not exposed before MICRO.
A retry may expose and correct that digest only through a no-cell audit before
execution.

M5 remains authoritative. MICRO v2 requires a separate preregistration.
