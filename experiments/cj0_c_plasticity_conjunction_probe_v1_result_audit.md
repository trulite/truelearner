# CJ0-C plasticity-only conjunction PROBE v1 result audit

Status: **FROZEN MATERIAL NEGATIVE; FIRST SCIENTIFIC COLLAPSE AT REVERSAL**.

## Atomic artifacts

- CSV SHA-256:
  `bf9b433117100e961dc9beee2619cb7c0909ad96ab73f9394d8f998bdb5b7941`;
- report SHA-256:
  `aa5c3310702a8120c0faa558edeadae41bbc3289f2326429127ce1657fb58da1`;
- CSV/report storage: `1,439` / `1,534` bytes;
- CSV shape: `4` data rows, `29` columns;
- protocol SHA-256:
  `adc58816e9f78634879c16140de2bb508c5f11261a13d871063e1aa0d68d019f`;
- authoritative law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`.

The runner wrote both result files through fresh same-directory staging files
and atomic rename. No staging path remains. No overwrite or rerun occurred.

## Result

All four fresh identity/layout/allocation/timing cells were exact qualitative
replicas and failed the same mandatory clause.

Before reversal every cell produced:

- learned A+B and C+D targets: present and distinct;
- summed live learned support: `138`;
- held-out learned/crossed/singleton outputs: `2/0/0`;
- full-deallocation detection and changed-experience bootstrap: pass;
- self-evidence control: pass;
- too-late, absent opportunity, stale, correlation-only, and traversal-only
  controls: pass;
- three/four-way ambiguity symmetry: pass;
- exact blank replay, matched route firing marginals, and natural quiescence:
  pass.

After `40` ordinary changed-experience rounds, every cell produced:

- new A+D and C+B targets: present and held-out outputs `2`;
- old A+B and C+D targets: still live and held-out outputs `2`;
- measured old-support sum: `138 -> 164`, strengthening rather than weakening;
- no evaluator invalidation or historical dead-record resurrection.

The result is therefore a material negative even though every pre-reversal
discriminator and common control passed.

## Exact first collapse

The new incident-support hook strengthens a target's incoming ARROWs only when
two distinct current eligible sources make that target fire. However, the
candidate ARROWs remain ordinary outgoing ARROWs and therefore also enter the
authoritative PX2 `apply_local_return` path unchanged.

During reversal, one source traverses both its obsolete and changed outgoing
ARROWs. A later ordinary traversed arrival at that same source within the
four-tick eligibility window invokes authoritative local return and
strengthens all still-eligible outgoing ARROWs. That physical update does not
require the obsolete target to fire or its old second contributor to be
currently present. Alternating `4/12` recurrence supplies this update often
enough that obsolete support grows despite pressure.

Thus the combined law does not satisfy its own plasticity-only condition:
conjunctive proposal and incident support cannot prevent the already-
authoritative unary return path from maintaining obsolete candidate matter.
This is not an evaluator defect, timing plumbing defect, result-accounting
defect, or fresh-seed defect.

## Why no retry is eligible

Repair requires a scientifically additional selection rule that exempts some
ordinary ARROWs from authoritative unary return or otherwise distinguishes
which physical topology/numeric signature receives which plasticity path.
Possible selectors are scientifically non-equivalent, and a new persistent
substrate marker would violate the lane stop rule. Changing PX2 would violate
authority. The protocol's exact candidate is therefore frozen negative; it
cannot be rescued under a mechanical retry.

MICRO, GATE, recursion, OR convergence, temporal-expressivity follow-up,
definitive evidence, PX3 advancement, and PX4--PX8 were not executed.

## Validation and isolation

- exact frozen start ancestry: pass;
- authoritative PX0--PX2 source changed paths: none;
- focused formatting/check/strict Clippy before execution: pass;
- no-argument/wrong-argument refusal: pass, exit `2`;
- no-CELL/ARROW and hash preflight: pass;
- organism-visible forbidden-token scan: zero hits;
- all finite executions naturally quiescent; no activity cutoff exists;
- ledgered published-row work: `93,528` operations;
- final persistent storage summed across cells: `8,704` bytes;
- broad historical tests: not run because shared/authoritative code did not
  change;
- definitive evidence: unspent.
