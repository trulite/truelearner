# DS-AP0 aftermath-to-plasticity activation

Status: **PREREGISTERED; DEVELOPMENT OUTCOME UNSPENT**

Exact parent: `12a620220e2dfb35f12d59cee821b49451d224d4` /
`ds2-after-ac0-mechanistic-retry-collapse-handoff`.

## Sole question

> Can ordinary physical activity produced by the AC0-selected A1 route reach
> the already-existing A1 local proposal/probation machinery without supplying
> a causal relation, endpoint role, direction label, or evaluator candidate?

## Frozen path

```text
selected existing A1 root
  -> root SPIKE / live A1 ARROW traversal
  -> anonymous traversal-local coactivity
  -> existing A1 local proposal former
  -> existing probationary support update
```

The new wire may copy only the member-relative endpoints already physically
bound to a traversed live arrow. It may not inspect downstream success, choose
which relation should be proposed, reverse or rank endpoints, synthesize a
candidate from a choice handle, or create a DS2-specific candidate type.

## Development requirements

- selected physical execution produces at least one ordinary local proposal;
- proposal formation and probation use the byte-frozen A1 functions and state;
- blocking, staling, or skipping execution produces no traversal-derived
  proposal;
- alternate physical route produces its own ordinary proposal opportunity;
- opaque-handle permutation preserves the associated physical route result;
- fresh occurrences and allocation/layout perturbation preserve proposal
  formation;
- unrelated activity with no traversed live A1 arrow produces no proposal;
- no causal/source/target label, evaluator direction, semantic credit, reward,
  or correctness enters the wire;
- the wire adds zero persistent state beyond the existing A1 probationary
  support it activates.

AP0 does not require supported and reverse candidates to differentiate. It
does not consolidate, prune, compile, or claim causal direction. After AP0 is
frozen, rerun the unchanged cumulative DS2 stage sequence and stop at the first
new missing path. M1 remains authoritative and M2 remains absent.
