# DS8 cumulative non-semantic-credit PROBE v1 negative audit

Outcome: **VALID DEVELOPMENT NEGATIVE; M5 REMAINS AUTHORITATIVE**.

The exact implementation at
`1f54752c14dbfc80e1f91015ca8bcb4ef3ed2240` passed E2B formatting and focused
library compilation before the one preregistered release command ran in
sandbox `ie2usmlbtjokk80bkr6zr`. The command exited `1` and reported a
duplicate-exact negative. It was not rerun or rescued.

The first failure is exactly:

```text
variation before consequence = false
```

The v1 wrapper conjunctively required every one of the 24 scheduled encounter
presentations to form and execute a variation. During the same blank-start
history, the frozen M5 allocator learned to suppress the varying-consequence
encounter. Suppressed encounters lawfully produced no downstream consequence,
so only 17 physical consequences occurred (`51` spikes and `34` arrow
traversals), rather than the wrapper's fixed expectation of 24.

This audit does not reinterpret the failed clause. The clause and observed
counts remain negative. It records only the mechanically visible dependency:

```text
learned M5 allocation suppresses a variation
  -> no variation executes
  -> no downstream consequence can physically follow it
```

The later functional observations remain development diagnostics only:
non-semantic contrast formed, active-only update passed, swapped consequence
history reversed allocation, learned admissions were `8/1`, shuffled values
reversed them, fresh identity/layout transferred, and authoritative M5
remained exact.

The independent source-audit failure remains unresolved at this freeze. A
static diagnostic may identify its exact predicate without rerunning the
probe. Any retry requires a new preregistration and may change only
mechanically false wrapper expectations or source-audit plumbing. The physical
consequence learner, differential linker, M5 mechanism, seed, and episode
history remain frozen unless a new scientific ambiguity is found.
