# Post-M6 DS4 physical-arrival initiation development protocol

Protocol identifier: `post-m6-ds4-arrival-initiation-v2`.

Status: **PRE-EVIDENCE NAMESPACE AMENDMENT; NO SUCCESSOR EVIDENCE SPENT**.

Protocol v1 was frozen at commit
`c76f5884af42b400bd9d4cf76fc58b669f3899ef` before a complete comparison
against the M6 definitive seed list exposed one collision: v1 retry seed
`74_500_000` was already spent by the M6 matrix. Nothing was compiled or
executed under v1. Version 2 changes only the development namespaces below;
the scientific target, first collapse, permitted gate, stages, controls, and
stopping rules are byte-identical to v1.

This is a separately named successor from authoritative M6. It is not a
rescue, amendment, rerun, or reinterpretation of the immutable cumulative DS4
attempt #1. This protocol governs PROBE, MICRO, and GATE only. It cannot create
M7 and it does not authorize a definitive execution.

## Frozen question

Starting from authoritative M6, can anonymous external activity physically
arrive, be organized by the frozen learned event mechanism, and cause the
frozen learned occurrence-role mechanism to select and initiate downstream
recurrence, while request meaning, `START`, evaluator-selected initiation, and
semantic terminal credit are absent?

The unchanged target is:

```text
anonymous external activity arrives
  -> learned event completion activity
  -> learned occurrence-role selection
  -> downstream recurrent activity
  -> frozen M6 delayed physical-consequence differential
  -> only the active occurrence-role trace changes
```

No `REQUEST`, `START`, `respond-now`, `task-begins-here`, expected answer,
correctness bit, target field, evaluator-selected initiating occurrence, or
equivalent covert channel may cross the organism boundary or the linker.
Physical arrival, local order, learned event organization, learned lifetime,
learned plasticity allocation, and learned non-semantic consequence/credit are
lawful because they belong to M6.

## Exact ancestry and frozen inputs

- authoritative M6 commit:
  `aa4e22efd8a65b7694956a53cfaa970582695215`;
- authority tags:
  `core-autonomy-checkpoint-established`,
  `m6-cumulative-nonsemantic-credit-authoritative`, and
  `ds8-cumulative-semantic-credit-definitive-authority-handoff`;
- M6 definitive CSV/report SHA-256:
  `0cb9ba779fca1899cf030d30358fe9354cfb7b2cccf87f32df3f6ea9ddfe91e4` /
  `6a5d938c3e021344b00f3a559593fee860b5f6cceb777c409ad8d59a2dd71872`;
- M6 authority handoff SHA-256:
  `6cdd015d6b20f10a95f26c33dfe30ceb834b2663f9912926752fa1fb204c9ca9`;
- frozen M3 cumulative event port SHA-256:
  `c4fc7aca11a5925effeb5a84b90184a70da0f66da7c063d0f87ba46ca36addf3`;
- frozen P4 occurrence-role source SHA-256:
  `2dbde723b394bcb3d788c796aa1745cd1cea392a64ab61497bb97474866144b8`;
- frozen M5 allocator source SHA-256:
  `e755a70deada891e5c4db3b55809ca84ea8ad31a8bd3affe564bf08a95f8dff7`;
- frozen M6 PROBE/GATE source SHA-256:
  `11b4229122b3e0788ca30c55579b91ffe07461de9a138860690134565fcf2ed6` /
  `19c9051d15023c5b88559cba4ee3b3eb55686d1a68e083ca260a4a65629e8f30`;
- immutable DS4 attempt #1 CSV/report SHA-256:
  `c6b626650fc199a8ebe2feae8115a8b27071088f963f919393f55e24fbe44a3a` /
  `97f1fc665e03be1ccd398dcdf34fbb262c525aabc23a812c8740c350dd890659`;
- post-M6 ordering amendment SHA-256:
  `609dc63ab8051316703899717fc30861d7a700d0ec60f205fa6d687ad478616d`;
- Lane A brief SHA-256:
  `fcf46f87b597ba8f264176c1e0ca2bec4a149b3cf094f7db03f53d4ef9a331e0`.

Every frozen source is composition-copied byte for byte. The successor may
add composition glue, transient fixtures, audits, controls, and reports. It
may not edit or reinterpret M0--M6, the old DS4 mechanism or result, or any
Lane B artifact.

## First collapse and only permitted gate

The frozen pre-M6 DS4 linker ends acquisition with:

```text
evaluator comparison -> functional bool -> active request-pattern feedback
```

That relation is a supplied semantic credit primitive and is forbidden after
M6. With it removed, the unchanged target has no lawful update from downstream
physical activity to the already-active occurrence trace. This is the first
developmental collapse.

Exactly one missing physical relation is forced:

```text
delayed raw recurrent CELL/ARROW/SPIKE activity
  -> frozen M6 consequence normalization and contrast
  -> transient learned differential
  -> active occurrence-pattern trace feedback
```

The raw activity is derived only from executed recurrence. A selected
occurrence that produces no recurrence produces no fabricated consequence and
no update. The M6 differential carries no evaluator answer or semantic
polarity. The gate adds no persistent representation: consequence shapes stay
in the frozen M6 learner, occurrence patterns stay in frozen P4, and the
differential is transient.

If this gate is insufficient, development stops unless the next missing edge
is uniquely mechanically forced. Any second plausible relation, semantic
primitive, upstream edit, or new representation is a scientific stop.

## Development populations

Fresh Lane A namespaces are disjoint from the old DS4 and all M6 development
and definitive populations:

```text
PROBE v1 collapse  base seed 140_000_000; one blank learner
PROBE retry        base seed 141_000_000; one blank learner
MICRO              base seeds 142_000_000 and 142_500_000; 8 held-out each
GATE               base seeds 144_000_000 through 146_500_000 by 500_000;
                   32 held-out each
```

Any future definitive protocol must use fresh namespaces and a separately
authorized one-shot workflow.

## Ordered stages and controls

The first collapse is reported in this order:

1. exact M6 authority, frozen-source, immutable-negative, protocol, and Lane
   isolation audits;
2. external activity physically reaches learned M3 completion;
3. completion activity reaches occurrence selection without a semantic start
   channel or a separately supplied target;
4. recurrent downstream activity reaches the frozen M6 consequence learner
   and only its active trace receives a transient differential;
5. one occurrence role consolidates from blank state;
6. fresh held-out activity transfers across depths `5,8,16,32`, all six
   serialization positions, fresh identities, relabelled M3 events, and
   reversed allocation;
7. determinism, non-plastic held-out state, lifecycle cleanup, physical work,
   and information-flow controls all pass.

Controls are conjunctive:

1. no arrival produces no event, selection, recurrence, consequence, or
   update;
2. arrival without learned event completion produces no selection or update;
3. missing close and invalid transition are silent; valid activity reenters;
4. no recurrent execution produces no fabricated consequence or update;
5. immediate activity is rejected by the frozen M6 minimum-delay rule;
6. absent active eligibility produces no update;
7. equal or shuffled consequence histories abstain;
8. raw-history swap reverses the learned differential;
9. fresh occurrence identities, M3 relabelling, layout, and allocation
   transfer;
10. the chosen opaque identity comes only from its anonymous occurrence;
11. expected answer, evaluator truth, semantic credit, request/start flags,
    stable occurrence identity, and Lane B state are absent from organism
    observations and persistent state;
12. held-out M3/P4/M6 state is unchanged, temporary state is erased,
    acquisition and held-out namespaces are disjoint, and duplicate execution
    is exact.

PROBE v1 freezes the expected semantic-credit collapse before the gate is
installed. The retry asks only whether the uniquely forced physical path is
nonzero. MICRO and GATE harden the same unchanged target; they may not tune the
gate after seeing a result.

## Readiness and authority boundary

Development readiness requires positive retry PROBE, MICRO, and GATE; all
ordered stages and controls; exact frozen hashes; atomic result artifacts; and
a clean committed implementation. The successor remains non-claim-eligible,
M6 remains authoritative, M7 remains absent, and DS5 remains blocked.

Definitive mode must reject before constructing a learner or enumerating a
seed. A separate authority workflow must preregister and authorize a fresh
single-execution matrix. Development must never rerun the spent DS4 or DS8
definitive evidence.
