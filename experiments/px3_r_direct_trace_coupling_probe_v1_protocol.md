# PX3-R direct physical trace coupling PROBE v1 protocol

Status: **PREREGISTERED; DEVELOPMENT PROBE EVIDENCE UNSPENT; PX3 ABSENT**.

## Frozen dependency and scope

The generic-opportunity collapse is frozen at commit
`21c606b02e3d63e4300999fb1a7b3e0a28b50023`, tag
`px3-r-trace-coupling-generic-opportunity-probe-v2-frozen-collapse`:
actual internally propagated trace firing cannot invoke existing generic local
proposal physics. This protocol preregisters the smallest ARM-A-specific
physical opportunity before implementing or executing it.

This is one independent mechanism-discrimination DEVELOPMENT arm. It cannot
advance PX3, run definitive evidence, alter authoritative PX0--PX2, consume
another arm, or begin PX4. The exact Class-D negative and its authoritative
ancestors remain byte-identical.

## Sole candidate law: local activity opportunity

An opt-in substrate has three environmental integers fixed before construction:

- maximum physical distance `8`;
- maximum recent-firing overlap `2` ticks;
- proposed-ARROW propagation delay `1` tick.

Whenever any live CELL fires, the substrate records that CELL's physical
firing tick. It mechanically inspects other live CELLs within the fixed
distance. For every locus whose recorded firing is no more than two ticks old,
it proposes any missing ordinary directed ARROW in both physical directions.
Each proposed ARROW has phase `0`, positive coupling `1`, resistance `1`, and
the fixed delay. Target order is physical-id order. After proposal, all ARROW
eligibility, returned-activity strengthening, coupling plasticity, ordinary
pressure, deallocation, generation invalidation, and SPIKE propagation are the
unchanged PX0--PX2 laws.

The opportunity has no CELL type, trace flag, route identifier, Event,
Episode, History, Pair, Group, member list, pair key, semantic boundary,
evaluator-selected endpoint, or serialized co-occurrence record. It applies to
all locally firing CELL matter. With the opportunity disabled, the copied arm
substrate must preserve the authoritative law behavior.

This PROBE authorizes no recruited shared CELL, new convergence, negative
coupling, semantic gate, hidden reset, evaluator cutoff, or second law.

## Four-route construction and matched marginals

The physical world contains four anonymous copies of the frozen PX2 route
motif. Each copy has arrival, correspondence, continuation, consequence,
participation-trace, outward, acquisition-driver, participation-driver, and
return-gate CELL matter plus ordinary retained ARROWs. The pre-existing shared
PX2 return hub may deliver ordinary returned activity, but it cannot retain or
select a relation. Trace thresholds and ordinary incoming coupling are fixed
so candidate coupling SPIKEs alone remain subthreshold and cannot autonomously
refire a trace.

Candidate trace loci occupy positions `0,1,2,3`; every other potentially
firing locus is more than distance `8` from them and from each other. Thus only
actual local trace coactivity can expose candidate inter-trace ARROWs.

Correspondence acquisition gives every route four uses at ticks
`0,16,32,48`. Candidate training begins at tick `66`, uses twelve rounds at
spacing `18`, presents routes `0+1` together and `2+3` together, separates the
two clusters by `8` ticks, and alternates cluster order each round. Each route
therefore receives identical frequency, traversal count, consequence count,
trace firing count, returned activity, early/late timing, ordinary pressure,
and outward effects.

The evaluator serializes, separately for each route:

- correspondence and direction ARROW resistance/live state;
- continuation, consequence, trace, ordinary return, candidate return, and
  outward counts;
- source and autonomous refiring counts.

It separately serializes the complete `4x4` physical inter-trace ARROW
resistance/live matrix. Scenario names and expected route combinations exist
only in the evaluator after construction and have zero causal path into the
substrate.

## Held-out discriminator

After training and a fixed `14`-tick gap, noninteracting complete-state clones
receive one physical occurrence of:

- trained organizations `0+1` and `2+3`;
- crossed organizations `0+3` and `2+1`.

A positive requires, before held-out use, live strong bidirectional ARROWs only
at `0<->1` and `2<->3`; all four individual route-strength vectors must be
equal. During held-out use, trained organizations must traverse two pre-existing
directions per organization, while a fresh crossed occurrence may expose only
the preregistered weak contemporary opportunity and must not become physically
equivalent to the recurrent trained structure. Exact total external arrivals
and outward effects must match.

## PROBE controls

Fresh namespaces are fixed for:

- positive opportunity and exact complete replay;
- opportunity absent with the same training schedule;
- actual participation with delay `6`, beyond the four-tick return window;
- temporally spaced participation at gap `4`, beyond the two-tick overlap;
- correlated external firing of nearby passive CELL matter without route
  traversal or trace firing;
- a single co-participation occurrence followed by `70` ticks of ordinary
  pressure.

The positive may pass only if recurrence plus actual candidate return produces
strong reusable structure; absent opportunity, non-overlap, correlation without
participation, participation without timely return, and stale single occurrence
do not. Every finite schedule must reach natural quiescence with zero autonomous
source refiring. Duplicate complete replay must be exact.

## Classification

- `DIRECT_TRACE_COUPLING_CANDIDATE`: all matched-marginal, held-out, return,
  control, audit, replay, and quiescence clauses pass;
- `FROZEN_NEGATIVE`: all controls pass but trained/crossed physical structure
  is not discriminated;
- `FIRST_CLAUSE_FAILURE`: any prerequisite/control fails.

Every result is frozen without rescue. A candidate permits a separately
preregistered MICRO; it grants no authority.

## Sole execution and atomic publication

The arm is implemented in a standalone crate whose default substrate has the
opportunity disabled. Authoritative files are never copied back or edited.
After commit/tag, source/hash/leak audit, formatting, focused compile, strict
lint, and no-CELL preflight, exactly one command may spend PROBE evidence:

```text
cargo run --release --manifest-path arms/px3-r-trace-coupling/Cargo.toml \
  --bin probe -- --probe
```

Atomic artifacts are:

- `results/px3_r_direct_trace_coupling_probe_v1.csv`;
- `results/px3_r_direct_trace_coupling_probe_v1.md`.

Staging paths use a leading dot and `.staging` suffix. Pre-existence is a hard
refusal. Publication uses create-new, sync, and same-directory rename. Exactly
one evidence-spent marker is emitted. Work-ledger fields, persistent bytes,
fingerprints, artifact bytes, and lane-added storage are accounted.
