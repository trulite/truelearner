# PX3-R direct physical trace coupling PROBE v2 protocol

Status: **PREREGISTERED MECHANICAL AMENDMENT; EVIDENCE UNSPENT; PX3 ABSENT**.

This protocol preserves the unexecuted v1 protocol at commit
`55c6c24f1a8e2c733ba256bc1f90de13be7d3250`, tag
`px3-r-direct-trace-coupling-probe-v1-protocol`. No CELL, candidate simulation,
preflight, or evidence command ran under v1.

## Pre-execution mechanical dependency

Read-only transition inspection found that a symmetric opportunity discovered
on the second of two overlapping firings is otherwise temporally asymmetric:
the newly proposed ARROW whose source is the earlier-fired locus would not see
that already-occurred firing, while the current-source ARROW would. At initial
resistance `1`, the untraversed direction can be removed by ordinary pressure
before the next `18`-tick recurrence. That would make persistence depend on
physical arrival order and leave per-route return counts unequal, violating
the already-preregistered discriminator before the scientific question is
reached.

## Sole amendment: retained local firing traverses the exposed edge

The v1 opportunity already retains the earlier locus's physical firing tick
for the two-tick overlap test. V2 states the mechanically complete edge
exposure:

- when the second local firing exposes two missing directed ARROWs, the current
  firing traverses its newly proposed outgoing ARROW through the unchanged
  ordinary outgoing scan;
- the still-within-overlap earlier physical firing traverses its newly proposed
  outgoing ARROW exactly once at exposure;
- both traversals receive the ordinary four-tick eligibility interval and
  ordinary SPIKE generation checks;
- no firing is invented or replayed at the CELL, and no other old firing can
  emit; the retained tick is cleared only by ordinary replacement with a newer
  firing;
- existing ARROWs continue to emit only from contemporary CELL firing under
  the unchanged law.

Thus the candidate opportunity remains one symmetric local physical edge
exposure, not a relation record or a second learning law. Both candidate
SPIKEs are subthreshold at the trace loci. Their actual arrivals provide the
ordinary returned activity that can strengthen the reciprocal eligible
ARROWs. Without timely arrival, both weak directions remain subject to
ordinary pressure and deallocation.

Every other v1 constant, construction, matched-marginal clause, held-out
discriminator, control, classification, atomicity rule, authority restriction,
and forbidden-information boundary remains exact.

The sole command and artifact paths are unchanged except that the executable
must hash-audit this v2 protocol. V1 remains an immutable preregistration
artifact; v2 is the only executable PROBE protocol.
