# Cumulative DS2 mechanistic retry after IR0

Status: **PREREGISTERED; DEVELOPMENT RETRY UNSPENT**

Exact enabling parent: `2e2c5cb718f2e4c92a68e3cfd8cb5dca0b047dce` /
`ds-ir0-dependency-invalidation-reopening-readiness`.

This retry changes no frozen DS2 stage or upstream mechanism. It mechanically
composes the frozen post-RT0 retry with IR0, which may satisfy only stage 8:
an incompatible current physical dependency invalidates the stale temporary
route, reopens ordinary generic A1 inference, executes the newly retained
direction, and preserves the old asset for compatible historical return.

The ordered stages remain:

```text
0  exact M1 and frozen mechanism fingerprints
1  learner-visible causal/source/target annotation absent
2  exact M1 interaction and learned boundary-role path intact
3  frozen DS1 selection physically actuates the A1 route
4  aftermath naturally contingent on physical route state
5  aftermath activates existing proposal/probation physics
6  downstream contrast differentiates probationary support
7  one surviving direction is retained, executable, and transferable
8  dependency change invalidates stale direction and reopens generic inference
```

The retry must stop at the first failure. It may not add a new learner,
candidate representation, direction label, evaluator preference, semantic
credit, or adapter inference. It is development-only and claim-ineligible.
M1 remains authoritative; M2 remains absent.
