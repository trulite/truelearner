# PX3-R direct trace-coupling generic-opportunity PROBE v1 protocol

Status: **PREREGISTERED; DEVELOPMENT PROBE EVIDENCE UNSPENT; PX3 ABSENT**.

This independent development arm starts from exact Class-D negative lineage
HEAD `873094497ff6eb74363191dc5edc479c7d66de72`. Its authoritative PX2 ancestor
is `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`. The frozen PX3 no-new-mechanism
negative, every PX0--PX2 authority artifact, and the old typed M3 artifacts are
read-only inputs and must remain byte-identical.

## Question and first-collapse rule

The sole question is whether the current generic local proposal law already
exposes an ordinary ARROW opportunity between two nearby physical loci when
both loci fire through actual retained participation traces.

The current law calls generic local proposal only for a firing caused by an
external SPIKE (`arrow.is_none()`). The preregistered fixture therefore compares:

- four nearby trace-bearing CELL loci fired by ordinary internally propagated
  participation activity; and
- an evaluator-only reference copy in which the same loci receive direct
  external arrivals.

The reference demonstrates whether locality and layout can propose ARROWs at
all. It is not candidate training and has no causal path into the actual-
participation cell.

If actual internal trace firing creates no generic proposal while the direct
external reference does, the mechanically unique first collapse is:
`GENERIC_PROPOSAL_REQUIRES_EXTERNAL_FIRING`. That negative must be frozen before
any arm-specific opportunity is preregistered. If actual trace firing does
create a local ARROW, this arm must not add a law and instead stops for a new
protocol around the discovered existing opportunity. Any other result is a
first-clause failure and receives no rescue or rerun.

## Fixed physical construction

- four trace-bearing CELL loci at positions `0,1,2,3`, region `0`, threshold
  `1`, resistance `1000`;
- per route, one participation-driver CELL and one consequence CELL, each far
  outside the generic radius, with fixed ordinary ARROWs
  driver-to-consequence-to-trace;
- six recurrence rounds; routes `0+1` participate together, then routes `2+3`
  four ticks later; cluster order alternates each round; round spacing is `12`;
- all four routes have exactly six driver firings, consequence firings, and
  trace firings;
- a fresh-namespace reference copy receives the same number and cadence of
  direct external trace-locus arrivals;
- no CELL or ARROW is selected after execution begins; no candidate edge is
  seeded; every finite queue must drain naturally; duplicate complete replay
  must be exact.

The evaluator serializes per-route firing counts, proposal count, arrow count,
candidate endpoint presence, complete/permanent fingerprints, every work-ledger
field, natural quiescence, and autonomous source refiring. Scenario names and
expected outcomes are evaluator-only.

## Anti-representation boundary

Organism-visible execution contains only CELL, ARROW, SPIKE, local position,
timing, resistance, generation, ordinary pressure, and retained substrate
activity. It contains no Event, Episode, History, Pair, Group, member list,
pair key, semantic boundary flag, evaluator-selected endpoint, typed adapter,
serialized co-occurrence record, old M3 schema, or renamed equivalent.

## Sole execution and atomic artifacts

After implementation is committed, tagged, hash-audited, formatted, compiled,
strictly linted, and passed through a no-CELL preflight, exactly one command may
spend this PROBE evidence:

```text
cargo run --release -p px0-physical-correspondence \
  --example px3_r_trace_coupling_generic_opportunity_probe -- --probe
```

Final artifacts are:

- `results/px3_r_trace_coupling_generic_opportunity_probe_v1.csv`;
- `results/px3_r_trace_coupling_generic_opportunity_probe_v1.md`.

Same-directory staging files use the same basenames with a leading dot and
`.staging` suffix. Pre-existing final or staging paths are a hard refusal.
Publication uses create-new, sync, and atomic rename. The command emits exactly
one evidence-spent marker. Pass, frozen collapse, and first-clause failure are
all published and never rerun.

This is DEVELOPMENT discrimination only. It cannot advance PX3, execute a
definitive matrix, modify PX0--PX2, consume another arm, or begin PX4.
