# PX5 LR-C cumulative physical plasticity-allocation authority protocol v1

Status: **PREREGISTERED; SERIAL AUTHORITY EVIDENCE UNSPENT; PX6 FORBIDDEN**.

## Exact cumulative parent

The sole parent is PX4 authority commit
`2348f4318e4c4ca85d6be06017e8ccd7be8b9c87`, tagged
`px4-lrc-lifetime-authority-v1`. This protocol is the first commit on branch
`research/px5-lrc-cumulative-authority`; its Git parent must be that exact
authority commit.

The retained physical sources are byte-frozen:

| source | SHA-256 |
|---|---|
| LR-C law, `crates/lr1-modulatory-physical-return/src/lib.rs` | `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10` |
| PX4 API, `arms/px4-lrc-lifetime/src/lib.rs` | `a201674f9d558b5bda20aef71e9857b632f8a6565f372aee88994a280e0fea71` |
| PX4 authority handoff | `848c3b030824d6bc404dddad9498046b55d1f71c4d7e4ff10fda05cffb29e995` |
| PX4 authority CSV | `050a2b489e41d13e8d8a3d55dd7d69c6e06894b85b2c172f7dc24614af09aeaa` |
| PX4 authority report | `445c465ba61cc12c0ece84a8ebb9a83bea1e67c1a4d640964cc7d93c0dbe4390` |
| PX4 active manifest v2 | `28924746e951645047225d8d20f5c5f98d93f349f46f7c6d7019e68632ce51b9` |
| PX4 taxonomy summary | `59c48919e91d698033a7931c15690fa148691a8afaf89cd22a4b7794ce4e2ee0` |

No unrelated PX4 development source is imported. The cumulative candidate may
use only the authoritative PX4 public API and its sole LR-C dependency.

## Preserved isolated PX5 development

The isolated development branch and all tags remain immutable. They are
referenced, not cherry-picked into the serial lineage:

| isolated artifact at `px5-lrc-allocation-development-ready-v1` | SHA-256 |
|---|---|
| evaluator source | `984bf7f675fc0ac3c396a8963acbfd740a298a0aa9808429bebd326db8967584` |
| PROBE protocol | `0464cccd9207b2238cfba32793c50cfb49ec30681f11c82ffa8663d5defa12b1` |
| MICRO protocol | `bc34b87a626b1e102242fce38efd28e063a285d4a02b3be8e2e9cf3455d2b1db` |
| GATE protocol | `460ee3b337468a667e90b0fc62ed8407098d24f7f8eccce9b5f16b4b2753339c` |
| result audit | `f3736a7448c952ad6bfd327a4296d436f3d3cc24ff3073a220d861ff8bdfea66` |
| PROBE CSV | `a567224a0fe9af9162a01f70f777e1a4bae96c4c4034a3d7606b33ebd246509d` |
| MICRO CSV | `faeb8e8bf803b62e4f6de57972a17ce1eeb1e00252e66aff3a2c075a99c4d14a` |
| GATE CSV | `be0134566fb1b15caa923661ddb69cd77aeca3af885ab0c45e516902983ff741` |

The frozen physical reduction is retained without importing the old evaluator:

```text
ordinary anonymous arrival
-> native weak local proposal and actual traversal
-> local eligibility
-> physically downstream qualified Modulatory return
-> increased resistance under ordinary pressure
-> unsupported deallocation
-> generation-safe stale blocking
-> ordinary reproposal and reacquisition
```

PX5 adds no organism source, field, mode, state, allocator, target list,
`History`, semantic admission, explicit plasticity invocation or typed handoff.
The new authority arm is evaluator-only.

## Fresh serial implementation boundary

The authority package is `arms/px5-lrc-allocation-authority`. It may depend
directly only on:

```text
px4-lrc-lifetime
lr1-modulatory-physical-return
```

It uses PX4's ordinary `arrive` API for all arrivals and the LR-C public
CELL/ARROW/SPIKE types for the isolated PX5 loaded geometry. It may contain
only explicit structs, fixed arrays, direct loops, read-only public
measurements, exact equality, bounded counters and write-once evaluator
publication.

Forbidden implementation techniques include unsafe code, interior-mutability
or global/thread-local hidden state, proc macros or generated code,
evaluator-conditioned organism branches, semantic enums/flags/adapters,
artificial lifetime or memory leaks, and serialization that participates in
organism behavior. A need for any such technique is an architectural and
scientific fork; freeze it and stop.

## Fresh definitive matrix

The eight explicit authority roots are:

```text
561001 561002 561003 561004 561005 561006 561007 561008
```

The three explicit unsupported loads are:

```text
8 32 128
```

This is exactly 24 fresh rows. Each row derives physical identities only below
`(root << 32) + 0x0800_0000`. Roots are disjoint from isolated PX5 bases,
PX4 authority roots `461001..461016`, and every earlier authority namespace.

The root index crosses returned-site order and position reflection:

- even index: returned site first; odd index: returned site last;
- index modulo four below two: forward positions; otherwise reflected;
- two identity replicates per order/reflection stratum.

Each row begins from a fresh complete substrate with one three-CELL returned
site and `load` two-CELL unsupported sites. Site spacing exceeds the unchanged
local-variation radius. No state crosses rows.

Each row executes twice from independently constructed complete state inside
the sole definitive command. Equality covers all serialized physical
observations, fingerprints, work, memory, quiescence and verdict fields. This
is the only exact replay and is not an additional evidence run.

## Eighteen conjunctive row clauses

Every row must pass all 18 clauses:

1. registered root/load/order/reflection stratum is exact;
2. blank variable-arrow count is zero and one anonymous arrival per source
   creates exactly `load + 1` native weak proposals;
3. every proposal traverses once and exactly one qualified LR-C update occurs;
4. after first propagation returned/unsupported resistance is exactly `4/1`;
5. at tick `30` exactly the returned variable arrow is live at resistance `1`
   and all `load` unsupported arrows are physically dead;
6. live variable allocation is exactly one at all registered loads;
7. ordinary pressure deallocates the returned arrow at tick `40`;
8. the dead handle remains blocked and cannot become live again;
9. the next ordinary PX4 arrival creates a distinct native arrow and the
   existing downstream loop raises it to resistance `4`;
10. after matched pressure to tick `70`, that replacement alone remains live;
11. four further ordinary reuse exposures create zero new proposals;
12. persistent bytes are identical before and after those reuse exposures and
    never exceed `24000` bytes;
13. measured row work is at most `100000` ledger operations;
14. matched return-before-proposal and late-return controls perform zero
    accepted updates and retain no candidate;
15. a queued weak generation crossing a pressure boundary is blocked, while a
    later ordinary arrival forms a distinct proposal that alone fires;
16. fresh PX4 `Field` recurrence yields resistance `4 -> 7`, mature reuse makes
    no proposal, disuse deallocates, and ordinary reacquisition is a distinct
    generation;
17. every propagation in the row naturally quiesces; and
18. the second fresh complete execution is exactly equal to the first.

The definitive verdict is `24/24` rows, `432/432` row clauses and all four
global clauses:

1. all 24 registered root/load cells appear exactly once;
2. all order/reflection/identity strata are complete;
3. structural measurements are invariant within each load; and
4. cumulative PX0--PX4+LR-C conformance and frozen hashes pass.

Total authority accounting is therefore `436/436` clauses.

## Hard engineering gates

The authority implementation must remain small, explicit, idiomatic and
reviewable. It must compile with `#![forbid(unsafe_code)]` and strict Clippy
`-D warnings`. Work is bounded by the frozen per-row ceiling. Natural
quiescence is mandatory. Memory stability means repeated reuse creates no
additional structural record and leaves persistent bytes exact; the existing
generation-safe tombstone created by one deallocation is part of the frozen
parent law and must not be hidden or leaked around.

No full workspace build or unrelated test suite is authorized. Before freeze,
one targeted E2B validation may run formatting, build the authority binary,
one no-world matrix-definition test, strict Clippy for this package, the
static coverage/leakage audit, and `--authority-preflight`. Preflight may not
construct a world, perform exact replay or create result artifacts.

## One-shot authority execution

After the implementation, static audit and implementation audit are committed
and tagged, a second fresh E2B sandbox executes exactly once:

```text
cargo run --release \
  --manifest-path arms/px5-lrc-allocation-authority/Cargo.toml \
  -- --authority-v1
```

The command repeats frozen hash/output/identity checks before its first world,
prints one evidence-spent marker, evaluates the full matrix once, and
atomically creates:

```text
results/px5_lrc_allocation_authority_v1.csv
results/px5_lrc_allocation_authority_v1.md
```

Overwrite, resume, partial selection, alternate roots/loads, rescue,
regeneration and rerun are forbidden. Any failure, interruption, malformed or
missing artifact is an immutable negative/incomplete result.

## Serial PX-C gate

Only after the functional evidence is frozen may fresh E2B audit sandboxes
replace exactly the two PX5 predecessor rows in manifest v2. PX4 and every
other row remain byte-identical. The immutable before side is the PX4 authority
taxonomy and exact manifest hash
`28924746e951645047225d8d20f5c5f98d93f349f46f7c6d7019e68632ce51b9`.

Required after conditions are:

```text
primary seams                   < 297
semantic guard                 <= 162
evaluator guard                <= 559
PX0--PX3+LR-C foundation seams      0
PX4 seams                           0
PX5 seams                           0
new seam kinds                      0
new guarded surfaces                0
unclassified active files           0
```

Taxonomy must run with the exact after-manifest hash. The comparator must use
the raw immutable PX4 authority inventory/guard/summary and require a strict
primary decrease. All artifacts are downloaded unchanged and hashed.

## Authority decision and stop rule

PX5 authority may be claimed only after the one-shot `436/436` functional
matrix, exact replay, bounded work, memory stability, natural quiescence,
cumulative PX0--PX4+LR-C conformance, complete coverage/leakage audit, and
serial PX-C gate all pass. Separate result-audit and authority-handoff commits
and tags are required.

If composition requires a new field, transition, mode, eligibility rule,
allocation law, memory trick, semantic adapter or evaluator-supplied site, the
lane freezes that diagnostic and stops. This workflow must not execute,
prepare or advance PX6.
