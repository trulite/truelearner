# PX3-R Arm B anonymous shared-CELL frozen-negative handoff

Status: **FROZEN NEGATIVE; DEVELOPMENT ARM COMPLETE; PX3 AUTHORITY ABSENT**.

## Outcome

Repeated co-participation can stabilize a pre-existing anonymous convergent
CELL/incident-ARROW structure under matched individual-route marginals, but
the retained PX0-PX2 substrate cannot satisfy the mandatory contemporary
swap/relearning discriminator.

Arm B is therefore a **frozen negative**, not a positive candidate. The v2
probe artifact is literally `FIRST_CLAUSE_FAILURE` because swap and the
preregistered spacing replica failed. The terminal scientific dependency is
unambiguous: after pressure removes an unused coupling-`2` opportunity ARROW,
the authoritative local proposal law recreates only coupling `1`; two such
ARROWs cannot fire the threshold-`4` anonymous CELL and cannot acquire return
support. Repair requires an additional ARROW allocation/proposal law or other
new substrate mechanism outside this arm.

This result has no authority to advance PX3, run definitive evidence, modify
PX0-PX2, consume another arm, or begin PX4.

## Exact source, commits, tags, and hashes

There is **no authoritative mechanism commit or tag**. The exact development
opportunity implementation and terminal negative are:

- frozen lineage start:
  `873094497ff6eb74363191dc5edc479c7d66de72`, tag
  `px3-physical-event-boundaries-frozen-negative-handoff-v1`;
- authoritative PX2 ancestor:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
  `px2-physical-causal-direction-authoritative`;
- v1 protocol: `3c4df518b5bcd570fc94614dc22ef91d53258ac7`, tag
  `px3-r-shared-cell-probe-v1-protocol`;
- v1 implementation: `351b77a1db94aca0bc227a26a1dba7b5dd614ede`,
  tag `px3-r-shared-cell-probe-v1-implementation`, source SHA-256
  `2268c4445b438ae8e3d4bd6e1cbdc93d5d217c39b0d8200ac1c0a4d8d7f61e4c`;
- frozen v1 first-clause failure:
  `74813a268f593340703f1fbe4510de57e3c25276`, tag
  `px3-r-shared-cell-probe-v1-first-clause-failure`;
- v2 protocol: `56bd44bfedbe7c5171106b1cb8c2f0c363128aa0`, tag
  `px3-r-shared-cell-probe-v2-protocol`;
- v2 implementation: `ccea20467736ad36faafaa9d5415612b2b81f80e`,
  tag `px3-r-shared-cell-probe-v2-implementation`;
- v2 source:
  `crates/px0-physical-correspondence/examples/px3_r_shared_cell_probe_v2.rs`,
  SHA-256
  `1ae7592134db8692a95b2ca38d837306c5021f79df9e2ddf9f5e32b6d660631d`;
- v2 organism-visible physical block SHA-256:
  `4a73fde5ef122d54411b35b260a4a28fc9dac7bb454a2a928a66c0302070269f`;
- terminal result commit: `55f2774becfaa9df802bece786c358e17f063d8e`, tag
  `px3-r-shared-cell-probe-v2-frozen-negative`.

Frozen result artifacts:

- v1 CSV/report SHA-256:
  `1222b90fee4ed0ce2a20db7ea751f4c469073cde854b1b8ca0e774f9faf8038a`
  and `d7e559e4ebe434e236e8af3e08d88843dc6a4761ba879d5984452a78bb658973`;
- v2 CSV/report SHA-256:
  `bb95ff9c05b9105ae2116c4efc6ab8c4f228907eb5feaa42e8a8f7836cae8298`
  and `e4e4b2c598c15fab0a03e259c22eec075a5d18a0ec5e2f6319b1c222277354d1`.

## Physical inputs consumed

Each fresh v2 matter consumed a complete symmetric six-site field. Each site
contained two threshold-`2` route-local source CELLS at positions `center-2`
and `center+2`, one anonymous threshold-`4` CELL at `center`, one outward CELL
in region `1`, two weak resistance-`1`/coupling-`2` incoming ARROWs, two
resistance-`1000` return ARROWs, and one resistance-`1000` outward ARROW.
Sites were `100` position units apart. Remote driver CELLS reached sources by
fixed coupling-`2` ARROWs; isolated distractor CELLS had no local neighbor.

Training consumed two simultaneous external unit SPIKEs at each of three
physical source CELLS for every route occurrence. Held-out use consumed one
external driver SPIKE per source. Execution used only retained CELL decay,
refractory state, deterministic SPIKE order, four-tick local eligibility,
`+3` local return, positive-coupling ceiling `2`, ten-tick ordinary pressure,
generation invalidation, local radius-`2` weak-ARROW proposals, region
crossings, and natural queue drain.

V2 primary acquisition used `16` rounds, round spacing `10`, alternating
early/late clusters at gap `4`, one distractor at each cluster, and a ten-tick
post-training pressure gap. Swap used `20` matched rounds. Every one of the 12
source ports fired exactly once per round and zero times autonomously.

## Physical structures and activity produced

Before swap, primary matter retained `162` ARROW records and `12,144`
persistent bytes. Only the two trained sites were executable:

- per-route resistance/live multiset:
  `0|0|33` / `false|false|true`, exact for all four routes;
- incident resistance at `A+B` and `C+D`: `33|33` each;
- all four other sites: `0|0`;
- training anonymous-CELL/outward counts: `16|0|0|0|0|16`;
- held-out trained use: one anonymous-CELL firing and one outward crossing;
- held-out crossed use: zero anonymous-CELL firing and zero outward crossing.

After swap, matter contained `336` retained live/dead ARROW records and
`23,280` persistent bytes because the authoritative substrate preserves dead
records and assigns new generations to physical proposals. Every incoming
opportunity was dead at resistance `0`; training and held-out anonymous-CELL
firings/outward crossings were all zero. Old organization weakened, but new
organization did not emerge.

Across the two one-shot probes, ledgered evidence work was `3,581,177`
operations (`1,880,777` v1 plus `1,700,400` v2). Frozen CSV/report storage was
`8,080` bytes for v1 and `8,055` bytes for v2.

## Explicit environmental opportunities and controls

The environment supplied only:

- the preregistered broad, complete six-site anonymous field, identical for
  every possible two-route combination;
- fresh namespaces, physical identities, mirrored layout, reversed allocation
  and physical identity order, and alternate deterministic arrival order;
- matched route frequency, total external SPIKEs, source firings, cluster
  early/late order, traversal opportunity, return opportunity, pressure, and
  training outward activity;
- gap-`4`/spacing-`10` primary timing and gap-`6`/spacing-`14` spacing replica;
- driver-mediated held-out use that cannot propose a missing ARROW;
- isolated distractors, exact duplicate complete-state replay, zero autonomous
  source refiring, and natural quiescence;
- correlated distractor activity without route participation;
- participation with blocked return;
- anonymous CELLS with absent weak incoming opportunity;
- a weak field made stale by `30` ticks of ordinary pressure;
- ambiguous three-route activity, which retained all three actually activated
  structures;
- genuine four-route co-participation, which retained all six structures;
- a fresh stable alternative, which retained `A+D` and `C+B` rather than the
  original organization.

No scenario name or expected organization selected a substrate CELL, ARROW,
update, deletion, or lifecycle.

## Swap/relearning behavior

V1 failed earlier because return plasticity raised a single trained incident
coupling to the threshold, making individual-route use sufficient; old
structures strengthened through swap. V1 is preserved and not evidence.

V2 fixed conjunctive execution without changing a law. It passed initial
trained-versus-crossed discrimination and weakened old sites `33|33 -> 0|0`,
but new sites remained `0|0`. The authoritative proposal law's coupling-`1`
new ARROW cannot recreate the preregistered coupling-`2` opportunity after
ordinary pressure. Therefore the required contemporary relearning behavior is
absent.

## Forbidden-information audit

The organism-visible block contains no `Event`, `Episode`, `History`, `Pair`,
`Group`, member list, pair key, semantic boundary/lifecycle flag,
evaluator-selected organization, old M3 schema, typed adapter, serialized
co-occurrence record, renamed equivalent, direct trace-to-trace coupling, or
downstream-continuation convergence.

It stores only `PlasticSubstrate`, CELL/ARROW handles, physical identity arrays,
raw resistance/live state, and work/activity counts. The six-site incidence
table, evaluator route names, schedules, expected sites, comparisons, and
serialization are outside the organism-visible block and have no update path
except the explicit physical SPIKE targets and arrival times.

## Unchanged port onto authoritative PX2

No port is applied because no mechanism survives Arm B. The cumulative port
contract remains the frozen Class-D rule: import no new mechanism; preserve the
no-addition block and every negative byte-for-byte; diagnose only a later
authoritative parent delta under a separately frozen protocol.

Exact unchanged hashes:

- `crates/px0-physical-correspondence/src/lib.rs`:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- `crates/px0-physical-correspondence/examples/px2_physical_causal_direction.rs`:
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- frozen PX3 no-new-mechanism source:
  `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba`;
- frozen PX3 no-new-mechanism CSV/report:
  `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38`
  and `021be8698c010df1e09dc45f2bf9968f2255b6eb7851c38f36fd93be72260d3b`.

## Files, validation, worktree, and remote

Arm-added files are exactly two fresh sources, six protocol/implementation/
result audit files plus this handoff, and four atomic result artifacts. No
pre-existing path changed.

Focused Rust formatting, compilation, and strict Clippy passed for both probe
sources. No-argument/wrong-argument refusal, no-CELL preflight, artifact
absence before each execution, exact lineage/tags/hashes, organism-visible
forbidden-token scans, CSV shape, duplicate replay, quiescence, source counts,
work ledgers, storage, and changed-path isolation passed. The v2 spacing
science control failed and is preserved. No broad historical suite ran because
shared code did not change. No authority/definitive matrix ran or was
simulated. MICRO and GATE were not entered.

At publication the worktree is clean, branch
`research/px3-r-shared-cell` and all six Arm B protocol/implementation/result
tags are pushed to `origin`, and local/remote branch HEADs are equal.

## Stop

Arm B stops at frozen negative. Reopening requires explicit authority for a
new substrate allocation/proposal law outside this arm; this handoff is not
that authority.
