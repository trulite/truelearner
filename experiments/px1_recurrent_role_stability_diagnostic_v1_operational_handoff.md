# PX1 recurrent role-stability diagnostic v1 operational handoff

The v1 diagnostic is frozen without a scientific classification because its
parent decoder rejected the child encoder's numeric boolean fields.

The lawful next action is a separately named v2 retry with:

- byte-identical PX0 learning law;
- identical margin, inhibition, distance, and timing arm physics;
- identical namespaces, schedules, controls, work bounds, and pass rules;
- fresh v2 result paths;
- one mechanical repair: accept the already emitted `0/1` boolean encoding;
- a pre-execution codec round-trip unit/preflight check.

No other mechanism, threshold, timing, topology, measurement, or scientific
criterion is authorized by this handoff. PX1 remains non-authoritative.

