# PX1 recurrent role-stability diagnostic v2 protocol

Status: **PREREGISTERED OPERATIONAL RETRY; DEVELOPMENT EVIDENCE UNSPENT**.

This is not a rerun or reinterpretation of v1. The immutable v1 attempt ended
without a scientific classification because its parent decoder accepted
`true/false` while its child encoder emitted boolean measurements as `0/1`.

## Frozen scientific design

The entire scientific design remains identical to
`px1_recurrent_role_stability_diagnostic_protocol.md`:

- the byte-identical authoritative PX0 law;
- the margin, inhibition, distance, and timing physical arms;
- all thresholds, couplings, delays, positions, schedules, namespaces, and
  exposure counts;
- primary plus fresh mirrored-transfer worlds;
- useful-return, source-refiring, role-formation, held-out effect,
  productive-recurrence, duplicate-replay, and quiescence measurements;
- per-arm pass rules and the ambiguity-preserving interpretation rule;
- the independent five-second evaluator timeout.

No arm mechanism, physical parameter, readout meaning, or pass threshold may
change in v2.

## Sole repair

The decoder may accept numeric `0` and `1` as the exact boolean representation
already emitted by the frozen encoder. It must continue accepting
`false`/`true` for defensive compatibility. No other decode coercion is
allowed.

Before development evidence, a codec-only mode must construct no organism
world and establish:

```text
encode(sample result)
→ decode
→ encode
= exact original bytes
```

## Fresh evidence surface

- CSV: `results/px1_recurrent_role_stability_diagnostic_v2.csv`;
- report: `results/px1_recurrent_role_stability_diagnostic_v2.md`;
- marker: `PX1_RECURRENT_ROLE_STABILITY_V2_DEVELOPMENT_EVIDENCE`.

Both result paths must be absent before the one v2 development invocation and
must be created atomically. The v1 result paths remain absent; the v1
operational-negative and handoff artifacts remain immutable.

## Status boundary

This remains development-only. A v2 result cannot create PX1 authority, PX-C,
a continuous organism, or Harness H1. Definitive execution remains forbidden.

