# PX3 recursive compression GATE result audit v1

Status: **GATE-A DEVELOPMENT POSITIVE; PX3 DEVELOPMENT-READINESS EVIDENCE**.

## Frozen evidence

- MICRO-A ancestor: `927c4cb860383c742138723c3788d4f264081b8d`;
- imported D2-A result commit:
  `3da0698e8a514b3ddac0c77b2fecd9e7c2f2bae2`;
- protocol commit: `5b23dea0f279bdb69ac827d41a9f840858d016d6`;
- execution-protocol commit: `67c424d78f5697f1d5672f96db979318c84ae03b`;
- implementation source commit:
  `da472e5867a34c089df863d61f385a801d4eb9f6`;
- exact preflight and evidence commit:
  `77b56e98089e49b3d825f5d6e7c2eb7af8fab16c`;
- E2B sandbox: `i6x9gykt9tvp6xfz5z8ra`;
- the sole registered `--gate` execution completed once and atomically
  published; it was not rerun;
- CSV SHA-256:
  `1c350355843ca0020a275813f1e7aa9fb36b63317375327b5bd437182d144469`;
- Markdown SHA-256:
  `873d7bd6e0ffb7848d23933a5dcddd118462ef0f747439fd8cb5bc78de9f5db2`;
- all four rows passed, replayed exactly and naturally quiesced.

## Progressive native formation

All worlds began with zero P->output candidate ARROWs. The same generic
threshold-crossing proposal route created exactly one candidate at each level:

```text
phase             active O/P/candidate/output/trace/M/credit   proposals
AB training       2|0|0                                        1
X+C training      2|2|0                                        1
Y+D training      2|2|2                                        1
```

Candidate impulse distinguished the first weak traversal from recurrent mature
traversal without changing the output interface:

```text
AB phase: 3|0|0
XC phase: 4|3|0
YD phase: 4|4|3
```

Every output execution, whether X, Y or Z, emitted two unit arrivals into its
ordinary threshold-two normalizer and exactly one derived participation trace.
The same output trace participated downstream and closed that level's R3
attribution relation. There was no second effect/event representation.

## Recurrence and persistence

The exact resistance trajectories matched preregistration in every insertion
and reflected-geometry world:

```text
after first AB / recurrent AB:  4|0|0  / 6|0|0
after first XC / recurrent XC:  8|4|0  / 10|6|0
after first YD / recurrent YD: 12|8|4  / 14|10|6
after tick-60 pressure:         13|9|5
```

Clones taken after the first AB, XC and YD exposure respectively lost the new
candidate to ordinary pressure: `0|0|0`. Thus every level could physically
execute once, but recurrence was required for reusable organization.

Across each row the three candidates had distinct physical ArrowIds and
generations. The generated report's 12 proposals aggregate three native
proposals across four rows; each continuing world proposed exactly AB, XC and
YD once.

## Context-free level-blind reuse

Held-out clones supplied neither context nor return. Mature coupling-two
candidates therefore had to drive their threshold-two output directly, and no
held-out execution could learn.

- mature AB produced one X outlet execution, two unit normalizer arrivals and
  exactly one X trace;
- timed X+C traversed the identical O->P->candidate->outlet->normalizer path
  and produced exactly one Y trace;
- timed Y+D traversed that same path and produced exactly one Z trace;
- the final context-free chain recorded candidate raw impulse `2|2|2`, output
  execution `1|1|1`, normalizer arrivals/impulse `2|2|2`, and derived trace
  firing `1|1|1`;
- no held-out stage produced a proposal, attribution firing or credit pulse.

Primitive A/B/C/D and derived X/Y/Z used the same normalizer helper and unit
trace interface. The downstream stage received only trace CELL identities; it
had no primitive/derived flag and could not observe the upstream candidate's
coupling or construction history.

## Timing and provenance controls

C alone and D alone produced their ordinary primitive trace but no higher
opportunity firing, proposal or output. In the gapped controls:

```text
AB -> X trace, then late C trace
  -> XC opportunity/P/candidate/Y = 0

AB -> X; X+C -> Y trace, then late D trace
  -> YD opportunity/P/candidate/Z = 0
```

The lower mature organizations still executed and normalized exactly once, so
these were overlap controls rather than missing-input controls. Normal/reversed
insertion and forward/reflected proposal geometry produced the same registered
physical counts.

## Interpretation and scope

This GATE integrates the independently established D1, R3, D2-A and MICRO-A
pieces into one level-blind recursive chain:

```text
A+B -> reusable X
X+C -> reusable Y
Y+D -> reusable Z
```

A composite physical organization therefore becomes one ordinary participant
at the next level, and the same formation, completed-loop credit, recurrence,
pressure and normalization mechanisms apply again without a representational
level change.

Together with frozen MICRO-A reversal, this is PX3 development-readiness
evidence for formation, replacement and recursive compression. It is not
definitive evidence or authority, does not advance PX4, and does not by itself
authorize changing any earlier authoritative stage.
