# CJ0-D local-subunit frozen-negative handoff

Status: **TERMINAL GATE NEGATIVE; RECURSIVE ARCHITECTURAL BLOCKER; LANE STOPPED**.

## Classification and boundary

The one CJ0-D local-subunit law passed flat matched-marginal discrimination,
mandatory contemporary reversal, self-evidence, and full-deallocation
bootstrap at PROBE/MICRO. It then failed required same-law recursion at GATE:
`A+B -> X`, `X+C -> Y`, `Y+D -> Z` did not reach Z. The lane is therefore a
frozen development negative, not a viable CJ0 solution.

The recursive failure creates the preregistered architectural choice and stops
the lane. GATE is terminal. No prohibited later-stage surface was created,
preregistered, simulated, or executed. PX3 and PX3-R Arms A/B/C remain
immutable negatives; PX4--PX8 remain parked and PX-C remains blocked.

## Exact ancestry and mechanism

- frozen start: `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
  `px2-physical-causal-direction-authoritative`;
- initial protocol: `73ba9f71d774269c2b823d9de31c24852bc604e8`, tag
  `cj0-d-local-subunit-development-protocol-v1`, SHA-256
  `940dc88e8a3f70c9dd7a9bc0eb42b1367273d4caea5bed50112bdc4ffce5d195`;
- clean implementation: `6cd7ee1a4e90c9cb37a3aa5bc4425bb286b7e963`, tag
  `cj0-d-local-subunit-development-implementation-v2`;
- final isolated build source SHA-256:
  `48ceb724aed7385f591e040a430f24eedb4a9ff1fab054ff628da3b6129213e3`;
- final evaluator SHA-256:
  `c9dd6c3c3d308917866f7dfb79cad6df12a08908b8b9b40e4586b53c6062042e`;
- generated complete substrate SHA-256:
  `e8d91f418d1079a3d841dc6f209caf1c82de0a5339857ac329580eab770e85b2`;
- exact candidate-law block SHA-256:
  `0942bb90376e492125ec776c927960b67d75205724e3d18326388bf6cecd3782`.

The candidate adds no persistent substrate field. On delivery of a positive
ordinary ARROW SPIKE, it checks existing physical state only:

1. the live target CELL already has positive subthreshold state at the same
   tick;
2. the current live incoming ARROW has positive coupling;
3. another live incoming ARROW to that CELL comes from a different source
   CELL, has positive coupling, and has the same extant local eligibility
   horizon.

If the target remains subthreshold, it emits ordinary SPIKEs only along live
positive outgoing ARROWs in the same region and within the authoritative local
radius `2`. It sets ordinary four-tick eligibility and uses the ordinary queue,
generation checks, return update, `+3` resistance, coupling ceiling `2`,
ten-tick pressure, and deallocation. A mature threshold-`4` CELL uses the
unchanged ordinary firing/outward scan. The law contains no semantic object,
member list, persistent relation key, level branch, or primitive/learned
special case.

## Physical consume/produce contract

The symmetric flat field consumes six anonymous two-input sites, two
threshold-`2` local source CELLS per site, one threshold-`4` local CELL, weak
coupling/resistance `1/1` incoming opportunities, ordinary local return
ARROWs, an ordinary outward ARROW, current SPIKE traversal, CELL state/tick,
eligibility, returned activity, pressure, generation, and natural queue drain.

The first genuine weak conjunction produces one subthreshold local integration
and two ordinary local return SPIKEs but no CELL firing or outward crossing.
Returned activity strengthens both actually traversed incoming ARROWs to
coupling `2`. Recurrence then reaches threshold `4` and emits the ordinary
outward output. Singleton or noncontemporary traversal emits no local return.
Unsupported structures weaken/deallocate; ordinary externally entered source
activity recreates missing coupling-`1` opportunities through the retained
radius-`2` proposal law.

This is the exact physical distinction from frozen PX3-R shared-CELL Arm B:
the weak recreated inputs can return support before the shared CELL fires.

## Development results

### Frozen failures before the valid PROBE

- PROBE v1 emitted its marker and mechanically aborted before publication on
  a backward evaluator time request; frozen at `7a7890e`, no scientific
  classification and no CSV.
- PROBE v2: frozen evaluator-fixture negative at `a995fe5`, `9/11` rows.
- PROBE v3: frozen absent-expectation negative at `da8ba01`, `10/11` rows.
- Generated Cargo storage was accidentally committed in implementation v1,
  then removed under a frozen storage-correction protocol. The tag and defect
  remain preserved; no result was executed from that correction.

### Positive PROBE v4

Commit/tag: `45bf6aaec6ebfdf0a24d2abafc5f0a39f7c51bf0`,
`cj0-d-local-subunit-probe-v4-positive`.

- `11/11` rows pass; exact duplicate state `11/11`;
- primary and mirrored/reversed cells: route source firing exactly
  `6|6|6|6|6|6|6|6|6|6|6|6`;
- held-out trained output `1|1`; crossed output `0|0`; singleton output
  `0|0|0|0`;
- alternative A+D/C+B organization passes;
- singleton, too-late, correlation without traversal, blocked return, absent
  opportunity, one-use stale opportunity, ambiguity, four-route activity,
  exact replay, and finite queue controls pass;
- work: `33,263` ledgered operations;
- CSV/report SHA-256:
  `313168dd2672373260a459ce4e79b4ed17d97451779dea83d6fa94deb2efd626` /
  `822bb52cae83ab30a1d6df0c83f064100834b146c23060264816010a424959f7`.

### Positive MICRO v1

Commit/tag: `93d48147ec95d9e6c7e88375eb49500d0d2f1ad8`,
`cj0-d-local-subunit-micro-v1-positive`.

- `7/7` rows pass, normal and mirrored/reversed exact replay;
- old A+B/C+D support reaches zero and original input generations are dead;
- new A+D/C+B support reaches coupling `2`, executes `1|1`, and old held-out
  use is `0|0`;
- old A-alone produces zero integration, CELL firing, returned arrival, and
  local return update;
- after all initial weak inputs deallocate, ordinary changed activity proposes
  fresh inputs, produces subthreshold integration before CELL firing, and
  becomes executable without historical resurrection;
- work: `109,461` operations;
- CSV/report SHA-256:
  `e0a3d78d99571dbaf14cba02c25e6056977c510e062c0770a91233ca597522a7` /
  `2f131d5574c6114b610dd77c90e3118df75077d9301a00c0b12cdad72a998399`.

### Terminal GATE v1

Commit/tag: `e31076602db3b4add20c8873d4a04fe13818e53f`,
`cj0-d-local-subunit-gate-v1-frozen-negative`.

- `16/19` rows pass;
- all `12/12` fresh namespace/layout/allocation/identity/insertion/timing cells
  pass matched discrimination and reversal with trained `1|1`, old/crossed
  `0|0`, old support zero, new support live, and exact replay;
- ordinary convergent ARROW propagation passes `A->C=1`, `B->C=1`, and
  `A+B->C=1` without an added OR law;
- temporal together, overlap/while, and B-absent-through-closure rows pass;
- ordered A-then-B and the duplicate within-residual row are frozen secondary
  expectation failures: forced quiescent scheduling left residual state `1`
  rather than preregistered `3`;
- recursion is the decisive failure: X/Y/Z incoming couplings are equal and a
  missing-D use is silent, but full held-out use never reaches Z;
- work: `648,334` operations;
- CSV/report SHA-256:
  `592b4bd1a2abbc1bd9d55eda16ebc24ad34563856ed0d8d9e4361368ce1731ab` /
  `0e08e1f4cc0da4419257ab44bb4d45e6e8b3d52b574e3423bd6e350942fff9d6`.

## Recursive blocker

Higher-level weak input opportunities exist from matter creation but ordinary
pressure removes them before the later X+C and Y+D stages. Both learned and
primitive participants reach the higher sites through ordinary internally
driven port CELLS. The retained anonymous proposal law runs only when a source
CELL fires from an external arrival, so an internally driven port cannot
recreate its dead outgoing opportunity. Y/Z therefore cannot bootstrap.

Repair requires an architectural choice: extend proposal to internally driven
CELL firing, stage/replenish higher opportunities, change their lifetime, or
externalize higher participants. Those are scientifically distinct changes;
none is authorized or attempted.

## Validation, accounting, and preserved defects

- authoritative PX0--PX2 law/source hashes remain exact:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` /
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- frozen PX3/PX3-R handoff commits remain
  `873094497ff6eb74363191dc5edc479c7d66de72`,
  `26aa795377c47ecf6fd28232865d5404408b6df9`,
  `82c0433329cf85bf3fe261661acd033011000656`, and
  `5feb9b4c4755ed40d58ffc9cb8769d5523ea46f0`; frozen PX3 and Arm-B source
  hashes remain
  `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba` and
  `1ae7592134db8692a95b2ca38d837306c5021f79df9e2ddf9f5e32b6d660631d`;
- current diff from the frozen start contains added paths only; no
  authoritative or frozen path changed;
- focused tests: `4` pass; strict focused Clippy: pass; broad workspace tests:
  pass; refusal and no-CELL preflight: pass;
- candidate-law forbidden-vocabulary scan: zero hits; tracked nested Cargo
  target files: zero; staging files: zero;
- every published CSV has exactly `30` columns and no malformed row;
- frozen v2/v3/v4/MICRO/GATE rows: `59`, exact duplicates `59/59`, work
  `859,371`, summed row persistent bytes `384,544`;
- terminal positive/negative evidence result storage plus v1 abort report:
  `29,643` bytes;
- current lane-added tree storage before this handoff: `117,577` bytes;
- the preserved implementation-v1 generated-build mistake leaves `92,822,886`
  uncompressed bytes of newly reachable blob history even though none remains
  in the current tree.

GATE fresh rows serialize `quiescent=false` because a default pressure-only
accumulator was merged into their aggregate row after the actual propagations.
The frozen pass clause checked the true initial/reversal propagation flags,
which were quiescent. This accounting defect is preserved, disclosed, and not
repaired behind the earlier recursive failure.

## Stop

The flat/reversal mechanism is informative but does not satisfy the recursive
CJ0 objective. No surviving law is handed forward. The next action, if any,
requires an explicit new development decision among the recursive
architectural repairs in a different lane.
