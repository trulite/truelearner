# DS1 after DS-C0 cumulative composition retry protocol

Status: **PREREGISTERED; DEVELOPMENT ONLY; NOT CLAIM ELIGIBLE**

This protocol is frozen from exact enabling parent
`5d4a791065fe14e3194ca73d84f141467a7ef903` /
`ds-c0-anonymous-credit-coupling-readiness`. M0
`1d74c0ed0b515446161a63a6d43ecbe27514dc85` remains the authoritative
cumulative mainline. DS-E0, DS-A0, DS-A1, DS-R0, and DS-C0 remain
enabling-only. M1 does not exist unless a separately authorized cumulative
freeze follows a complete retry pass.

## Sole question

> Once frozen C0 has physically coupled anonymous returned evidence to the
> earlier selected affordance, how far does the byte-identical frozen DS1
> learner progress through its already-existing update and boundary-role
> reconstruction machinery?

The retry adds no learner, polarity, consequence interpretation, update
bridge, correctness signal, strength observer, boundary role, or held-out
answer. It invokes the frozen C0 harness read-only, inventories only paths
that already exist, and freezes the first unavailable dependency.

## Immutable lineage

Stage 0 requires all of the following exact evidence:

- enabling parent commit:
  `5d4a791065fe14e3194ca73d84f141467a7ef903`;
- authoritative M0:
  `1d74c0ed0b515446161a63a6d43ecbe27514dc85`;
- frozen C0 mechanism SHA-256:
  `5c8d00189593ca2f7efb47165efddf85111259f90433a016e5822b5b9578aed2`;
- frozen C0 readiness SHA-256:
  `a69e440639bc37eefc0e9f30402cde6c3b5dec945d95a77060513d7a96491572`;
- frozen marked DS1 learner SHA-256:
  `adec6a422e69e7f90bff6482776ea9aa91ae89e5e8d59183f6228165f9f7ff0e`;
- frozen results-tree digest:
  `491a63c17ba35d768b630720063793a4db09686cfe7cb33694fd80ea63bbd4e4`.

C0, its complete enabling stack, the marked DS1 learner, and all upstream
sources are read-only. A byte change is a stage-0 collapse, not an authorized
implementation edit. No broad regression is required because no shared or
frozen mechanism may change.

## Exact composition boundary

The retry invokes the frozen C0 development harness directly. C0 must produce
the same actual E0 event, two physical A1 roots, opaque frozen-DS1 choice,
selected route execution, R0 evidence return, four-field evidence surface,
and one temporary evidence-to-choice coupling as its readiness freeze.

The retry may inventory whether an existing source/runtime path carries that
coupling into frozen DS1 `apply_consequence`. It may not:

- create or infer a favorable/unfavorable bit;
- label activity good, bad, correct, wrong, accepted, or rejected;
- call, wrap, or emulate `apply_consequence`;
- compare alternatives or synthesize a counterfactual outcome;
- mutate or inspect frozen DS1 strengths after an earlier collapse;
- perform held-out reconstruction after an earlier collapse.

Any update must occur through a path already present before this retry. Every
absent path must be mechanically derived and mutation-sensitive, never a
literal scientific assertion.

## Ordered stages and stopping rule

The stages are frozen exactly as follows:

0. M0 lineage and frozen C0 controls remain valid;
1. frozen E0 event formation succeeds;
2. frozen A1 forms multiple executable affordances;
3. byte-identical frozen DS1 sees two opaque handles;
4. byte-identical frozen DS1 chooses one handle;
5. the selected physical route executes;
6. frozen R0 forms one anonymous temporary return relation;
7. the exact four-field anonymous evidence surface is populated;
8a. frozen C0 forms one anonymous evidence-to-choice coupling with no polarity;
8b. frozen DS1 updates through an already-existing path;
9. frozen DS1 boundary-role strengths diverge;
10. held-out boundary-role reconstruction succeeds.

The first false stage is the only collapse. Every later stage is `BLOCKED` and
its work/storage fields remain blank. No rescue follows in this lane.

If stage 8b is false after 8a passes, the interpretation is limited to: the
anonymous temporal ownership relation is present, but frozen DS1 has no
existing organism-visible path that converts it into an update. This does not
show that event formation, alternatives, choice, execution, returned evidence,
or coupling are absent. If 8b passes but stage 9 fails, freeze strength
separation. If stage 9 passes but stage 10 fails, freeze held-out recovery.

Only passage through stage 10 is development eligibility for a separately
authorized M1 freeze. This retry itself never creates M1.

## Required audits and controls

For MICRO seed 100 and GATE seeds 100..104:

- exact C0 mechanism/readiness and marked-DS1 fingerprints;
- all frozen C0 stages and all 19 C0 controls pass unchanged;
- two roots, two handles, one choice, selected execution, R0 return, four
  evidence fields, one eligibility cell, and one anonymous coupling exist;
- coupling polarity fields and frozen DS1 updates remain independently counted;
- update-definition, coupling-to-update edge, runtime update, strength
  observation, and held-out reconstruction paths are inventoried separately;
- source mutations inserting an update edge, strength observation, or held-out
  reconstruction must change only the corresponding inventory;
- no semantic consequence, evaluator effect, truth filler, stable occurrence,
  action opcode, expected choice, hidden fallback, or economic signal enters
  organism-visible state or update wiring;
- no stage-8b-or-later work is imputed after collapse;
- all temporary C0 state is already cleaned by the frozen parent.

## Work, execution, and freeze

Report frozen E0, A1, R0, and C0 work/storage exactly as produced by C0. Count
only evaluator-side source/path comparisons added by this retry. Maintenance
and carrying remain zero. Blank downstream columns after collapse are distinct
from measured zero.

- MICRO: seed 100;
- GATE: seeds 100..104;
- deterministic, single-threaded, release execution;
- `--definitive` rejects before the harness with status 2 and preserves the
  results digest;
- validate only formatting, strict release Clippy, focused release tests,
  MICRO, and GATE locally and on persistent E2B;
- use only
  `/Users/satya/.cache/truelearner/ds1-after-c0-composition-e2b.json` for E2B
  state and leave the sandbox running;
- create no result artifact, definitive outcome, M1, DS2 work, or polarity gate.

Freeze the exact first-collapse handoff, source/fingerprint/path audit,
leak/control audit, dependency delta, physical ledger, local/E2B validation,
and results-digest preservation.
