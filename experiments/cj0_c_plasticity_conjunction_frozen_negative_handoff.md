# CJ0-C plasticity-only conjunction frozen-negative handoff

Status: **FROZEN MATERIAL NEGATIVE; PROBE REVERSAL COLLAPSE; CJ0 LAW ABSENT**.

## Outcome and classification

The exact plasticity-only candidate passed initial matched-marginal substrate
discrimination but failed mandatory reversal in all four fresh physical
cells. It is a development negative, not an authority result and not a rescue
of PX3 or PX3-R.

Initial learned/crossed/singleton outputs were `2/0/0`. Changed A+D/C+B
organization also formed and executed (`2` outputs), including after full
candidate-ARROW deallocation. But old A+B/C+D support strengthened from `138`
to `164` and still produced `2` held-out outputs after reversal. Old
organization therefore did not weaken, stale, or deallocate.

MICRO, GATE, recursion, OR convergence, temporal expressivity, definitive
evidence, PX3 advancement, PX4--PX8, and PX-C were not entered.

## Exact lineage, commits, tags, and hashes

- authoritative start:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
  `px2-physical-causal-direction-authoritative`;
- v1 protocol commit/tag: `f651f7d0a844de0bc81f63587ce5e775c780e2bf`,
  `cj0-c-plasticity-conjunction-probe-v1-protocol`;
- v2 cadence commit/tag: `860d3cb715c701daf9abc55e04c0118f943b03a5`,
  `cj0-c-plasticity-conjunction-probe-v2-protocol`;
- first implementation commit/tag:
  `2c5b34cef4ea7b8a12a1369f3268fad13300dd20`,
  `cj0-c-plasticity-conjunction-probe-v1-implementation`;
- packaging-clean implementation commit/tag:
  `7343bb289a5c5d084897aae85d6e2dc5ba9d1136`,
  `cj0-c-plasticity-conjunction-probe-v1-implementation-clean`;
- frozen negative commit/tag:
  `51175b35d442dc6eb90b56e64866c0425f929735`,
  `cj0-c-plasticity-conjunction-probe-v1-frozen-negative`.

Exact bytes:

- authoritative PX0--PX2 law:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- authoritative PX2 execution:
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- v1 protocol:
  `a9821e060394e77f5e15c90710e8d1809133d800091cf0cd530792af1be54427`;
- v2 cadence:
  `adc58816e9f78634879c16140de2bb508c5f11261a13d871063e1aa0d68d019f`;
- mechanical authority include build script:
  `b249097dee53f68e0bb73a7da690b06eb0d306e555d92f3b653a19d514528f1f`;
- organism-visible candidate addition:
  `078e6d9fc653b8c8f07243b581c99a5e6a9fbb4371d5c92e197b6bcecda8fd40`;
- evaluator/runner:
  `004bd46c169881a8f545cb79d8de2a2da398430639915eef43378de032312aa6`;
- result CSV/report:
  `bf9b433117100e961dc9beee2619cb7c0909ad96ab73f9394d8f998bdb5b7941`
  / `aa5c3310702a8120c0faa558edeadae41bbc3289f2326429127ce1657fb58da1`.

The first implementation tag intentionally remains as a transparent record of
the accidental tracked Cargo build directory. The subsequent clean tag
removes only reproducible build products; all scientific bytes are identical.

## Exact candidate law

The candidate added no persistent field and no execution primitive.

1. Two distinct same-position CELL loci had to fire at the same tick through
   live incoming ARROW traversal with current eligibility.
2. Their co-firing exposed ordinary coupling-`2`, resistance-`1`, delay-`1`
   ARROWs from both loci into the first local live CELL with no live incident
   ARROW. Existing shared live topology suppressed duplicate exposure.
3. A firing target strengthened current eligible incoming ARROWs by `+3` only
   when at least two distinct source loci were eligible.
4. Ordinary ARROW scanning, SPIKE delivery, threshold-`4` CELL firing,
   authoritative returned activity, unsupported-use pressure, ten-tick
   pressure, decay, refractory state, generation invalidation, ordering, and
   natural quiescence remained unchanged.

Learned and primitive contributors entered the candidate identically through
ordinary ARROW traversal. External correlation without traversal could not
propose. The retained structure was only ordinary CELL/ARROW matter and
existing numeric fields; no typed or renamed semantic representation existed.

## Physical consume/produce contract

Each PROBE cell consumed four threshold-`1` contributor loci reached through
ordinary driver ARROWs, sixteen threshold-`4` uncommitted CELL loci at the
same physical position, raw external SPIKE schedules, and frozen PX0--PX2
timing/plasticity/pressure. Four configurations varied physical namespace,
translated position, construction order, reservoir allocation order, and
arrival-origin order.

Sixteen initial rounds physically produced two two-incident convergent
structures. Every contributor fired exactly `16` times; cluster order
alternated with exact `4/12` recurrence marginals. Held-out activation was
ordinary unary ARROW propagation into a threshold CELL: either source alone
delivered impulse `2 < 4`; both delivered `4` and fired it.

Forty changed-experience rounds physically produced two new convergences, but
also preserved the old convergences. A separate `1,000`-tick pressure replica
deallocated every candidate ARROW, then bootstrapped changed structure from
ordinary activity without a mature higher-order firing. Dead historical
records remained dead; no generation resurrected.

## PROBE controls and serialized stages

Across all four cells:

- held-out A+B/C+D vs A+D/C+B: `2/0`, pass;
- all singletons: `0`, pass;
- too-late second contributor: silent, pass;
- correlated external firing without traversal: no proposal, pass;
- traversal with the second contributor absent: silent, pass;
- absent reservoir opportunity: no proposal, pass;
- stale candidate ARROWs after pressure: dead, pass;
- three/four-contributor ambiguity: symmetric `3/6` and `6/12`
  target/incident-ARROW counts, pass;
- fresh identity/layout/allocation/arrival variants: exact qualitative
  replicas, pass;
- exact replay: pass;
- natural quiescence and no autonomous source refiring: pass;
- self-evidence: old target received a subthreshold update but did not fire,
  become a contributor, or strengthen its old incident support, pass;
- independently serialized blank/acquired/reversed/bootstrap permanent
  fingerprints: present in every row;
- reversal old weakening/deallocation: fail (`138 -> 164`, old output `2`).

## Exact blocker

Candidate ARROWs are ordinary outgoing ARROWs, so they necessarily enter the
authoritative PX2 unary local-return path in addition to the new incident-
support path. During reversal, a later traversed arrival at one source within
the existing eligibility window strengthens every still-eligible outgoing
ARROW from that source. The obsolete target need not fire and its old second
contributor need not be present. Ordinary recurrence therefore manufactures
support for obsolete candidate structure even though it does not manufacture
false co-firing evidence at the target.

Repair would require a second scientifically meaningful selector or
exemption deciding which ordinary ARROWs may receive unary PX2 return versus
convergent-only support. Selecting by topology, target threshold/coupling,
allocation class, or a new persistent marker gives distinct laws. A marker is
forbidden; changing PX2 is forbidden; choosing among the remaining laws is an
architectural decision. The lane therefore stops rather than rescuing the
negative.

## Validation, accounting, storage, and isolation

- focused format/check/strict Clippy: pass;
- focused inherited library test: `1` pass, `0` fail;
- no-argument/wrong-argument refusal: pass, exit `2`;
- no-CELL/ARROW and exact hash preflight: pass;
- atomic artifact/staging audit: pass;
- CSV: `4` rows x `29` columns;
- organism-visible forbidden-token audit: zero hits;
- authoritative changed paths from frozen start: none;
- lane current-tree additions: `14` paths, `57,185` bytes before this handoff;
- result storage: `2,973` bytes;
- ledgered published-row work: `93,528` operations;
- final persistent storage summed across cells: `8,704` bytes;
- broad historical suite: not run because no shared source changed;
- definitive evidence: unspent.

PX3 and PX3-R negatives remain immutable. No authoritative source or result
was rerun, regenerated, modified, or advanced.
