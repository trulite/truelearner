# PX6 physical consequence-credit no-new-mechanism GATE v3 protocol

Status: **PREREGISTERED PHASE-CORRECTED RETRY; EVIDENCE UNSPENT; AUTHORITY ABSENT**.

GATE v1 (`32/48`) and v2 (`40/48`) remain immutable negatives. V3 uses fresh
namespaces beginning `0x6_8200_0000` and fresh write-once artifacts.

Every v2 CELL, ARROW, bridge, coupling, delay, return route, distractor,
presentation schedule, expected final vector, and control remains byte-exact.
No new physical structure or law is added.

## Sole physical schedule correction

In each block-switch cell, the same two ordinary external SPIKEs arrive at the
delayed participant CELL one tick—not three ticks—before its first
presentation. This follows the immediately preceding ordinary pressure step,
so the substrate-created reserve-1 local proposal has a physical opportunity
to traverse and receive return before the next pressure boundary.

The opportunity remains explicitly environmental. Local geometry alone selects
the nearby target. Direct ARROW construction at the switch remains forbidden.

## Sole accounting correction

The serialized return-arrival count subtracts all known external source
arrivals: two per ordinary participating presentation plus the two explicit
proposal SPIKEs in the delayed switch side. No substrate state or execution
depends on this measurement.

## Pass and stopping rule

All `48/48` cells and all v2 clauses must pass with exact replay and natural
quiescence. Any negative is frozen. A positive establishes development
readiness only, classifies the lane under A--D, and requires the mandatory
unchanged-port contract. Authority and definitive evidence remain absent.
