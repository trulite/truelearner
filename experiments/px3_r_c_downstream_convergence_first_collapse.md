# PX3-R Arm C downstream-convergence first-collapse freeze

Status: **FROZEN MECHANICAL ABSENCE; DEVELOPMENT ONLY; PX3 ABSENT**.

## Frozen start

- exact Class-D negative lineage HEAD:
  `873094497ff6eb74363191dc5edc479c7d66de72`;
- authoritative PX2 ancestor:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- authoritative PX0-PX2 law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- authoritative PX2 source SHA-256:
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- frozen PX3 no-addition source SHA-256:
  `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba`;
- frozen PX3 negative handoff SHA-256:
  `a029f250ed88f8f2fc164e0d2c9042675bf0a8c9ae51c89cf83ad1aa42e4fa9b`;
- frozen PX3 result CSV/report SHA-256:
  `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38` /
  `021be8698c010df1e09dc45f2bf9968f2255b6eb7851c38f36fd93be72260d3b`.

The checkout was clean before this artifact was written. The PX2 commit is an
ancestor of the exact PX3-R start. No frozen file is modified by this freeze.

## Neutral Arm C question

Can recurrent activity through multiple learned physical routes select broad,
weak physical opportunities onto the same ordinary downstream continuation,
so endpoint convergence persists and distinguishes a recurrent organization
from individually equal route strengths?

## Exact first collapse

The authoritative topology contains four independent copies of the PX2 route
motif, route-local participation traces, route-local returned strengthening,
and a shared return hub. It contains no physical downstream opportunity field
from those routes.

Adding direct sibling arrows from one route continuation to several possible
downstream continuations would not repair the absence. In the frozen law,
`apply_local_return` updates every live, eligible arrow whose `from` CELL is the
CELL receiving returned activity. All direct sibling arrows fired by that CELL
would therefore be strengthened together. Existing return locality cannot
select one sibling opportunity by the downstream continuation that actually
participated.

The exact first missing edge is therefore:

> no independently eligible ordinary weak approach from each learned route to
> each possible downstream continuation exists in the frozen PX0-PX2 graph.

This is a topology/opportunity absence, not permission to change the substrate
law. The already-frozen no-new-mechanism negative remains material and exact.

## Only mechanically admissible Arm C dependency

A later, separately preregistered Arm C development protocol may expose a
complete symmetric Cartesian field of physically ordinary, edge-local
approach CELLs and weak approach-to-continuation ARROWs. Each weak ARROW must
have its own approach CELL as source, so ordinary return locality can act on
the ARROW that physically participated. Every route must receive the same
opportunities, and replenishment, if any, must expose the full field without
using the evaluator's trained/crossed expectation.

The downstream CELLs must be ordinary continuation CELLs already present from
the start of a cell. No CELL may be created for a duo, no dedicated shared CELL
may be recruited, and no trace may couple to another trace. The candidate, if
one develops, is only the physical fact that independently retained ARROWs
from different learned routes end at the same ordinary continuation.

## Still forbidden

- any change to `crates/px0-physical-correspondence/src/lib.rs`;
- Event, Episode, History, Pair, Group, member lists, pair keys, semantic
  boundary flags, evaluator-selected pairing, old M3 schema, typed adapters,
  serialized co-occurrence state, or renamed equivalents in organism-visible
  execution;
- direct trace-to-trace coupling or a dedicated shared CELL;
- scenario names, expected pairings, or evaluator comparisons entering the
  substrate;
- rescue of a failed stage, definitive evidence, an authority matrix, PX3
  advancement, PX4 composition, or consumption of another arm.

## Disposition

The first collapse is frozen without entering a new CELL or executing an Arm C
opportunity. The next authorized step is only a separate preregistration of the
mechanically admissible opportunity and development-only PROBE/MICRO/GATE.
