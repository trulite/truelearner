# M4 cumulative learned-lifetime authoritative handoff

M4 is the authoritative cumulative ancestor.

```text
M0  learned/compiled correspondence
M1  + learned anonymous boundary roles
M2  + learned role-relative causal direction and physical execution
M3  + learned event organization
M4  + learned use-dependent persistence and forgetting
```

The narrow cumulative M4 mechanism is:

```text
ordinary recurrence/use
        -> local scalar resistance grows

ordinary completed activity / non-use / competition
        -> local scalar resistance is spent

zero resistance
        -> physical allocation disappears

later lawful return
        -> reuse and strengthen if still allocated
        -> ordinary reacquisition if removed
```

Contradiction requires no special memory invalidation operation. Changed lawful
activity builds its own protection while the unused old structure experiences
the same ordinary pressure as every other allocation. A physically removed
structure cannot execute a stale learned path.

Frozen definitive evidence:

- protocol:
  `b1b2a9252ee4211ffc0cf3f9789040b0f24ced7e`;
- implementation/audit:
  `70675525dbff092aa00aa04bfdd9e883211547c3`;
- CSV:
  `5c4a2e2b021a26a4cc2161202dd9a62205d426ba361f90a69d00ceb3df470a83`;
- report:
  `c418f6b5fb5f8f3f83e385c75cd23fb8c88def3650ff80a13a491d0979944768`.

The authoritative cumulative prefix is now:

```text
DS0 + DS1 + DS2 + DS3 + DS6
```

Under the frozen reordered sprint policy, the realized lineage is:

```text
M3 -> DS6 -> M4 -> DS7 -> M5 -> DS8 -> M6
   -> separately named DS4 successor -> M7 -> DS5 -> M8
```

Cumulative DS7 development may now consume learned M4 lifecycles. Supplied
request/start and finish/output conventions may remain only at the external
harness boundary and may not identify plastic sites, proposals, eligibility,
or retained changes. No DS7 definitive matrix is yet authorized.

The development GATE's preserved 5,001-cell overrun is not part of the
definitive claim. The authoritative result uses only the sixteen explicit fresh
blank-start cells in the definitive protocol.

