# PX3 participation-trace organization PROBE protocol v2

Status: **PREREGISTERED; PROBE EVIDENCE UNSPENT**.

This protocol supersedes the unexecuted v1 PROBE protocol solely to correct a
mechanical reading of the frozen PX0 law. All hypotheses, topology, scenarios,
dimensions, outcome predicates, commands, artifact paths, write-once rules and
later-stage refusals in v1 remain frozen except for the exact native proposal
resistance and consequent schedule below.

No v1 world was constructed, no substrate propagation occurred, no evidence
marker was emitted and no result or staging artifact exists.

## Correct native proposal geometry

PX0 generic local proposal assigns every candidate:

- delay equal to physical distance (`2` here);
- coupling `1`;
- resistance `1`, independent of distance;
- live eligibility only after the proposing opportunity fires and emits.

Therefore an unsupported priming emission cannot first expire and still leave a
live resistance-one candidate. The v1 sentence claiming initial resistance `2`
and expiry to `1` is retired.

## Correct frozen schedule

- tick `0`: every anonymous opportunity receives the same external threshold-2
  pulse, generically proposes its sole distance-two candidate and emits once;
- tick `2`: unsupported candidate units reach their consequences without
  context or return;
- training source entries begin at tick `2`;
- tick `3`: authoritative PX1 traces fire; simultaneous participants fire only
  their shared opportunity, whose real candidate traversal refreshes eligibility
  through tick `7`; shared context also fires and emits identically to every
  consequence;
- tick `5`: real candidate and context units coincide; unused priming
  eligibility has expired and ordinary expiry deallocates every untraversed
  resistance-one candidate; the traversed candidate remains eligible;
- tick `6`: the shared PX3 return reaches every opportunity; only the actually
  traversed candidate has live eligibility and strengthens from resistance `1`
  to `4`, with coupling raised to `2`;
- recurrent second training source entries begin at tick `11`; pressure at tick
  `10` has reduced the selected resistance from `4` to `3`;
- ticks `12/14/15`: trace overlap, consequence and return repeat, raising the
  selected resistance from `3` to `6`;
- held-out clones advance through the fixed pressure gap to tick `50`; a
  one-exposure candidate deallocates, while a recurrent candidate remains live
  at resistance `2` and coupling `2`.

The late A/B control schedules A at tick `2` and B at tick `4`, producing trace
firings at ticks `3` and `5`. The repeated-A control supplies both entries at
tick `2`. Held-out trained AB, crossed AD and singleton A entries begin at tick
`50`; held-out gapped A/B begins A at tick `50` and B at tick `52`.

## Frozen execution surface

The package, `--preflight` and sole `--probe` evidence commands, 24-row matrix,
artifact destinations and schemas remain exactly those in v1. The executable
must hash and audit the cumulative development protocol, unexecuted v1, and this
v2 correction. It must implement no v1 schedule switch and no MICRO/GATE
surface.
