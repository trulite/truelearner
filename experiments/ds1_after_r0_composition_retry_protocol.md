# DS1 after DS-R0 cumulative composition retry protocol

Status: **PREREGISTERED; DEVELOPMENT ONLY; NOT CLAIM ELIGIBLE**

This protocol is frozen from exact enabling parent
`e0db45c2eb6837236aba9b2859b390db14795c7f` /
`ds-r0-anonymous-post-action-evidence-return-e2b-validated`. M0
`1d74c0ed0b515446161a63a6d43ecbe27514dc85` remains the authoritative
cumulative mainline. DS-E0, DS-A0, DS-A1, and DS-R0 remain enabling-only.
M1 does not exist unless a separately authorized cumulative freeze follows a
complete retry pass.

## Sole question

> Once actual post-action activity has already formed the anonymous DS-R0
> return relation and exact evidence surface, how far does the byte-identical
> frozen DS1 learner progress through its existing update and boundary-role
> reconstruction machinery?

The retry adds no learner, consequence interpretation, credit mapping, update
bridge, correctness signal, boundary role, or held-out answer. It reproduces
the frozen R0 chain unchanged, inventories only paths that physically exist,
and freezes the first unavailable dependency.

## Immutable lineage

Stage 0 requires all of the following exact evidence:

- enabling parent commit:
  `e0db45c2eb6837236aba9b2859b390db14795c7f`;
- authoritative M0:
  `1d74c0ed0b515446161a63a6d43ecbe27514dc85`;
- frozen R0 mechanism SHA-256:
  `f17afa482bf345eb680463f7418b6b6c2553cd78eab9b4dbfce74f7ca1483d51`;
- frozen R0 readiness SHA-256:
  `0888ddb8187f606ec7fac72369d4e8b397b624226ddd709540c882925dae82e5`;
- frozen R0 E2B amendment SHA-256:
  `729948af6d49b9428f04091529d087127b78102f063c87d3593a412713c6e7b3`;
- frozen marked DS1 learner SHA-256:
  `adec6a422e69e7f90bff6482776ea9aa91ae89e5e8d59183f6228165f9f7ff0e`;
- frozen results-tree digest:
  `491a63c17ba35d768b630720063793a4db09686cfe7cb33694fd80ea63bbd4e4`.

The R0 mechanism, its E0/A1 imports, the marked DS1 learner, and every upstream
source are read-only. A byte change is a stage-0 collapse, not an authorized
implementation edit. No broad regression is required because no shared or
frozen mechanism may change.

## Exact composition boundary

The retry invokes the frozen R0 development harness directly. R0 must produce
the same actual E0 event, two physical A1 roots, opaque frozen-DS1 choice,
selected route execution, fresh return activity, learned temporary return
relation, and four-field evidence surface as its readiness freeze.

The retry may inspect the already-populated anonymous evidence surface and
inventory whether an existing source/runtime path carries it into frozen DS1
`apply_consequence`. It may not:

- turn the return relation into a boolean or signed value;
- label later activity good, bad, correct, wrong, accepted, or rejected;
- decide which alternative deserved credit;
- call or wrap `apply_consequence`;
- compare alternatives or synthesize a counterfactual outcome;
- mutate frozen DS1 strengths for measurement;
- perform held-out reconstruction after an earlier collapse.

Any update must occur through a path that already exists before this retry.
Any zero path must be mechanically derived and mutation-sensitive, never a
literal assertion used as the scientific result.

## Ordered stages and stopping rule

The stages are frozen exactly as follows:

0. M0 lineage and frozen correspondence/R0 controls remain valid;
1. frozen E0 event formation succeeds;
2. frozen A1 forms multiple executable affordances;
3. byte-identical frozen DS1 sees two opaque handles;
4. byte-identical frozen DS1 chooses one handle;
5. the selected physical route executes;
6. frozen R0 forms one anonymous temporary return relation;
7. the exact four-field anonymous evidence surface is populated;
8. frozen DS1 updates through an already-existing coupling;
9. frozen DS1 boundary-role strengths diverge;
10. held-out boundary-role reconstruction succeeds.

The first false stage is the only collapse. Every later stage is `BLOCKED` and
its work/storage fields remain blank. No rescue follows in this lane.

If stage 8 is false, the exact interpretation is limited to a missing existing
coupling between returned anonymous evidence, the selected alternative, and
frozen DS1 update. It is not evidence that post-action activity or R0 evidence
is absent. If stage 8 is true but stage 9 is false, freeze strength separation.
If stage 9 is true but stage 10 is false, freeze held-out reconstruction.

Only passage through stage 10 is development eligibility for a separately
authorized M1 freeze. This retry itself never creates M1.

## Required audits and controls

For MICRO seed 100 and GATE seeds 100..104:

- exact R0 mechanism/readiness/E2B and marked-DS1 fingerprints;
- all frozen R0 stages and all 22 R0 controls pass unchanged;
- one actual E0 event, two structural roots, two opaque handles, one frozen
  DS1 choice, one selected route execution, three activity occurrences, two
  physical propagation relations, one temporary return relation, and four
  exact evidence fields are present per seed;
- frozen R0 reports zero DS1 updates before the retry boundary;
- update-definition, evidence-to-update call edge, runtime update, strength
  observation, and held-out reconstruction paths are counted independently;
- source mutations that insert an evidence-to-update edge, a strength
  observation, or a held-out reconstruction call must change the corresponding
  inventory;
- no semantic consequence word, evaluator effect, truth filler, stable
  occurrence identity, action opcode, expected choice, or hidden fallback
  enters organism-visible state or update wiring;
- no stage-8-or-later learner work is imputed after collapse;
- all temporary R0 state is already cleaned by the frozen parent.

## Work, execution, and freeze

Report frozen E0, A1, DS1-choice, and R0 work/storage exactly as produced by
the parent. Count only evaluator-side source/path comparisons added by this
retry. Maintenance and carrying remain zero. Blank downstream columns after
collapse are distinct from measured zero.

- MICRO: seed 100;
- GATE: seeds 100..104;
- deterministic, single-threaded, release execution;
- `--definitive` rejects before the harness with status 2 and preserves the
  results digest;
- validate only formatting, strict release Clippy, focused release tests,
  MICRO, and GATE locally and on persistent E2B;
- use only
  `/Users/satya/.cache/truelearner/ds1-after-r0-composition-e2b.json` for E2B
  state and leave the sandbox running;
- create no result artifact, definitive outcome, M1, DS2 work, or credit gate.

Freeze the exact first-collapse handoff, source/fingerprint/path audit,
leak/control audit, dependency delta, physical ledger, local/E2B validation,
and results-digest preservation.
