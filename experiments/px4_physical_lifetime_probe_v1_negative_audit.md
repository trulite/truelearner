# PX4 physical learned-lifetime PROBE v1 negative audit

Status: **FROZEN DEVELOPMENT NEGATIVE; NO RERUN OR RESCUE**.

## Frozen result

- implementation commit/tag:
  `b273d071720531d0046b60c09b9edfb9d147cc2f` /
  `px4-physical-lifetime-development-implementation-v1`;
- CSV SHA-256:
  `b7a16d1b5916c2d168366a718e61f46dbb965a0235e12eec3d4a424c2785263e`;
- Markdown SHA-256:
  `5bae8e3175a94a104d85a6eac48ead135e1e2ccf55c5cb508228e6d099d73c61`;
- result: `0/6`, frozen FAIL.

The single write-once command exited 1. The artifacts have not been edited or
regenerated.

## Independently exposed collapses

### Accounting collapse

`source_exact=false` in all six rows. Every frozen ancestor hash and ancestry
check independently matches. The failure is local to the new scanner: its
dynamic source scan reconstructs forbidden strings that are also present as
literal data in the adjacent substrate-only scan list, so it finds its own
audit specification in the harness source. No organism execution depends on
this predicate.

### First physical edge

Every initially weak direction opportunity reports trained resistance `0|0`.
The implementation introduced reserve-3 candidates after acquisition at the
substrate's then-current tick, but scheduled first possible traversal 34 ticks
later. At propagation, ordinary ten-tick pressure correctly applied the
elapsed steps before candidate traversal, reached zero, advanced generation
from 1 to 2, and refused execution.

This is a mechanically unique experiment-timing collapse: a claimed
contemporary weak opportunity was physically present during a long pre-use
absence. It does not discriminate learned-lifetime sufficiency.

The later forward-to-reverse row independently shows that a genuinely
contemporary opposite candidate can be protected (`final resistance 0|34`,
effects `0|1`), but v1 remains conjunctively negative.

## Authorized next diagnostic

A separately preregistered fresh-identity PROBE v2 may make exactly two
measurement/construction corrections:

1. build both forbidden-token scan lists without embedding either completed
   token in the scanned source;
2. advance ordinary time to the preregistered first-use tick before physically
   introducing each weak candidate, so introduction and first possible use are
   contemporary.

No resistance, pressure, return, deallocation, reproposal, coupling, threshold,
experience count, later gap, physical expectation, or substrate source may
change. V1 identities and result paths are permanently spent.
