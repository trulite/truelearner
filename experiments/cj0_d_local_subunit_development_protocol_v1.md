# CJ0 Arm D local-subunit development protocol v1

Status: **PREREGISTERED DEVELOPMENT ONLY; EVIDENCE UNSPENT; NO AUTHORITY EXECUTION**.

## Frozen start and preserved material

This isolated arm begins exactly at commit
`2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
`px2-physical-causal-direction-authoritative`. The authoritative PX0--PX2
source files are read-only. The frozen PX3 Class-D result and PX3-R Arms A,
B, and C are read-only material negatives and are not ancestors to import,
rescue, or reinterpret.

The following frozen bytes must remain exact:

| material | SHA-256 |
|---|---|
| authoritative PX0--PX2 law, `crates/px0-physical-correspondence/src/lib.rs` | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| authoritative PX2 example | `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5` |
| frozen PX3 source | `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba` |
| frozen PX3 CSV | `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38` |
| frozen PX3 report | `021be8698c010df1e09dc45f2bf9968f2255b6eb7851c38f36fd93be72260d3b` |
| frozen PX3 handoff | `a029f250ed88f8f2fc164e0d2c9042675bf0a8c9ae51c89cf83ad1aa42e4fa9b` |
| frozen PX3-R Arm B v2 source | `1ae7592134db8692a95b2ca38d837306c5021f79df9e2ddf9f5e32b6d660631d` |
| frozen PX3-R Arm B v2 CSV | `bb95ff9c05b9105ae2116c4efc6ab8c4f228907eb5feaa42e8a8f7836cae8298` |

No PX3, PX4--PX8, PX-C, definitive, or authority execution is authorized.

## One candidate law

The arm tests exactly one substrate law in an isolated source copy:

> When a positive ordinary ARROW delivery reaches a live CELL at the same
> physical tick as retained subthreshold state from a different live incoming
> ARROW source, and both incoming ARROWs carry the same current local
> eligibility horizon, the still-subthreshold CELL emits ordinary SPIKEs only
> along its live, positive, same-region outgoing ARROWs whose endpoints are
> within the existing local variation radius. The CELL itself has not fired.

The condition is evaluated from existing CELL state/tick, current SPIKE and
ARROW identity, incident ordinary topology, and existing ARROW eligibility.
It adds no persistent field, level, contributor list, relation key, semantic
object, evaluator selector, or primitive/learned branch. The emitted SPIKEs
use ordinary queue ordering, generation checks, eligibility, returned
activity, pressure, and deallocation. A mature CELL firing continues to use
the unmodified ordinary outward scan.

Fixed values are the authoritative local radius `2`, local eligibility window
`4`, local-return resistance increment `3`, coupling ceiling `2`, pressure
period `10`, and weak proposal coupling/resistance `1/1`. The anonymous local
CELL threshold is `4`; its two route-local sources have threshold `2`.

This differs physically from frozen PX3-R Arm B at its exact missing edge.
Two recreated coupling-`1` inputs still do not fire the threshold-`4` CELL,
but their actual contemporary arrival may emit local return before CELL
firing. Returned activity can therefore strengthen both recreated ordinary
incoming ARROWs. No already-mature shared CELL is required for bootstrap.

## Complete symmetric physical field

Every four-route matter contains all six unordered two-route sites. Every site
contains two route-local source CELLS at physical offsets `-2/+2`, one local
threshold-`4` CELL at the center, and one outward CELL in region `1`. The two
source-to-local opportunities begin at coupling/resistance `1/1`; local-to-
source return ARROWs and local-to-outward ARROWs are ordinary fixed physical
scaffold supplied identically at all six sites. Each route occurrence enters
all three of its source ports. Missing local opportunity ARROWs may be
recreated only by the authoritative anonymous radius-`2` proposal law when
ordinary external activity fires that source port.

The evaluator schedules external SPIKEs and reads copied physical state. It
does not select a site, install a relation, trigger a local update, delete an
ARROW, declare a change, or feed an expected result into execution.

## Matched-marginal acquisition and held-out use

The positive world trains `A+B` and `C+D` for six rounds, alternating which
cluster occurs first. Each route has identical occurrence count, source
SPIKEs, source firings, traversals, local return opportunity, outward training
effects, timing marginals, pressure exposure, and physical identity frequency.
Held-out nonplastic clones receive `A+B`, `C+D`, `A+D`, and `C+B` with the
same route-use mechanics. The trained uses must each produce one outward
crossing; crossed uses must produce zero. `A`, `B`, `C`, and `D` alone must
produce zero outward crossing and zero local-subunit event.

Every independently meaningful stage serializes source firing, incoming
traversal, local-subunit event, local CELL firing, returned arrival, local
return update, outward crossing, each incoming resistance/live/coupling,
pressure/deallocation, quiescence, work, persistent bytes, arrow count,
complete fingerprint, permanent fingerprint, and exact duplicate equality.

## PROBE

PROBE executes fresh primary, mirrored/reversed allocation and identity,
stable-alternative, singleton-only, too-late, correlation-without-traversal,
traversal-with-blocked-local-return, absent-opportunity, stale-opportunity,
ambiguity, genuine four-route activity, exact replay, and natural-quiescence
cells. The positive clause is conjunctive:

1. frozen hashes, ancestry, isolated paths, and fixed constants are exact;
2. matched individual marginals are exact;
3. trained held-out uses cross and crossed/singleton uses do not;
4. local events require actual contemporary distinct-source traversal;
5. no-return, absent, stale, correlation-only, and too-late controls leave no
   reusable outward organization;
6. ambiguity and four-route activity expose all physically supported sites
   without exclusive semantic choice;
7. no autonomous source refiring occurs, every queue drains naturally, and
   duplicate complete state is exact.

Any scientific failure is frozen and stops this arm before MICRO.

## MICRO: mandatory contemporary reversal and bootstrap

Starting from positive acquisition, ordinary experience changes to `A+D` and
`C+B` for eighteen rounds under the same complete field and matched route
marginals. There is no evaluator invalidation, pair-change SPIKE, reset,
selected deletion, or topology rewrite.

Required result:

- old `A+B`/`C+D` incoming support weakens and deallocates under ordinary
  pressure plus unsupported use;
- new `A+D`/`C+B` support forms, fires, and executes;
- old held-out uses no longer execute and new held-out uses do;
- an old learned site receiving `A` alone produces neither a fresh local event
  nor returned support;
- after a separately serialized full-deallocation gap, ordinary changed
  activity recreates coupling-`1` opportunities, produces a subthreshold local
  event before any local CELL firing, and later forms executable organization;
- post-deallocation exact replay uses current matter only; no dead generation
  or historical structure is resurrected.

Any failure is a frozen MICRO negative. A mechanically unique serialization,
accounting, fresh-seed, or forced-timing defect may be corrected only after a
new protocol commit/tag; scientific clauses may not be rescued.

## GATE

GATE uses twelve fresh namespaces across normal/mirrored layouts, normal/
reversed allocation, normal/reversed identities, arrival insertion reversal,
round spacings `10/12/14`, cluster gaps `3/4/5`, and exact duplicate replay.
All PROBE and MICRO clauses remain conjunctive. GATE additionally requires
fresh-identity independence, permutation/mirror equality at the declared
physical invariants, bounded work, no autonomous recurrence, no staging-file
leak, and exact atomic serialization.

GATE is development evidence only. PASS does not authorize a definitive or
authority matrix.

## Recursion, ordinary convergence, and time

After a positive flat prototype and before GATE classification, the same law
is tested without level-specific code through `A+B -> X`, `X+C -> Y`, and
`Y+D -> Z`. `X` and `Y` are the exact ordinary local CELLS produced by the
lower use; the construction API, threshold, ARROW law, return, pressure, and
measurement are identical to primitive inputs. Both incoming supports at each
level must acquire equally, held-out execution must reach `Z`, and crossed or
missing primitive activity must not.

Without adding OR, a separate ordinary convergent-ARROW control must show
`A -> C`, `B -> C`, and `A+B -> C` through the existing threshold/refractory
physics.

The surviving law plus existing CELL transient state/time must serialize
distinct outcomes for: same-tick together; ordered one-tick `A` then `B`;
continued `A` while `B` arrives at a later same-tick opportunity; a delivery
inside residual-state time but outside the same-tick local-subunit condition;
and `B` absent until the residual state physically closes. No logical
operator, clock label, closure flag, or typed sequence may be added.

## Execution discipline and artifacts

The isolated binary must refuse no arguments and wrong arguments, and
`--preflight` must enter no CELL and publish no result. Each eligible stage is
executed literally once with `--probe`, `--micro`, or `--gate`; it emits one
evidence-spent marker and atomically renames stage-specific CSV and report
staging files. Existing or staging artifacts cause refusal.

Before each execution: focused format/check/test/strict Clippy, exact source
hashes, ancestry, protocol hash, fresh namespaces, forbidden-token isolation,
artifact absence, source/leak audit, and preflight must pass. After each
execution: freeze the result commit and annotated tag before proceeding.

Stop immediately if the one law fails scientifically, more than one
scientifically distinct law is required, a new persistent substrate field is
needed, semantic representation appears, PX0--PX2 must change, recursion
creates an architectural choice, or staging cannot remain evaluator-only.

