# PX3-R ARM A direct physical trace coupling frozen-negative handoff

Status: **FROZEN MATERIAL NEGATIVE; PX3 AUTHORITY ABSENT**.

## Outcome

Direct ordinary ARROW coupling between locally co-active participation-trace
loci is **not sufficient** for reusable co-participation organization under the
mandatory held-out discriminator.

Recurrence and returned activity do create strong physical `0<->1` and
`2<->3` structure under exact individual-route marginals. The failure is at
reuse: an ordinary edge transmits whenever its individual source route fires.
Crossed use therefore activates old trained edges as well as exposing fresh
weak crossed edges. The candidate remembers adjacency but cannot make old
transmission conditional on contemporary joint participation.

Repair requires coactivity-gated transmission, a recruited shared
convergence, or another conditional substrate law. Those are outside direct-
edge ARM A. There is no positive candidate, MICRO, swap/relearning execution,
GATE, definitive matrix, PX3 authority, or PX4 composition.

## Frozen lineage and exact mechanism candidate

- exact requested Class-D start:
  `873094497ff6eb74363191dc5edc479c7d66de72`;
- authoritative PX2 ancestor:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- generic-opportunity collapse commit/tag:
  `21c606b02e3d63e4300999fb1a7b3e0a28b50023`,
  `px3-r-trace-coupling-generic-opportunity-probe-v2-frozen-collapse`;
- first candidate failure commit/tag:
  `005991d8a3dcf54e205fac478d2998e498f36693`,
  `px3-r-direct-trace-coupling-probe-v1-first-clause-failure`;
- executed phase-corrected mechanism commit/tag:
  `15fc87716e5239de3628f4617c20d695d9878fd2`,
  `px3-r-direct-trace-coupling-probe-v2-implementation`;
- frozen-negative result commit/tag:
  `18610eb8888ab54a77008f2908c22e2f4b059fe5`,
  `px3-r-direct-trace-coupling-probe-v2-frozen-negative`.

Executed mechanism sources and hashes:

- `arms/px3-r-trace-coupling-v2/src/lib.rs`:
  `0027d8356170a673c3045980fed8a5a3f1509277072753bba03cb3b43143f6c9`;
- `arms/px3-r-trace-coupling-v2/src/arm.rs`:
  `abec17d32b110538133044f2366b27f4104b632101ed2d0f34e13f10d484a409`;
- `arms/px3-r-trace-coupling-v2/src/bin/probe.rs`:
  `de2f55f74f0bc8a9b12cd0ad07c403278ecc72bcf61fa097f470be77f3a8569e`;
- executable retry protocol:
  `f671332544b3e7bf0e291d22fcf10ff4416b3f1bfa0b710e07a554933cb19a6c`;
- result CSV/report:
  `421008eefe9e6a6ae82c0871b5f85bc31a3e722b4304f5b7433974200fcb89bb` /
  `ce52593ea917b638e77f923746af6ef42638bf3a40e0a528f4dbf428ac650c28`.

No mechanism is authoritative or eligible to port forward as PX3.

## Physical inputs consumed

The decisive PROBE consumed only retained physical matter plus the
preregistered opportunity:

- four anonymous PX2 route copies with arrival, correspondence-end,
  continuation, consequence, participation-trace, outward, acquisition-driver,
  participation-driver, and return-gate CELL matter;
- the retained PX2 physical return hub, used only to complete each individual
  participation trace and incapable of selecting a relation;
- correspondence acquisition at ticks `0,16,32,48`;
- matched direction maturation with the arm opportunity absent: `0+1` at tick
  `64`, `2+3` at tick `72`;
- twelve candidate rounds beginning at tick `84`, spacing `18`, clusters
  `0+1` / `2+3` eight ticks apart, with early/late cluster order alternating;
- ordinary external SPIKE arrivals, traversal, outward crossing, returned
  trace activity, four-tick eligibility, `+3` return strengthening, ten-tick
  pressure, decay, refractory state, generation invalidation, deterministic
  ordering, and natural queue drain.

## Explicit environmental opportunity

At quiescence after equal PX2 direction maturation, the environment installed
one preregistered numeric opportunity:

- local radius `8`;
- recent-firing overlap `1` tick;
- proposed ARROW delay `1` tick;
- proposed coupling `1`, resistance `1`, phase `0`;
- missing directed ARROWs exposed symmetrically between any two live local CELL
  loci whose physical firings overlap;
- the retained earlier local firing traverses its just-exposed direction once;
  the current firing traverses through the ordinary outgoing scan.

There is no trace type or route fact in this law. Trace loci happen to be the
only simultaneously firing CELL matter within the radius in the positive
layout. Six-tick delay, absent-opportunity, four-tick spacing, passive
correlation, and stale controls are explicit physical variants.

## Structures and activity produced

Under matched positive training:

- individual correspondence resistance: `23|23|23|23`;
- individual direction resistance: `18|18|18|18`, all live;
- continuation/consequence/trace/return/outward counts:
  `12|12|12|12` for every measure;
- candidate arrivals: `12|12|12|12`;
- candidate impulse: `23|23|23|23`;
- only `0<->1` and `2<->3` remained live, resistance `17` in each direction;
- candidate cell topology/storage: `56` ARROWs / `5,976` persistent bytes;
- complete duplicate replay: exact;
- autonomous source refiring: zero;
- every finite queue: naturally quiescent.

Controls left no live inter-trace matrix for opportunity absent, return delay
`6`, temporal spacing `4`, passive correlation without route participation,
or a single stale occurrence after `80` ticks of pressure opportunity.

## Mandatory trained-versus-crossed discriminator

Individual route frequency, total external activity, timing marginals,
traversal counts, return counts, pressure, outward effects, and serialized
route strength were equal. Held-out outward effects were `1|1|1|1` for both
trained and crossed schedules.

The physical responses differed, but in the disqualifying direction:

- trained `0+1` / `2+3` local impulse: `2|2|2|2`;
- crossed `0+3` / `2+1` local impulse: `3|3|3|3`;
- trained post-use matrix: only the old resistance-`17` edges;
- crossed post-use matrix: old resistance-`17` edges plus newly returned
  resistance-`4` crossed edges.

The extra crossed impulse is the old edge transmitting on individual source
firing. Thus crossed organization is not isolated from the sum of individually
learned paths, and the preregistered positive clause is false.

## Swap and relearning

Not executed. The core held-out discriminator failed at PROBE, so MICRO and its
swap schedule were scientifically ineligible. Direct edges already show the
blocking behavior: old edges transmit during crossed contemporary use. Testing
weakening/new formation cannot repair the missing contemporary co-participation
condition without adding a second law. No swap or multi-organization
persistence claim is made.

## Forbidden-information audit

Organism-executable source contains no exact Event, Episode, History, Pair,
Group, member, boundary, semantic, evaluator, serializer, old-M3, or DS3 token.
It contains no member list, pair key, evaluator endpoint, typed adapter,
co-occurrence record, relation serializer, semantic flag, hidden reset, or old
M3 schema. Scenario names and expected masks exist only in the evaluator
binary, which the physical library cannot read. Full executable-token audit:
pass.

The four-by-four matrix is a post-propagation evaluator serialization of
ordinary ARROW endpoints/resistance. It is not organism state and has no causal
path back into execution.

## Unchanged-port contract onto PX2

Authoritative files changed: none. Direct audit from the frozen start reports
only added paths. Exact retained hashes remain:

- PX0--PX2 law:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- PX2 execution source:
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- frozen PX3 Class-D handoff:
  `a029f250ed88f8f2fc164e0d2c9042675bf0a8c9ae51c89cf83ad1aa42e4fa9b`;
- frozen PX3 negative CSV:
  `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38`.

The isolated arm began as an exact copy of the authoritative law. Its diff is
limited to the opt-in opportunity, recent physical firing ticks, opportunity
installation/fingerprint/storage, and isolated construction/evaluator files.
No negative or authority artifact is imported back.

## Files, validation, and accounting

New files are confined to:

- `arms/px3-r-trace-coupling/` (frozen failed implementation);
- `arms/px3-r-trace-coupling-v2/` (executed negative implementation);
- `experiments/px3_r_*trace_coupling*.md` protocols/audits/handoff;
- `results/px3_r_*trace_coupling*.csv|md` atomic results.

Validation:

- exact clean start and PX2 ancestry: pass;
- formatting and focused compile for each frozen implementation: pass;
- strict focused Clippy: pass;
- v2 inherited library test: `1` pass, `0` fail;
- no-argument/wrong-argument refusal and no-CELL preflight: pass;
- source/hash/lineage/forbidden-token audits: pass;
- decisive CSV shape: `6` rows, `24` columns;
- existing-path modifications from frozen start: none;
- broad historical suite: not run because shared code did not change;
- definitive evidence/authority matrix: not run.

Ledgered physical work is `1,998` generic-collapse operations,
`12,375,887` failed-v1 candidate operations, and `12,489,546` decisive-v2
operations: `24,867,431` total. The runaway generic v1 reference is frozen as
unledgered first-clause failure and excluded rather than estimated. Atomic
result storage is `7,515` bytes. Before this handoff document, the lane adds
`32` tracked files and `214,217` bytes.

At the pre-push audit, the isolated branch had no remote counterpart on
`origin` (`git@github.com:trulite/truelearner.git`). The handoff commit/tag is
created next, generated build directories are removed from the worktree, and
the isolated branch plus all lane tags are pushed. Final remote equality is
reported after that push.
