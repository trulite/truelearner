# PX6 LR-C cumulative physical-consequence authority protocol v1

Status: **PREREGISTERED; SERIAL AUTHORITY EVIDENCE UNSPENT; PX7 FORBIDDEN**.

## Exact cumulative parent

The sole parent is PX5 authority commit
`7392505c26edfe9fa5d9d74dc42fed4a0cb7b902`, tagged
`px5-lrc-allocation-authority-v1`. This protocol is the first commit on branch
`research/px6-lrc-cumulative-authority`; its Git parent must be that exact
authority commit.

The retained physical sources and cumulative evidence are byte-frozen:

| artifact | SHA-256 |
|---|---|
| LR-C law, `crates/lr1-modulatory-physical-return/src/lib.rs` | `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10` |
| PX4 API, `arms/px4-lrc-lifetime/src/lib.rs` | `a201674f9d558b5bda20aef71e9857b632f8a6565f372aee88994a280e0fea71` |
| PX5 authority handoff | `1e6c947660b55d6f21060734c57a54f56541303b65069f23010344ca7d362a97` |
| PX5 authority CSV | `5ccfa15b6da93ac276b9474c4d501ef9c7769748c52dbf7a8882620758b1259a` |
| PX5 authority report | `e96622614e4c9569f1f90d60fa0ef822072afae5e09c316b2c37344e31f194ed` |
| PX5 active manifest v3 | `32fda2d86f6c836438fdd01fc5433c8731f4ec92e3e052e5d1a64fe751d15388` |
| PX5 taxonomy summary | `c6e5ebc2bf9ca33387a52fa2e7e900bb2946cc357bcfe2ff08820022a2bf9e37` |

No unrelated PX4, PX5, or isolated PX6 development residue may be imported.
The cumulative candidate may use only the authoritative PX4 public API and
its LR-C dependency; PX5 added no active source.

## Preserved isolated PX6 development

The isolated development branch and all three
`px6-lrc-physical-consequence-credit-development-*` tags remain immutable.
They are referenced, not cherry-picked into the serial lineage:

| isolated artifact | SHA-256 |
|---|---|
| evaluator source | `b2876df8dfb8adc6cf0e406ecf5264410e06b65901f1e802c111962a4c60759a` |
| evaluator Cargo manifest | `1a9825f414d04ff89f1d3123b6bfeae8539b6a2e76b01926a3b1146be6b4d80c` |
| development protocol | `724d7e0e56c3ab706fca7b7e34ebe8f90c394c0fad3db19d7e151da58585586b` |
| positive development CSV | `bfb63d4b5041534803221e989d90e16043859c81b7c52aef29995c44ece888e4` |
| positive development report | `c97c639383e0e6f6d7caab68a8eadb848cf865ea84b0bec4e4c828cb7ccdaa09` |

The scientifically retained reduction is:

```text
actual Drive traversal
-> live local eligibility on the traversed outgoing ARROW
-> actual downstream physical participation
-> actual qualified Modulatory traversal to that source
-> local resistance/coupling change under ordinary pressure
-> generation-safe deallocation and ordinary reacquisition
```

PX6 adds no organism source, state, transmission mode, outcome/reward object,
semantic history, route owner, evaluator-supplied value, explicit credit call,
new plastic update, or typed handoff. The authority arm is evaluator-only.

## Fresh serial implementation boundary

The authority package is `arms/px6-lrc-consequence-authority`. It may depend
directly only on:

```text
px4-lrc-lifetime
lr1-modulatory-physical-return
```

It may port only the isolated evaluator's explicit physical geometries and
predicates, replacing development identities, commands, publication, resource
accounting, and cumulative controls. All arrivals enter through the existing
LR-C/PX4 interfaces. Verdicts and serialization are downstream observations
and cannot affect organism execution.

Forbidden implementation techniques include unsafe code, interior mutability,
global/thread-local hidden state, proc macros or generated code,
evaluator-conditioned organism branches, semantic enums/flags/adapters,
artificial lifetime or memory leaks, and measurement feedback into later
physical input. Needing any new field, mode, transition, eligibility rule,
plastic update, pressure rule, semantic adapter, or memory trick is a genuine
scientific fork: freeze the diagnostic and stop before authority evidence.

## Fresh definitive matrix

The eight explicit authority roots are:

```text
661001 661002 661003 661004 661005 661006 661007 661008
```

The three explicit dense ordinary-Drive loads are:

```text
8 32 128
```

This is exactly 24 fresh rows. Each row derives all physical identities below
`(root << 32) + 0x0700_0000`. These roots and namespaces are disjoint from the
isolated PX6 namespaces, PX5 roots `561001..561008`, PX4 roots
`461001..461016`, and all earlier authority evidence.

The root index crosses all eight combinations of returned side, arrival order,
and reflected layout. Load index crosses direct/relayed return shape and,
together with root index, return ticks `0..4`. Every registered return arrives
within the unchanged four-tick LR-C eligibility window. No state crosses rows.

Each row contains the smallest three-CELL positive loop plus only the CELL or
ARROW needed by each isolated control. Dense traffic consists solely of
independent ordinary Drive traversals. Every row executes twice from fresh
complete state inside the sole definitive command; this embedded equality is
the only authority replay.

## Twenty-three conjunctive row clauses

Every row must pass all 23 clauses:

1. registered root/load/side/order/reflection/shape/delay and namespace are exact;
2. the candidate traverses, receives exactly one Modulatory delivery/update,
   and changes resistance/coupling exactly `1/1 -> 4/2` amid exactly `load`
   distractor crossings;
3. Modulatory traversal without candidate participation makes zero update;
4. ordinary Drive arrival while the candidate is eligible makes zero update;
5. blocked downstream firing makes zero Modulatory delivery/update;
6. missing downstream return makes zero Modulatory delivery/update;
7. immediate physical return makes exactly one qualified local update;
8. return at the four-tick eligibility boundary makes exactly one update;
9. stable repeated and physically changing return paths each strengthen the
   same traversed local structure to resistance `7`;
10. moving the physical return topology moves survival between symmetric
    sources independently of reversed arrival order;
11. three simultaneous lawful loops make exactly three strictly local updates;
12. pressure retains accepted structure and deallocates unsupported structure;
13. a dead candidate generation stays blocked while ordinary reproposal creates
    and strengthens one distinct live generation;
14. fresh PX0 local-correspondence conformance passes;
15. fresh PX1 actual-participation conformance passes;
16. fresh PX2 physical-causal-direction conformance passes;
17. fresh PX3 repeated co-participation organization conformance passes;
18. fresh PX4 recurrence, pressure, deallocation and reacquisition conformance
    passes through the authoritative PX4 API;
19. fresh PX5 loaded physical-allocation conformance passes at the registered
    load using only native proposal/traversal/modulation/pressure;
20. complete per-row physical work is at most `150000` ledger operations;
21. maximum live-world persistent bytes are at most `32000`, and repeated
    reuse changes neither structural count nor persistent bytes;
22. every propagation naturally quiesces; and
23. the second independently constructed complete execution is exactly equal
    to the first across every serialized observation, resource measurement,
    fingerprint and predicate.

The row threshold is `24 * 23 = 552` clauses.

## Six global clauses

The complete matrix must additionally establish:

1. all 24 root/load cells occur exactly once;
2. all eight side/order/reflection strata occur exactly three times;
3. direct and relayed paths plus every return tick `0..4` are represented;
4. frozen parent/protocol/source hashes and namespace disjointness are exact;
5. fresh cumulative PX0--PX5+LR-C conformance passes in every row; and
6. every row is bounded, memory-stable, naturally quiescent, and exact on replay.

The definitive conjunctive threshold is therefore `24/24` rows and
`558/558` clauses.

## Hard engineering gates

The authority implementation must remain explicit, small, idiomatic and
reviewable. It must compile with `#![forbid(unsafe_code)]` and strict Clippy
with warnings denied. The evaluator must serialize complete work and memory
measurements rather than substitute a partial positive-loop ledger.

No full workspace build or unrelated suite is authorized. One formatting-only
E2B canonicalization may occur before freeze and does not compile or construct
a world. After all source, manifest, audit and hash edits are batched and
committed, one fresh targeted frozen-validation sandbox may run only:

```text
cargo fmt --check
cargo build --release for the authority package
one no-world matrix-definition test
strict package Clippy with warnings denied
static hash/dependency/coverage/leakage audit
release --authority-preflight
```

Preflight may validate hashes, constants, cardinality, namespace disjointness,
artifact absence and the execution firewall. It may not construct a substrate,
call the row runner, perform replay, emit an evidence marker, or create a
result artifact.

## One-shot authority execution

After the implementation/audit commit is tagged, one new E2B sandbox executes
exactly once:

```text
cargo run --release \
  --manifest-path arms/px6-lrc-consequence-authority/Cargo.toml \
  -- --authority-v1
```

The command rechecks frozen inputs and the no-world preflight, emits exactly
one evidence-spent marker before its first world, evaluates all 24 rows and
their embedded duplicate states once, and atomically creates:

```text
results/px6_lrc_consequence_authority_v1.csv
results/px6_lrc_consequence_authority_v1.md
```

Overwrite, resume, partial selection, alternate roots/loads, rescue,
regeneration and rerun are forbidden. Any failure, interruption, malformed or
missing artifact is an immutable negative or incomplete authority result.

## Serial PX-C gate

Only after functional evidence is frozen may manifest v4 replace exactly the
two PX6 predecessor rows in PX5 manifest v3 with the shared authoritative LR-C
physical-consequence source. All PX0--PX5, PX7 and PX8 rows remain exact.

The immutable before side is PX5 authority taxonomy with manifest hash
`32fda2d86f6c836438fdd01fc5433c8731f4ec92e3e052e5d1a64fe751d15388`.
Required E2B after conditions are:

```text
primary seams                   < 283
semantic guard                 <= 119
evaluator guard                <= 477
PX0--PX3+LR-C foundation seams      0
PX4 seams                           0
PX5 seams                           0
PX6 seams                           0
new seam kinds                      0
new guarded surfaces                0
unclassified active files           0
```

Taxonomy must use the exact v4 manifest hash. The comparator must consume the
raw immutable PX5 authority inventory, guard inventory, and summary, require a
strict primary decrease, and serialize exact kind/layer/novelty deltas.

## Authority decision and successor stop

PX6 authority may be claimed only after the one-shot `558/558` matrix,
complete resource accounting, memory stability, natural quiescence, exact
replay, cumulative PX0--PX5+LR-C conformance, complete coverage/leakage audit,
and serial PX-C comparator all pass. Separate result-audit and authority
handoff commits/tags are required.

The final handoff may establish PX6 only. It must not preregister, prepare,
execute, reinterpret, or advance PX7.
