# DS1-after-D2 leak and negative-control audit

All five frozen D2 controls passed for every MICRO/GATE seed:

1. equal compatibility abstains;
2. compatible with neither abstains;
3. removed matching route abstains;
4. swapping alternative order reverses the relation;
5. all temporary D2 state cleans up.

The acquisition path receives no evaluator-correct role, expected alternative,
reward, acceptance, rejection, or semantic polarity. Evaluator truth is read
only after acquisition to count correctly mature patterns and held-out
successes.

The composition deliberately does not reinterpret D2's structural attribution
as evidence that the selected route was correct. The exact 50% held-out result
is therefore preserved as the scientific collapse rather than repaired through
an evaluator-derived sign.

Fresh held-out episodes use new occurrence identities. All 16 held-out
episodes per GATE seed produced a frozen choice, so the failure cannot be
explained by abstention or missing execution.
