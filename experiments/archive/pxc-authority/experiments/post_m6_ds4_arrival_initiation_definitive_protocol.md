# Post-M6 DS4 arrival-initiation definitive protocol

Protocol identifier: `post-m6-ds4-arrival-initiation-definitive-v2`.

Status: **PRE-EVIDENCE CLIPPY-ALLOWANCE CORRECTION; NO DEFINITIVE EVIDENCE SPENT**.

Version 1 was frozen at commit
`6ba5deee16c37362f99833d1c5afb53b4dee2a2f` before authority implementation.
Its first no-cell validation compiled successfully and then stopped at strict
Clippy because the command named `manual_range_contains` instead of the
documented byte-frozen M5/M6 `derivable_impls` Rust 1.97 style allowance. No
test, audit mode, learner, positive control, definitive seed/cell, evidence
marker, or artifact executed. Version 2 corrects only that lint allowance
name. The scientific question, matrix, order, seeds, derived namespaces,
controls, thresholds, conjunction, commands other than this lint name,
evidence boundary, failure semantics, sandbox/state, and atomic paths are
unchanged.

This protocol authorizes exactly one future definitive authority matrix and no
other claim-eligible execution. The authority implementation must first be
committed, tagged, and validated without constructing a learner, enumerating a
definitive cell, or executing any positive development control. Once the
definitive runner emits its single evidence-spend marker immediately before
cell 0, the complete matrix is spent. It may never be rerun, rescued, tuned,
amended, replaced, partially reused, or reinterpreted.

## Definitive question and narrow claim

Starting from authoritative M6, is supplied semantic request/start meaning
unnecessary because physical external arrival, learned event organization, and
non-semantic delayed physical consequence can make an anonymous occurrence
acquire the initiation role needed for correct, explicit, naturally quiescent
execution?

The only tested causal path is:

```text
anonymous physical external arrival
  -> byte-frozen learned M3 event completion
  -> byte-frozen anonymous P4 occurrence selection
  -> downstream recurrent physical activity
  -> byte-frozen M6 delayed consequence normalization and contrast
  -> transient learned differential
  -> already-active anonymous occurrence trace
  -> one acquired initiation role
  -> held-out explicit execution and natural quiescence
```

No `REQUEST`, `START`, `respond-now`, `task-begins-here`, evaluator-selected
initiation, expected answer or trace, target channel, semantic correctness,
reward, loss, stable occurrence identity, or Lane-B SSA state may enter the
organism or authority call. Evaluator answer comparison is lawful only after a
complete execution and only for external scoring.

## Independently audited frozen ancestry

The authority audit resolved and compared the current bytes to their original
tags before this protocol was written:

- authoritative M6 commit/tags:
  `aa4e22efd8a65b7694956a53cfaa970582695215` /
  `core-autonomy-checkpoint-established`,
  `m6-cumulative-nonsemantic-credit-authoritative`, and
  `ds8-cumulative-semantic-credit-definitive-authority-handoff`;
- development readiness:
  `7c39cb4306c61b8e67119c91c3f9e3802dab9a39` /
  `post-m6-ds4-arrival-initiation-development-readiness`;
- exact mechanism snapshot:
  `e11a6a7927d86bbe0fff5f55942a8b418de39de1` /
  `post-m6-ds4-arrival-initiation-gate-implementation-v4`;
- mechanism SHA-256:
  `67e170f12d7b7649a0a291ddfc16cd80e4b5c15564b65cd09c884f3e52b9ac5b`;
- development runner SHA-256:
  `0a7bc106fe5135c04c4e62aed8de77d50ba7b0756ea51548d391ce33c00796e2`;
- development protocol SHA-256:
  `01c47af6fe1be9dc1e48a4b81a94e194df36d1631e1d650c8f2e94284bd42d6b`;
- development GATE SHA-256:
  `d16b22930e18f5c58b6490ce24f41fc260c5f0afb84b589fb6a5c27ba73b97e0`;
- readiness handoff SHA-256:
  `db788154859f1b1038533738f4700caf00eb41e80ff6fb060a2a7d08ce0155c0`.

The mechanism snapshot descends from M6 without changing M6. Readiness has the
mechanism snapshot as its parent and adds only the frozen GATE result and
handoff.

The following development evidence is immutable and was byte-compared with
its original tag:

| stage | commit/tag | result SHA-256 | paired audit/handoff SHA-256 |
|---|---|---|---|
| semantic-credit collapse | `bf84c0350fae5139e44741a71dfd231588a909f9` / `post-m6-ds4-arrival-initiation-probe-v1-negative` | `46dac216ed3977ec8d12821af1b5b69d93f6932e20364c4d4c1b5809b3fba9c1` | `660ba16162153ed909e3c28476282647bd0b3d8b8414663575efe8795db4d504` |
| positive retry | `4d246f6e6f60588b9c32a9278d5072ec30c4ecb3` / `post-m6-ds4-arrival-initiation-probe-positive` | `4720e0558fb0241ec893fa451c097c384d7e246a1efe3488a375e3077415f7fa` | `bef1c81f4dc48e1fcbe463d1518dad6fec14a5c32fa43f1b4ac82146a56abb07` |
| positive MICRO | `aad0891f057e6aa8ef3d62b587abc3ede138023c` / `post-m6-ds4-arrival-initiation-micro-positive` | `c77de17b00a1e60a5e512d7544c09735d3b84bd3f46a9b3793e215072c6504a8` | `d3c3535bfdb28476ab5d5401b245b34984a1311bc03cd68248406bb99ae0b17f` |
| positive GATE/readiness | `7c39cb4306c61b8e67119c91c3f9e3802dab9a39` / `post-m6-ds4-arrival-initiation-gate-positive` | `d16b22930e18f5c58b6490ce24f41fc260c5f0afb84b589fb6a5c27ba73b97e0` | `db788154859f1b1038533738f4700caf00eb41e80ff6fb060a2a7d08ce0155c0` |

The original cumulative DS4 definitive negative remains immutable at tag
`ds4-cumulative-request-start-definitive-negative`: CSV
`c6b626650fc199a8ebe2feae8115a8b27071088f963f919393f55e24fbe44a3a`
and Markdown
`97f1fc665e03be1ccd398dcdf34fbb262c525aabc23a812c8740c350dd890659`.
The authoritative M6 CSV/Markdown remain exact at
`0cb9ba779fca1899cf030d30358fe9354cfb7b2cccf87f32df3f6ea9ddfe91e4`
and
`6a5d938c3e021344b00f3a559593fee860b5f6cceb777c409ad8d59a2dd71872`.

The current M0--M6 definitive CSV/Markdown pairs were independently
byte-compared to their authority tags and must remain exact:

| stage | authority commit | CSV SHA-256 | Markdown SHA-256 |
|---|---|---|---|
| M0 | `0c81f80397c8b28e7586cd598d00114de1a8a788` | `7883f71918d48c4c622d7cd2d9dd7561f5954f7287f8bc6abb535f5a9f994a55` | `a788106462498dd7581fcbd324d6fbc71a1ca0a46c3390a4d289ae180731edad` |
| M1 | `16a1002b59bf0dbc23a6b6bf03572efca53b33ce` | `fede145a50bc059ffcd19a26dc65763843a83b1644c89bd44a3b27e8cd7cea27` | `5971c78eb2688e9fb2d31e59b8d835000ece4923b84aef57d6a1ef48f9295bea` |
| M2 | `162a5b2082a8c1ac9ede45bc5178fecf3509b476` | `68d6dd31ca15e206b382f3ef6592804882eecfe09efd6696b8ed403dc6304159` | `a67f6d0acc2dc01a8456922153b64b97ad2493f2e7bf50b4d5268533a134f2e1` |
| M3 | `ffcdfe8b36fc62348b7ebcb09aaf4797f6146ba8` | `ac8c0a6c9b7badfa263ceb054ffe59c11162b1ca256c56cc6df5f0d378179401` | `ab77bd12b705b8620b6315260f8bb5b4df6efc961f1d20a0dd521af403e1ac5f` |
| M4 | `8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2` | `5c4a2e2b021a26a4cc2161202dd9a62205d426ba361f90a69d00ceb3df470a83` | `c418f6b5fb5f8f3f83e385c75cd23fb8c88def3650ff80a13a491d0979944768` |
| M5 | `9c5ba68a6a4ae37b51575ebaae414ab51a248575` | `86d9f6e3a8ab4ad5c242e0d7c619d8eda99e0da47faff623f26c8c6835b9a99a` | `a336633c73565261d357a67ca02df3047ffcaf88488153bb2f43b621818ba5f0` |
| M6 | `aa4e22efd8a65b7694956a53cfaa970582695215` | `0cb9ba779fca1899cf030d30358fe9354cfb7b2cccf87f32df3f6ea9ddfe91e4` | `6a5d938c3e021344b00f3a559593fee860b5f6cceb777c409ad8d59a2dd71872` |

The frozen M3 port, P4 occurrence-role source, M5 allocator, and M6 linker
remain exact at `c4fc7aca11a5925effeb5a84b90184a70da0f66da7c063d0f87ba46ca36addf3`,
`2dbde723b394bcb3d788c796aa1745cd1cea392a64ab61497bb97474866144b8`,
`e755a70deada891e5c4db3b55809ca84ea8ad31a8bd3affe564bf08a95f8dff7`,
and `1f68f7e943f37c42d29f16fe26f0d851a59361ed4c1f4273a82d0537f935d343`.

No Lane-B SSA source exists in the current tree, build composition, frozen
request linker, or frozen M6 linker. The authority wrapper may add no Lane-B
source, dependency, input, state, branch, identifier, or runtime call.

## Byte-frozen mechanism and authority-only layer

The implementation must composition-copy the exact mechanism snapshot without
editing or reformatting it. The authority layer may only:

- supply one literal fresh seed and the fixed held-out count to a fresh
  blank-start frozen snapshot;
- run the entire same physical cell a second time for the preregistered exact
  duplicate control;
- expose existing measurements, independently reconjoin predicates, and add a
  focused missing/stale/invalid-path execution control using the frozen APIs;
- audit source ordering, hashes, literal arrays, namespace separation,
  information flow, Lane-B isolation, output absence, and frozen artifacts;
- serialize returned measurements to fixed create-new staging and final paths.

It may not change or duplicate the learner, event gate, request-role learner,
M6 consequence learner, linker, recurrence rule, thresholds, constants,
information flow, development artifacts, or any frozen pass predicate. It may
not add organism state or learning input.

The authority call boundary must contain only the literal cell seed, fixed
held-out count, and frozen authority-cell call. A source-order audit must prove
that physical execution precedes the M6 differential, that the differential
can update only the already-selected occurrence trace, and that evaluator
answer comparison occurs after the linker and cannot reach learner state.

## Fresh explicit blank-start matrix

Run exactly sixteen sequential, deterministic, single-threaded cells in this
literal order:

```text
700_123_457   710_123_457   720_123_457   730_123_457
740_123_457   750_123_457   760_123_457   770_123_457
780_123_457   790_123_457   800_123_457   810_123_457
820_123_457   830_123_457   840_123_457   850_123_457
```

Each cell owns the half-open derived namespace
`[base, base + 6_300_000)`. Adjacent bases are separated by `10_000_000`.
The frozen cell's greatest direct derived input is below `base + 6_200_128`.
All regions are mutually disjoint. The highest development base is
`146_500_000`; its corresponding derived region ends below `152_700_000`.
The authority regions therefore begin more than `547_000_000` above every
post-M6 development-derived namespace and are absent from every prior
development/definitive seed manifest.

Every cell starts twice from blank, independent M3, P4, and M6 learner state.
No occurrence, identity, route, handle, layout, allocation, event, consequence
history, proposal, value, eligibility, or learner state crosses executions or
cells. Each complete execution uses:

```text
M3 acquisition streams          2
P4 acquisition depths           1,2,3,4 rotating
competence cap                  4,000 acquisition episodes
held-out executions             64 fresh non-plastic episodes
held-out depths                 5,8,16,32 rotating
held-out event fixtures         standard/relabelled alternating
serialization requirement       all 6 positions inside every cell
duplicate requirement           exact equality of the complete cell
```

Total held-out population is `1,024` scored executions in the first physical
copy plus the preregistered exact duplicate copy. The duplicate is a matrix
control, not a rerun of the matrix.

## Per-cell ordered conjunction

Every cell reports the first collapse in this order:

1. **P0 source and authority:** exact M6, mechanism, development stages,
   original negative, M0--M6 artifacts, source-order/information-flow audit,
   and Lane-B isolation.
2. **P1 physical arrival and event requirement:** nonzero learned arrival and
   event activity; no arrival, subthreshold event, missing close, and invalid
   transition cannot select, initiate, recur, fabricate consequence, or
   update; a later valid event reenters.
3. **P2 recurrence and consequence requirement:** recurrence, raw delayed
   consequence, M6 observations, active-trace updates, and physical work are
   nonzero; non-recurrent and immediate paths fabricate no lawful update.
4. **P3 one anonymous initiation role:** the blank P4 learner reaches
   competence within 4,000 episodes and consolidates exactly one role; every
   selection originates only from its anonymous occurrence.
5. **P4 held-out function and transfer:** all `64/64` held-out executions are
   correct, explicit, and naturally quiescent across all four depths, both M3
   fixtures, fresh occurrence/route/handle identities, changed layouts and
   allocation, and all six serialization positions.
6. **P5 non-plasticity and lifecycle:** held-out M3/P4/M6 state is unchanged,
   acquisition and held-out namespaces are disjoint, temporary state is
   erased by natural quiescence, and physical work is positive.
7. **P6 twelve controls:** all numbered controls below pass.
8. **P7 exact duplicate:** the complete second blank-start physical execution
   equals the first byte-for-byte in returned state and measurements.

Later predicates cannot rescue an earlier collapse. A cell passes only if
P0--P7 all pass. The matrix passes only if all sixteen cells pass.

## Frozen controls in every cell

1. No physical arrival produces no event, selection, execution, recurrence,
   consequence, or update.
2. Arrival without a learned event produces generic activity but no learned
   completion, selection, execution, or update.
3. Missing close and invalid transition cannot initiate or execute; a later
   valid learned event reenters.
4. Selection without valid recurrent execution produces no fabricated
   consequence or update.
5. Immediate physical activity is rejected by the frozen M6 minimum-delay
   rule.
6. Absent active eligibility produces no M6 update.
7. Equal and shuffled consequence histories abstain.
8. Swapping only raw consequence histories reverses the learned differential.
9. Fresh occurrence identities, route/answer handles, M3 relabelling, depth,
   layout, allocation, and all six serialization positions transfer.
10. The selected opaque identity comes only from its anonymous occurrence;
    symmetric indistinguishable histories acquire no role.
11. Expected answer, evaluator truth, semantic credit, request/start flags,
    target channel, stable occurrence identity, and Lane-B state are absent
    from organism observations, persistent state, and the authority call;
    evaluator comparison is post-execution only.
12. M3/P4/M6 held-out state is non-plastic, acquisition and held-out
    namespaces are disjoint, temporary state quiesces, and the complete
    physical execution duplicates exactly.

All `192/192` numbered cell controls must pass.

## Conjunctive interpretation

PASS requires all of the following together:

```text
16/16 cells pass P0..P7
16/16 blank learners acquire exactly one anonymous initiation role
1,024/1,024 first-copy held-out executions correct, explicit, and quiescent
all 6 serialization positions observed inside every cell
192/192 numbered controls pass
16/16 complete physical duplicate executions exact
zero first collapses
all frozen source/artifact/information-flow audits true
```

On PASS only:

```text
M7 becomes the authoritative cumulative ancestor.
The post-M6 DS4 arrival-initiation successor becomes definitive positive.
DS5 finish/output becomes eligible for separately named development.
Program-priority decisions remain outside evidentiary scope.
```

Any scientific predicate, cell, control, or conjunction failure is an
immutable definitive negative. M6 remains authoritative, M7 remains absent,
and DS5 remains blocked. No aggregate coverage, incidental success, later
predicate, development result, or explanation may amend the frozen clause.

## No-cell implementation validation and refusal

The authority binary must expose only `--audit` and `--definitive`. It may
accept no seed, held-out, output, cell selector, development, replay, resume,
append, overwrite, partial, or alternate-mode argument. No default execution
is allowed.

`--audit` must verify hashes, literal arrays, derived namespace separation,
source order, information flow, Lane-B isolation, frozen artifacts, and absence
of final/staging paths. It must evaluate zero positive controls and zero
definitive cells. Focused tests may exercise only no-cell preflight and atomic
create-new publication in bounded temporary paths.

The exact remote no-cell validation sequence is:

```text
cargo fmt --all -- --check
cargo check --bin post_m6_ds4_arrival_initiation_definitive
cargo clippy --release --bin post_m6_ds4_arrival_initiation_definitive -- -D warnings -A clippy::derivable-impls -A clippy::manual-is-multiple-of -A clippy::manual-div-ceil
cargo test --release post_m6_ds4_arrival_initiation_definitive::tests:: --lib
cargo test --release --bin post_m6_ds4_arrival_initiation_definitive
cargo run --release --bin post_m6_ds4_arrival_initiation_definitive -- --audit
test $(cargo run --quiet --release --bin post_m6_ds4_arrival_initiation_definitive >/tmp/post-m6-ds4-no-args.out 2>&1; echo $?) -eq 2
```

No broad historical suite is authorized because frozen shared mechanism bytes
must not change. Any source/hash mismatch stops authority before evidence.

## Fresh E2B authority state and exact definitive command

Every Rust command runs from a clean committed Git archive through:

```text
/Users/satya/work/br/truelearner/.env.e2b
/Users/satya/work/br/truelearner/scripts/e2b_persistent.py
/Users/satya/.cache/truelearner/post-m6-ds4-arrival-initiation-definitive-authority-e2b.json
```

The state path and all output paths were absent at preregistration. The helper
must create a fresh `truelearner-rust-1-97-worker` sandbox distinct from
development sandbox `icmxrqcsf8br7shgus934`, record its ID, and leave it
running after final audit.

After successful no-cell validation, the sole evidence-spending Cargo command
is exactly:

```text
cargo run --release --bin post_m6_ds4_arrival_initiation_definitive -- --definitive
```

It must be invoked exactly once from the clean tagged implementation snapshot.
The runner repeats no-cell preflight, emits exactly one
`POST_M6_DS4_DEFINITIVE_EVIDENCE_SPENT` marker immediately before cell 0, runs
all cells in literal order, and records its Cargo process exit (`0` PASS, `1`
valid scientific FAIL). The surrounding E2B command may capture that one exit
and perform only file hash/existence checks; it may not invoke Rust again.

Infrastructure failure before the spend marker spends no scientific evidence
and may be corrected only by restoring the exact committed implementation and
command. Any panic, interruption, infrastructure failure, serialization
failure, partial artifact, or missing artifact after the marker is an
immutable incomplete negative and forbids retry.

## Atomic write-once artifacts

The pre-implementation digest of all sorted current `results/` file hashes is:

`558168c61dac6f714329e2e0b0872073cea35f01d3b1a8b9cadd4570374a90ea`.

The definitive runner must fully write and sync fixed create-new staging
files, then atomically publish without replacement to:

```text
results/.post_m6_ds4_arrival_initiation_definitive.csv.staging
results/.post_m6_ds4_arrival_initiation_definitive.md.staging
results/post_m6_ds4_arrival_initiation_definitive.csv
results/post_m6_ds4_arrival_initiation_definitive.md
```

All four paths must be absent locally and remotely before execution. Final
artifacts are created even for a complete scientific FAIL, then frozen without
rescue. Existing results must remain byte-identical. The result, independent
result audit, and authority handoff must be committed/tagged separately after
the executed implementation snapshot; no result commit may alter executed
source bytes.

This protocol commit spends no definitive evidence and authorizes no execution
until the exact authority implementation and implementation audit are
separately committed and tagged.
