# PX2 physical causal direction trace-sufficiency GATE v1 negative handoff

Status: **GATE v1 NEGATIVE; PX2 AUTHORITY ABSENT; PX3 BLOCKED**.

The negative is localized rather than a rejection of trace-based causal
direction. Dense topology, distractor activity, longer pre-traversal delay,
parallel paths, mirrors, and even a `3:1` wrong-way consequence frequency did
not defeat traversal-based direction selection.

Two different frontiers remain and must not be conflated:

1. **interleaved-history asymmetry:** equal total participation collapses onto
   the direction that participates first under the tested reserve/pressure
   history;
2. **post-forgetting opportunity timing:** fresh correspondence and fresh
   direction opportunities form, but the direction opportunities physically
   die during the preregistered delay before contemporary use.

The frozen GATE must not be rerun or rescued. A future successor, if authorized,
should split these into independently named diagnostics while leaving the PX1
law byte-identical:

- an order/pressure diagnostic around alternating histories;
- an immediate-versus-delayed proposal-use diagnostic after full forgetting.

PX2 MICRO remains positive development evidence. PX0 and PX1 remain
authoritative and unchanged. No PX2 definitive matrix is eligible from this
negative, and PX3 remains blocked.
