# Unchanged DS1-on-DS-E0 cumulative composition-attempt preregistration

Protocol identifier: `ds1-after-e0-cumulative-composition-attempt-v1`

Status: **PREREGISTERED BEFORE IMPLEMENTATION**. This is a narrow
development-only composition attempt. MICRO and GATE may establish only an
implementation readiness or exact first collapse. No definitive command,
claim-eligible execution, result artifact, cumulative positive, or M1 creation
is permitted by this protocol.

## Exact lineage and sole question

- exact enabling parent:
  `d154fde5632c0ba9d76fc2d1d1a700276045adc8`;
- parent tag: `ds-e0-cumulative-anonymous-event-formation-readiness`;
- authoritative M0:
  `1d74c0ed0b515446161a63a6d43ecbe27514dc85`;
- M0 plus DS-E0 is an enabling ancestor only;
- M0 and all M0/IP0 evidence remain read-only and authoritative;
- M1 does not exist initially and may not be created in this lane;
- frozen isolated DS1 source commit:
  `d8051b9aebadcb093d6012505a5c44c4a416a482`;
- frozen DS1 marked learner SHA-256:
  `adec6a422e69e7f90bff6482776ea9aa91ae89e5e8d59183f6228165f9f7ff0e`.

The sole question is:

> Can the byte-identical frozen DS1 learner consume DS-E0-produced
> `Neighborhood` representations through the current substrate, and what is
> the first remaining missing prerequisite, if any?

This attempt does not search for a replacement mechanism. It does not alter or
rescue M0, DS-E0, E0-B, DS1, or any later stage.

## Immutable source boundary

The following exact enabling-parent files are read-only:

| Boundary | Enabling-parent SHA-256 |
|---|---|
| DS-E0 E0-A, E0-B, and embedded frozen DS1 source | `src/ds_e0_anonymous_event_formation.rs`: `fc5d426cc8a5116dbd2749b914e6c30db88529d3070a844a20fc76ac88782615` |
| M0 parent mechanism | `src/ffs_same0.rs`: `50cf169bb293177a35270adde656f28f98e68c83a4d39d2876399261b7ee697c` |
| M0 compiled-correspondence mechanism | `src/ffs_same0/cs0a.rs`: `430cd2206c8baa7106c4de7f203d4d0c48b544290e6266596ebcdb91d02655c9` |
| DS-E0 serializer/provenance audit | `experiments/ds_e0_cumulative_serializer_provenance_audit.md`: `edfa40c5cedd9359b0677d36a26844a502a2c5b05e7f4c0b137c8922ff4c11a7` |
| DS-E0 dependency manifest | `experiments/ds_e0_cumulative_dependency_manifest.csv`: `c3b9a5daf1e27a98283432e0f992c1cb4d801505c1790af99a22ba408ca06181` |

Before MICRO/GATE, the implementation must independently hash the inclusive
marked text from `DS1_LEARNER_BEGIN` through `DS1_LEARNER_END` and require the
frozen value above. The E0-A/E0-B source file, serializer implementation, M0
sources, frozen learner fields, inference, choices, evidence updates,
consolidation, invalidation, reopening, and lifecycle must remain byte-identical
to the enabling parent. A mismatch collapses at stage 0 and stops execution.

E0-B remains a format-only, one-to-one copy from an already-formed
`EventRelations`; it may not access raw activity, search, infer membership,
form a window, choose alternatives, or read consequence/evaluator truth.

## Narrow composition boundary

The new code may contain only a MICRO/GATE composition harness, runner, source
and dependency audits, accounting, and reports. It may schedule legal raw
M0/DS-E0 episodes, run the existing E0-A/E0-B path, invoke the byte-identical
DS1 learner on the resulting representation, execute an actual already-existing
substrate action if one is naturally exposed, observe ordinary environment
dynamics, and score evaluator-side outcomes only afterward.

It may not add an adapter, route target, boundary/role label, source/target,
consume/produce meaning, filler equality or `SAME`, stable identity token,
event/window truth, evaluator membership, expected action, answer, economics,
or fallback. It may not recreate the isolated DS1 synthetic two-route fixture,
manufacture two anonymous action alternatives, interpret DS1 choice index 0/1
as correctness, derive consequence from expected evaluator ordering, or use
evaluator truth to create route-contingent consequence. It may not synthesize
action semantics, physical effects, acquisition history, or terminal
consequence wiring to make DS1 mature.

If the current substrate exposes no two actual anonymous actions at compatible
scope, stage 4 is unavailable, not a zero-work success. If an action cannot be
physically executed, stage 5 is unavailable. If ordinary route-contingent
consequence does not naturally return, stage 6 is unavailable. The harness
must freeze the first such stage and perform no rescue.

## Ordered first-collapse matrix

The following order is exact and mandatory:

0. exact lineage and hash/fingerprint proof;
1. DS-E0 event formation;
2. format-only E0-B serialization;
3. frozen DS1 `Neighborhood` consumption/invocation;
4. actual anonymous boundary-ordering/action alternatives available from the
   current substrate;
5. selected DS1 action physically executes through the current substrate;
6. route-contingent ordinary consequence naturally returns at compatible
   scope/lifetime;
7. unchanged DS1 acquisition/consolidation;
8. fresh/relabel/layout transfer;
9. contradiction invalidation and generic reopening;
10. cumulative functional recovery.

The first failed or unavailable stage is frozen exactly. Every causally later
stage is `BLOCKED`; unavailable is never reinterpreted as success. Independently
auditable source hashes may still be reported. No collapse may be rescued.

## Development matrix and execution lock

- MICRO: seed `100`, 16 DS-E0 acquisition episodes and 8 read-only
  presentations per legally reachable arm.
- GATE: seeds `100..104`, 32 DS-E0 acquisition episodes and 16 read-only
  presentations per legally reachable arm.

Cells are deterministic, single-threaded, and tiny. Only format, strict release
Clippy, focused tests, release MICRO, and release GATE are permitted locally and
in E2B. Broad legacy regression is omitted because shared/frozen behavior must
not change.

`--definitive` must reject in the runner before any harness call and must create
no artifact. No file may be written under `results/`. No definitive/result
artifact exists or may be created during this attempt.

Dedicated E2B state is exclusively:

```text
/Users/satya/.cache/truelearner/ds1-after-e0-cumulative-e2b.json
```

The persistent sandbox is reused or created using the existing E2B runner and
credential file, never killed, and left running.

## Freeze vocabulary and mandatory handoff

The handoff outcome is exactly one of:

- `CUMULATIVE DS1 DEVELOPMENT COLLAPSE AT <exact stage>`; or
- `CUMULATIVE DS1 IMPLEMENTATION READY`.

If all development gates pass, the longer label is
`CUMULATIVE DS1 IMPLEMENTATION READY / M1 PENDING CLAIM-ELIGIBLE RUN`.
Neither vocabulary is a scientific result or cumulative positive, and neither
creates M1.

The freeze must include the protocol commit/tag; exact parent and source
fingerprints; independent frozen learner hash proof; E0 serializer/provenance
continuity proof; an actual evidence/action/consequence dependency manifest;
a leak audit proving absence of the isolated fixture and evaluator route
adapter; the complete ordered first-collapse report; a physical work/storage
ledger with unavailable fields blank; local and E2B validation; exact
implementation/readiness or collapse commit/tag; and a clean worktree. It must
state explicitly: **M0 authoritative; enabling parent `d154fde`; M1 does not
exist.**
