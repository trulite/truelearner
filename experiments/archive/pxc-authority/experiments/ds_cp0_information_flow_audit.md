# DS-CP0 information-flow and leak audit

The only new bridge input is the structural source shape carried by the
already-executed frozen D3 direction arrow. The bridge compares that shape with
normalized effects physically produced by the two already-existing A1 routes.

Frozen source audit:

```text
existing D3 form-direction calls       2
existing D3 physical-execution calls   1
existing A1 observe sites              2
structural match sites                 1
direct support mutations               0
new learner types                      0
semantic/causal fields                 0
```

No affordance slot, chosen/unchosen flag, evaluator direction, source/target
role, filler identity, correctness, reward, or polarity reaches probation.
Opaque handle permutation does not change which structural template matures.
Absent or ambiguous physical direction produces no differential update.
