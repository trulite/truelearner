# DS7 cumulative learned plasticity-allocation definitive protocol

Protocol identifier: `ds7-cumulative-plasticity-allocation-definitive-v1`

Status: **PREREGISTERED BEFORE AUTHORITY IMPLEMENTATION OR EXECUTION**.

This protocol authorizes exactly one future definitive authority command. The
authority wrapper, source/refusal audit, and write-once result schema must be
committed, tagged, and compiled in a fresh dedicated E2B sandbox before that
command runs. Compilation and preflight must evaluate zero definitive cells.
Once the command starts its first cell, the evidence is spent and this protocol
cannot be tuned, rescued, amended, rerun, or reinterpreted.

## Definitive question and claim boundary

> Starting blank from authoritative M4 in every cell, does the byte-frozen DS7
> mechanism learn from ordinary local encounter history where future
> structural variation should be admitted, keep distractor work sparse as load
> scales, repair useful missing structure through learned rather than shuffled
> allocation, and preserve M4 exactly without receiving a supplied plasticity
> site, encounter class, evaluator proposal, or `LEARN_HERE` signal?

The outcome is conjunctive. PASS requires all of the following:

1. productive encounters receive more future plasticity;
2. distractor admission remains sparse as distractor count scales;
3. structural work is reduced relative to ungated local variation;
4. useful missing structure is reacquired;
5. learned allocation, not shuffled allocation, drives that repair;
6. removed or stale structure cannot execute;
7. allocation transfers across fresh opaque identities and absolute layouts;
8. authoritative M4 lifetime behavior remains exact;
9. no supplied encounter classes, evaluator proposal sites, or `LEARN_HERE`
   signal enters the learner.

The still-supplied delayed semantic outcome is the named DS8 supply. It may
reach only the active physical eligibility trace after a local proposal has
formed and executed. This matrix does not establish DS8, request/start,
finish/output, language, planning, or program priority.

## Frozen authority and lineage

Authoritative readiness commit/tag:
`e7d153f334ddf2997c338d55a8aa63ffcc55a7e8` /
`ds7-cumulative-plasticity-allocation-definitive-readiness`.

Authoritative M4 commit/tag:
`8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2` /
`m4-cumulative-learned-lifetime-authoritative`.

The pre-protocol authority audit verified these exact SHA-256 values:

| frozen artifact | SHA-256 |
|---|---|
| DS4--DS8 target freeze | `f10f9d7b16106b6014767ff6188a6d556145ba3e5b4335e28de245c7622a7595` |
| reordered sprint amendment | `609dc63ab8051316703899717fc30861d7a700d0ec60f205fa6d687ad478616d` |
| M4 authoritative handoff | `35bb33bddb014a08a0f1602520290bc1704032aefa3762052331434c88ef66ba` |
| M4 definitive CSV | `5c4a2e2b021a26a4cc2161202dd9a62205d426ba361f90a69d00ceb3df470a83` |
| M4 definitive Markdown | `c418f6b5fb5f8f3f83e385c75cd23fb8c88def3650ff80a13a491d0979944768` |
| M4 lifetime source | `3d5659fb26ae804dee6122408f9d703ea1f226349772883075a42686ac3fd110` |
| DS7 activation handoff | `f7290c939c54b78986596e937d4932335f995766f292c613eb522f552bb3e892` |
| DS7 dependency audit | `40305dd998b5fe80db9d4fcceee154288ea523f90bc57f26079025a7492b2509` |
| dependency manifest | `138827ef5e9d761cd7cb58a672ed4a4776618d5eb56f5f4e88852ce18cb67504` |
| local-plasticity parts source | `704f757888d9b3bc89a5a3f5387f3422efb9dd4c746e3784506411b1da763b15` |
| PROBE protocol | `78109beadb9b96164d7a259f88cb73710a10fd6cdf773b053922743c5d8c7044` |
| final M4-linked allocator source | `e755a70deada891e5c4db3b55809ca84ea8ad31a8bd3affe564bf08a95f8dff7` |
| PROBE runner | `74b2e93e439d5d07cb80a079b762dcd2cca7646bae94a70c930c0663b14b2305` |
| PROBE result | `93c11d30b9f3ebfbf6de68a315b3410275acc4031186eaa7e747a81f78a8ded4` |
| PROBE result audit | `b6d9f11fcb26d9702d95db989991558f3f86ca99dfe360b597b09a2b84d474f2` |
| MICRO v3 protocol | `1648b4a0e19a13c918a3c92ad221c93b7ae145a34c6e960e57fd9d0f91506eb9` |
| MICRO source | `3fd46245614c17476b8fa44887e4a710d0c358ecc4f7ebe7da5fbdb5bb9459ec` |
| MICRO runner | `e5042196e2f8c6dc238e17fc43c765ba9b612bf9ba0c0a5080be1005c3126114` |
| MICRO result | `fa3d0510e0d821d32b2e3dd8c5bc9f357fca653f6e96f7d9d6b8fa19e61a377b` |
| MICRO result audit | `bc4e2cd797fb31133b315d8844301ca9734835176713cced0b049508475e84ff` |
| development handoff | `e2cd6556b31c15457e6e0accf6b1369d0140378138fc063c417372da09fb3a1c` |
| historical GATE v2 protocol at `12ada39` | `7f03c573fa6d5ad32f0401a1c8c260a844e9f943998a97fcc9a213a7636b61e3` |
| historical GATE v2 source at `5d901ab` | `519b7049da1f3d412132860e8e21f48731186b2675ea5a35a4bdc2da2a09098e` |
| historical GATE v2 runner at `5d901ab` | `c2adb04e707acc6f375b66d29a1a28a06b060d21b1a5ac51820bdb29d7800a32` |
| immutable GATE v2 negative result | `cc0278c7476f50c505d7b8813c326203467b6b8b4e17c07f03188891750fccc9` |
| immutable GATE v2 negative audit | `9dbc561d7ec25ad9308df8285454e3b2a0f3c4dfcbb8502b1c1f98cd2cb2e58b` |
| v2 collapse handoff | `10d35f4e0c29ead317ab3bd7254a83752877de582cb6f321b2676f187709e477` |
| operative GATE v3 protocol | `324d328ed1ec1f20edfa3e5372a5fcefcca37d973e7b033f50cd2a0d26cfc9f5` |
| byte-frozen GATE v3 source | `abaedd16717543270c5ed0ef2c8a16e3a4c0fed0215764443948c36d4adfa297` |
| GATE v3 runner | `606f1a3900f0f251da090ac9cfada39e35e5bfd0db301f0f945d4a3408cad97c` |
| GATE v3 result | `195827830258b36586d2311dc14b636b0d9b19404ce73cf2e5f2905e8f464baa` |
| GATE v3 result audit | `50c82f70fb081c43991fff17ee7685aea451220454344a209fad10c3fae78f9b` |
| definitive-readiness handoff | `41f1104f6b8fdf8d54a0b3420d3ede7899e1e606ad08ba7d7f48e62895e945fc` |
| readiness build composition | `1b5c4136acfa4ca0ad2b476a3d454baba54a65f4725cde0c4b9b8a9d1328b9a7` |

The authority wrapper must recheck all current-tree hashes above, recheck the
historical v2 blobs by embedded expected digests, and refuse before any cell
if any current frozen input differs. The v2 result, audit, and collapse remain
immutable and are never regenerated.

## Byte-frozen mechanism boundary

The wrapper must compositionally include the exact readiness source bytes for:

```text
src/ds7_cumulative_plasticity_allocation_gate.rs
src/ds7_cumulative_plasticity_targeting_probe.rs
src/ds6_cumulative_lifetime_probe.rs
```

It may expose a fresh seed and load to the existing v3 cell, aggregate the
returned measurements, perform source/hash/namespace/output preflight, and
serialize the frozen measurements. It may not rewrite or change the encounter
snapshot, prototype learner, value update, admission threshold, exploration
period, event batching, eligibility, proposal path, route execution, 424-event
withholding phase, scalar use/pressure lifecycle, controls, or M4 anchor.

The definitive wrapper adds no organism state. In particular it may not add a
supplied sensory/internal/irrelevant or productive/distractor encounter class,
proposal/candidate/target-site input, `LEARN_HERE` signal, route or future-use
bit, lifetime class, evaluator deletion, or pre-proposal semantic outcome.

## Fresh blank-start matrix

The matrix contains exactly sixteen explicit seed bases and three explicit
distractor loads. Rust ranges or generated seed arrays are forbidden:

```text
30_000_000  30_500_000  31_000_000  31_500_000
32_000_000  32_500_000  33_000_000  33_500_000
34_000_000  34_500_000  35_000_000  35_500_000
36_000_000  36_500_000  37_000_000  37_500_000

loads: 8, 32, 128
```

This is exactly 48 cells. Each base owns the half-open namespace
`[base, base + 400_000)`. The existing cell derives identities only through
`base + 300_005`, so all derived identities remain inside their owner. Adjacent
bases are separated by 500,000. The lowest authority base is more than five
million above the last development base `24_500_000` and is disjoint from the
complete development-derived region. No one of the 18 GATE cells is reused.

Every `(base, load)` cell independently invokes the frozen v3 cell from a new
blank `PlasticityPath`: zero proposals, encounter prototypes, value records,
and eligibility traces. No learned state is shared across cells. The explicit
base alternation also exercises the frozen identity/layout permutations.

## Frozen per-cell task and measurements

Each cell executes the byte-identical v3 physical phases: eight broad blank
training sweeps; retained-route training; 32 fresh held-out traversals; one
allocation/economy sweep; learned-value shuffle; matched correct and shuffled
missing-edge aging and repair; locality/eligibility/identity/layout controls;
and the exact M4 anchor.

Every cell passes only if all frozen v3 predicates pass and the authority
wrapper independently reconjoins these measurements:

- blank acquisition formed all four productive route edges and distractor
  variation before delayed outcome;
- held-out execution is exactly `32/32`;
- productive admission is exactly `4/4`;
- distractor admission is at most `ceil(load / 8)`, separately for loads
  `8`, `32`, and `128`;
- admitted structural work is at least 50 percent below the always-open local
  baseline;
- shuffled learned value reverses productive/distractor admission;
- correct and shuffled withheld edges enter at resistance `105`, finish at
  `0`, and are physically removed;
- the stale edge cannot execute;
- learned allocation reacquires the missing edge and restores exactly `32/32`
  held-out execution;
- shuffled allocation fails to repair on the matched first non-exploratory
  encounter;
- retained-allocation economy, locality, eligibility, fresh-identity, and
  absolute-layout controls pass;
- the frozen source/information audit passes;
- unchanged M4 passes with exact lifetime vector `[1, 3, 6, 13, 27]`.

The matrix is evaluated once. There is no second evaluation, selective-cell
mode, or definitive test path. Determinism is inherited from the already
frozen duplicate-exact development GATE; definitive evidence is not doubled.

## Pre-execution refusal and source validation

Before authority execution, a no-cell `--audit` mode must pass in the dedicated
E2B sandbox. It must verify frozen hashes, exact explicit seed/load arrays,
namespace ownership/disjointness, all nine source/claim gates that do not
require a cell, and absence of both final paths. Focused tests may exercise
only source audit, namespace validation, and create-new refusal against
non-definitive temporary paths. They must not call the definitive cell.

The sole `--definitive` mode must repeat that preflight before the first cell.
It must refuse if either final path exists. There is no default run, alternate
seed/load/result argument, development mode, resume, append, or overwrite.

## Write-once E2B authority execution

Use `/Users/satya/work/br/truelearner/.env.e2b` and the new dedicated state
file:

```text
/Users/satya/.cache/truelearner/ds7-cumulative-definitive-authority-e2b.json
```

That state path must not exist before the first connection. The sandbox must
be fresh, distinct from DS6 and DS7 development sandboxes, and left running.
All Rust formatting, compilation, focused tests, audit execution, and the
single definitive command occur only there from clean committed Git archives.

The command must create-new and fully write before process exit:

```text
results/ds7_cumulative_plasticity_allocation_definitive.csv
results/ds7_cumulative_plasticity_allocation_definitive.md
```

Both paths must be absent locally and remotely before the run. A valid FAIL,
panic, interruption after the first cell, serialization failure, or missing
artifact is an immutable negative/incomplete authority outcome and forbids a
rerun.

## Conjunctive authority clause

PASS requires all 48 blank-start cells and every frozen measurement, hash,
namespace, write-once, source/information-flow, and M4 gate to pass. There is
no aggregate tolerance.

On PASS only:

```text
M5 becomes the authoritative cumulative ancestor.
DS8 cumulative becomes eligible.

Program-priority decisions are outside the evidentiary scope.
```

On FAIL:

```text
freeze the exact negative;
M4 remains authoritative;
M5 remains absent.
```
