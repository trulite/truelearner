# Byte-identical DS1 cumulative composition retry after DS-D2

Status: **PREREGISTERED DEVELOPMENT COMPOSITION ATTEMPT; NOT CLAIM ELIGIBLE**

Exact parent: `9b2c9f27f4e6e9c8d0e4f79f3225106de0344faf` /
`ds-d2-differential-evidence-formation-readiness`. M0 remains authoritative.
M1 does not exist before this attempt.

## Sole question

> When the exact frozen DS-D2 temporary direction is mechanically delivered to
> the byte-identical frozen DS1 consequence surface, does DS1 update, develop
> discriminative boundary-role strengths, and recover boundary roles on
> held-out fresh episodes?

No upstream mechanism may change. The frozen C0 interaction stack, DS-D2
formation rule, and DS1 learner must be hash-verified before execution.

## Mechanical direction bridge

For each acquisition episode:

1. frozen DS1 chooses one of two anonymous live alternatives;
2. that route is treated as the physically selected route;
3. frozen DS-D2 receives the two frozen A1 role-relative route traces and the
   returned trace produced by the selected route;
4. DS-D2 may form its ordinary temporary directional ARROW;
5. the bridge supplies `true` to the existing frozen DS1 boolean consequence
   input iff the DS-D2 ARROW identifies the alternative DS1 selected.

The bridge may compare only the opaque local alternative index already present
at the DS1 choice surface with the source CELL of the already-formed DS-D2
ARROW. It may not inspect expected roles, evaluator answers, correctness,
reward, polarity, event signature, or held-out truth.

This mapping deliberately tests the exact information DS-D2 currently
provides: structural attribution of returned activity to a live alternative.
It must not be strengthened into a comparison against the evaluator-correct
alternative.

## Ordered stages

The frozen earlier interaction stack is revalidated before the new stages.
The remaining authoritative stages are:

```text
8c  one DS-D2 directional relation reaches frozen DS1
9   frozen DS1 update fires on every acquisition episode
10  all acquired boundary-role patterns develop unequal alternative strengths
11  every held-out fresh episode reconstructs the evaluator boundary role
```

Stop at the first failed stage:

- stage 9 failure: D2 is physically real but not interface-sufficient;
- stage 10 failure: updates occur without discriminative strengths;
- stage 11 failure: attribution drives plasticity but does not generalize to
  the boundary-role relation;
- stage 11 pass: the development implementation is M1-eligible, but M1 is
  created only by the frozen handoff explicitly authorized by this protocol.

## Development matrix and controls

- MICRO: seed 100, 8 acquisition episodes, 8 held-out episodes;
- GATE: seeds 100..104, 32 acquisition episodes and 16 held-out episodes each;
- four boundary-role signatures must be represented at GATE;
- every held-out occurrence identity is fresh;
- alternative order and allocation/layout perturbations must preserve the
  represented relation;
- swapped D2 direction must change the boolean delivered to DS1;
- tied, neither-compatible, duplicate, removed-route, or cleaned D2 state must
  deliver no update;
- no semantic evaluator field may enter acquisition;
- no definitive execution or result artifact is permitted.

Report update calls, strength divergence, correctly matured patterns, held-out
attempts/successes/abstentions, learner work/storage, D2 work, and the first
collapse. Preserve results digest
`491a63c17ba35d768b630720063793a4db09686cfe7cb33694fd80ea63bbd4e4`.

Use only `/Users/satya/.cache/truelearner/ds1-after-d2-e2b.json` for E2B and
leave the persistent sandbox running.
