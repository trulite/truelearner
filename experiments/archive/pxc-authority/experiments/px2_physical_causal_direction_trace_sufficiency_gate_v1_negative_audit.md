# PX2 physical causal direction trace-sufficiency GATE v1 negative audit

Status: **IMMUTABLE DEVELOPMENT NEGATIVE; PX2 NON-AUTHORITATIVE**.

## Evidence spend

- frozen implementation commit:
  `f2dca1acefb77ce5ce7694531489a61a7a6a8be2`;
- result commit:
  `fd6a4c53b3412875d9d4b6d46d0bcbfa7bd3b8a0`;
- sole command:
  `cargo run --release --quiet -p px0-physical-correspondence --example px2_physical_causal_direction -- --gate`;
- evidence marker count: `1`;
- command exit status: `0`;
- rerun/rescue count: `0`.

The command writes the conjunctive result and does not encode pass/fail in its
process status. The frozen result itself is **NEGATIVE: `28/44` cells passed**.

## Frozen artifacts

- CSV SHA-256:
  `ef63c70d3ce980d71cbe1e085174b654bd4dcc4505d3e308e2ed59a34abeaec5`;
- report SHA-256:
  `6ae6b252eb292a492ca776daec410ead4fc6faf9ff31abe3e1681b109865ba6b`;
- implementation SHA-256:
  `8cdd72cff084c6a85d65629fd6504f5ca96f14d281a7a5ac518fd9c4754579ec`;
- protocol SHA-256:
  `fd0235ef42b54e4cc5a7e0f2673d57249f8b60b4bb2ca7ddc35396c162e75917`.

All `44/44` duplicate physical developments were exact. Every training,
held-out, post-gap, first-lifecycle, and stale-lifecycle execution was naturally
quiescent, with zero autonomous source refiring.

## What passed

All four strata passed all seven non-interleaved/non-lifecycle families:

- forward and reverse direction selection;
- correlation-only rejection;
- genuine joint participation;
- traversal without return;
- both adversarial-correlation mirrors.

The law survived `0,8,24,48` active distractor pairs, traversal delays `3..=6`,
`1..=4` parallel weak paths, mirrored placement, allocation/insertion reversal,
and fresh identities. Selected parallel paths ended at resistance `40..41`;
non-selected paths ended at `0`. In adversarial worlds, the non-traversed side
produced `72` consequences against `24` on the traversed side, yet only the
traversed direction matured and executed.

## Frozen failures

### Interleaving: 8/8 negative

Both directions' continuation cells fired `12|12`, but the direction presented
first monopolized the reusable basin:

- forward-first: consequences `24|12`, final live paths `N|0`, held-out `1|0`;
- reverse-first: consequences `12|24`, final live paths `0|N`, held-out `0|1`;
- `N` tracked physical path abundance exactly (`1,2,3,4`);
- winning-path minimum resistance was `4..5`; losing paths deallocated.

Thus equal total continuation participation did not preserve both directions.
The one-experience ordering difference changed which weak structure survived
long enough to keep receiving returned activity.

### Lifecycle: 8/8 negative

Initial directional acquisition, held-out execution, complete old-direction and
PX0-correspondence deallocation, stale generations, zero stale execution, fresh
PX0 correspondence (`1|1`), fresh direction ArrowIds, and quiescence all passed.

Opposite contemporary training then failed before returned directional evidence
could form: the opposite continuation cell fired `24` times, but its consequence
fired `0`, local return was `0`, all fresh direction paths ended at resistance
`0`, and held-out/post-gap effects were `0|0`.

The frozen schedule places approximately 30 ticks between creation of each
reserve-`3` fresh direction opportunity and its first possible use. Under the
unchanged ten-tick ordinary-pressure law, that interval is mechanically
sufficient to spend the complete reserve. The result therefore does not isolate
an inability to reacquire direction from a physically insufficient
proposal-to-use schedule; the negative remains immutable either way.

Post-evidence formatting, focused strict Clippy, focused substrate tests, hashes,
and diff checks passed. No definitive evidence ran.
