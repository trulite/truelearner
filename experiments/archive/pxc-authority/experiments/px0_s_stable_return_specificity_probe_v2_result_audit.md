# PX0-S stable return specificity PROBE v2 result audit

Status: **DEVELOPMENT POSITIVE; NO DEFINITIVE EXECUTION; PX0 AUTHORITY ABSENT**.

## Frozen outcomes

PROBE v1 remains an immutable negative at tag
`px0-s-stable-return-specificity-probe-v1-negative`. Its physical endpoints
already separated stable from sparse return, but its executable predicate
incorrectly required every scheduled dense-path opportunity to complete a
return.

The v2 implementation changed only:

- four fresh namespace bases;
- the acceptance condition from exact per-presentation return counts to the
  preregistered behavioral condition that the repeatedly active dense path is
  learned;
- the report title.

The substrate law, physical topology, schedule, context count, timing, active
inputs, support devices, work, and outcome measurements remained unchanged.

## Positive physical result

Both stability-swap arms produced the same result:

```text
35 cross-context stable returns  -> resistance 65 -> held-out effect 1
 6 sparse genuine returns        -> resistance  1 -> held-out effect 0
                                                  -> eventual deallocation
```

All four stable support-device identities and delays produced ordinary return.
The sparse route was not return-free: it received six genuine physical returns
in each arm. It nevertheless failed to remain reusable after its return path
was absent across the final interleaved contexts.

The two recurring dense-path controls showed that a path of the same physical
type is learnable when it is repeatedly available. Both routes executed alone;
simultaneous activation produced zero privileged crossing. Route and layout
mirrors were exact in their functional measurements.

Complete-state replay was exact and every propagation became naturally
quiescent.

## Scientific boundary

This development result supports the narrow claim:

> With the active PX0 law unchanged, recurrence and ordinary pressure retain a
> correspondence whose physical return remains reliable across changing
> contexts, while a genuine but sparse dense-topology return fails to remain
> reusable.

It does not rescue the failed PX0 definitive v1, create PX0 authority, or
authorize PX1. MICRO and GATE must test fresh timing, layout, density,
interleaving, and stability-change conditions before any new authority matrix
is eligible.

## Frozen identities

- protocol commit: `86cde3a`
- v1 implementation: `fbe23c8`
- v1 negative: `301233f`
- v2 evaluator-only implementation: `0a1e111`
- active law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`
- v2 report SHA-256:
  `df351a88e606917a8df6d045c1ea994b313c150c6e40f43e0970a3ea7a72cef8`
- v2 summary SHA-256:
  `0fabaf045f400a66039d99119440ff17f905d198d00a5c7e77a3b479bdc1ba2b`
- v2 context ledger SHA-256:
  `1761b15dc40ba9a6cff8505cdeb24ac7e1bbe9704cc09a0aec6587b66f6a0e2e`
