# PX8 LR-C cumulative physical-closure authority protocol v1

Status: **PREREGISTERED; SERIAL AUTHORITY EVIDENCE UNSPENT; FINAL PX-C AUTHORITY FORBIDDEN**.

## Exact cumulative parent

The sole parent is PX7 authority commit
`9e2aca8933df168780dd6d7e6f00c3d9feae98ee`, tagged
`px7-lrc-arrival-authority-v1`. This protocol is the first commit on branch
`research/px8-lrc-cumulative-authority`; its Git parent must be that exact
authority commit.

The retained serial stack is PX0 physical correspondence, PX1 physical roles,
PX2 causal direction, LR-C qualified modulation, PX3 reusable organization,
PX4 recurrence-earned persistence, PX5 participation allocation, PX6
traversal-bound physical consequence, and PX7 anonymous physical-arrival
initiation. Retained frozen hashes are:

| artifact | SHA-256 |
|---|---|
| LR-C law | `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10` |
| authoritative PX4 API | `a201674f9d558b5bda20aef71e9857b632f8a6565f372aee88994a280e0fea71` |
| authoritative PX7 source | `d248a8af479872d8148115a405ae7332f7d24ca229378d3fde898ffd3d19e63e` |
| PX7 authority CSV | `96baddb76ef1c58dc0173f14a8fabded8c6237e73b729030e4698cf2fe300137` |
| PX7 authority report | `9b23ad3bd34050e1f13b8638b05baa5b9e29adc6cef5208db60dde36b7b058dc` |
| PX7 active manifest v5 | `db4758baa5aeba36a87251f7d2ccb85cd2215f9489a1189eae4fd9d6408001c2` |

No isolated PX4--PX7 development artifact may be imported.

## Preserved isolated PX8 development

The isolated branch `research/px8-lrc-cumulative-development` and its complete
tag history remain immutable. The accepted terminal references are:

```text
px8-lrc-physical-closure-development-protocol-v1
px8-lrc-physical-closure-implementation-v9
px8-lrc-physical-closure-probe-positive-v1
px8-lrc-physical-closure-micro-positive-v1
px8-lrc-physical-closure-gate-positive-v1
px8-lrc-physical-closure-surface-audit-v1
px8-lrc-pxc-taxonomy-positive-v1
px8-lrc-pxc-readiness-delta-positive-v1
px8-lrc-physical-closure-development-ready-v3
```

Frozen isolated development establishes this reduction:

| artifact | SHA-256 |
|---|---|
| active PX8 source | `8623cae7dc1b14e666140a192c49e59ae72df681bc14f0359f0c4465e0d11e8f` |
| active-source commit | `d6cb28160f53d399c7a0af9f8fe121bb1d132aa4` |

Only that byte-exact active source and the scientifically frozen physical
geometry and predicates may be ported. Development identities, runners,
commands, reports, PX-C outputs, handoff prose, workspace residue, and
publication behavior are forbidden.

## Retained physical claim

This candidate adds no substrate law. The complete physical reduction is:

```text
learned recursive organization becomes physically complete
-> its ordinary terminal physical participant crosses the retained boundary
-> ordinary downstream physical return traverses that same completed route
-> retained qualified Modulatory traffic changes the route
-> the whole field becomes naturally quiescent
```

There is no terminal object, Episode, Query, begin/reset, evaluator finish,
manual temporary cleanup, completion flag, route selector, or stored scheduler.
Held-out reuse supplies ordinary physical Drive traffic only. The crossing and
return must be observed from ordinary LR-C field traffic.

Any need for a new stored lifecycle state, semantic completion predicate,
finish signal, cleanup phase, scheduler, work token, route selector, field
kind, transition, modulation rule, identity rule, memory trick, or
evaluator-conditioned organism branch is a genuine scientific fork. Freeze
the diagnostic and stop before authority evidence.

## Fresh packages and identities

The ported active package is `arms/px8-lrc-physical-closure`. Its library must
be byte-identical to isolated tag `px8-lrc-physical-closure-implementation-v9`.
It contains the byte-identical retained LR-C source as a private module and has
no Cargo dependency or public evaluator surface.

The sole authority evaluator is `arms/px8-lrc-closure-authority`. Its direct
dependencies are exactly:

```text
px8-lrc-physical-closure
px7-lrc-arrival
```

PX7 is evaluator-side cumulative conformance only. The authority arm exports
no organism API.

The sixteen fresh roots are exactly `861001..861016`. Every primary physical
namespace is `root << 32`; compact topology controls use non-overlapping
offsets inside that namespace and PX7 conformance uses
`(root + 1_000_000) << 32`. These identities are disjoint from isolated PX8,
PX7 authority, and all earlier cumulative evidence. The root index crosses all
four construction-order/reflection strata exactly four times; all four twist
strata occur exactly four times. No state crosses roots.

## Fourteen conjunctive row clauses

Every root executes twice from independently constructed fresh complete state
and must pass:

1. repeated physical formation produces retained qualified modulation and
   learned route updates;
2. completed held-out reuse produces exactly one outward crossing;
3. that crossing produces exactly one downstream physical return and retained
   qualified Modulatory route change;
4. incomplete, blocked, and pressure-stale learned routes produce no crossing;
5. open and aged compact physical routes produce no crossing;
6. a zero-length physical topology produces exactly one outward crossing;
7. duplicate arrivals and duplicate recursive construction each still produce
   exactly one outward crossing;
8. branch and cycle physical topologies produce no crossing;
9. retained-body pause is inert and resumed reuse equals uninterrupted reuse;
10. every completed, negative, compact, paused, resumed, and cumulative field
    reaches natural quiescence without explicit cleanup;
11. total row work is `<=20000` field-event observations;
12. maximum persistent bytes are `<=8192` and retained persistent size is
    stable across pause and held-out reuse;
13. the retained PX0--PX7+LR-C stack conforms through fresh PX7 physical
    maturation, boundary-only held-out initiation, exactly-once outward
    crossing, downstream return, qualified route change, and quiescence; and
14. the independently reconstructed duplicate execution is byte-for-byte
    identical in every serialized observation.

The row threshold is `16 * 14 = 224` clauses.

## Six global clauses

The complete matrix must additionally establish:

1. all sixteen roots occur exactly once;
2. all construction/reflection and twist strata are exactly balanced;
3. all primary, compact-control, and cumulative-conformance identities are
   fresh and non-overlapping;
4. every row passes cumulative PX0--PX7+LR-C conformance with no new law;
5. every row is work-bounded, memory-bounded, structurally stable, naturally
   quiescent, and exact on replay; and
6. frozen parent/protocol/source hashes, dependency closure, complete active
   coverage, evaluator separation, execution firewall, and one-shot
   publication are exact.

The definitive conjunctive threshold is `16/16` rows and `230/230` clauses.

## Batched implementation and sole targeted validation

All source, Cargo, coverage, leakage, firewall, protocol-hash, and static-audit
edits must be batched before validation. No Rust, project program, or project
audit may execute locally. Avoid workspace-wide or unrelated compilation.

One fresh E2B sandbox may perform the sole targeted frozen validation:

```text
package rustfmt check
strict authority-package Clippy with warnings denied
one fully qualified no-world matrix-definition test
static hash/dependency/coverage/leakage/firewall audit
release --authority-preflight
```

Cargo may reuse that targeted package build inside the single validation
event. The test and preflight may check only constants, hashes, identity
cardinality, namespace bounds, dependency closure, artifact absence, and the
runner firewall. They may not construct a body, call a row runner, emit an
evidence marker, or create a result artifact.

## One-shot definitive execution

After the implementation/audit commit is frozen and tagged, one new E2B
sandbox executes exactly once:

```text
cargo run --release \
  --manifest-path arms/px8-lrc-closure-authority/Cargo.toml \
  -- --authority-v1
```

The command rechecks all frozen inputs and no-world preflight, emits exactly
one evidence-spent marker before the first body, evaluates all sixteen roots
and their embedded duplicate states once, and atomically creates:

```text
results/px8_lrc_closure_authority_v1.csv
results/px8_lrc_closure_authority_v1.md
```

Overwrite, resume, partial selection, alternate roots, rescue, regeneration,
or rerun is forbidden. Failure, interruption, or malformed/missing output is
an immutable negative or incomplete authority result.

## Serial PX-C gate against PX7

Only after definitive functional evidence is frozen may manifest v6 replace
exactly the PX8 predecessor row in manifest v5 with the ported active PX8
source. Every PX0--PX7 row remains byte-identical.

The immutable before side is the PX7 authority taxonomy under manifest hash
`db4758baa5aeba36a87251f7d2ccb85cd2215f9489a1189eae4fd9d6408001c2`.
Required after conditions are:

```text
primary seams                         < 110 (target 0)
semantic guard                       <= 36
evaluator guard                      <= 136
PX0--PX7+LR-C aggregate foundation      0
new seam kinds                          0
new guarded surfaces                    0
unclassified active files               0
```

Taxonomy must run in fresh E2B with the exact v6 manifest hash. The comparator
must consume the immutable raw PX7 inventory, guard inventory, and summary,
require strict primary decrease, and serialize exact kind, layer, novelty, and
coverage deltas.

## Authority decision and successor stop

PX8 authority may be claimed only after the one-shot `230/230` result,
complete resource accounting, stable memory, natural quiescence, exact replay,
cumulative PX0--PX7+LR-C conformance, complete coverage/leakage proof, and
serial PX-C comparator all pass. Separate result-audit and clean authority
handoff commits/tags are required.

The handoff may establish cumulative PX8 physical-closure/emission authority
only. It must not claim final PX-C continuous-organism authority; that remains
a separate, freshly preregistered workflow.
