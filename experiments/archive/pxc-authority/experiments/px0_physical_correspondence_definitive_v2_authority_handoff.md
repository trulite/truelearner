# PX0 definitive v2 authority handoff

## Frozen state

```text
PX0 definitive v1        FAIL, immutable
PX0 definitive v2        FAIL, immutable
PX0-S development        Classification A positive
PX0 authority            absent
PX1                      blocked
PX-C                     blocked
continuous organism      absent
Harness H1               blocked
```

The v2 authority matrix executed exactly once and failed `2/208` conjunctive
claims: `P6` in cells `7` and `15`. No authority tag or PX1 eligibility tag was
created.

The result is narrower than a failure of physical correspondence acquisition
or stable-return specificity. Both failed cells still acquired and executed
their contemporary B route, and all sixteen cells passed physical probation,
sparse incidental suppression, recurrent dense-path learning, stability swap,
forgetting, reproposal, ambiguity/absence, replay, and quiescence.

But the protocol required all of P6, not merely executable B. Because the
write-once artifacts did not serialize P6's return-count and resistance
subclauses separately, the exact subclause must remain unresolved unless a new,
separately named development diagnostic is authorized. The spent cells may not
be recreated for that purpose.

## Immutable identities

- protocol tag: `px0-physical-correspondence-definitive-v2-protocol`;
- implementation tag:
  `px0-physical-correspondence-definitive-v2-authority-implementation`;
- negative tag: `px0-physical-correspondence-definitive-v2-negative`;
- result audit tag:
  `px0-physical-correspondence-definitive-v2-result-audit-negative`;
- final handoff tag:
  `px0-physical-correspondence-definitive-v2-authority-handoff-negative`.

Artifacts:

- `results/px0_physical_correspondence_definitive_v2.csv`;
- `results/px0_physical_correspondence_definitive_v2.md`;
- `experiments/px0_physical_correspondence_definitive_v2_result_audit.md`.

No further action is authorized by this handoff. The scientific next question,
if pursued, is a fresh development-only diagnosis of the spacing-17,
reverse/mirrored, high-density P6 boundary—not a rescue or rerun of v2.
