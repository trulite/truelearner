# PX3 physical event organization definitive result audit v1

Status: **DEFINITIVE NEGATIVE; PX3 AUTHORITY ABSENT; EVIDENCE PERMANENTLY SPENT**.

## Sole execution

The preregistered command executed exactly once from frozen implementation
commit `da89a11b9014dec628743848ec0bb5a86d74a5bc` in fresh dedicated E2B
sandbox `iz37tc0lg4rv42oxuawcx`:

```text
cargo run --manifest-path arms/px3-physical-event-organization-definitive/Cargo.toml \
  --release -- --definitive
```

It emitted exactly one
`PX3_PHYSICAL_EVENT_ORGANIZATION_DEFINITIVE_EVIDENCE_SPENT` marker and
atomically published both artifacts. There was no rerun, rescue,
regeneration, tuning or protocol amendment.

- CSV SHA-256:
  `3fa85616ae97faef0db2200941a41a6ea7d51f9e254267f84fbed8684a0e0d06`;
- Markdown SHA-256:
  `3a8f96587b751f3e19303582861d8320c0a78ae1aa25da24b8959efb809c3317`.

## Conjunctive outcome

- cells: `48/64` passed;
- independently serialized claims: `592/640` passed;
- exact complete-state duplicates: `64/64`;
- natural quiescence: `64/64`;
- full recursive world: `16/16` passed;
- return-without-joint world: `16/16` passed;
- same-path amplitude/repetition world: `16/16` passed;
- recurrence-without-return world: `0/16` passed;
- total ledgered work: `344,240` operations.

The failed world produced the same exact physical measurements in all four
strata and all sixteen fresh namespaces. This is not an allocation, reflection,
identity, replay, quiescence or accounting boundary.

## Physical counterexample

The registered no-return schedule placed AB exposures at starts `0` and `4`.
The first episode produced:

```text
tick 1: P fires and proposes weak P->X candidate
        candidate traverses with coupling 1
        eligibility remains live through tick 5
tick 2: uniform context plus candidate fires X
tick 3: P and X traces arrive at M
        no global return; M does not fire
```

The second episode's joint opportunity reached P at tick 5. PX0 applies local
return to every eligible outgoing arrow at the source before it updates the
source CELL with the arriving spike. Therefore that ordinary upstream O->P
arrival strengthened the still-eligible candidate even though the downstream
attribution route never completed:

```text
candidate resistance/coupling before second episode: 1 / 1
ordinary second-episode arrival at P while eligible:  4 / 2
M firing:                                               0
M->P credit crossing:                                  0
second candidate traversal impulse:                    2
context-free held-out X execution:                     1
```

The serialized totals were consequently:

```text
candidate crossings:       2|0|0
candidate impulse:         3|0|0   (registered 2|0|0)
trained resistance:        4|0|0   (registered 1|0|0)
held-out output/trace:      1|0|0   (registered 0|0|0)
attribution/credit:         0|0|0
post-pressure resistance:  0|0|0
```

P2, P5 and P6 were false in each of the 16 no-return cells. P0, P1, P3, P4,
P7, P8 and P9 passed. Later ordinary pressure eventually removed the
unsupported structure, but it had already become immediately reusable without
downstream return, falsifying the definitive necessity claim.

## What remains positive

The negative does not erase the frozen development results:

- every full recursive definitive cell reproduced native
  `A+B -> X`, `X+C -> Y`, `Y+D -> Z` with one ordinary trace per derived
  participant;
- return without joint participation formed nothing;
- strong and repeated A alone formed nothing;
- all gapped, one-exposure, context-free normalization, insertion and reflected
  geometry controls passed;
- no oscillator, autonomous P refiring or memory leak occurred.

R3 established that a completed trace-attribution route with unit echo is a
safe sufficient credit path. This definitive counterexample shows it is not
yet an exclusive credit path: unrelated later arrival at an eligible source
can still be interpreted by existing PX0 plasticity as return.

## Scientific classification and consequence

This is the D1-R provenance ambiguity resurfacing across adjacent physical
episodes:

```text
participation gating:       solved
safe attributable return:   sufficient
exclusive return necessity: unsolved
```

The narrow authoritative claim required downstream physical return. It is
therefore false for the frozen mechanism and matrix. PX3 is not authoritative;
PX4 remains blocked. Authoritative PX0--PX2 and all frozen PX3 development
records remain unchanged.

Any future PX3 reopening must begin from this exact counterexample and test a
new preregistered physical discriminator between upstream recurrence at P and
completed downstream return. It may not reinterpret the failure as pressure,
timing noise or evaluator error, and this definitive command may never run
again.
