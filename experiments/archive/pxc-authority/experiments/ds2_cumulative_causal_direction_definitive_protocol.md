# DS2 cumulative causal-direction de-supply definitive protocol

Status: **PREREGISTERED; DEFINITIVE OUTCOME UNSPENT**

Exact frozen development parent: `4cb6d65e615c674e61f2cd5340c37fa12b2e1ed8` /
`ds2-cumulative-development-complete`.

## Question

> Does the exact cumulative M1 interaction stack acquire, retain, physically
> execute, invalidate, reopen, and historically reuse a role-relative
> direction from ordinary anonymous propagation and downstream consequence,
> without learner-visible causal direction, source/target metadata, semantic
> polarity, or evaluator-created candidates?

No capability, mechanism, threshold, evidence channel, or frozen development
source may change.

## Frozen matrix

- seeds: `0..15`, unused by development MICRO/GATE;
- four anonymous interaction contexts per seed;
- acquisition: the frozen 16-support E0/A1/CP0/RT0/IR0 lifecycle;
- changed physical dependency in every context;
- compatible current-dependency control in every context;
- absent/ambiguous current-signal control in every context;
- reversed allocation, padded layout, and opaque-handle permutation in every
  context;
- historical dependency return after changed-context reopening;
- duplicate deterministic replay of every complete seed cell;
- cells single-threaded and independent.

Every seed must report:

- 4/4 retained directions execute on fresh occurrences;
- 4/4 incompatible dependencies produce exact structural mismatch;
- 4/4 stale temporary routes invalidate and physically abstain;
- 4/4 ordinary generic A1 inference paths reopen;
- 4/4 changed directions install and execute;
- 4/4 historical directions execute again with zero historical
  reacquisition;
- 4/4 compatible current dependencies preserve the retained asset;
- 4/4 absent/ambiguous signals preserve the retained asset;
- 4/4 layout/allocation/handle permutations preserve the lifecycle;
- zero retained occurrence IDs, opaque handles, concrete destinations,
  direction slots, causal labels, semantic polarity, or evaluator-direction
  edges;
- byte-identical duplicate replay.

The frozen nine-stage cumulative development audit must also remain entirely
`READY`.

## Outcome

The definitive claim is conjunctive across all sixteen seeds. If every cell and
source/information-flow audit passes, M2 becomes the authoritative cumulative
ancestor:

```text
M2
  learned/compiled filler correspondence
  + learned interaction substrate and boundary roles
  + learned role-relative causal direction
  + local invalidation, generic reopening, and historical reuse
```

Any failed seed freezes the cumulative negative. M1 remains authoritative and
no later scaffold may repair the result.

## Execution discipline

- implement a separate wrapper over the byte-frozen `4cb6d65` development
  ancestor;
- development and enabling runners remain definitive-forbidden;
- local/E2B preflight may run formatting, strict Clippy, focused tests, and one
  non-claim audit using development seed 100;
- execute the frozen `--definitive` command exactly once on E2B;
- serialize only after the complete matrix finishes;
- create, never overwrite:
  `results/ds2_cumulative_causal_direction_definitive.csv` and
  `results/ds2_cumulative_causal_direction_definitive.md`;
- preserve every existing result byte-for-byte;
- download and retain the write-once artifacts unchanged;
- do not open cumulative DS3 before the outcome is frozen.
