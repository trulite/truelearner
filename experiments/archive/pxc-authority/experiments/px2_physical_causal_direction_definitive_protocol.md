# PX2 substrate-native physical causal direction definitive protocol

Status: **PREREGISTERED; DEFINITIVE EVIDENCE UNSPENT; PX2 AUTHORITY ABSENT**.

## Frozen starting point

The authority workflow begins exactly at boundary-diagnostics handoff commit
`a33549317f4e4c5790aa2f174e8f5e72902e7bb0`, tag
`px2-causal-direction-boundary-diagnostics-handoff-v1`.

| frozen artifact | SHA-256 |
|---|---|
| authoritative PX0 substrate law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| authoritative PX1 definitive CSV | `6613ff0a96bb3a60fbe7afeb92cd64edced3c6df5dcc04fe47518db158dd88f6` |
| PX1 result audit | `fa4a516fcb6977a45e547ca1bb3b7db3b427c05b381fb60d2700e92fa2ae7c70` |
| PX1 authority handoff | `ab4142a24f6ca1095c1c1364f391253752808382ac6ee70ef9d49eac722df28c` |
| positive PX2 MICRO CSV | `62ff9fa71834edcc0cb0b71232f6bc02221d48e5862d3b35ccc1187f8973d937` |
| immutable PX2 GATE v1 negative CSV | `ef63c70d3ce980d71cbe1e085174b654bd4dcc4505d3e308e2ed59a34abeaec5` |
| H1 matched-schedule result audit | `c1c3c6dd80265d5e864fa7c346b8effde8e4a522a0826067c3e30dcbba202dd9` |
| O1 opportunity-window CSV | `4794a4715f00261a7441c602f60f144dded9d7e28d62336f1a41e40242c9dd2a` |
| O1 result audit | `0b19aa3eb3a83f40087393920edd995a02c4a2e9a416c15c07b9591d2dd0d126` |
| boundary-diagnostics handoff | `f9a28eff7bef80b9beadf9d610f94c46bcb2ec4da9c29a06a91115e1abe363bd` |
| frozen developed PX2 source | `af0c781eb0b53a7e972497ab4e247e8db2c74b5cf61e06e4de82a8da7be74151` |

PX0/PX1 physics and the developed PX2 law may not change. Authority may add
only fresh fixed cells, evaluator-side clause serialization, source/leak
audits, refusal/preflight surfaces, and write-once publication.

The GATE v1 negative remains immutable. This protocol does not rescue or
reinterpret it. H1 and O1 experimentally falsified its two over-strong
invariance requirements.

## Narrow authority claim

> On the physicalized substrate, reusable causal direction is selected by
> actual traversed participation followed by ordinary returned activity;
> temporal correlation alone is insufficient. Which competing causal
> structures survive is additionally path-dependent because ordinary
> recurrence and lifetime pressure act on the developing physical graph.

The second sentence is a measured boundary, not a symmetry requirement.

## Exact fresh matrix

The definitive matrix contains exactly `112` blank deterministic cells:
sixteen fresh seed namespaces crossed with all seven causal worlds. Seed `s`
and world `w` use namespace:

```text
0x5_4200_0000 + s * 0x1000_0000 + w * 0x0100_0000
```

No namespace, CELL, ARROW, or SPIKE identity appeared in development evidence.
Seeds rotate through the four frozen hardened strata in order. This covers:

- side spacing `36, 48, 64, 80`;
- traversal delay `3, 4, 5, 6`;
- distractor load `0, 8, 24, 48`;
- one through four parallel physical direction paths;
- normal and mirrored placement;
- normal and reversed allocation;
- normal and reversed arrival insertion.

Every cell is executed twice from byte/execution-identical complete state for
deterministic replay. Evaluator observations operate on clones and have no
causal path back into the substrate.

## Seven causal worlds

1. forward traversal with ordinary return;
2. reverse traversal with ordinary return;
3. matched consequences and return activity without candidate traversal;
4. forward traversal with physical return blocked;
5. genuine joint traversal and return;
6. forward traversal while wrong-way observational consequences occur at
   least three times as often;
7. reverse traversal while wrong-way observational consequences occur at
   least three times as often.

The adversarial worlds serialize traversal and observation counts separately.
The statistically dominant non-traversed direction must not mature; the less
frequent actually traversed direction must mature and execute.

## Independently serialized physical stages

For both directions every row records:

- correspondence resistance;
- live direction-path count and resistance minimum/maximum;
- actual candidate traversal;
- consequence firing;
- participation-trace arrival and firing;
- local returned-activity coincidence;
- outward effect;
- held-out and post-gap execution;
- autonomous source refiring;
- quiescence and work;
- complete-state fingerprint and duplicate equality.

The following developmental-boundary fields are recorded but do not impose
symmetry:

- complete direction-participation schedule class;
- arrival/insertion order;
- first-use delay after weak direction opportunity formation;
- the O1 first-observed-dead opportunity boundary (`30` ticks);
- final live paths and resistance for both directions.

No clause requires equal counts to produce equal final strength. No clause
requires a weak unused proposal to survive an arbitrary delay.

## Ten conjunctive per-cell claims

1. `P0` frozen hashes, fixed parameters, and fresh namespace are exact;
2. `P1` PX0 correspondence is acquired on both anonymous sides;
3. `P2` actual candidate traversal matches the physical world;
4. `P3` participation traces and local return coincide only as physically
   permitted;
5. `P4` direction maturation follows traversal plus return and no other side;
6. `P5` held-out and post-gap direction execution matches matured structure;
7. `P6` correlation without traversal and traversal without return produce no
   reusable direction;
8. `P7` more-frequent wrong-way observation cannot override the actually
   traversed direction;
9. `P8` autonomous source refiring is zero;
10. `P9` every propagation is naturally quiescent and complete-state replay is
    exact.

The outcome is conjunctive: `112/112` cells and `1,120/1,120` claims must pass.
Every `P0–P9` bit is serialized separately. Any failed clause is an immutable
definitive negative.

## Anti-cheat and isolation

Organism-visible execution contains only ordinary CELL state, ARROW
topology/coupling, SPIKE occurrences, local timing/order, thresholds,
participation traces, pressure, local plasticity, and physical return.

Forbidden active representations include cause, source, target, direction,
intervention, correlation, correct/wrong, selected path, provenance owner,
return owner, episode, history, lifecycle class, old M2 schema, or renamed
equivalents. Scenario names, schedule labels, expected maturation, and all
pass clauses are evaluator-only.

PX2 consumes actual PX0+PX1 substrate state. No old M0/M1/M2 schema,
serializer, behavioral adapter, semantic credit, new trace, new threshold,
new damping rule, or evaluator-selected local update may execute. The crate
remains dependency-free.

## Pre-evidence validation

The committed and tagged implementation must pass formatting, focused
compilation/tests, strict Clippy, zero-dependency/source audits, frozen hashes,
fresh-namespace audit, no-cell `--preflight`, refusal without `--definitive`,
and absence of final/staging artifacts. Preflight may not enter any cell or
emit the evidence marker.

## Sole execution and no rescue

The exact command executes once:

```text
cargo run --release -p px0-physical-correspondence \
  --example px2_physical_causal_direction -- --definitive
```

After validation it emits exactly one
`PX2_PHYSICAL_CAUSAL_DIRECTION_DEFINITIVE_EVIDENCE_SPENT` marker, executes all
cells literally once (plus preregistered exact duplicates), and atomically
publishes PASS or scientific FAIL artifacts. No rerun, rescue, regeneration,
tuning, or protocol amendment is allowed after the marker.

## Atomic artifacts

The preregistration digest of sorted pre-existing result hashes is
`6235145cbc1593247fed40b3b75410dfea17d22bbbe8d615051edbd07b256c3d`.
These paths are absent:

```text
results/.px2_physical_causal_direction_definitive.csv.staging
results/.px2_physical_causal_direction_definitive.md.staging
results/px2_physical_causal_direction_definitive.csv
results/px2_physical_causal_direction_definitive.md
```

## Program consequence

PASS makes PX2 the authoritative cumulative physicalized ancestor and makes
PX3 development eligible. It does not preregister or execute PX3.

FAIL leaves PX2 non-authoritative and PX3, PX-C, the continuous organism, and
Harness v0 blocked. Neither outcome authorizes changing authoritative PX0/PX1
or any frozen PX2 development/diagnostic record.

