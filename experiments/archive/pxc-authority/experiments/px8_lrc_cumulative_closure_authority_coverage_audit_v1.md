# PX8 LR-C cumulative closure authority coverage audit v1

Status: **FROZEN SOURCE/DEPENDENCY CLASSIFICATION; AUTHORITY EVIDENCE UNSPENT**.

## Exact serial lineage

Protocol commit `17df7940a6bf858f8f9d14d00e7be8df167248be` is directly
parented by exact PX7 authority
`9e2aca8933df168780dd6d7e6f00c3d9feae98ee`. The isolated PX8 development
branch and all of its tags remain unchanged. Only the byte-exact active PX8
library and its scientifically frozen physical geometry/predicates are ported.

The active surface remains PX7 manifest v5, SHA-256
`db4758baa5aeba36a87251f7d2ccb85cd2215f9489a1189eae4fd9d6408001c2`,
until definitive functional evidence is frozen. The registered v6
transformation replaces only:

```text
PX8,src/post_m7_ds5_closure_emission.rs,predecessor-target
```

with:

```text
PX8,arms/px8-lrc-physical-closure/src/lib.rs,authoritative-physical-closure-emission
```

No PX0--PX7 row may change.

## Complete cumulative active dependency closure

The unique active Rust closure is exactly:

| source | classification |
|---|---|
| `crates/lr1-modulatory-physical-return/src/lib.rs` | authoritative PX0--PX3+LR-C law and shared PX5/PX6 physical reduction |
| `arms/px4-lrc-lifetime/src/lib.rs` | authoritative PX4 physical API |
| `crates/px7-lrc-arrival/src/lib.rs` | authoritative PX7 physical-arrival topology/participation surface |
| `arms/px8-lrc-physical-closure/src/lib.rs` | new PX8 physical-closure/emission topology/participation surface |

PX8 includes the exact retained LR-C file as a private module and declares no
Cargo dependency. This is source reuse, not a copied or changed law: the path
resolves to the same unique LR-C file and its hash is frozen. PX8 contains no
local law copy, generated include, optional feature, build script, macro
source, evaluator dependency, development runner, or frozen-mechanism crate.
PX5 and PX6 add no active source.

## Evaluator-only classification

`arms/px8-lrc-closure-authority/src/main.rs` is evaluator-only. It owns fresh
matrix identities, ordinary physical input schedules, observations,
conjunctive predicates, cumulative PX7 conformance, resource accounting,
embedded independent replay, input hashes, the execution firewall, and
write-once publication. It exports no organism API and dependency flow is
one-way from evaluator to PX8 and PX7.

The authority Cargo manifest, protocol/audit prose, result serialization,
static audit script, taxonomy/comparator scripts, every earlier evaluator,
and all result artifacts are evaluator-only. No DS predecessor, isolated PX8
runner/report, development handoff, or evaluator surface is reachable from an
active crate.

## Functional and resource coverage

Every active PX8 mechanism is exercised: recursive learning, completed reuse,
incomplete reuse, blocked and pressure-stale reuse, duplicate recursive input,
pause/fingerprint/resume, persistent-size observation, and every compact form
(`Direct`, `Open`, `Aged`, `Fork`, and `Ring`). The matrix observes exact
outward crossing, exact physical return, qualified update, work, persistent
size, fingerprints, and natural quiescence. Fresh PX7 maturation and held-out
arrival separately exercise the complete retained serial stack.

Each row requires field work `<=20000`, maximum observed persistent bytes
`<=8192`, stable retained allocation, complete natural quiescence, and exact
independently reconstructed equality. The evaluator contains no unsafe code,
interior mutability, hidden global/thread-local state, generated code,
semantic mechanism object, artificial leak, manual cleanup, or measured-result
feedback into later physical input.

Active sources: **4 unique files**. New active PX8 sources: **1**. Evaluator
sources: **1**. Unclassified candidate sources: **0**.
