# PX4 LR-C cumulative physical lifetime authority protocol v1

Status: **PREREGISTERED; AUTHORITY EVIDENCE UNSPENT; PX5--PX8 FORBIDDEN**.

## Immutable parent and permitted claim

The sole serial parent is PX3+LR-C authority commit
`f9057fe78a86db9111b0b69310d03accef3bc970`, tagged
`px3-lrc-physical-event-authority-v2`. The authority workflow starts from clean
PX4 development-ready commit `20bc9ce384b74b6e5cca04f4bed2599932a34e92`.
No PX5--PX8 result, implementation, interpretation or authority may enter the
PX4 verdict.

The only claim under test is:

> Physical lifetime is ordinary retained ARROW resistance under ordinary
> pressure. It is earned by recurrent qualified participation, expressed as
> reuse advantage, lost by disuse, and reacquired only by a fresh physical
> proposal. Existing eligibility and qualified LR-C Modulation are sufficient;
> no lifetime-specific substrate law exists.

The retained LR-C law must remain byte-identical at
`crates/lr1-modulatory-physical-return/src/lib.rs`, SHA-256
`7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10`.
The active PX4 mechanism remains only `arms/px4-lrc-lifetime/src/lib.rs`.
After this protocol freezes, no substrate, active-mechanism, evaluator,
schedule, predicate, identity or publication change is allowed before the
one-shot matrix. A failure is immutable and ends this lane.

## Leakage exclusions

The organism may contain no lifetime object or field, `History`, episode or
reset boundary, cleanup or delete semantic, typed lifetime handoff,
evaluator-derived lifetime input, or explicit lifetime mechanism invocation.
The evaluator may insert anonymous ordinary arrivals, clone fresh worlds,
observe public physical state, and serialize observations. It may not write a
candidate, resistance, eligibility, pressure, generation or plasticity value.

All pressure observations use a frozen non-adaptive schedule. For recurrence
counts `1,2,4,8`, the evaluator executes all 24 registered ten-tick pressure
observations from fresh clones. It records the first physical deallocation but
does not use an observed resistance or deallocation to choose a later organism
input. The preregistered resistance and deallocation sequences are both
`4,7,12,22`.

The static leakage audit must prove:

- zero forbidden semantic identifiers or calls in the active PX4 mechanism;
- zero forbidden lifetime/history/boundary/cleanup/delete declarations or
  member accesses in the authority evaluator;
- no active-source change from development readiness;
- the only direct active-library dependency is the authoritative LR-C crate;
- the authority runner and its wrapper are evaluator-only; and
- all candidate active sources are classified, leaving foundation seams zero.

## Fresh authority matrix

The evaluator binary is freshly named `px4_lrc_lifetime_authority_v1`; the
protocol namespace is `px4-lrc-cumulative-lifetime-authority-v1`; artifacts
are `results/px4_lrc_lifetime_authority_v1.csv` and `.md`.

Sixteen identity roots `461001..461016` are disjoint from development roots
`151001`, `152001..152102` and `153001..153008`, and from PX3 definitive
identities. Every internal fixture receives an additional disjoint offset of
`10000..70000`. The rows cross:

- normal and reversed cell allocation;
- forward and reflected physical layout;
- absolute schedule origins `200` and `400`; and
- two fresh identity replicates per allocation/layout/schedule stratum.

Both schedule origins are exact multiples of the retained ten-tick pressure
period. No candidate exists during the initial absolute-time displacement, so
the two origins change evaluator schedule identity without adding a boundary,
history or substrate event. Supported exposures retain the established
within-exposure physics: source Drive at `origin+t`, return Drive at
`origin+t+2`, candidate crossing at `+2`, and qualified Modulatory arrival at
`+3`. Recurrences begin at relative ticks `0,5,10,...`. Controls and pressure
ticks are translated by the same registered origin.

Each row executes twice from fresh complete state. Exact equality includes
physical fingerprints, generations, crossings, work ledgers, quiescence and
every serialized observation. This is replay inside the single definitive
command, not a second evidence run.

## Forty-one conjunctive clauses per row

Each row has 40 core clauses plus exact replay:

1. one unsupported exposure physically deallocates;
2. one qualified exposure reaches resistance `4` and coupling `2`;
3. return-alone, late-return and Drive-return controls cannot credit it;
4. every one-exposure control naturally quiesces;
5. recurrence counts `1,2,4,8` yield resistance `4,7,12,22`;
6. the fixed 24-step pressure scan yields first deallocation at
   `4,7,12,22`, with every registered penultimate live and terminal dead;
7. both curves are strict and every propagation quiesces;
8. reuse makes no proposal, crosses with mature impulse `2`, and receives only
   the ordinary qualified `+3` resistance update;
9. disuse deallocates; a new source firing reacquires a distinct generation;
10. changed experience deallocates the old path, retains and reuses the
    recurrent new path, and requires no semantic instruction;
11. a queued stale generation is blocked while a fresh generation alone fires,
    with two identities, one firing and one physical deallocation;
12. reflected fresh layouts are structurally invariant;
13. cumulative PX0, PX1, PX2, PX3 and LR-C conformance all hold; and
14. the second fresh execution is exactly equal to the first.

The global matrix clause is layout/schedule invariance. Authority-functional
success is therefore exactly `16/16` rows and `657/657` clauses, with exact
replay and natural quiescence true.

## Required controls and cumulative conformance

PX0 requires weak physical formation, qualified maturation, pressure loss,
stale blocking and fresh reacquisition using only CELL/ARROW/SPIKE physics.
PX1 requires actual source participation; return alone and late return have no
effect. PX2 requires only the traversed `source -> effect` direction. PX3
requires recurrence-earned reusable organization and changed-experience loss.
LR-C requires eligible traversed structure plus qualified Modulatory return;
Drive return cannot substitute.

No row may omit one exposure, recurrence-earned persistence,
pressure/deallocation, reuse advantage, disuse, reproposal/reacquisition,
changed experience, stale-generation blocking, return-alone, late-return,
Drive-return, replay, quiescence, layout/identity invariance or cumulative
conformance.

## Freeze, E2B preflight and one-shot command

The protocol, evaluator and audit script are committed and tagged before any
definitive cell runs. A fresh preflight E2B sandbox may run formatting, release
build, release tests, strict Clippy, the static audit and the evaluator's
no-world `--authority-preflight` refusal/identity/artifact checks. Preflight
must use a unique state file and cannot create a world or result artifact.

The sole definitive command is then executed exactly once from the unchanged
frozen commit in a second fresh E2B sandbox with another unique state file:

```text
cargo run --release --manifest-path arms/px4-lrc-lifetime/Cargo.toml \
  --bin px4_lrc_lifetime_authority_v1 -- --authority-v1
```

The command atomically creates the registered CSV/Markdown pair and refuses
overwrite. A nonzero exit, missing artifact, malformed artifact, any failed row
or clause, or absence of the unique completion line is the immutable result.
There is no correction, rescue, regeneration, replay command or rerun.

## PX-C authority gate

Only after immutable matrix evidence is frozen may fresh E2B sandboxes run the
static coverage/leakage audit and the existing v2 taxonomy/comparator. The
before side is exactly the current authoritative PX3 parent baseline artifacts,
not any future lane. The after side is the frozen PX4 active-surface manifest
and exact authority evidence commit. Required results are:

```text
primary seams             368 -> 297 (or a stronger justified decrease)
semantic guard                    <= 218
evaluator guard                   <= 752
new seam kinds                         0
new guarded surfaces                   0
PX0--PX3+LR-C foundation seams          0
unclassified active source files       0
```

The raw taxonomy inventories, summaries, guard inventories and comparator
outputs must be downloaded unchanged and hashed. Existing negative outputs and
any new negative are immutable.

## Authority decision

PX4 authority may be claimed only if the preregistered one-shot matrix,
`657/657` clauses, exact replay, natural quiescence, cumulative PX0--PX3+LR-C
conformance, static leakage/coverage audit and PX-C gate all pass. A separate
result audit and a separate authority handoff must be committed and tagged.

Any evidence for a new stored field, new transition, new physical mode, new
eligibility rule or new law is a genuine scientific fork. Freeze that fork and
stop; do not select or implement it in this workflow. A positive PX4 result
does not run or authorize PX5--PX8.
