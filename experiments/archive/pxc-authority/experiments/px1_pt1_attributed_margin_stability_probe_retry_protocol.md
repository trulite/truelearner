# PX1-PT1 attributed-margin stability PROBE retry protocol

Status: **PREREGISTERED MECHANICAL RETRY; EVIDENCE UNSPENT**.

The immutable PROBE v1 negative showed successful PX0 acquisition and
differential branch firing, but the weak continuation died in flight before
outlet coincidence or attribution could occur.

The retry keeps byte-identical:

- PX0 law;
- all cells, arrows, topology, thresholds, delays, phases, and couplings;
- acquisition and exposure schedules;
- support and global-return paths;
- effect-triggered trace coincidence;
- all independently serialized metrics and pass rules;
- namespace and duplicate-replay policy.

The sole physical change is:

```text
both anonymous continuation opportunities
initial resistance 2 → 3
```

This is the minimum reserve that remains live after the frozen tick-66 and
tick-70 ordinary-pressure applications. It is symmetric and contains no route,
role, or answer information.

Fresh artifacts:

- `results/px1_pt1_attributed_margin_stability_probe_retry_v1.csv`;
- `results/px1_pt1_attributed_margin_stability_probe_retry_v1.md`.

The v1 negative and its audit remain immutable. A retry result cannot create
PX1 authority; positive development only makes MICRO eligible.

