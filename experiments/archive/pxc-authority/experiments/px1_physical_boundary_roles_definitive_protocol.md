# PX1 substrate-native physical boundary roles definitive protocol

Status: **PREREGISTERED; DEFINITIVE EVIDENCE UNSPENT; PX1 AUTHORITY ABSENT**.

## Frozen starting point

The authority workflow begins exactly at development-readiness commit
`a6b9a96bd983396ee4c4903b0965168f0d06a57c`, tag
`px1-physical-boundary-roles-development-readiness`.

| frozen artifact | SHA-256 |
|---|---|
| authoritative PX0 active law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| PX1 GATE implementation | `b0d549077ca49b4bebd3a692ad3906f800bc94aae348301a2206f03f16b90b07` |
| PX1 GATE CSV | `1b75fb15972e226d4cb047c69925f9d5452601b3b15be17434aedb5b37935ebf` |
| PX1 GATE result audit | `32d01b3c3bb5b101d51865e0367509556bb34ca80bc5054589fdbffce0e2a84a` |
| PX1 development-readiness handoff | `8d08350ecbd03ba336447f5cb24c28120f57f08448d057e8d2542d5d4690da75` |

The PX0 law and developed PX1 physical mechanism may not change. Authority may
add only fresh fixed cells, evaluator-side serialization, source/leak audits,
and write-once result publication.

## Narrow authority claim

> Boundary-role behavior can emerge directly from PX0 physical correspondence
> through transient participation traces and ordinary returned activity, with
> no typed role/provenance representation, while preserving useful recurrence
> and natural quiescence.

The claim does not supply one-role exclusivity. If two continuations genuinely
participate and receive ordinary return, both may mature.

## Exact fresh matrix

The definitive matrix contains exactly `96` blank deterministic cells: sixteen
fresh seed namespaces crossed with all six causal worlds. Seed `s` and world
`w` use namespace:

```text
0xd100_0000_0000 + s * 0x0100_0000 + w * 0x0010_0000
```

No namespace, cell, arrow, or spike identity appeared in development evidence.
The sixteen seeds rotate through the six frozen GATE strata in order, thereby
covering side spacing `32, 40, 48, 56, 64`, exposure spacing `11, 12, 13`,
held-out gaps `18..28`, post-gap gaps `52..72`, normal/mirrored placement, and
normal/reversed allocation.

Every seed runs all six worlds:

1. support A;
2. support B;
3. no support;
4. physical participation with global return blocked;
5. identical global return without continuation participation;
6. genuine joint participation.

Each cell is executed twice from byte/execution-identical complete state for
deterministic replay. Evaluator observations operate on clones and have no
causal path back into the learned fixture.

## Independently serialized observables

For A and B separately, every row records:

- correspondence resistance;
- continuation resistance;
- training branch firing;
- training outlet firing;
- training trace arrival;
- training trace firing;
- training local return;
- held-out branch firing;
- held-out outlet firing;
- held-out trace arrival;
- held-out trace firing;
- held-out local return;
- held-out outward crossing;
- post-gap outward crossing.

It also records training/held-out/post-gap source refiring, quiescence, work,
complete fingerprint, duplicate equality, and every pass bit below. No compound
accounting clause may conceal which physical stage failed.

## Thirteen conjunctive per-cell claims

1. `P0` frozen hashes, fixed matrix parameters, and fresh namespace are exact;
2. `P1` PX0 physical correspondence is acquired on both anonymous sides;
3. `P2` scenario-appropriate branch and outlet participation is exact;
4. `P3` trace arrivals are exact for actual effects and shared global return;
5. `P4` trace firing requires physical coincidence and is exact;
6. `P5` local returned activity reaches exactly the participating traces;
7. `P6` continuation persistence/maturation is exact;
8. `P7` held-out branch and outlet stages are exact;
9. `P8` held-out trace arrival and trace firing stages are exact;
10. `P9` held-out local return and outward crossing are exact;
11. `P10` supported behavior survives the fixed post-gap and unsupported behavior does not appear;
12. `P11` autonomous source refiring is zero in training, held-out, and post-gap;
13. `P12` every propagation is naturally quiescent and complete-state duplicate replay is exact.

The outcome is conjunctive: `96/96` cells and `1,248/1,248` claims must pass.
Any single failed claim is an immutable definitive negative.

## Measurement semantics

The blocked-return world requires eight A branch firings, at least one actual A
outlet firing, one direct trace arrival per outlet firing, zero trace firing,
zero local return, full continuation decay, and zero held-out crossing. It does
not require an unsupported reserve-3 continuation to execute eight times.

The threshold-1 shared hub emits once per propagation. A mature continuation
therefore gives its own trace one direct arrival plus the one shared global
arrival; all other traces receive only the shared arrival.

These are evaluator observations only. Counts, expected maturity, and world
names never enter organism-visible state.

## Anti-cheat and isolation

Organism-visible execution contains only ordinary CELL state, ARROW topology
and coupling, SPIKE occurrences, local timing/order, thresholds, pressure,
local plasticity, and physical return. Forbidden active representations include
`RelationMotif`, `Neighborhood`, `Episode`, `Query`, `History`, typed role,
selected branch, provenance owner, `return_for`, START, FINISH, correct/wrong,
or renamed equivalents.

PX1 consumes PX0-produced physical arrows directly. No old M0/M1 schema,
serializer, behavioral adapter, semantic role flag, or evaluator-selected local
credit may execute. The crate remains dependency-free.

## Pre-evidence validation

The committed/tagged implementation must pass formatting, focused compilation
and tests, strict Clippy, zero-dependency/source audits, every frozen hash,
fresh-namespace audit, no-cell `--preflight`, refusal without `--definitive`,
and absence of final/staging artifacts. Preflight may not enter any cell or emit
the evidence marker.

## Sole execution and no rescue

The exact command executes once:

```text
cargo run --release -p px0-physical-correspondence \
  --example px1_pt1_attributed_margin_stability -- --definitive
```

After validation it emits exactly one
`PX1_PHYSICAL_BOUNDARY_ROLES_DEFINITIVE_EVIDENCE_SPENT` marker, executes all
cells literally once (plus preregistered exact duplicates), and publishes PASS
or scientific FAIL artifacts. No rerun, rescue, regeneration, tuning, or
protocol amendment is allowed after the marker.

## Atomic artifacts

The preregistration digest of sorted pre-existing result hashes is
`1e6d369b75aa639351a84a367bcee1f6d3f792f92d6259c002019c2ea8a51480`.
These paths are absent:

```text
results/.px1_physical_boundary_roles_definitive.csv.staging
results/.px1_physical_boundary_roles_definitive.md.staging
results/px1_physical_boundary_roles_definitive.csv
results/px1_physical_boundary_roles_definitive.md
```

## Program consequence

PASS makes PX1 the authoritative cumulative physicalized ancestor and makes
PX2 development eligible. PX2 must consume the actual physical PX0+PX1 state;
the old M1 output schema remains forbidden.

FAIL leaves PX1 non-authoritative and PX2, PX-C, the continuous organism, and
Harness H1 blocked. Neither outcome authorizes changing authoritative PX0 or
the frozen PX1 development record.
