# PX3 physical event-boundary frozen-negative handoff

Status: **MATERIAL FROZEN NEGATIVE; OUTCOME D; PX3 AUTHORITY ABSENT**.

## Outcome

Existing authoritative PX0–PX2 physics is **not sufficient** for the old-M3
event-boundary behavioral capability. It preserves transient temporal gaps and
route-local recurring participation, but it does not retain which routes
recurred together and cannot distinguish a trained pair from a crossed pair
after acquisition activity is absent.

Lane classification: **D — genuinely new representation or substrate law
required**. Continuing would require relation-specific persistent structure
that is absent from PX0–PX2, or a new physical law that creates such state.
Both are outside this lane's authority. There is no PX3 mechanism, MICRO,
GATE, definitive matrix, authoritative ancestor, or PX4–PX8 advance.

## Frozen mechanism

No new mechanism is frozen. The exact no-addition result consists of:

- authoritative PX0–PX2 law:
  `crates/px0-physical-correspondence/src/lib.rs`, SHA-256
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- authoritative PX2 execution source:
  `crates/px0-physical-correspondence/examples/px2_physical_causal_direction.rs`,
  SHA-256
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- frozen no-addition implementation commit
  `61bc6a70f9bd1a4caf62cbd5dddce1911a2ce19b`, tag
  `px3-physical-event-boundaries-no-new-mechanism-probe-v4-implementation`,
  source
  `crates/px0-physical-correspondence/examples/px3_physical_event_boundaries_probe_v3.rs`,
  source SHA-256
  `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba`;
- organism-visible physical block SHA-256
  `ac11bd435098469cdf2a16b3d75dddf4285396c3a75aa31a87bff1f775142fee`;
- frozen-negative result commit
  `a7da9f0c4b4d3c60b338ba7612a94b6a3d43fb71`, tag
  `px3-physical-event-boundaries-no-new-mechanism-probe-v4-frozen-negative`;
- result CSV SHA-256
  `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38`;
- result report SHA-256
  `021be8698c010df1e09dc45f2bf9968f2255b6eb7851c38f36fd93be72260d3b`.

The organism-visible block is byte-identical to the failed v2 block. V2 is
preserved at commit `809a392008f17e9c2de6a1c75b097a79ef065802`, tag
`px3-physical-event-boundaries-no-new-mechanism-probe-v2-first-clause-failure`;
its CSV/report hashes are
`5ae68dad569f943d08d945014ed5491a93eb30021b1afc78966ed39fd15b4cc4`
and `941c1754004b43eee9a99b969bfbad9e1fd75257091fc475e4dbb196084eef66`.

## Consumes physically

The no-addition discriminator consumes only actual retained physics:

- four anonymous physical copies of the PX2 route motif;
- per copy: arrival CELL at position `36*i`, threshold `4`; nearby
  correspondence CELL at `+2`, threshold `2`; continuation CELL at `+8`,
  threshold `2`; consequence CELL at `+10`, threshold `2`; participation-trace
  CELL at `+16`, threshold `2`; outward CELL in region `1`, threshold `1`;
  acquisition driver, participation driver, and return gate CELL;
- one shared context CELL and the retained shared return-hub CELL, both
  threshold `1`;
- positive-coupling acquisition, traversal, consequence, trace, local return,
  hub, context, and outward-crossing ARROWs with the frozen PX2 delays;
- weak continuation-to-consequence ARROWs at resistance `3`;
- external source SPIKE sets, actual continuation/consequence traversal,
  actual trace firing and returned SPIKE arrival, four-tick local eligibility,
  `+3` local return strengthening, ten-tick ordinary pressure, CELL decay,
  refractory state, generation invalidation, deterministic arrival ordering,
  region crossing, and natural queue drain;
- no relation-specific topology: exactly `56` ARROWs and `5,408` persistent
  bytes in each positive cell.

There is no conceptual grouping fact in organism state. Reference A and B are
evaluator descriptions of different external arrival times only.

## Produces physically

For all four positive fresh-identity/layout cells, the retained substrate left:

- correspondence resistance `22|22|22|22`;
- direction resistance `17|17|17|17`, all four ARROWs live;
- training continuation, consequence, trace, return, and outward-crossing
  counts `12|12|12|12`;
- exact trained/crossed/gapped/singleton outward counts `2/2/2/1`;
- shared-hub firing counts `1/1/2/1` for close/crossed/gapped/singleton use;
- exact post-gap trained-pair crossing count `2`;
- natural quiescence and zero autonomous source refiring after every finite
  propagation;
- no relation ARROW, relation CELL, pair-specific coupling, or reusable
  trained-versus-crossed execution difference.

Blocked physical return produced resistance `0|0|0|0`, four deallocations, and
no held-out crossing. A once-returned control left weak route-local structure
immediately, then ordinary pressure physically removed its executable effect
before the fixed post-gap observation. No evaluator deletion occurred.

## Assumptions and environmental physical opportunities

- authoritative PX0–PX2 commit:
  `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`, tag
  `px2-physical-causal-direction-authoritative`;
- correspondence acquisition arrivals at ticks `0,16,32,48`, four uses per
  route;
- weak direction opportunities added only after actual correspondence
  acquisition;
- main first arrival tick `64`; 12 rounds; round spacing `18`; simultaneous
  co-participation within a cluster; second cluster at `+8`; early/late order
  alternates each round;
- one source-threshold SPIKE set and one participation-driver SPIKE for every
  explicitly active route;
- held-out simultaneous use receives exactly one shared context SPIKE;
- the six-tick gapped control receives one context SPIKE at each physical
  arrival;
- held-out and post-gap observations execute on nonplastic clones after gaps
  `14` and `34`;
- the isolated subthreshold control receives one recurrence round and `70`
  ticks of ordinary pressure opportunity;
- blocked return omits only the consequence-to-hub ARROW;
- allocation-reversed replicas use namespaces `0x8_4300_0000` and
  `0x8_5300_0000`; primary/negative cells use the exact `0x7_...` namespaces
  frozen in the v3 protocol;
- scenario schedules and pass clauses are evaluator-only external fixtures;
  they do not construct typed organism state, select a local path, trigger a
  local update, or declare a cut;
- no hidden fixture scheduling or typed construction exists.

## Forbidden

- typed adapter;
- reconstruction of any old M schema;
- semantic labels or evaluator-selected local paths;
- importing evidence or mechanisms from another parallel PX lane;
- Event, Episode, History, Boundary, grouping labels, or renamed equivalents
  in organism-visible execution;
- relation CELLs, pair tokens, evaluator-selected relation ARROWs, serializers,
  hidden resets/cutoffs, or modification of authoritative PX0–PX2.

## Cumulative port rule

- import the frozen mechanism byte-for-byte onto the later authoritative
  parent;
- make no anticipatory redesign;
- run a physical dependency/path audit;
- the first incompatibility is the next cumulative collapse;
- resolve only that parent delta under a separately frozen protocol.

For this Class-D negative there is no new mechanism to import. If a later
serial parent reopens the question, it must first port the frozen no-addition
organism block byte-for-byte, preserve all protocols and both negatives, and
diagnose only the parent delta. It may not reinterpret this negative as
permission to add relation state.

## Validation and accounting

- focused formatting, compile, and strict Clippy: pass;
- no-argument/wrong-argument refusal and no-CELL preflight: pass;
- frozen source/result hashes and exact parent ancestry: pass;
- organism-visible forbidden-token audit: pass;
- CSV shape: `6` rows, `41` columns;
- exact duplicate cells: `6/6`;
- v2 work: `631,187` ledgered operations;
- decisive v4 work: `720,918` ledgered operations;
- v2 result storage: `3,933` bytes;
- decisive result storage: `4,146` bytes;
- total lane-added storage before this handoff: `124,284` bytes;
- authoritative PX0–PX2 files changed: none;
- broad historical tests: not run because no shared code changed;
- definitive evidence/matrix: not run;
- worktree at frozen-result commit: clean.

## Blocker

PX3 cannot advance under the standing representation/substrate boundary.
Progress needs user/program authority for a genuinely new relation-specific
representation or substrate law under a separate lane and separately frozen
protocol. This lane stops with authority absent.

