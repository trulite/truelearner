# PX3 physical event-boundary no-new-mechanism PROBE v4 result audit

Status: **FROZEN MATERIAL NEGATIVE; PX3 ABSENT; EVIDENCE SPENT**.

## Sole execution

The v4-authorized command executed exactly once from frozen implementation
commit `61bc6a70f9bd1a4caf62cbd5dddce1911a2ce19b`, tag
`px3-physical-event-boundaries-no-new-mechanism-probe-v4-implementation`:

```text
cargo run --release -p px0-physical-correspondence \
  --example px3_physical_event_boundaries_probe_v3 -- --probe
```

It emitted exactly one
`PX3_PHYSICAL_EVENT_BOUNDARIES_NO_NEW_MECHANISM_PROBE_V3_EVIDENCE_SPENT`
marker and exited `1` with the preregistered scientific classification
`NO_RELATION_SPECIFIC_STATE_IN_PX0_PX2`. There was no rerun, rescue,
regeneration, tuning, or source/protocol change after the marker.

## Frozen artifacts

- CSV:
  `results/px3_physical_event_boundaries_no_new_mechanism_probe_v3.csv`;
  SHA-256
  `685dc04db32a5785224c62ba5b589fa8e1e37382a8b613f5f2b5e396aa005f38`;
- report:
  `results/px3_physical_event_boundaries_no_new_mechanism_probe_v3.md`;
  SHA-256
  `021be8698c010df1e09dc45f2bf9968f2255b6eb7851c38f36fd93be72260d3b`;
- executed source SHA-256:
  `39ec595fc1204a29083d271ebcadcdb7950c07d1c44e4ce07c0107fca54730ba`;
- organism-visible block SHA-256:
  `ac11bd435098469cdf2a16b3d75dddf4285396c3a75aa31a87bff1f775142fee`;
- v4 protocol SHA-256:
  `fbb6661861b796bdaf4be5d93afd71b1ac42a09168dc2e6d3008c5bc996695f8`.

Staging paths are absent. The failed v2 source and artifacts remain exact and
were not touched.

## Conjunctive result

Every prerequisite/control passed:

- frozen source/parent audit: pass;
- per-route continuations, consequences, participation traces, local returns,
  and outward crossings: `12|12|12|12` in all four positive cells;
- correspondence resistance: `22|22|22|22`;
- direction resistance/live: `17|17|17|17` / all live;
- trained held-out pair: two exact outward crossings;
- crossed held-out pair: two exact outward crossings;
- gapped held-out use: two exact outward crossings;
- singleton: one exact outward crossing;
- post-gap trained pair: two exact outward crossings;
- shared-hub firings for close/crossed/gapped/singleton: `1/1/2/1`;
- blocked return: all four direction candidates deallocated and no held-out
  crossing;
- single recurrence: once-returned candidates present immediately, then no
  crossing after the fixed pressure gap;
- reverse allocation and fresh-identity replicas: exact behavioral match;
- natural quiescence and zero autonomous source refiring: every run;
- duplicate complete replay: `6/6` cells;
- ledgered work: `720,918` operations.

References A and B had matched marginals, identical relation-normalized route
state, identical topology (`56` ARROWs, `5,408` persistent bytes), and symmetric
held-out execution. Exact fingerprints differed with fresh physical identity,
as expected; no comparison fed back into organism execution.

## Scientific conclusion

PX0–PX2 already provide transient temporal discrimination: close activity
produces one shared-hub firing, a six-tick separation produces two, and every
finite schedule drains naturally. They also provide route-local reusable
participation through actual traversal plus returned trace activity.

They do **not** preserve which routes recurred together. The shared hub and
route-local resistance retain participation marginals only. Once the
acquisition stream is absent, the trained pair and a crossed pair execute
symmetrically. Therefore old-M3 event-boundary behavior does not emerge from
existing PX0–PX2 physics with no new mechanism.

Proceeding would require relation-specific persistent structure (a new
representation) or a new substrate law. Both are forbidden in this lane.
There is no MICRO, GATE, definitive matrix, authority claim, or later-stage
advance.

