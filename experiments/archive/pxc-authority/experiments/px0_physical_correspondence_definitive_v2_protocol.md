# PX0 substrate-native physical correspondence definitive v2 protocol

Status: **PREREGISTERED; V2 DEFINITIVE EVIDENCE UNSPENT; PX0 AUTHORITY ABSENT**.

PX0 definitive v1 remains an immutable scientific failure. This protocol does
not amend, reinterpret, rescue, or rerun it. V2 is a fresh authority matrix
that incorporates the dense-topology return-specificity boundary exposed by
v1 and resolved developmentally by PX0-P1 and PX0-S.

## Frozen authority ancestor

The workflow begins exactly at PX0-S Classification A readiness commit
`a77a564f741e280bb674ff6e1cb6a28df4d80790`, tag
`px0-s-stable-return-specificity-development-classification-a`.

| frozen artifact | SHA-256 |
|---|---|
| active PX0 physical law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| retained substrate physics | `6aa28a76e1362ac8dfb1d33fb68807da40e7604dfdc8cca9efa1e314e3ce4263` |
| PX0 definitive v1 CSV | `da356bc46a9d83d0cd749bcaa697cba66393b7d694de500e2208565806d680d1` |
| PX0 definitive v1 report | `7e2c06d63332a680d46031c49d5dc245c6a4f381d7c646a2b0474580469a09b7` |
| PX0 definitive v1 audit | `575da84eb9ced6c48309f1f15ef75dd19fd99e8c112c6bb37f3d5be7bce68a14` |
| PX0 definitive v1 handoff | `aa38d12edb1d83a1249f76c24c6bd6e5ac7af6c4569c4f2a6ff97cca7f2b29d8` |
| PX0-P1 result audit | `e9c92d461fb53898fe2c01c3ef3a06633ca3b98d245c2095bb758e6dcbe13c78` |
| PX0-P1 handoff | `a9b2272b968755ff0503efc65e898c0a211a224cd8684422f35818b5f58fc906` |
| PX0-S protocol | `3f14e74de3c331eb6657e09077be147ed83cebb5325ea9e11d263230c623e30f` |
| PX0-S PROBE correction audit | `51174002f79c281ba98f49c921f788d14c7389ab558c332caa12a8dcb0eeada2` |
| PX0-S readiness | `a3ba35f6755b0363db809575182296d9cead69a30bb0842a527c4ae4389fcad7` |

The authority implementation may add only fixed physical worlds, an
evaluator/authority wrapper, audits, and atomic write-once serialization. It
may not modify the active physical law or retained physics.

## Authority question

> Does one unchanged CELL/ARROW/SPIKE law implement a complete lifelong
> physical correspondence cycle in which physically reliable return across
> experience becomes reusable structure, while absent or sparse incidental
> return does not?

No evaluator supplies correct/wrong, relation identity, lifecycle class,
probation status, or reusable status. All return paths are genuine physical
paths. Reliability exists only as recurrence through changing physical
experience.

## Exact fresh matrix

The matrix contains exactly 16 blank deterministic cells. Cell `i` uses base
namespace `0x12000000 + i * 0x100000`; these namespaces and their derived
control ranges appeared in no PX0, PX0-R, PX0-P1, PX0-S, or v1 definitive
cell.

Across cells, fixed matrix dimensions vary:

- initial route `A = i mod 3`;
- contemporary stable route `B = (A + 1) mod 3`;
- spare route `C = (A + 2) mod 3`;
- forward/reverse cell allocation by parity;
- direct/mirrored layout by `i mod 4`;
- route stride `12 + 2 * (i mod 8)`;
- context spacing `10 + (i mod 8)` ticks;
- dense distractor load from `{0, 2, 4, 8, 16, 24, 32, 40}`;
- sparse-return phase `i mod 4`;
- four support-device identities/delays in a cell-specific permutation;
- ordinary arrival order alternated and mirrored.

No route index enters organism-visible state.

## Exact continuous primary sequence

Each primary fixture remains continuous and is never reset:

1. Four anonymous presentations with ordinary return acquire A.
2. Held-out A produces exactly one crossing.
3. A survives a bounded absence and reuses the same live arrow identities.
4. Ordinary pressure fully deallocates every original A variable arrow.
5. Time alone creates no proposal and stale A cannot execute.
6. Renewed anonymous activity creates fresh physical candidates.
7. The contemporary world supplies stable return to B in every interleaved
   context through four changing support devices/delays. A receives genuine
   incidental dense-topology return only in one context phase per cycle.
8. A final three-context tail omits A's incidental driver.
9. Held-out B produces exactly one crossing. Held-out A produces zero.
10. Old A arrows remain dead; fresh B is live; sparse fresh A becomes
    subthreshold and fully deallocates under continued pressure.

The primary specificity sequence uses eight four-context cycles plus the
three-context tail. Stable and incidental return are both accepted as ordinary
physical evidence when they occur.

## Independent physical controls

Every cell also contains fresh, namespace-disjoint fixtures for:

- **return-free probation:** broad candidates form, receive zero physical
  return, never execute, and deallocate;
- **stable dense return:** a return path of the same dense cross-route physical
  type recurs and becomes executable;
- **stability swap:** which route receives cross-context reliable return is
  swapped and the retained executable correspondence swaps with it;
- **absence:** no return yields no reusable correspondence;
- **ambiguity:** equally recurrent alternatives remain viable individually but
  simultaneous activation has no privileged outward winner;
- **complete-state replay:** identical state and arrivals produce identical
  execution and final fingerprint.

## Thirteen conjunctive claims

Every cell must pass all thirteen:

1. `P0` frozen hashes, namespace, physical dimensions, and source isolation are exact;
2. `P1` initial anonymous activity plus ordinary return acquires A;
3. `P2` bounded absence preserves and reuses the same live A identities;
4. `P3` sufficient ordinary pressure fully deallocates original A;
5. `P4` stale original A cannot execute and time alone creates no proposal or resurrection;
6. `P5` renewed activity creates fresh proposal identities after forgetting;
7. `P6` contemporary reversed world retains B and restores exactly one crossing;
8. `P7` genuinely return-free fresh proposals never execute and deallocate;
9. `P8` sparse genuine dense-topology return does not remain reusable;
10. `P9` stable dense-topology return remains learnable;
11. `P10` swapping which return is stable swaps retained correspondence;
12. `P11` absent return and equal ambiguity produce no privileged survivor/winner;
13. `P12` fresh identity/layout/timing transfer, deterministic replay, natural quiescence, stale-path blocking, and complete work/storage/provenance accounting all hold.

The result is conjunctive: `16/16` cells and `208/208` claims must pass. Any
single claim failure is a definitive PX0 v2 negative.

## Anti-cheat and audit rules

Organism-visible execution contains only frozen CELL/ARROW/SPIKE state,
topology, timing, thresholds, local plasticity, return, pressure, and
deallocation. Forbidden active representations include motif, relation,
episode, query, history, correct/wrong, probation, reusable, stable/incidental,
route class, evaluator mutation, or renamed equivalents.

The wrapper may construct physical topology and anonymous arrival schedules and
may observe traces, crossings, identities, resistance, work, storage, and
fingerprints. It may never mutate, select, strengthen, weaken, delete, restore,
or label a learned arrow.

The crate must remain dependency-free. The active-law hash, retained-physics
hash, v1 negative artifacts, PX0-P1, and PX0-S artifacts must remain exact.

## Pre-evidence validation

Before evidence, the committed/tagged implementation must pass formatting,
focused compilation/tests, strict Clippy, zero-dependency/source audits, all
frozen hashes, a no-cell `--preflight-v2`, refusal without the sole authority
flag, and refusal if final or staging artifacts already exist. Preflight emits
no evidence marker and runs no matrix cell.

## Sole execution and no rescue

The exact command is executed once from the clean frozen implementation:

```text
cargo run --release -p px0-physical-correspondence --example definitive -- --definitive-v2
```

After repeating no-cell preflight, the runner emits exactly one
`PX0_V2_DEFINITIVE_EVIDENCE_SPENT` marker immediately before cell zero. It
publishes complete PASS or scientific FAIL artifacts with atomic create-new
semantics and exits `0` or `1` respectively.

Any interruption, panic, infrastructure failure, or partial serialization
after the marker is an immutable incomplete negative. No rerun, rescue,
regeneration, tuning, or protocol amendment is permitted.

## Atomic artifacts

The preregistration digest of sorted pre-existing result-file hashes is
`7d714d70c6940c3cf37cab42bb0dbfeb59be25efd11aa95f70f9e9097cce4092`.
These paths are absent at preregistration:

```text
results/.px0_physical_correspondence_definitive_v2.csv.staging
results/.px0_physical_correspondence_definitive_v2.md.staging
results/px0_physical_correspondence_definitive_v2.csv
results/px0_physical_correspondence_definitive_v2.md
```

## Program consequence

PASS creates authoritative PX0 and makes cumulative PX1 development eligible.
PX1 must consume PX0-produced physical structure directly; serialization into
old M0/M1 representations or a behavioral adapter remains forbidden.

FAIL leaves PX0 authority absent and PX1, PX-C, the continuous organism, and
Harness H1 blocked. Neither outcome authorizes changing the frozen law.
