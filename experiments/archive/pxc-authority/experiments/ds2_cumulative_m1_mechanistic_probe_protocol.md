# DS2 cumulative M1 mechanistic-regime probe

Status: **PREREGISTERED; DEVELOPMENT PROBE UNSPENT**

Authoritative parent: `16a1002b59bf0dbc23a6b6bf03572efca53b33ce` /
`m1-cumulative-boundary-role-authoritative`.

## Frozen expectation

> No new enabling developmental stack is assumed for DS2. First attempt
> causal-direction reconstruction using only structures already available in
> authoritative M1 and ordinary CELL/ARROW/SPIKE learning physics.

DS2 removes learner-visible causal annotation equivalent to `A caused B` while
retaining physical directional propagation. It may not introduce a causal
object, causal episode, intervention adapter, direction label, source/target
role, evaluator direction, or semantic success signal.

## Question

Does authoritative M1 already expose a genuine local interaction history in
which the chosen learned affordance physically produces an effect, anonymous
aftermath returns through the frozen relation stack, and contrasting downstream
consequences can drive ordinary local direction proposals?

The first probe is deliberately not a new learner. It inventories and exercises
the exact M1 call graph. The isolated DS2 fixture and its `LocalObservation`,
`Position`, supplied boundary role, supplied context, intervention record, and
consequence record are forbidden as replacements or adapters.

## Ordered stages

0. Exact authoritative M1 source, result artifacts, and frozen lineage match.
1. No learner-visible causal/source/target annotation or evaluator direction is
   reachable during M1 acquisition.
2. The exact M1 path forms the learned event, multiple real affordances, frozen
   DS1 choice, returned evidence, attribution, downstream contrast, and learned
   boundary role.
3. The selected DS1 affordance is physically executed through its already
   existing A1 CELL/ARROW route before the downstream consequence is observed.
4. The returned consequence is naturally route-contingent: changing only the
   selected affordance can change the later ordinary activity without an
   evaluator-created consequence packet.
5. Existing local activity exposes both ordered direction candidates to
   ordinary proposal/probation physics without endpoint or intervention labels.
6. Contrasting interactions strengthen the supported direction and weaken or
   prune the reverse/noncausal candidates.
7. One role-relative direction consolidates into reusable local structure and
   transfers across fresh occurrences.
8. A changed physical dependency invalidates the stale direction and reopens
   ordinary inference.

Stop at the first missing physical path. No later stage may be simulated,
adapted, or repaired in this attempt.

## Interpretation

- Passing stages 0–4 would show that M1 supplies a genuine causal experience
  substrate rather than co-present action and consequence descriptions.
- Passing stages 5–8 without a new enabling gate would support entry into the
  mechanistic regime: higher organization arises through ordinary substrate
  learning over M1's own interaction histories.
- Any collapse identifies the smallest hidden dependency. M1 remains
  authoritative; no failure weakens the already-frozen boundary-role result.

This probe is development-only, creates no definitive artifact, cannot advance
the cumulative prefix, and does not authorize DS3 or compilation.
