# PX1-PT1 attributed-margin stability MICRO v2 protocol

Status: **PREREGISTERED MEASUREMENT-ONLY SUCCESSOR; EVIDENCE UNSPENT; PX1 NON-AUTHORITATIVE**.

## Immutable predecessor

PX1-PT1 MICRO v1 remains an immutable `8/12` negative. Its two mirrored
blocked-return cells and two mirrored joint-participation cells failed only
over-specified accounting predicates. The v1 CSV SHA-256 is
`d32e174f77c2440c52baf8978370ff684ddfffb45a72938cbc8cdbb7099f93c2` and
the v1 negative-audit SHA-256 is
`05a847240a6186a3705b591487673905d6b329090145cbebcdc1094e560a9e36`.

## Frozen physical experiment

MICRO v2 keeps byte-identical PX0/PT0 learning laws and the same reserve `3`,
margin threshold `4`, CELL/ARROW/SPIKE topology, thresholds, delays, couplings,
acquisition count, exposure count, physical schedules, six worlds, and
primary/mirrored allocation layouts. It uses fresh namespaces and new
write-once result paths.

No physical mechanism, experience, or control is changed. Only the two failed
evaluator predicates are corrected.

## Corrected independent predicates

1. **Blocked return:** require eight A branch firings, at least one A outlet
   firing, exactly one direct trace arrival per actual outlet firing, zero trace
   firing, zero local return, complete continuation decay, and zero held-out
   execution. The scientific requirement is actual participation without
   maturation when global return is blocked—not eight successful executions of
   a reserve-3 unsupported continuation.
2. **Joint participation:** a threshold-1 shared hub may fire once when both
   outlets reach it in one propagation. Held-out trace arrivals must therefore
   equal one shared global arrival plus one direct arrival for each mature
   continuation: `[2,2]`. Both trace cells, local returns, and outward effects
   must still be `[1,1]`.

Every other v1 predicate remains unchanged and independently serialized:
support A/B, no support, return without effect, correspondence acquisition,
continuation resistance, training and held-out causal stages, post-gap
recurrence, zero source refiring, quiescence, work/fingerprint accounting,
fresh mirrored/reversed transfer, and exact duplicate replay.

## Pass and stopping rule

All `12/12` fresh cells and every independent clause must pass. The run is
write-once. Any failure freezes MICRO v2 without rescue or rerun. A positive
result makes GATE development eligible; it does not authorize definitive
execution or create PX1 authority.
