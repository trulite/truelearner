# DS1 after R0 leak and control audit

The composition retry adds no organism mechanism. It reads the compact frozen
R0 report and inventories the already-existing source/runtime paths.

## Preserved positive chain

All MICRO/GATE cells retained:

- exact R0, readiness, E2B, DS1, M0, and result fingerprints;
- every frozen R0 ordered stage;
- all 22 frozen R0 fresh-identity, relabeling, layout, permutation,
  ambiguity, negative, staleness, lifetime, and cleanup controls;
- two physical structural roots and two opaque one-to-one handles;
- one frozen DS1 choice with zero update during R0;
- one selected route execution through live CELL/ARROW/SPIKE physics;
- one anonymous temporary return relation and four exact bridge fields.

## Forbidden channels remain absent

The retry contains zero source edges for:

- evaluator effect or expected choice to DS1 update;
- correct/wrong, reward, accepted/rejected, or other semantic update calls;
- hidden boolean or signed consequence construction;
- stable filler, prior/future occurrence, route destination, or answer tokens;
- action opcode interpretation;
- strength mutation for measurement;
- held-out reconstruction after collapse;
- M1 creation or fallback.

The retry does not call `apply_consequence`. Its stage-8 path count is derived
from production source excluding test-only mutations. Each asserted absence is
mutation-tested.

## Ordered-stop controls

Stages 0–7 must all pass before stage 8 can be evaluated. Stage 8 requires an
existing update edge and a runtime update. Because both are zero, stage 8 is
the single collapse and stages 9–10 are `BLOCKED`. A unit test perturbs every
possible collapse index and verifies that no later stage is reported READY.

Definitive execution is refused before harness entry. Local and E2B checks
confirmed exit 2 and byte-identical results-tree digest. No result artifact was
created.
