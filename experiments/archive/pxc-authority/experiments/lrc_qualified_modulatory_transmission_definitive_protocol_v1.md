# LR-C qualified modulatory transmission definitive protocol v1

Status: **PREREGISTERED; IMPLEMENTATION ABSENT; DEFINITIVE EVIDENCE UNSPENT**.

## Frozen starting point

- candidate-law commit/tag: `967ab7d` /
  `lrc-qualified-modulatory-candidate-law-v1`;
- candidate-law specification SHA-256:
  `7c0fe9b86a99f8618f98fd40cd53c41429c3d7cafaa7d773ab315562d410a9bc`;
- active law source SHA-256:
  `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10`;
- LR2 selection result commit/tag: `59ae1b5` /
  `lr2-bc-dense-conformance-result-v1`.

Spent PX0--PX2 definitive matrices are historical authority and may not be
rerun. LR2 is development evidence and may not be promoted. This workflow uses
fresh identities and fresh physical histories throughout.

## Narrow authority claim

> A physical Drive/Modulatory ARROW transmission distinction is sufficient to
> prevent ordinary execution traffic from changing recently eligible local
> structure while allowing timely Modulatory traversal to change it without
> ordinary CELL excitation. The same successor substrate preserves fresh
> cumulative physical correspondence, participation-attributed boundary roles
> and traversal-attributed causal direction.

The claim contains no reward, correctness, outcome, contributor, route-owner
or causal-label semantics. It does not claim that a physically traversed
Modulatory route is beneficial; only its local physical effect is claimed.

## Exact fresh matrix

The definitive matrix contains exactly `496` blank deterministic cells:
sixteen fresh seed namespaces crossed with twelve law-attack worlds and
nineteen cumulative conformance worlds.

Seeds:

```text
6101, 6113, 6121, 6131,
6143, 6151, 6163, 6173,
6197, 6203, 6211, 6221,
6229, 6247, 6257, 6263
```

Cell namespace:

```text
0x7_3300_0000_0000
+ seed * 0x1000_0000
+ suite * 0x0100_0000
+ world * 0x0010_0000
```

Seeds rotate through sixteen fixed strata crossing normal/mirrored placement,
normal/reverse CELL allocation, normal/reverse ARROW allocation, normal/reverse
arrival insertion, return delay `1..=4`, source spacing `32,48,64,80`, and
dense ordinary distractor loads `8,16,32,64`. No identity or complete state
appeared in LR2 development.

Every cell is executed twice from byte/execution-identical complete blank state
for exact replay. Evaluator observations operate only after propagation and
have no path back into active state.

## Twelve primary law-attack worlds

1. candidate traverses, then ordinary Drive reaches its source: candidate
   update `0`; Drive may execute the source normally;
2. Modulatory route traverses without candidate traversal: update `0` and
   source firing `0`;
3. candidate traverses, timely Modulatory route traverses: exactly one update
   `1/1 -> 4/2`, source refiring `0`;
4. a coupling-four Modulatory traversal reaches a threshold-two source:
   activation/firing remain zero, proving effect rather than weak amplitude;
5. candidate traverses, then all dense ordinary distractors reach the same
   source: update `0` regardless of distractor load;
6. two return routes have fixed physical identities; route R0 is Drive and R1
   Modulatory: credit follows R1 only;
7. the same physical routes swap modes: credit follows R0 only;
8. candidate traverses twice with two separately completed timely Modulatory
   returns: exactly two lawful updates, each consuming one eligibility;
9. two outgoing candidates from one source both traverse, then one local
   Modulatory arrival: both eligible local structures may update exactly once;
10. candidate traverses and Modulatory return arrives after eligibility
    expiry: update `0`;
11. renewed Drive and lawful Modulatory return arrive at the same tick:
    exactly one plasticity update; ordinary execution remains independently
    observable; and
12. dense parallel sources, candidates, returns and crossed routes: only
    candidates with both actual eligibility and local Modulatory arrival learn;
    all propagation quiesces.

Every world records actual ARROW modes and traversals. Mode is never inferred
from resistance or behavior. Swapped worlds use the same route identities,
amplitudes and timing; only their physical mode fields differ.

## Fresh cumulative successor conformance

The remaining nineteen worlds reproduce the frozen authority-level behavioral
surfaces on the successor law without executing spent authority fixtures.

### PX0 -- six worlds

1. recurring anonymous physical traversal plus Modulatory return acquires
   executable correspondence;
2. a single returned traversal remains probationary and dies under pressure;
3. return changes only currently eligible corresponding structure;
4. learned structure survives bounded absence then forgets under sufficient
   ordinary pressure;
5. renewed physical activity after deallocation creates fresh ARROW identity
   and generation; and
6. repeated contemporary return reacquires reusable correspondence.

### PX1 -- six worlds

1. support A;
2. support B;
3. no support;
4. physical continuation participation with Modulatory return blocked;
5. identical Modulatory return without continuation participation; and
6. genuine joint participation and local return.

Claims cover actual branch/outlet participation, transient eligibility,
differential persistence, held-out reuse, recurrence, zero autonomous source
refiring and natural quiescence.

### PX2 -- seven worlds

1. forward traversal with Modulatory return;
2. reverse traversal with Modulatory return;
3. matched consequence and return activity without candidate traversal;
4. forward traversal with return blocked;
5. genuine joint traversal and return;
6. forward traversal while non-traversed wrong-way observations occur at least
   three times as often; and
7. the mirrored reverse case.

Direction may mature only from actual traversal plus timely Modulatory return.
Correlation alone and traversal without return remain insufficient.

## Independently serialized claims

Every cell row records:

- seed, stratum, suite, world and fresh namespace;
- every relevant ARROW identity, generation, endpoint and physical mode;
- candidate traversal counts and source firing counts;
- Drive and Modulatory delivery counts separately;
- eligibility before/after the registered return boundary;
- accepted/rejected update counts;
- resistance, coupling and liveness before/after;
- held-out effects where registered;
- proposals, pressure, deallocations, work and persistent bytes;
- complete and permanent fingerprints;
- natural quiescence and exact duplicate replay; and
- twelve separate conjunctive pass bits `P0..P11`.

No CSV field may contain a comma. Each suite and world receives the same twelve
claim columns; inapplicable columns assert fixture integrity, unchanged frozen
law, accounting and isolation rather than being omitted.

The definitive outcome requires:

```text
496 / 496 cells
5,952 / 5,952 independently serialized claims
```

Any failed cell or claim is an immutable definitive negative.

## Anti-cheat and isolation

The authority harness may schedule physical inputs and observe serialized
state only. It may not mutate resistance, coupling, liveness, eligibility,
mode, activation or generations; synthesize accepted updates; consult world
kind from active physics; or suppress execution. The active law contains no
evaluator type and no dependency on historical M0--M3 mechanisms.

The source audit rejects semantic identifiers including `REWARD`, `CREDIT`,
`CORRECT`, `OUTCOME`, `RETURN_FOR`, `CAUSE_ID`, `COMPOSITE` and `EVENT` in
active-law identifiers. The word Modulatory names only the registered physical
transmission effect.

## Pre-evidence and sole execution

All Rust formatting, compilation, tests, lint, preflight and definitive
execution occur only in E2B. Preflight verifies frozen hashes, source audit,
matrix cardinality, fresh namespaces, duplicate construction and absence of
final/staging artifacts. It enters no definitive cell.

After protocol and implementation commits/tags are frozen, exactly one
`--definitive` command executes all cells plus their registered duplicates and
atomically publishes CSV and Markdown artifacts. No failed result may be
rerun, tuned, rescued or overwritten.

## Program consequence

A positive result makes LR-C the authoritative continuous-plasticity successor
law and establishes fresh cumulative conformance of PX0--PX2 capabilities on
that law. Historical PX0--PX2 authority remains historically intact.

PX3 still does not become authoritative. A positive result only permits fresh
PX3 development to resume from the LR-C successor substrate. A negative result
freezes the first failed physical edge and leaves PX3 blocked.
