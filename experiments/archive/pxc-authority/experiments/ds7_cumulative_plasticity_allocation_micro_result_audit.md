# DS7 cumulative plasticity-allocation MICRO result audit

Status: **POSITIVE DEVELOPMENT MICRO; CUMULATIVE GATE ELIGIBLE**.

Frozen lineage:

- authoritative M4:
  `8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2`;
- positive PROBE result:
  `fa363fc5c0f8e4f56885f5165f3891de2279bf34`;
- operative MICRO v3 protocol:
  `6384f27` /
  `ds7-cumulative-plasticity-allocation-micro-protocol-v3`;
- formatted implementation plus packaging repair:
  `97222c72fc9fefa13975de5bb230abf7f2bf3aa0`;
- M4-linked PROBE source SHA-256:
  `d1e14001902b6bafe6049ef01a9990ffcc91c22180543672c30a13fdb3ee7dc9`;
- MICRO source SHA-256:
  `3fd46245614c17476b8fa44887e4a710d0c358ecc4f7ebe7da5fbdb5bb9459ec`;
- runner SHA-256:
  `e5042196e2f8c6dc238e17fc43c765ba9b612bf9ba0c0a5080be1005c3126114`;
- v3 protocol SHA-256:
  `1648b4a0e19a13c918a3c92ad221c93b7ae145a34c6e960e57fd9d0f91506eb9`.

## Pre-execution amendments and compile stop

No evidence was spent under protocol v1 or v2. Before implementation or seed
execution:

- v2 corrected the deterministic exploration arithmetic from an impossible
  `<=1/32` to the forced `<=4/32` bound and the corresponding economy floor;
- v3 froze one M4 linker completion: local gate consultation is ordinary use
  of the consulted value record and applies the exact scalar `+2` recurrence
  increment.

The first v3 build stopped before execution because a relative source-audit
include could not resolve inside the generated frozen composition directory.
Commit `97222c72fc9fefa13975de5bb230abf7f2bf3aa0` replaced that relative path
with the repository-absolute compile-time path. No encounter seed ran before
this packaging repair.

## Execution

The complete two-cell MICRO then ran once in persistent E2B development
sandbox `iyrkw7af5qpmwwfmq3bwm`, from the clean immutable repair commit. The
sandbox was left running.

```text
cargo fmt --check                                      PASS
focused release unit test                              1 / 1
release MICRO                                          PASS, 2 / 2 cells
duplicate exact                                        PASS
```

No broad repository suite, cumulative GATE, or definitive matrix ran.

## Scientific reading

The result is stronger than the PROBE in three ways:

1. blocked and interleaved acquisition histories converge on the same learned
   allocation behavior over fresh identities and changed absolute layouts;
2. learned allocation suppresses unnecessary proposal work by 43.75 percent
   while retaining every productive opportunity in the fixed evaluation;
3. allocation is reversible: ordinary exploration lets a previously negative
   encounter accumulate changed experience until it becomes directly admitted.

The result remains deliberately narrower than a cumulative gate. It has not
yet shown that the learned allocator can acquire and preserve the full frozen
downstream structural-plasticity behavior across multiple seeds, loads,
controls, and M4 histories.

