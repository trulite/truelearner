# DS8 cumulative non-semantic-credit definitive readiness handoff

Status: **DEFINITIVE READY; EVIDENCE UNSPENT; M5 AUTHORITATIVE**.

Exact readiness ancestor:
`07dff3aa759a4a372ece2cd7775ceff9ab7a36c5` plus the positive GATE freeze that
contains this handoff.

## Frozen cumulative parent and mechanism

- authoritative M5:
  `9c5ba68a6a4ae37b51575ebaae414ab51a248575` /
  `m5-cumulative-learned-plasticity-allocation-authoritative`;
- M5 allocator SHA-256:
  `e755a70deada891e5c4db3b55809ca84ea8ad31a8bd3affe564bf08a95f8dff7`;
- M5 definitive CSV/report SHA-256:
  `86d9f6e3a8ab4ad5c242e0d7c619d8eda99e0da47faff623f26c8c6835b9a99a` /
  `a336633c73565261d357a67ca02df3047ffcaf88488153bb2f43b621818ba5f0`;
- DS8 organism linker fragment SHA-256:
  `1f68f7e943f37c42d29f16fe26f0d851a59361ed4c1f4273a82d0537f935d343`;
- positive GATE source/runner SHA-256:
  `19c9051d15023c5b88559cba4ee3b3eb55686d1a68e083ca260a4a65629e8f30` /
  `7e31925ce8122ea35f0c86243b4998fbe19a1319724e70c8c7293b7dad11ef6a`.

The authority agent must recompute these hashes from the clean readiness
commit. It may wrap and audit the frozen mechanism but may not change its
learner, linker, thresholds, topology law, lifecycle, or M5 ancestor.

## Definitive scientific claim

The matrix asks only:

> Can cumulative M5 acquire, retain, selectively allocate, lose, and
> reacquire useful structural change from evaluator-blind delayed physical
> consequence contrast, with zero privileged semantic correctness or reward?

The PASS claim is conjunctive:

1. blank-start useful route acquisition;
2. exact delayed physical consequence accounting;
3. `32/32` held-out route execution;
4. productive admission `4/4` and distractor admission no greater than the
   frozen one-in-eight exploration rate as load scales;
5. at least 50% structural-work reduction;
6. raw-consequence history swap reverses allocation;
7. learned M5 value shuffle reverses allocation;
8. ordinary pressure physically removes a useful edge and stale execution is
   impossible;
9. non-semantic learned allocation reacquires that edge and restores `32/32`;
10. fresh consequence topology, occurrences, endpoint identities, layouts,
    and equal magnitudes preserve the law;
11. positive PROBE/MICRO controls and authoritative M5 remain exact;
12. zero correctness, wrongness, reward, loss, expected answer/trace, or covert
    route/timing/magnitude/omission/reset/namespace polarity reaches the
    organism;
13. frozen v1/v2 negatives and their artifacts remain byte-identical.

## Authority execution constraints

The authority agent should freeze a fresh explicit matrix disjoint from all
development seeds `40,000,000` through `44,500,000`; sixteen fresh seed bases
across loads `8`, `32`, and `128` are recommended. Every cell must start blank
in a fresh namespace and use the balanced de-aliased four-topology schedule.

Use a fresh dedicated E2B state and sandbox, separate from development sandbox
`ie2usmlbtjokk80bkr6zr`. Perform formatting, focused compilation, source/hash
audits, and definitive refusal/no-cell tests before execution. Then execute the
write-once matrix exactly once. Create result artifacts atomically. No rerun,
rescue, tuning, partial reuse, or reinterpretation is permitted.

PASS:

```text
M6 becomes the authoritative cumulative ancestor.
The core-autonomy checkpoint is established.
A separately named post-M6 DS4 request/start successor becomes eligible.

Program-priority decisions are outside the evidentiary scope.
```

FAIL leaves M5 authoritative and M6 absent. Development may diagnose the
frozen failure only after the authority result is committed.
