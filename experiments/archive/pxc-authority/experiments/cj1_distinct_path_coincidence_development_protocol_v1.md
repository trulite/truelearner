# CJ1 distinct-path coincidence development protocol v1

Status: **PREREGISTERED; DEVELOPMENT EVIDENCE UNSPENT**.

## Frozen start and unchanged-port contract

- branch: `research/cj1-distinct-path-coincidence`;
- authoritative start: `2fbee861a0aeed335d3ffa8f9095ca28f2ac6129`;
- authoritative PX0 law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`;
- frozen PX1 PT0 source SHA-256:
  `f0b754ed6f7b0603668319a0735da91b4c168f909d4024fd5ce5e2aea4197410`;
- authoritative PX2 executed source SHA-256:
  `c47d605371d5787cffc7d456f1d9e38168b4b203063fb9dcdeefcf630fa4aed5`;
- PX2 definitive protocol, CSV and report SHA-256:
  `d55a33c76b3a2f5f9421e85147e116e8a67ef99e9401f630cecccd065f519cd2`,
  `921e433e3bf358e89e3f8f288b4ab0472e9503a2a3ac25fe037a2b7f6cf6eb18`,
  and `eef9c336baea6aa1e5c2debde2e1286b2839759c55fd5fc008c7775fd4103cda`.

PX0--PX2, PX3 Class D, every CJ0 lane and negative, and all parked lanes are
read-only. CJ1 may add only isolated development code, protocols and results.
There is no definitive, authority, PX3-restart or PX-C surface.

## Question

Can the already-authoritative path-local eligibility trace, ordinary decay,
inhibition, closure and CELL/ARROW/SPIKE dynamics make one physical path
contribute no more than once to a still-live local coincidence opportunity,
while allowing two physically different traversed paths to contribute once
each?

Physical path participation is the only discriminating fact. External origin
and evaluator provenance are recorded only as read-only fixture truth. They
never enter the substrate or select activity.

## Stage 0: unchanged-physics PROBE

The first execution imports the exact PX0 crate without a wrapper law. Each
arrival reaches a source CELL, traverses an ordinary incoming ARROW, and then
reaches one threshold-2 local CELL. The fixed matched cells are:

1. two unit firings through one physical path;
2. one impulse-2 firing through one physical path;
3. two unit firings through two different traversed paths;
4. two different paths with the second outside the measured physical window;
5. two different paths together inside the window;
6. four immediate firings through one path;
7. one-path burst plus one different path;
8. one path plus a burst on the other path;
9. three different paths;
10. one mature coupling-2 path;
11. fresh coupling-1 paths together;
12. the same external source through two physical paths versus two external
    sources through one shared physical path.

All cells match total entered impulse, timing and physical effects where the
comparison permits it. The PROBE serializes actual traversal, trace state,
local state, transmission, return, strengthening, resistance/lifetime,
held-out execution, work/storage, deallocation, replay and quiescence.

Existing physics is Classification A only if every row accepts genuine
different-path participation and rejects one-path count or amplitude
substitution. Otherwise freeze the earliest ordered collapse and continue only
to the single candidate below.

## Sole candidate after an unchanged-physics collapse

No extra stored field is allowed. The only candidate reuses the existing
ARROW-local transient eligibility value as the contribution trace at the
receiving local CELL:

> A traversed path with a live eligibility trace can make no further local
> contribution before ordinary expiry or successful returned-activity
> closure. A first traversal supplies one bounded local contribution,
> independent of carried amplitude.

The trace is written only by actual traversal, is cleared only by the existing
return/expiry/pressure paths, and is never permanent. Threshold-1 propagation
remains ordinary. There is no new stored field, object, identity surface,
historical resurrection, invalidation signal or evaluator-driven choice. If
this description requires more than the existing transient value, if two
mechanically credible rule placements remain after the unchanged PROBE, or if
the rule needs a second scientific arm, stop as Classification D.

## Candidate PROBE, MICRO and GATE

PROBE uses seeds `2101, 2111`, normal layout, threshold 2, coupling 1/2 and
load 0. MICRO uses seeds `2203, 2207, 2213`, normal/mirror layouts,
thresholds 2/3, coupling 1/2, loads 0/4 and both insertion orders. GATE uses
seeds `2309, 2311, 2333, 2339`, normal/mirror layouts, thresholds 2/3/4,
coupling 1/2, loads 0/4/12, sparse/parallel paths and both insertion orders.
Every stage starts from fresh identities and contains exact duplicate replay.

Each stage conjunctively covers:

- matched one-path repetition, one strong traversal, genuine different-path
  participation, inside/outside window, AAAA, A+B, A+A+B, A+B+B and three
  different paths;
- mature high-strength one-path input versus fresh low-strength different
  paths;
- one external source through two physical paths versus two external sources
  through one shared path;
- traversal/no traversal, timely/late/absent return, old self-evidence,
  matched marginals, pressure and experience count;
- full forgetting, stale refusal, generic reproposal and contemporary
  relearning;
- fresh identity/layout/mirror, allocation/insertion permutation, dense
  return, parallel paths, distractors, blocked/stale activity, ambiguity and
  nearby nonparticipation;
- level-free `A+B -> X`, `X+C -> Y`, `Y+D -> Z`, with primitive and learned
  physical outputs entering through the same API;
- immediate repeated A suppression and post-expiry fresh A participation;
- held-out execution, exact replay, natural quiescence and no runaway.

The mandatory reversal is one continuing physical instance. It first learns
A+B support for X and C+D support for Y, then receives A+D and C+B. Old
singleton/burst exposure continues throughout. Old routes must receive no
false self-support, lose resistance and deallocate; new crossed physical
routes must be generically proposed, strengthened by actual crossed traversal
and ordinary return, and execute held out. No schedule observation supplies
evidence or chooses a route.

## Independent serialization and controls

Every row independently records:

1. external entry and actual ARROW traversal;
2. existing path-local trace before/after and its ordinary close/expiry;
3. local CELL state before/after;
4. emitted/suppressed transmission and physical effect;
5. ordinary return arrival and trace-local update;
6. strengthening/coupling;
7. resistance, pressure, lifetime and deallocation;
8. held-out execution and stale refusal;
9. native work by operation and persistent/temporary storage;
10. complete/permanent fingerprints, replay, quiescence and runaway status.

Nearby activity that did not traverse the candidate path scores as
nonparticipation. A passing outcome may not depend on external-origin identity,
CELL handles, allocation order, insertion order, layout, mirror, evaluator
provenance or expected-result branches.

## Gates and classification

- **A:** unchanged PX0--PX2 physics passes the complete development ladder;
- **B:** unchanged physics freezes a collapse, then the sole transient
  path-local rule passes PROBE, MICRO and GATE including reversal;
- **C:** path-local saturation is insufficient and the next physical boundary
  is frozen without another rule;
- **D:** more than one scientific rule/placement is plausible, a persistent
  variable or recursive architecture is required, or interpretation forks;
- **E:** material negative under an interpretable complete matrix.

PROBE, MICRO and GATE are each write-once and frozen by result audit before the
next stage. A mechanical accounting, seed, schedule or evaluator defect may be
corrected only under a fresh preregistered protocol with the law byte-exact.
Any scientific row failure stops advancement. No broad historical/workspace
suite runs unless authoritative shared code changes (which this lane forbids).

## Publication and validation

Artifacts use create-new staging files, sync, atomic rename and staging-remnant
audits. Before every evidence run: exact hashes, clean tree, absent result
paths, fixed dimensions, refusal of later stages and source-leak audit. After
every run: schema/row uniqueness, independent accounting, exact hashes,
focused formatting/check/tests, strict Clippy and clean remote parity. Commits
and annotated tags freeze protocol, implementation, each result and terminal
handoff separately.
