# CJ1 distinct-path coincidence terminal handoff v1

Status: **TERMINAL DEVELOPMENT CLASSIFICATION D**.

## Outcome

CJ1 does not establish Classification B and does not authorize MICRO, GATE,
definitive work, an authority claim, PX3 restart or PX-C.

The unchanged-physics PROBE was negative at mature one-path coupling amplitude.
The shared-path topology correction passed. The sole preregistered candidate
was then implemented without a new stored field and executed at PROBE in E2B.
Its generated report claimed 26/26, but the frozen result audit invalidated that
claim: the repeated-path fixtures did not produce repeated source firings or
repeated traversals of the same physical ARROW.

## Physical boundary

Under the authoritative PX0 law, a CELL that fires at tick `t` is refractory
until `t + 1`. A single outgoing ARROW can therefore traverse at most once at
tick `t`. By the next possible source firing, one bounded unit at the receiving
CELL has undergone ordinary unit decay and is no longer a still-live local
coincidence contribution. The event needed to isolate repeated use of one path
inside the opportunity does not exist in the frozen dynamics.

The candidate did bound a mature coupling-2 traversal to one local unit, but
its path-trace repeat-suppression clause was never discriminated from ordinary
source refractoriness and local decay. Repair would require changing at least
one additional physical boundary—refractoriness, local decay, emission
multiplicity, or what counts as the same path—or accepting evaluator intent in
place of actual traversal. Each is outside the sole-candidate contract.

This is Classification D under the parent protocol: the interpretation forks
or requires another scientific rule. It is not a positive candidate result and
not evidence that path-local saturation alone is sufficient.

## Frozen chain

- candidate protocol tag: `cj1-candidate-probe-protocol-v1`;
- executed candidate implementation tag:
  `cj1-candidate-probe-implementation-v3`;
- invalid raw result/audit tag: `cj1-candidate-probe-v1-invalid`;
- raw CSV SHA-256:
  `38378eac9abf9609f2c73a3a05dafbbafe959fe6623660f6cf04d29850021b76`;
- raw generated report SHA-256:
  `16739fe0a9c78fc7faf41c1137e5627c5c9d550b94ea5231ea1c0f40f3885060`;
- result audit SHA-256:
  `01d520a24d894a3a4057edd07a688ac7c48d8ac28e8101b55654646757f3c727`;
- E2B sandbox: `i6x9gykt9tvp6xfz5z8ra`.

PX0--PX2 and all earlier results remain byte-exact. No local Rust process was
started. The prior local memory incident remains attributable to the orphaned
CJ0-F reciprocal-propagation test process, not this CJ1 execution.

## Authorized next work

CJ1 is closed. Any future attempt must begin as a new preregistered development
lane at the newly exposed physical event boundary; it may not revise these
write-once artifacts or continue to CJ1 MICRO. Runtime/compile/test execution
for such work remains E2B-only unless explicitly overridden by the user.
