# DS7 cumulative plasticity-allocation GATE v2 negative audit

Status: **DEVELOPMENT NEGATIVE FROZEN; NO AUTHORITY CHANGE**.

Frozen lineage:

- authoritative M4:
  `8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2`;
- development readiness:
  `9e7d197e2915fa0c550160d2cdd3dbb04884f168`;
- GATE v1 protocol (unspent):
  `1eaac7a`;
- operative GATE v2 protocol:
  `12ada39` /
  `ds7-cumulative-plasticity-allocation-gate-protocol-v2`;
- formatted v2 implementation:
  `5d901ab1e8ad9af2c80f06b26ebcbdf47ba72443`;
- E2B development sandbox:
  `iyrkw7af5qpmwwfmq3bwm`.

Version 1 spent no evidence. Version 2 added only the preregistered physical
event batching required to keep candidate load from accelerating the M4 clock.

## Execution

The focused release GATE test executed the complete fixed 18-cell matrix. It
computed the matrix twice before the conjunctive assertion, and duplicate
exactness passed. The assertion then failed because every cell had the same
withheld-edge branch failure. The subsequent release runner in the command
chain did not execute.

No broad repository suite or definitive matrix ran.

## Exact localization

The following passed in all 18 cells:

- blank acquisition of all four route edges plus distractor variation;
- 32/32 fresh physical held-out traversals;
- exact productive admission `4/4`;
- exact deterministic distractor exploration `1/8`, `4/32`, and `16/128`;
- proposal-work reduction of 58.33, 77.78, and 84.85 percent;
- learned-value shuffle reversal;
- retained-allocation economy;
- locality, eligibility, fresh-identity, and layout controls;
- frozen source/information audit;
- unchanged M4 exact lifetime vector;
- duplicate exactness.

The first collapse is the matched missing-edge phase. The v2 instrumentation
conjoined correct and shuffled removal before reporting it, so it cannot yet
distinguish:

```text
edge never physically reached zero
edge reached zero but was reopened during aging
correct and shuffled branches diverged before stale check
```

The next authorized action is a one-cell, no-new-mechanism diagnostic that
reports each branch's withheld resistance and allocation state at every M4
pressure boundary. It may not change pressure, use, admission, value, route,
or repair behavior. Only after that diagnostic localizes the mechanical edge
may a separately preregistered GATE retry run.

## Authority state

```text
M4 authoritative
DS7 PROBE positive
DS7 MICRO positive
DS7 GATE v2 negative and immutable
M5 absent
```

