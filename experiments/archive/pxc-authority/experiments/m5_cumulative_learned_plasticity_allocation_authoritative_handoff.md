# M5 cumulative learned plasticity-allocation authoritative handoff

M5 is the authoritative cumulative ancestor.

```text
M0  learned/compiled correspondence
M1  + learned anonymous boundary roles
M2  + learned role-relative causal direction and physical execution
M3  + learned event organization
M4  + learned use-dependent persistence and forgetting
M5  + learned allocation of structural plasticity
```

The narrow cumulative M5 mechanism is:

```text
ordinary local coactivity and adjacency
  -> broad local variation from blank state
  -> activity-derived eligibility after execution
  -> delayed active-path experience
  -> learned encounter prototype and value
  -> more future plasticity for productive recurring encounter histories
  -> sparse exploration keeps rejected histories revisitable

ordinary reuse
  -> M4 scalar resistance grows

ordinary non-use and completed-event pressure
  -> resistance is spent
  -> zero physically removes proposal/prototype/value allocation
  -> removed structure cannot execute
  -> later learned local coactivity can reacquire useful missing structure
```

No supplied encounter class, sensory/internal/irrelevant class, evaluator
proposal/candidate/target site, route-specific growth slot, `LEARN_HERE`
signal, probation TTL, temporary/permanent or consolidation lifetime class,
evaluator deletion, or pre-proposal correctness flow enters this learner.

Frozen definitive evidence:

- protocol commit/tag:
  `be8d6ade88c7a9ace32c5f705b333a60627cc415` /
  `ds7-cumulative-plasticity-allocation-definitive-protocol-v1`;
- authority implementation/audit commit/tag:
  `cac86fdb9800c88e6add63c3d57ab8f284b74608` /
  `ds7-cumulative-plasticity-allocation-definitive-implementation`;
- CSV SHA-256:
  `86d9f6e3a8ab4ad5c242e0d7c619d8eda99e0da47faff623f26c8c6835b9a99a`;
- report SHA-256:
  `a336633c73565261d357a67ca02df3047ffcaf88488153bb2f43b621818ba5f0`.

The 48-cell definitive matrix passed over sixteen fresh blank-start identity
namespaces and three distractor loads. Productive admission was `4/4` at every
load; distractor admission was `1/8`, `4/32`, and `16/128`; structural-work
reduction was `58.33%`, `77.78%`, and `84.85%`. Every missing route edge was
removed at resistance zero, blocked from stale execution, and restored to
`32/32` held-out execution by learned allocation. Shuffled allocation failed
the matched repair. Authoritative M4 remained exact in all 48 cells.

The authoritative cumulative prefix is now:

```text
DS0 + DS1 + DS2 + DS3 + DS6 + DS7
```

Under the frozen reordered sprint policy, the realized lineage is:

```text
M3 -> DS6 -> M4 -> DS7 -> M5 -> DS8 -> M6
   -> separately named DS4 successor -> M7 -> DS5 -> M8
```

Cumulative DS8 development is now eligible from byte-frozen M5. The remaining
supplied semantic outcome may still reach only the delayed physically active
trace after proposal and execution; DS8 must remove that semantic
`correct`/`wrong` supply without reconstructing it through route, timing,
magnitude, omission, reset, or namespace. Supplied request/start and
finish/output conventions remain external harness boundaries and may not
enter credit acquisition, eligibility, selection, persistence, execution, or
update.

No DS8 definitive matrix is preregistered or authorized by this handoff.
Program-priority decisions remain outside evidentiary scope.
