# PX3 on LR-C fresh integrated parallel gates protocol v2

Status: **PREREGISTERED DEVELOPMENT; BOTH V2 GATES UNSPENT**.

Parent: frozen v1 result audit. V1 remains uninterpretable because lifecycle
omitted three registered controls and recursion retained one evaluator
predicate from the retired P-trace attribution geometry.

## Frozen physical hypothesis

The physical law and integrated chain are unchanged from v1:

```text
two unit participation traces -> threshold-two opportunity O
O + uniform background -> P
P -> native candidate traversal -> eligibility
candidate + context -> downstream effect
effect + physical world return -> threshold-two relay
relay -> Modulatory ARROW -> P
```

All computation traffic is Drive. Only relay-to-P is Modulatory. There is no
active R3/R4/R5/R6 attribution geometry and no Event, member, contributor,
composite or level state.

## Gate L v2 -- complete lifecycle coverage

Fresh seeds `8303`, `8311`, `8317`, `8329` cross insertion and reflection.
Before the continuing reversal instance, three independent fresh namespaces
must establish:

1. a coupling-four A physical path executes once and normalizes to exactly one
   A trace, with zero pair opportunity/source/candidate/proposal/update;
2. two simultaneous scheduled A entries yield the lawful refractory single A
   traversal/trace, with zero pair activity/proposal/update; and
3. A followed by B two ticks later yields two primitive traces but zero
   pair source/candidate/proposal/update.

Each control must quiesce and have zero live candidates after bounded pressure.

The continuing instance repeats v1 unchanged: one supported exposure dies;
adjacent unsupported AB episodes traverse twice but receive zero updates and
die; recurrent AB/CD mature; pressure removes them after support changes;
fresh AD/BC identities mature; held-out behavior follows only AD/BC.

## Gate R v2 -- corrected execution-only accounting

Fresh seeds `8401`, `8419`, `8423`, `8431` cross insertion and reflection. The
three physical stages and schedules are unchanged from v1.

Training with a completed world return must fire the relay once per lawful
candidate execution and deliver one Modulatory crossing. Context-free reuse,
single-sided controls and gapped controls supply no world return and therefore
must serialize zero relay firings and zero Modulatory crossings. They must
still serialize exactly the expected ordinary output traces: final full reuse
is one X, one Y and one Z trace.

All other v1 recursion predicates remain frozen, including one-exposure death,
resistance trajectories, proposal counts, normalization amplitudes, exact
replay and quiescence.

## Execution and verdict

The two v2 implementations must be frozen before either matrix executes. Rust
formatting, compilation, tests, lint and runtime occur only in separate E2B
sandboxes. The matrices then execute concurrently in two new E2B sandboxes and
publish v2 artifacts once.

V2 development readiness requires both generated reports positive. Any further
failure is frozen. A joint positive permits a separate fresh PX3 authority
workflow; it does not promote PX3 automatically.
