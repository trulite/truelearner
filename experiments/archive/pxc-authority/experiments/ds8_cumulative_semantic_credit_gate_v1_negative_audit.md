# DS8 cumulative non-semantic-credit GATE v1 negative audit

Outcome: **VALID DEVELOPMENT NEGATIVE; M5 REMAINS AUTHORITATIVE**.

The exact implementation
`0bc976ce62e42f482633fb45de1075ba68ebb954` passed E2B formatting and focused
library compilation before the preregistered 18-cell matrix executed once in
sandbox `ie2usmlbtjokk80bkr6zr`. It exited `1` with a duplicate-exact negative.
No cell was rerun or rescued.

The main acquisition and allocation law generalized uniformly over six fresh
seed namespaces, four rotated stable consequence topologies, alternating
layouts, and distractor loads `8`, `32`, and `128`. All cells acquired and
executed the four-edge route `32/32`, admitted `4` useful encounters and only
`ceil(load/8)` distractors, reduced structural work by `58.33%` to `84.85%`,
and reversed allocation under both raw-history and learned-value shuffles.

The M4/M5 lifecycle also uniformly reduced a withheld useful edge from
resistance `105` to zero, physically removed it, and blocked stale execution.
The first unresolved downstream result was reacquisition: every cell reported
`0/32`. The separately conjoined retained-economy predicate was also false in
every cell.

These failures are not reinterpreted. A separately preregistered diagnostic
retry may expose, without changing the organism mechanism:

```text
post-age route prototype/value presence and resistance
post-age learned consequence direction
repair encounter admission
repair execution
repair differential application
retained proposal count and live route-edge count
```

It must stop at the first absent physical edge. Any repair may change only a
mechanically demonstrated wrapper/linker mismatch while preserving seeds,
loads, histories, thresholds, frozen M5/DS8 mechanism, and every passing v1
control.
