# LR-C qualified modulatory transmission authority handoff v1

Status: **LR-C AUTHORITATIVE; FRESH PX3 SUCCESSOR DEVELOPMENT ELIGIBLE**.

## Authoritative successor law

> An ARROW traversal has one of two physical effects. Drive may change CELL
> activation and cause execution but cannot directly change eligible
> structure. Modulatory traversal may change recently eligible outgoing ARROW
> structure at its target but does not change CELL activation and cannot itself
> cause execution.

This is a physical transmission distinction, not reward, correctness, credit,
outcome identity or causal metadata.

## Frozen lineage

- law candidate: `967ab7d` /
  `lrc-qualified-modulatory-candidate-law-v1`;
- definitive protocol: `26cd4d8` /
  `lrc-qualified-modulatory-definitive-protocol-v1`;
- definitive implementation: `39ec76064d8a088851a51673fa2f998324b20c5f` /
  `lrc-qualified-modulatory-definitive-implementation-v1`;
- positive evidence: `496/496` cells and `5,952/5,952` claims;
- active law SHA-256:
  `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10`.

The active-law SHA above refers to the frozen source path
`crates/lr1-modulatory-physical-return/src/lib.rs`. Historical PX0--PX2
authority remains intact, and fresh LR-C conformance passed for all three
cumulative capability surfaces.

## Successor physical stack

```text
retained CELL / ARROW / SPIKE physics
        ↓
LR-C Drive / Modulatory transmission effect
        ↓
PX0 physical correspondence conformance
        ↓
PX1 participation-attributed boundary-role conformance
        ↓
PX2 traversal-attributed causal-direction conformance
```

The first physical distinction between signals that execute and signals that
alter eligible learning is now authoritative in the successor stack.

## PX3 boundary

Fresh PX3 development may resume from LR-C. It must use the authoritative
physical distinction directly:

```text
joint candidate actually traverses
→ candidate becomes eligible
→ downstream physical consequence completes
→ a physical Modulatory return reaches the eligible source
→ structure may strengthen
→ the return cannot re-excite the source
```

PX3 may not introduce `CREDIT`, `REWARD`, `RETURN_FOR`, outcome identity,
contributor identity, Event objects, composite flags or level-specific APIs.
The physical topology must determine which Modulatory route traverses; the
mode supplies only its local effect.

Earlier PX3 development positives remain development evidence. The failed PX3
authority run remains negative and spent. A new integrated PX3 protocol,
implementation freeze and fresh authority matrix are required.
