# PX7 LR-C cumulative physical-arrival authority protocol v1

Status: **PREREGISTERED; SERIAL AUTHORITY EVIDENCE UNSPENT; PX8 FORBIDDEN**.

## Exact cumulative parent

The sole parent is PX6 authority commit
`1ca6df74eacbb743f23ca5b5810919985036cd64`, tagged
`px6-lrc-consequence-authority-v1`. This protocol is the first commit on branch
`research/px7-lrc-cumulative-authority`; its Git parent must be that exact
authority commit.

The retained serial stack is PX0 physical correspondence, PX1 physical roles,
PX2 causal direction, LR-C qualified modulation, PX3 reusable organization,
PX4 recurrence-earned persistence, PX5 participation allocation, and PX6
traversal-bound physical consequence. Retained active hashes are:

| artifact | SHA-256 |
|---|---|
| LR-C law | `7226a0e4af0ff484c6fd61c46c9073ce8363692100c2a090b0ce64483f3cfc10` |
| authoritative PX4 API | `a201674f9d558b5bda20aef71e9857b632f8a6565f372aee88994a280e0fea71` |
| PX6 authority CSV | `9e14b0f065ba37966c2ffc300f6d149b0847d092cb90e666456d3889d889d9c6` |
| PX6 authority report | `94a088fc732c24385a3b581af0e5cea2638645806c8bb8a73c81bfa39c9ec5a2` |
| PX6 active manifest v4 | `653289cf42577dabb242475fd88abe24405b3e9a7e3cd4f2961489cc5fe6953a` |

No isolated PX4--PX6 development artifact and no PX8 surface may be imported.

## Preserved isolated PX7 development

The isolated branch `research/px7-lrc-cumulative-development` and these tags
remain immutable and are referenced rather than cherry-picked:

```text
px7-lrc-arrival-initiation-development-protocol-v1
px7-lrc-arrival-initiation-implementation-v1
px7-lrc-arrival-initiation-probe-positive-v1
px7-lrc-arrival-initiation-micro-positive-v1
px7-lrc-arrival-initiation-gate-positive-v1
px7-lrc-pxc-taxonomy-v1
px7-lrc-pxc-readiness-comparator-v1
px7-lrc-development-readiness-v1
```

Frozen isolated artifacts establish the development reduction:

| artifact | SHA-256 |
|---|---|
| active PX7 source | `d248a8af479872d8148115a405ae7332f7d24ca229378d3fde898ffd3d19e63e` |
| evaluator source | `6c18448ff0552a0932a2daa69aad09cc8d5c77b4073eeb231dd14f19950b4751` |
| development protocol | `c6d0ef4e166de7e351a16e6804f200ab1cccda518f29cf2b6f2c3e95eedbed57` |
| positive GATE CSV | `bf1e263d05e4b01663cbf7ee8dc913048ccae81d80136699047a3606bb7338d3` |
| positive GATE report | `cbe5ffbe3199ef4d56819bf3b48212bfe28ba7230fc1fbe379cb089f08b13207` |

Only the active source and the scientifically frozen geometry/predicates may
be ported. Development identities, commands, reports, PX-C artifacts, handoff
prose, workspace edits, and publication behavior are forbidden residue.

## Retained physical claim

The authority candidate must add no substrate law. The complete organism path
is the retained physical reduction:

```text
anonymous position-addressed arrival
-> ordinary LR-C Drive participation
-> recursively matured physical organization
-> ordinary outward region crossing
-> natural quiescence
```

Maturation is repeated physical co-participation followed by actual downstream
return through the existing qualified Modulatory law. Held-out initiation may
supply boundary arrivals only. It may not supply return, composite, downstream,
outward, request, start, invocation, query, session, answer, reward, correctness,
route-selection, or level-selection input.

All boundary and composite participation uses one position-addressed arrival
method. Any need for a new stored scheduler, work token, lifecycle boundary,
semantic state, mechanism selector, field, mode, transition, eligibility rule,
plastic update, pressure rule, memory trick, or evaluator-conditioned organism
branch is a genuine scientific fork. Freeze the diagnostic and stop before
authority evidence.

## Fresh authority package and identities

The sole authority package is `arms/px7-lrc-arrival-authority`. Its direct
dependencies are exactly:

```text
px7-lrc-arrival
px4-lrc-lifetime
lr1-modulatory-physical-return
```

The PX7 crate itself depends only on the retained LR-C law. The PX4 dependency
is evaluator-side cumulative conformance only. The authority arm exports no
organism API.

The sixteen fresh roots are exactly `761001..761016`. Every physical namespace
is `root << 32`; changed-organization controls use `(root + 1_000_000) << 32`.
These identities are disjoint from isolated PX7 roots `7_700_001`,
`7_710_001..7_710_004`, `7_720_001..7_720_016`, PX6 roots `661001..661008`,
and every earlier cumulative matrix.

The root index crosses all four construction-order/reflection strata exactly
four times. Standard and altered organization occur twice in every stratum.
No state crosses roots.

## Twelve conjunctive row clauses

Every root executes twice from fresh complete state and must pass:

1. no-arrival silence;
2. subthreshold silence;
3. unmatched and outward-only causal controls;
4. weak-before-maturation versus mature boundary-only initiation;
5. overlap/distractor invariance;
6. three recurrent initiations;
7. retained-body resume after an unrelated naturally quiescent batch;
8. pressure-stale route suppression;
9. changed-organization discrimination with fresh identity/layout;
10. cumulative PX0--PX6+LR-C correspondence, participation, direction,
    recursive organization, persistence, allocation, and qualified-consequence
    conformance through actual observed traffic;
11. complete batch work `<=4096`, maximum persistent bytes `<=4096`, stable
    retained structure, level-blind participation, and natural quiescence; and
12. exact equality of the independently reconstructed duplicate execution.

The row threshold is `16 * 12 = 192` clauses.

## Six global clauses

The complete matrix must additionally establish:

1. all sixteen roots occur exactly once;
2. all four construction/reflection strata occur exactly four times and each
   contains balanced standard/altered organization;
3. all roots and changed-organization namespaces are fresh and pairwise
   disjoint from isolated/cumulative evidence;
4. every row passes cumulative PX0--PX6+LR-C conformance without a new law;
5. every row is work-bounded, memory-bounded, structurally stable, naturally
   quiescent, and exact on replay; and
6. frozen parent/protocol/source hashes, dependency closure, execution firewall,
   one-shot publication, and PX8 absence are exact.

The definitive conjunctive threshold is `16/16` rows and `198/198` clauses.

## Batched implementation and targeted validation

All source, Cargo, coverage, leakage, firewall, protocol-hash, and static-audit
edits must be batched before validation. No Rust, project program, or project
audit may run locally. Avoid workspace-wide or unrelated compilation.

One fresh E2B sandbox may perform the sole targeted frozen validation:

```text
package rustfmt check
strict package Clippy with warnings denied
one fully qualified no-world matrix-definition test
static hash/dependency/coverage/leakage/firewall audit
release --authority-preflight
```

Cargo may reuse that package build inside the single validation event. The
test and preflight may check only constants, hashes, identity cardinality,
namespace bounds, dependency closure, artifact absence, and the runner
firewall. They may not construct a body, call the row runner, emit an evidence
marker, or create a result artifact.

## One-shot authority execution

After the implementation/audit commit is frozen and tagged, one new E2B
sandbox executes exactly once:

```text
cargo run --release \
  --manifest-path arms/px7-lrc-arrival-authority/Cargo.toml \
  -- --authority-v1
```

The command rechecks all frozen inputs and no-world preflight, emits exactly
one evidence-spent marker before the first body, evaluates all sixteen rows and
their embedded duplicate states once, and atomically creates:

```text
results/px7_lrc_arrival_authority_v1.csv
results/px7_lrc_arrival_authority_v1.md
```

Overwrite, resume, partial selection, alternate roots, rescue, regeneration,
or rerun is forbidden. Failure, interruption, or malformed/missing output is
an immutable negative or incomplete authority result.

## Serial PX-C gate against PX6

Only after functional evidence is frozen may manifest v5 replace exactly the
PX7 predecessor row in manifest v4 with the new active PX7 source. Every
PX0--PX6 and PX8 row remains byte-identical.

The immutable before side is the PX6 authority taxonomy under manifest hash
`653289cf42577dabb242475fd88abe24405b3e9a7e3cd4f2961489cc5fe6953a`.
Required after conditions are:

```text
primary seams                         < 246
semantic guard                       <= 83
evaluator guard                      <= 318
PX0--PX6+LR-C aggregate foundation       0
new seam kinds                           0
new guarded surfaces                     0
unclassified active files                0
```

Taxonomy must run in fresh E2B with the exact v5 manifest hash. The comparator
must consume the raw immutable PX6 inventory, guard inventory, and summary,
require strict primary decrease, and serialize exact kind/layer/novelty deltas.

## Authority decision and successor stop

PX7 authority may be claimed only after the one-shot `198/198` result,
complete resource accounting, stable memory, natural quiescence, exact replay,
cumulative PX0--PX6+LR-C conformance, complete coverage/leakage proof, and
serial PX-C comparator all pass. Separate result-audit and clean authority
handoff commits/tags are required.

The handoff may establish PX7 only. PX8 must not be preregistered, prepared,
executed, reinterpreted, or advanced.
