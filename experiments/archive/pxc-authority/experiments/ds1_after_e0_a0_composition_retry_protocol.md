# Byte-identical frozen DS1 after DS-E0 + DS-A0 composition retry preregistration

Protocol identifier: `ds1-after-e0-a0-composition-retry-v2`

Status: **PREREGISTERED BEFORE IMPLEMENTATION**. This is a development-only
MICRO/GATE composition attempt. It cannot run definitive, write under
`results/`, support a cumulative scientific claim, or create authoritative M1.
M0 remains authoritative throughout.

## Exact lineage and immutable boundary

- exact readiness parent and tag:
  `ad503db3855884b8cab85903edfacfc753fa746e` /
  `ds-a0-anonymous-boundary-action-formation-readiness`;
- authoritative M0: `1d74c0ed0b515446161a63a6d43ecbe27514dc85`;
- frozen DS-E0 source: `src/ds_e0_anonymous_event_formation.rs`, SHA-256
  `fc5d426cc8a5116dbd2749b914e6c30db88529d3070a844a20fc76ac88782615`;
- accepted DS-A0 mechanism: `src/ds_a0_anonymous_boundary_action_formation.rs`,
  SHA-256 `3eb802f394a225a4ad7f0938b4a672723da2c1303ff95e805423de8161057527`;
- frozen marked DS1 learner, inclusive `DS1_LEARNER_BEGIN` through
  `DS1_LEARNER_END`: SHA-256
  `adec6a422e69e7f90bff6482776ea9aa91ae89e5e8d59183f6228165f9f7ff0e`;
- frozen M0 and compiled correspondence sources:
  `50cf169bb293177a35270adde656f28f98e68c83a4d39d2876399261b7ee697c`
  and `430cd2206c8baa7106c4de7f203d4d0c48b544290e6266596ebcdb91d02655c9`;
- prior unchanged-DS1 stage-4 audit source:
  `a4deadedfde7b9896d64d0cacd41560441ea85cf3bda119a5d09aa3aaddcd7a0`.

Those bytes and all earlier frozen sources are read-only. The retry may not
change DS1 learning, state, inference, consolidation, invalidation, choice, or
consequence semantics, and may not change E0 or A0 to accommodate DS1.

The sole question is whether the already-existing E0 serialized relational
value, the already-installed A0 executable roots, and the unchanged DS1 call
surfaces compose without supplying any missing semantics. This protocol does
not authorize a rescue or the next dependency.

## Only legal wiring

The implementation may add one narrow composition harness/runner plus audits
and handoff documents. It may compile byte-identical E0 and A0 mechanisms in a
private composition scope and add composition-only access shims in that scope.
Those shims may only:

1. return the `Neighborhood` produced by the existing E0-A `form` followed by
   the existing format-only E0-B `serialize_once` path;
2. return one opaque handle per root already installed by the existing A0
   `form_routes` path, after formation and before bridging;
3. pass the opaque alternative index selected by unchanged DS1 mechanically to
   the corresponding existing handle and invoke the existing A0
   `execute_handle` path.

No shim may construct, name, rank, inspect, or interpret an action; inspect an
effect to create credit; expose a concrete root/destination to DS1; or add a
consequence path. E0 fields must be one-to-one copies from the already-existing
`EventRelations`. A0 inventories and mappings must be derived from the actual
installed root/bridge state, never fixture/evaluator truth, a hardcoded
availability boolean, or a literal absence count.

## Forbidden mechanisms

No post-action consequence wiring, consequence adapter, evaluator correctness,
rank, source/target/boundary truth, `SAME`/filler leak, stable occurrence token,
hidden executor, isolated-DS1 adapter, manufactured history, or token-to-Rust
operation switch is permitted. At least two distinct A0 physical effects must
exist before choice, but their contents may be used only by evaluator-side
post-hoc controls and never fed back to DS1. No DS2+ work is authorized.

## Ordered composition stages and stop rule

The first failed or unavailable stage is frozen exactly; every later stage is
`BLOCKED` and no repair is attempted.

0. exact lineage and immutable hashes;
1. actual DS-E0 learned temporary relational structure exists;
2. format-only E0 `Neighborhood` serialization is sufficient and byte-identical
   frozen DS1 consumes it;
3. actual DS-A0 plastic route formation occurs before bridge;
4. opaque alternatives are greater than one, one-to-one to real installed
   roots, and reach the frozen DS1 choice surface without semantic translation;
5. frozen DS1 chooses an opaque alternative;
6. the selected root executes through ordinary live CELL/ARROW/SPIKE
   propagation;
7. a natural post-action consequence becomes visible to frozen DS1 through an
   already-existing parent-substrate path only;
8. frozen DS1 acquisition/update occurs through unchanged `apply_consequence`;
9. held-out boundary-role reconstruction and functional controls pass;
10. invalidation, reopening, and reconsolidation pass;
11. recursive compatibility/economics passes only if already naturally
    available; otherwise it is `BLOCKED`, never synthesized.

In particular, if stage 7 has no existing path, the exact outcome is
`CUMULATIVE DS1 DEVELOPMENT COLLAPSE AT 7. natural post-action consequence`
and stages 8..11 are `BLOCKED`.

## Controls, instrumentation, and mutation audit

MICRO uses seed `100`; GATE uses seeds `100..104`. Each seed is deterministic,
single-threaded, compact, and performs no hot-loop I/O. Acquisition/evaluation
depths remain tiny and may reuse the already-frozen E0/A0 development depths.

The audit must cover fresh occurrence identities; relabel, allocation, layout,
and handle permutation; E0 membership and A0 roots derived from learned state;
two pre-choice distinct physical effects; opaque-only DS1 alternatives; and
semantic invariance under handle permutation except through the actually
chosen route. Persistent state must not retain occurrence identities, concrete
destinations, filler tokens, or evaluator tokens.

Choose calls, action-to-execution paths, `apply_consequence` calls,
consequence paths, and update counts must be mechanically derived from source
call graph and runtime instrumentation. A derived no-path zero must be tested
against a mutated inventory that introduces a path; it may not be assigned as
evidence. The handoff must state the exact dependency delta from the prior
stage-4 collapse.

## Execution lock and validation

`--definitive` must reject in the runner before any harness call and must leave
the `results/` digest unchanged. Permitted validation is local and persistent
E2B format, strict release Clippy, focused release tests, release MICRO, and
release GATE. Broad legacy regression is omitted because shared/frozen behavior
must remain unchanged.

Dedicated E2B state is exclusively:

```text
/Users/satya/.cache/truelearner/ds1-after-e0-a0-cumulative-e2b.json
```

The existing persistent E2B helper and credentials may reuse or create the
sandbox. It must be left running and never killed.

## Freeze vocabulary and handoff

If every permitted development stage passes, the only readiness label is
`M1_ELIGIBLE / cumulative definitive pending`. Otherwise the label is
`CUMULATIVE DS1 DEVELOPMENT COLLAPSE AT <exact first stage>`. Neither is a
scientific result, neither changes M0 authority, and neither creates M1.

The final freeze must include: this protocol commit/tag; immutable
fingerprints; ordered stage report; exact first collapse; mechanically derived
action/consequence call-path and update inventories; leak/control and mutation
audits; dependency delta; physical work/storage ledger; targeted local/E2B
validation; exact freeze commit/tag; blockers; a clean worktree; and the
explicit statement **M0 `1d74c0e` remains authoritative and M1 is absent**.
