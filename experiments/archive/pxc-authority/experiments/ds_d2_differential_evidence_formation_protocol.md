# DS-D2 anonymous differential-evidence formation protocol

Status: **PREREGISTERED DEVELOPMENT GATE; ENABLING ONLY; NOT CLAIM ELIGIBLE**

Exact parent: `353285fda96061bdcab640e53d77e710be966f06` /
`ds-d1-stage8b-functional-sufficiency-complete`. M0 remains authoritative and
M1 remains absent.

## Sole question

> Given two actual learned anonymous A1 alternatives and anonymous returned
> activity, can ordinary local structural consistency form one temporary
> directional relation without supplied good/bad, expected/unexpected,
> chosen/unchosen, or correct/incorrect semantics?

DS-D2 stops at the directional relation. It must not call frozen DS1, update
strengths, or test boundary-role recovery.

## Frozen physical input

Use the exact frozen E0+A1 lineage. For each seed:

1. E0 forms the actual anonymous event;
2. A1's existing plasticity installs two structurally distinct executable
   CELL/ARROW roots;
3. each live route exposes its role-relative local execution trace through
   ordinary root-spike propagation in a temporary counterfactual branch;
4. one actual selected route emits returned anonymous activity;
5. DS-D2 compares only local trace/activation structure.

The evaluator may verify which root produced a trace but may not supply that
identity to the differential mechanism. The mechanism receives only two
anonymous structural predictions and one anonymous returned structural trace.

## Formation rule

For each alternative, local equality/coactivity comparisons produce a
structural compatibility score. Form one temporary directional ARROW only if:

- exactly one alternative is structurally compatible with the returned trace;
- the other is not compatible;
- all compared structure is role-relative and contains no occurrence identity.

Equal support, support for neither, or duplicate predictions must abstain.
The directional result is an anonymous alternative index local to the current
temporary bridge; it is not favorable/unfavorable polarity.

## Required controls

- evidence equally compatible with both alternatives: abstain;
- evidence compatible with neither: abstain;
- swapping anonymous alternative order swaps the direction;
- fresh occurrence identities preserve the relational result;
- allocation and handle permutation preserve the represented relation;
- shuffled/corrupted evidence forms no stable direction;
- same total activation magnitude with reversed structural relation reverses
  direction;
- duplicate route/effect abstains;
- removed route invalidates its direction;
- all temporary directional state is erased;
- persistent state contains no occurrence, handle, destination, selected index,
  evaluator effect, truth role, or semantic polarity.

## Outputs and stopping rule

Report separately:

- actual A1 alternatives available;
- unique structural compatibility;
- one temporary direction formed;
- all controls;
- work and temporary/persistent storage;
- DS1 calls and updates, both required to remain zero.

MICRO seed 100 and GATE seeds 100..104 are development-only. `--definitive`
must reject with status 2. Validate focused formatting, strict Clippy, tests,
MICRO, and GATE locally and on E2B using only
`/Users/satya/.cache/truelearner/ds-d2-differential-evidence-e2b.json`; leave
the sandbox running. Preserve results digest
`491a63c17ba35d768b630720063793a4db09686cfe7cb33694fd80ea63bbd4e4`.

A positive DS-D2 does not create M1. Only a separately preregistered unchanged
DS1 cumulative retry may test whether the learned directional relation is
interface-sufficient and useful.
