# CJ1 shared-path fixture correction protocol v1

Status: **PREREGISTERED MECHANICAL CORRECTION; EVIDENCE UNSPENT**.

The immutable unchanged-physics PROBE v1 remains negative at its earlier valid
`mature-one-path` row. Its final `two-origins-shared-path` row is invalid for
the exact construction defect frozen in the result audit: an unconditional
A->locus ARROW remained alongside A->shared, B->shared and shared->locus.

This correction changes only topology construction for one fresh row. In the
corrected world:

- A and B are separate threshold-2 source CELLs;
- A->shared and B->shared are the only outgoing source paths;
- the threshold-2 shared CELL has exactly one outgoing shared->locus ARROW;
- shared->locus is the only incoming ARROW to the threshold-2 local CELL;
- A and B fire together with matched total entered impulse 4;
- the required result is two source-to-shared traversals, one shared-to-local
  traversal, one local arrival, zero local firings and zero effects;
- the same instance is exercised held out after ordinary decay;
- exact duplicate replay, natural quiescence, work/storage and fingerprints
  are required.

The authoritative PX0 law remains exact at SHA-256
`3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`.
The frozen PROBE artifacts remain exact at SHA-256
`82840f4e16063da3301710b2524299b6092f34695850a7499667f38cd88b481e`
and `506e18f9443ddd722ca17d68117e5da555e323c8921bf5b0fe69270962934b31`.

The correction has a fresh command and fresh create-new artifact names. It may
not change a scientific expectation, rerun any prior row, add a law or advance
to candidate evidence. After a passing frozen correction, the sole candidate
already preregistered in the main protocol may be implemented.
