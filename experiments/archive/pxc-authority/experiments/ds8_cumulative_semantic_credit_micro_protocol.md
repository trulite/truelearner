# Cumulative DS8 non-semantic-credit MICRO

Protocol identifier: `ds8-cumulative-semantic-credit-micro-v1`.

Status: **PREREGISTERED DEVELOPMENT MICRO; OUTCOME UNSPENT**.

Exact positive PROBE parent:
`54bd82920ad675afec54762ffefd2b616008a865` /
`ds8-cumulative-semantic-credit-probe-positive`.

Frozen inputs:

- positive PROBE source SHA-256:
  `11b4229122b3e0788ca30c55579b91ffe07461de9a138860690134565fcf2ed6`;
- exact organism-path fragment SHA-256:
  `7d9bfa61ccfcb7e99d5863306920a5a1700aa395e33901a70b76d5ccad27aa17`;
- positive result/audit/handoff SHA-256:
  `a1acd9c35cb5da4ab1c9aa341926dae6d8d8cb5e10e216be2089879328d8404d` /
  `00f788e2b3a6930fed7ec69626062f5d6631167002a179cc96e7499d5280502a` /
  `2653b6db073e3241a4d98e223cbc545826b57ecf272f2bc073f58dac98060087`.

## Sole question

> Does the byte-identical non-semantic consequence linker acquire selective
> M5 allocation under two different temporal histories, while preserving its
> physical attribution and perturbation controls?

## Frozen cells

Two fresh blank-start cells are fixed:

```text
seed 41000000   interleaved histories
seed 41500000   blocked history: varying consequence first, recurrent second
```

Each cell uses fresh M5 identities, fresh consequence occurrences, and an
alternating absolute layout. The physical mechanism fragment must be extracted
byte-identically from the positive PROBE source at build time.

## Conjunctive requirements

Each cell passes only if:

1. both encounter histories form and execute from blank state;
2. every executed variation produces exactly one delayed three-spike,
   two-arrow consequence and suppressed variations produce none;
3. one recurrent-versus-varying structural contrast forms despite equal total
   consequence magnitude;
4. the recurrent-consequence history receives more differential support and
   at least `7/8` future admissions, while the other receives at most `1/8`;
5. swapping raw consequence histories reverses future allocation;
6. shuffling learned M5 values reverses or destroys allocation;
7. fresh consequence occurrence relabeling and fresh endpoint identities do
   not change the result;
8. reversed absolute layout preserves the learned law;
9. absent execution, immediate consequence, equal stability, shuffled
   consequence structure, and removed eligibility abstain;
10. source audit finds the exact frozen fragment and zero forbidden semantic
    channel;
11. authoritative M5 remains exact;
12. the complete two-cell report is duplicate-exact.

Run formatting and focused compilation in the dedicated E2B development
sandbox, then execute MICRO once in release mode. No definitive artifact or M6
claim is authorized. PASS makes cumulative GATE eligible; FAIL freezes the
first collapse without rescue.
