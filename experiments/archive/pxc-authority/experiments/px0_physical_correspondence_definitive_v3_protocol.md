# PX0 substrate-native physical correspondence definitive v3 protocol

Status: **PREREGISTERED; V3 DEFINITIVE EVIDENCE UNSPENT; PX0 AUTHORITY ABSENT**.

PX0 definitive v1 and v2 remain immutable failures. V3 is not a correction,
rerun, rescue, or reinterpretation of either matrix. It is a fresh authority
experiment whose measurement semantics are motivated by the separately frozen
PX0-D2 accounting-only result.

## Frozen starting point

The authority workflow begins exactly at D2 handoff commit
`19bbaa9e2e670f51bbaacd1a32fe9707ad519d81`, tag
`px0-d2-dense-corner-handoff-classification-a`.

| frozen artifact | SHA-256 |
|---|---|
| active PX0 law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |
| retained physics | `6aa28a76e1362ac8dfb1d33fb68807da40e7604dfdc8cca9efa1e314e3ce4263` |
| definitive v1 CSV | `da356bc46a9d83d0cd749bcaa697cba66393b7d694de500e2208565806d680d1` |
| definitive v1 report | `7e2c06d63332a680d46031c49d5dc245c6a4f381d7c646a2b0474580469a09b7` |
| definitive v2 CSV | `486970d22b7282c8a89b2f06b811644841b6b905563c9e12e676b3da33150119` |
| definitive v2 report | `460d183d9cfb8e0f244209801b4cb4f140dc474089120473fd1de5bf738aba33` |
| definitive v2 audit | `c52cd71b61d32d379412cbd423f226f3e0784c876f012dcb65d25304a059b815` |
| definitive v2 handoff | `63cddb2f6ef8113ef1ee7f9d87ddc217b35df95b7c3d62dad0bf826429ffff33` |
| D2 cell matrix | `2314704d08f318c394e07c3eb6bc856a1b958c6d8d45bb173d044aba0eea02ac` |
| D2 trajectory ledger | `2a55880a0ca822e0b6d691c5ec0ccd7f83360bf80fbadfe3576042bf9feb4828` |
| D2 result audit | `c34fe93cf7666f1be84c7978bcbe5ef98a0cd54d9714407603da6bbfa2e69f80` |
| D2 handoff | `b914bd244e44db5d0e23a029581780646f271e1a0c623a449f0e5c708f0917b4` |

The active law and retained physics may not change. V3 may add only fresh fixed
worlds, evaluator-only cloned observations, audits, and write-once authority
serialization.

## Authority target

> Recurring anonymous physical configurations can acquire, retain, forget, and
> reacquire executable correspondence from physically reliable returned
> activity. Return-free and sparsely incidental alternatives do not remain
> reusable, while reliable return remains learnable across dense, changing
> topology.

V3 does not require every offered return to complete. It records opportunity
and completion separately. Authority concerns durable reusable physical
correspondence, resistance/lifetime separation, and behavior.

## Exact fresh matrix

The matrix contains exactly 24 blank deterministic cells. Cell `i` uses base
namespace `0x16000000 + i * 0x100000`; no cell, control, or spike identity
appeared in previous development or authority evidence.

The fixed matrix crosses:

- all three initial/current/spare route rotations;
- normal and reverse allocation;
- direct and mirrored layout;
- spacing `10..=19`, including the D2 spacing-17 boundary;
- route stride values from `12,16,20,24,25,27,28`;
- dense distractor loads from `0,4,16,32,36,44,48`;
- all four incidental phases;
- four support-device identities/delays in rotated and reversed orders;
- alternating physical arrival order.

The matrix includes fresh configurations near both v2 and D2 boundaries but
reuses no spent complete physical state.

## Continuous primary history

Each primary fixture runs without reset:

1. initial A acquisition from four ordinary return experiences;
2. held-out A execution;
3. bounded-absence reuse of the same live A identities;
4. full forgetting through ordinary pressure;
5. stale A attempt with zero crossing and fresh weak reproposal;
6. changed-world experience: eight four-context cycles with a stable B return
   opportunity every context and a genuine dense incidental A opportunity in
   one phase per cycle;
7. three-context tail without the incidental A driver;
8. final cloned A/B behavioral tests and exact duplicate replay;
9. evaluator-only cloned no-use lifetime measurement.

Independent fresh fixtures test return-free probation, recurrent dense return,
stable-source swap, absence, and equal ambiguity.

## Independently serialized observables

Every cell row must contain separate columns for:

- stable return opportunities;
- completed stable returns;
- sparse return opportunities;
- completed sparse returns;
- first mature B context;
- number of B-executable observed contexts;
- stable final resistance;
- sparse final resistance;
- stable no-use deallocation delay;
- sparse no-use deallocation delay;
- stable held-out effect;
- sparse held-out effect;
- sparse eventual deallocation;
- return-free proposals, returns, effects, and deallocation;
- recurrent dense-path returns and effects;
- stability-swap returns and effects;
- absence effect;
- ambiguity effect and live alternatives;
- old/fresh identities and old-path liveness;
- proposals, deallocations, queue comparisons, total work, storage, complete
  fingerprint, permanent fingerprint, replay, and quiescence.

Every pass bit below is also serialized as its own CSV column. No compound P6
or hidden subclause is permitted.

## Twenty-four conjunctive claims

1. `P0` frozen hashes and exact fresh matrix parameters;
2. `P1` initial A physical structure acquired;
3. `P2` initial A held-out effect equals one;
4. `P3` bounded absence preserves and reuses identical live A arrows;
5. `P4` ordinary pressure fully deallocates original A;
6. `P5` stale original A produces zero effect and time creates no proposal;
7. `P6` renewed activity creates fresh identities distinct from original A;
8. `P7` stable-return opportunities equal the fixed 35-context world schedule;
9. `P8` completed stable returns are recorded and exceed completed sparse returns;
10. `P9` sparse-return opportunities equal the fixed eight-context schedule;
11. `P10` B reaches executable maturity within the observed 35 contexts;
12. `P11` final stable resistance is strictly greater than sparse resistance;
13. `P12` stable no-use lifetime is strictly longer than sparse no-use lifetime;
14. `P13` final held-out B effect equals one;
15. `P14` final held-out sparse A effect equals zero;
16. `P15` sparse A fully deallocates under continued no-use pressure;
17. `P16` genuinely return-free proposals receive zero return, never execute, and deallocate;
18. `P17` a recurrent dense physical return path is learned and executes;
19. `P18` swapping the stable physical return source swaps retained execution;
20. `P19` absent return produces zero executable correspondence;
21. `P20` equally recurrent ambiguity keeps both alternatives viable but produces no privileged simultaneous crossing;
22. `P21` original arrows remain stale and contemporary structure has fresh physical provenance;
23. `P22` exact complete-state duplicate replay and fingerprint equality;
24. `P23` every propagation is naturally quiescent and work/storage/proposal/deallocation accounting is complete.

The outcome is conjunctive: `24/24` cells and `576/576` claims must pass. Any
single claim failure is an immutable definitive v3 negative.

## Measurement semantics

Return opportunity is an evaluator description of a physically offered support
path. Completed return is an observed spike reaching the relevant source within
ordinary dynamics. Neither value enters the organism.

No minimum completion fraction is supplied beyond `P8`: completed stable
return must be nonzero and strictly exceed completed sparse return. Maturation,
resistance, lifetime, and execution are tested independently. A lawful early
occlusion therefore appears as latency/accounting data rather than being
silently treated as behavioral failure.

## Anti-cheat and isolation

Organism-visible state remains only CELL/ARROW/SPIKE topology, timing,
thresholds, local plasticity, physical return, pressure, and deallocation.
Forbidden active representations include relation/motif objects, episode,
history, query, correct/wrong, probation, reusable, stable/incidental class,
return counter, maturity label, evaluator mutation, or renamed equivalents.

Evaluator behavioral probes and lifetime measurements operate only on exact
clones and have zero causal path back into the primary fixture. The wrapper may
observe but never mutate learned arrows.

The crate remains dependency-free. Active-law, retained-physics, v1/v2
negative, PX0-P1/PX0-S, and D2 hashes must be exact.

## Pre-evidence validation

The committed/tagged implementation must pass formatting, focused compilation
and tests, strict Clippy, zero-dependency/source audits, every frozen hash,
fresh-namespace audit, no-cell `--preflight-v3`, refusal without the v3 flag,
and absence of final/staging artifacts. No cell or evidence marker may run
during validation.

## Sole execution and no rescue

The exact command executes once:

```text
cargo run --release -p px0-physical-correspondence --example definitive -- --definitive-v3
```

After preflight it emits exactly one
`PX0_V3_DEFINITIVE_EVIDENCE_SPENT` marker, executes cells `0..23` literally,
and publishes complete PASS or scientific FAIL artifacts atomically. No rerun,
rescue, regeneration, tuning, or protocol amendment is allowed after the
marker.

## Atomic artifacts

The preregistration digest of sorted pre-existing result hashes is
`c84d00e8dbcea86add2537d3be2c00f241c5c5db5e91176d7b04800fd2a8ceb7`.
These paths are absent:

```text
results/.px0_physical_correspondence_definitive_v3.csv.staging
results/.px0_physical_correspondence_definitive_v3.md.staging
results/px0_physical_correspondence_definitive_v3.csv
results/px0_physical_correspondence_definitive_v3.md
```

## Program consequence

PASS makes PX0 authoritative and unblocks cumulative PX1 development. PX1 must
consume PX0-produced physical structure directly in the same substrate; old
M0/M1 schemas and behavioral adapters remain forbidden.

FAIL leaves PX0 non-authoritative and PX1, PX-C, the continuous organism, and
Harness H1 blocked. Neither result authorizes changing the frozen law.
