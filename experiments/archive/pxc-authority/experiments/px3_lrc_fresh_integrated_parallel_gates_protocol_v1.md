# PX3 on LR-C fresh integrated parallel gates protocol v1

Status: **PREREGISTERED DEVELOPMENT; BOTH GATES UNSPENT**.

Start: clean authoritative LR-C v2 result `c087d7c` /
`lrc-qualified-modulatory-definitive-positive-v2`.

## Hypothesis

Reusable event-like organization can grow from overlapping unit PX1
participation traces when actual candidate traversal creates eligibility and a
completed downstream physical consequence returns through a Modulatory ARROW.
Ordinary renewed Drive may execute the source but cannot strengthen eligible
structure. Mature organization emits one ordinary normalized participation
trace and can recurse without level-specific representation.

R3, R4, R5 and R6 are diagnostic history only. Neither gate contains their
P/effect attribution traces, return accumulator, trace-readout detector or
excitatory echo geometry.

## Shared physical chain

```text
two participant traces -> threshold-two opportunity O
O unit + uniform background unit -> threshold-two source P
P fires -> native local P->effect proposal/traversal -> eligibility
candidate unit + uniform context unit -> downstream effect
effect unit + physical world-return unit -> return relay
return relay -> Modulatory ARROW -> P
P's eligible candidate strengthens; P activation is untouched
```

All ordinary computation ARROWs are Drive. Only relay-to-P ARROWs are
Modulatory. A relay can fire only after both the candidate-produced effect and
the ordinary world-return path physically participate. The mode has no reward,
correctness, pair identity or outcome semantics.

Every primitive and derived participant uses the same PX1 normalization motif:
one outlet execution sends two unit arrivals and yields exactly one unit trace
firing independent of upstream coupling.

## Parallel gate L -- lifecycle and reversal

Four fresh seeds cross reflection, allocation and insertion order. Six pair
opportunities over A/B/C/D exist symmetrically; uniform background, context and
world return reach every corresponding local structure.

Registered controls:

1. strong A alone, repeated A and gapped A then B never fire a pair source;
2. one supported AB exposure executes but its candidate dies under pressure;
3. two immediately adjacent AB episodes without context/world return create
   zero plasticity updates and the candidate dies--the exact old authority-v1
   counterexample;
4. recurrent supported AB and CD mature and survive bounded pressure;
5. after support changes, old AB/CD receive no Modulatory return and deallocate;
6. recurrent supported AD and BC create fresh identities and mature; and
7. final context-free held-out behavior follows AD/BC only.

One continuing reversal instance uses the frozen MICRO timing with each lawful
relay-to-P modulation arriving while candidate eligibility is live. No
evaluator inserts, strengthens, revives or deletes candidate ARROWs.

## Parallel gate R -- recursive compression

Four disjoint fresh seeds instantiate three identical joint stages:

```text
A trace + B trace -> X outlet
X trace + C trace -> Y outlet
Y trace + D trace -> Z outlet
```

Each stage has identical O, P, native proposal, candidate, context/effect,
world-return relay and Modulatory return topology. X/Y/Z outlets use the same
normalizer as A/B/C/D. No active state contains primitive/composite, event,
member or level fields.

Registered controls require:

- one exposure at each level dies under pressure;
- recurrent completed loops mature each level;
- mature coupling 1, 2 or 4 normalizes to one derived trace firing;
- X+C and Y+D form only when their two unit traces overlap;
- gapped and single-sided histories do not form or execute the higher stage;
- the final context-free chain produces exactly one X, Y and Z trace; and
- all three levels have the same proposal, eligibility, modulation and
  execution API.

## Evidence and verdict

Both implementations must be frozen before either result runs. Rust formatting,
compilation, tests, lint and execution occur only in two fresh E2B sandboxes.
The two development commands may run concurrently and each publishes once.

Every row serializes candidate identities/generations, proposals, actual
traversals, source/effect/relay firings, Drive and Modulatory deliveries,
plasticity updates, resistance/coupling/liveness, normalization, held-out
behavior, work, storage, fingerprints, replay and quiescence.

PX3 development readiness requires both gates positive. Any failure is frozen
without tuning or rerun. A joint positive permits a separately fresh PX3
authority workflow; it does not promote PX3 automatically.
