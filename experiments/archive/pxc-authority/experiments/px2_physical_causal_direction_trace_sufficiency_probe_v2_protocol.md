# PX2 physical causal direction trace-sufficiency PROBE v2 protocol

Status: **PREREGISTERED MEASUREMENT-ONLY SUCCESSOR; EVIDENCE UNSPENT; PX2 NON-AUTHORITATIVE**.

## Immutable predecessor

PROBE v1 remains an immutable `4/5` negative. Its CSV SHA-256 is
`bfc2963d2afcda7de28fc6c7e2636f34800a2b7e3fa51056319bc71fe8bb0de9` and
its negative-audit SHA-256 is
`45c6f331fe7d29403314495f9db532ef85ec03b4d6f32469d3d8f3d995982cb8`.

V1 established the forward, reverse, correlation-only, and joint physical
discriminations. The blocked-return control failed only because its evaluator
predicate demanded eight consequence executions from an unsupported reserve-3
continuation.

## Frozen physical experiment

V2 keeps byte-identical PX0/PX1 physics, topology, candidate reserve,
thresholds, couplings, delays, timing, five worlds, acquisition history,
experience count, and all serialized stages. It uses fresh namespaces and new
write-once result paths.

No mechanism or physical experience changes.

## Sole corrected predicate

In participation without global return, require:

- participating continuation fires eight times;
- its weak consequence path executes at least once before lawful decay;
- the independent matched consequence executes eight times;
- each actual consequence firing supplies exactly one direct trace arrival;
- trace firing is `[0,0]`;
- local return is `[0,0]`;
- candidate resistance ends `[0,0]`;
- held-out and post-gap execution are `[0,0]`.

The observed execution count is serialized. V2 does not prescribe continued
execution after an unsupported candidate physically deallocates.

Every other v1 predicate remains exact: both consequences/traces/local returns
in the forward, reverse, correlation-only, and joint worlds; directional
resistance and held-out execution; PX0 correspondence; zero source refiring;
quiescence; work/fingerprint accounting; and exact replay.

## Pass and stopping rule

All `5/5` fresh worlds and every clause must pass. A failure is frozen without
rescue or rerun. A positive result makes a mirrored/fresh-layout MICRO eligible
but does not authorize MICRO execution, GATE, definitive evidence, PX2
authority, or PX3.
