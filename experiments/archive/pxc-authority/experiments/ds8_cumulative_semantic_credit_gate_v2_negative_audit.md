# DS8 cumulative non-semantic-credit GATE v2 negative audit

Outcome: **VALID DEVELOPMENT NEGATIVE; M5 REMAINS AUTHORITATIVE**.

The exact implementation
`c274cbae56e5b4604f202d68e7d82afc2bf564b4` passed E2B formatting and focused
library compilation before the 18-cell matrix executed once in sandbox
`ie2usmlbtjokk80bkr6zr`. It exited `1` with a duplicate-exact negative. No cell
was rerun or rescued.

The added seventh ordinary pressure tick reduced unused retained proposals to
`5`, `8`, and `20` at loads `8`, `32`, and `128`, while all four route edges
remained live. Retained economy therefore passed in all cells. Every other v1
passing result remained exact.

Repair instrumentation localized the remaining collapse after admission and
execution but before differential application. Route prototype and value
resistance remained high after aging, so M4/M5 state was not missing.

The frozen exploration period is eight encounters. The v1/v2 varying
consequence fixture used topology `sweep mod 4` during retained training.
Sparse distractor executions therefore sampled sweeps separated by eight and
repeated the same topology. From the learner's lawful observations, the
distractor consequence became recurrent. Both route and distractor histories
then met the physical repeatability threshold, so the frozen consequence
linker abstained and differential application returned false.

This is a valid aliasing result, not a learner error. A mechanical retry may
change only the evaluator-side varying-topology schedule to a period coprime
with eight while still using all four equal-magnitude topologies. It may not
change M5 exploration, the DS8 learner/linker, thresholds, seeds, loads,
lifecycle, or pass criteria. The v2 negative remains immutable.
