# Cumulative DS7 plasticity-allocation path-existence PROBE

Protocol identifier: `ds7-cumulative-plasticity-allocation-probe-v1`.

Status: **PREREGISTERED DEVELOPMENT PROBE; OUTCOME UNSPENT**.

Exact parent/audit commit:
`72b1935c51f78900fcc401d1dadcf20c06afe438`.

Authoritative M4:
`8db47281a7c9c97cbb52ced6fc3dcff0e7efa9b2` /
`m4-cumulative-learned-lifetime-authoritative`.

Frozen inputs:

- activation handoff SHA-256:
  `f7290c939c54b78986596e937d4932335f995766f292c613eb522f552bb3e892`;
- dependency audit SHA-256:
  `40305dd998b5fe80db9d4fcceee154288ea523f90bc57f26079025a7492b2509`;
- dependency manifest SHA-256:
  `138827ef5e9d761cd7cb58a672ed4a4776618d5eb56f5f4e88852ce18cb67504`;
- P2/P2.1/P2.2 source SHA-256:
  `704f757888d9b3bc89a5a3f5387f3422efb9dd4c746e3784506411b1da763b15`;
- M4 lifetime source SHA-256:
  `3d5659fb26ae804dee6122408f9d703ea1f226349772883075a42686ac3fd110`.

## Sole question

> Does a physical path exist from ordinary local coactivity, through an M4-
> governed local variation and delayed active-path experience, to learned
> allocation of future plasticity—without supplied endpoint classes,
> candidate sites, proposal lists, lifetime classes, or evaluator choice of
> where change occurs?

This is a one-seed, very-small path-existence diagnostic. It is not a MICRO,
GATE, definitive matrix, or M5 claim.

## Frozen seed and blank start

The only seed is `20_000_000`, disjoint from all DS6 development and definitive
namespaces. Every run begins with:

```text
zero proposals
zero encounter prototypes
zero encounter-value records
zero eligibility traces
fresh opaque endpoint identities
fresh local layout
```

The implementation must be duplicate-exact within the same process.

## Frozen physical port

The probe may reuse only the following mechanics:

1. simultaneously active endpoints within a fixed local radius create an
   encounter opportunity;
2. the encounter snapshot is built from observed spike-arrival provenance,
   local occupancy/live adjacency, and scalar use/pressure state—not endpoint
   kind, role, task phase, future route, or evaluator identity;
3. P2.2-style recurring exact snapshots recruit encounter prototypes;
4. every proposal, prototype, and value record starts at scalar resistance
   one, gains resistance only through ordinary reuse, and experiences the same
   pressure tick after every four completed local encounters;
5. zero resistance physically removes the allocation and any stale path to it;
6. executing a variation leaves an activity-derived eligibility trace;
7. the still-supplied DS8 outcome may update only that delayed active trace;
8. accumulated encounter value changes future admission, with low-rate
   deterministic exploration preserved;
9. authoritative M4 behavior is invoked unchanged and must remain exact.

No `TEMPORARY`, `PERMANENT`, TTL, episode lifetime, `consolidated`, retention
oracle, special delete/invalidate operation, sensory/internal/irrelevant
encounter class, `LEARN_HERE`, target site, candidate list, answer route, or
pre-proposal correctness value may occur in the organism path.

## Frozen encounter histories

The probe contains two recurring physical encounter patterns. They differ only
in observed local activity/adjacency history and use fresh opaque identities
on each presentation.

```text
pattern P  local variation executes; delayed active trace receives support
pattern N  local variation executes; delayed active trace receives rejection
```

The semantic polarity is an admitted DS8 supply for this stage. It occurs only
after site formation and execution. It is forbidden from the snapshot,
prototype recruitment, proposal formation, and eligibility choice.

After both histories, fresh P and N encounters test whether learned value
admits more future plasticity for P than N. A shuffled-value control swaps the
learned association while leaving encounter physics unchanged.

## Conjunctive path-existence checks

The PROBE is positive only if every check passes:

1. repeated physical P and N encounters recruit reusable prototypes from blank
   state;
2. proposals arise at locally coactive sites before any delayed outcome;
3. no-coactivity and outside-radius encounters create zero proposals;
4. only the executed proposal's active eligibility trace is updated;
5. P receives greater future plasticity admission than N after experience;
6. shuffled learned values destroy or reverse that allocation preference;
7. fresh opaque identities and a permuted local layout preserve the learned
   allocation law;
8. unused proposal/prototype/value allocations disappear under ordinary M4
   pressure, while recurrently used ones remain and strengthen;
9. a removed proposal has no executable stale path and can be ordinarily
   reacquired by later recurrence;
10. source audit finds zero forbidden endpoint/lifetime/site-selection fields
    or pre-proposal semantic outcome flow;
11. the unchanged authoritative M4 gate cell passes with exact lifetime vector
    `[1, 3, 6, 13, 27]`;
12. the complete report is duplicate-exact.

## Interpretation

PASS means only that the first absent cumulative DS7 path now exists and a
MICRO is warranted. M4 remains authoritative.

FAIL freezes the exact first collapse. Mechanical absence is repaired at that
edge and retried under a new preregistration. A need for a new persistent
representation, multiple equally supported physical targeting laws, or any
semantic leak is a scientific stop rather than an automatic repair.

