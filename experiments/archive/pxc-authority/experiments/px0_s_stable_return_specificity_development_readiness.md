# PX0-S stable return specificity development-readiness handoff

Status: **CLASSIFICATION A DEVELOPMENT POSITIVE; FRESH PX0 AUTHORITY MATRIX ELIGIBLE; PX0 AUTHORITY ABSENT**.

## Outcome

With the active PX0 law byte-identical, ordinary physical recurrence and
pressure distinguished cross-context reliable return from genuine but sparse
dense-topology return:

```text
return remains physically reliable across changing contexts
    -> resistance accumulates
    -> correspondence remains executable

return occurs only in sparse dense-topology contexts
    -> physical evidence is accepted when it occurs
    -> support is spent during intervening contexts
    -> correspondence becomes subthreshold and deallocates
```

No return was labeled correct, wrong, real, or accidental inside the
substrate. The active learner received only ordinary CELL/ARROW/SPIKE events.

## Frozen development sequence

1. PROBE v1 is an immutable negative. Its implementation required a scheduled
   dense opportunity to complete return on every presentation, which was
   stronger than the preregistered learnability control.
2. PROBE retry v2 changed only fresh namespaces, that evaluator predicate, and
   the report title. Both route/layout mirrors passed.
3. MICRO passed four fresh arms plus recurring, absent-return, and continuous
   stability-switch controls.
4. GATE passed twelve fresh arms and all five controls.

No failed artifact was overwritten or reinterpreted as positive.

## GATE evidence

Across twelve specificity arms:

- `412/412` cross-context stable returns occurred;
- `79` genuine sparse dense-topology returns occurred;
- stable resistance ended in the range `30..=71`;
- sparse final resistance ended in the range `0..=1`;
- `12/12` stable routes produced exactly one held-out outward crossing;
- `12/12` sparse routes produced zero held-out outward crossings;
- `12/12` sparse route families eventually fully deallocated;
- `12/12` complete-state held-out replays were exact;
- every propagation became naturally quiescent.

The arms crossed:

- all three route positions;
- direct and mirrored physical layouts;
- context spacing `10..=21` ticks;
- route stride `12..=34`;
- all four incidental phases;
- multiple support-device orders and delays;
- local distractor/candidate loads `0..=40`;
- cycle counts `6..=10`.

The recurring dense-path controls learned both physical routes when both return
relations remained available: each route executed alone and simultaneous
activation produced no privileged crossing. The absent-return controls learned
neither route. In the continuous switch control, the reliable return moved
from route 0 to route 1 while both structures were live; after continued
ordinary experience, route 0 produced zero held-out effect and route 1
produced one.

The GATE ledger records `6,145,569` operations. Specificity fixtures accounted
for `236,096` persistent bytes. Raw per-context work and outcomes are retained
in the 412-row context ledger.

## Integrity

- active PX0 law SHA-256:
  `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d`
- protocol commit: `86cde3a`
- frozen v1 negative commit: `301233f`
- positive PROBE v2 commit: `81edab8`
- GATE implementation commit: `fa8ec38`
- positive MICRO commit: `4365d15`
- GATE report SHA-256:
  `40adfe1f31bce98c193cd84bebea27530836446ecb87909d82c629499a3a8b8a`
- GATE summary SHA-256:
  `b5a78eebedd0a30dd213304f778aa77113dc101f87188ca33ac47e0d45b97b43`
- GATE context ledger SHA-256:
  `57085bbc9e4ed9b8ae5a80bab856f060d7399912d627530deed12fa06537fa97`

Formatting, strict focused Clippy, the focused substrate test, source/law hash,
and development-only definitive refusal all passed.

## Classification and authority boundary

Freeze **PX0-S-A — stable return specificity positive**:

> Persistent physical structure reflects return evidence that remains reliable
> across changing experience. Genuine but sparse incidental return is accepted
> locally when it occurs, yet does not remain reusable under ordinary pressure.

This narrows the PX0 definitive v1 failure to its one-context experience
distribution. It does not amend or rescue that negative.

PX0 remains non-authoritative. PX1, PX-C, the continuous organism, and Harness
H1 remain blocked. The only eligible next action is a separately preregistered,
fresh, one-shot PX0 authority matrix combining acquisition, survival-window
reuse, true forgetting, generic reacquisition, physical probation, and
dense-topology stable-return specificity.
