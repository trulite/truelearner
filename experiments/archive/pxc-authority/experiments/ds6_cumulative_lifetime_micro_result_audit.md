# DS6 cumulative lifetime MICRO result audit

Outcome: **DEVELOPMENT NEGATIVE AT USEFUL-PERSISTENCE GATE; IMMUTABLE**.

Protocol: `ds6-cumulative-lifetime-micro-v1`, amended before evidence at
`0fb127b1d3818fcf2a96077d650f5a96c2068a2d`.

Implementation commit/tag:
`130028cf968435cc916870a76d6dc2aff785f769` /
`ds6-cumulative-lifetime-micro-implementation`.

Result SHA-256:
`0df1f28e1ddbcc580d963fffd03dab4b2450dc4ffb0eafa381f28015341e3e2d`.

The complete two-seed matrix ran once in E2B sandbox
`iyrkw7af5qpmwwfmq3bwm`. Its create-new artifact was written before process
exit and downloaded unchanged.

## Frozen outcome

```text
seed 107000    useful persistence 3/4    FAIL
seed 108000    useful persistence 4/4    PASS

both seeds:
one-offs removed                    16/16
short-gap survival/re-strengthening PASS
long-gap disappearance              PASS
contradiction retention loss        PASS
stale learned path blocked          PASS
removed-state reacquisition         2/2
fresh identity/layout               PASS
economy and controls                PASS
duplicate                           exact
```

MICRO is conjunctive, so its verdict is negative. DS6 GATE is not eligible.
M3 remains authoritative and M4 remains absent.

## First physical collapse

In seed `107000`, recurrence histories were acquired sequentially in ascending
strength order `4, 6, 8, 10`. The four-use signature was therefore both:

```text
evaluator-designated useful on a future return
and
the earliest acquired / longest ordinarily unused signature
```

Ordinary pressure physically removed it before return. In seed `108000`, the
same histories were acquired in reverse temporal order; the four-use signature
was most recent and all four survived.

This is not permission to reinterpret the frozen gate. The required 4/4 useful
survival failed. It does show that the mechanism responded to physical
recurrence and elapsed disuse rather than to evaluator future usefulness.

## Mechanically forced follow-up

The result confounds two lawful causal variables:

```text
recurrence strength
elapsed ordinary disuse / pressure
```

The next diagnostic must match elapsed pressure when comparing recurrence
histories and match recurrence when comparing disuse histories. It must use
fresh seeds and a separately named protocol. It may not change the scalar
record, `+2` recurrence update, fourth-completion pressure clock, `-1` pressure
update, or zero-deallocation rule.

Future evaluator usefulness cannot be a retention input or a pass-condition
justification for a history whose learned physical lifetime has already
expired. The lawful question is whether survival is ordered by the history
available before return.

