# CORE0 v2 immutable scheduler-integration negative

The sole v2 execution emitted `CORE0_V2_EVIDENCE_SPENT`, then stopped before
matrix publication because the Production timing wheel rejected a scheduled
arrival earlier than its mechanical head.

V2 produced no capability result. The public runtime admission contract had
already accepted the admission tick relative to the organism clock; therefore
the failure is not to be rescued by silently inserting an idle tick.

A separately frozen observer-only diagnostic may repeat construction solely to
print the active profile, gate and mechanics immediately before each execution.
It cannot change a world, state transition, admission, predicate or runtime
byte. Its only purpose is to classify the v2 stop as evaluator timing or a
Reference/Production representation leak.
