# CORE1-E13 history composition — immutable v1 partial result

## Status

**PARTIAL INTEGRATION NEGATIVE. No E13 scientific classification.**

The sole v1 evidence execution emitted
`CORE1_E13_HISTORY_COMPOSITION_V1_EVIDENCE_SPENT` and stopped when the first
CORE1-B transient-history attempt found no generated contact pair.

## What v1 established

CORE1-A completed Reference, exact replay, and Production for all four
contexts. Every execution agreed exactly:

```text
ordinary context prime             no action
negative contact early             no action
positive contact early             intended action
actual world change                consequence admitted
Modulatory delivery                1
plasticity updates                 2
natural quiescence                 true
fresh context                      no learned action
```

The old SSA2-P history principle therefore repairs the E13 action bootstrap.
No chooser, RNG, winner, or sign preference is required.

The later CORE1-A failure is also precise: source-local Academy consequence
updates occur, but the generated contact path is absent by the end of the
consequence transition, so it cannot re-execute.

## Why the matrix stopped

CORE1-B/C generic variation constructs in stages:

```text
first ordinary source activity
→ direct signed alternatives

later ordinary source activity
→ contact subdivisions of existing direct alternatives
```

The v1 fixture primed each context only once before requesting contact-local
early/late history. CORE1-A creates contacts in one opportunity; CORE1-B/C do
not. The stop is therefore a fixture sequencing mismatch, not evidence against
generic variation or history-sensitive affordance resolution.

## Frozen artifact

- Complete CORE1-A rows: 12 (`4 contexts × Reference/replay/Production`)
- Reference replay exact: true
- Reference/Production exact: true
- CORE1-B/C: not observed
- E14/ARC: not run
- Matrix SHA-256:
  `5fa3fe2164b60b58c93870d801856e7e80d3d9d0b23df6ed3ad6dd13830e9b94`

The lawful v2 correction is fixture-only: give every context two fixed ordinary
primes before the unchanged early/late history pair. No runtime or learning
law changes are justified.
