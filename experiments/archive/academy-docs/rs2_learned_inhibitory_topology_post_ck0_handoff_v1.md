# RS2 learned inhibitory topology post-CK0 handoff v1

RS2 remains scientifically unaccepted after its sole post-CK0 evidence run.

## Frozen status

```text
CK0 v2                         DEVELOPMENT-READY
J0 retained replay             PASS
CV0/J0+SV1 retained replay     PASS

RS2 post-CK0                   IMMUTABLE NEGATIVE
CE1                            BLOCKED
FD2 v2                         BLOCKED
frozen ARC A2 replay           BLOCKED
```

The post-CK0 matrix completed `180/180` cases and `360/360` rows. All
representation, renaming, replay, checkpoint-continuation, and
Reference/Production comparisons were exact. Seven of nine families passed;
two frozen predicates failed systematically for all roots, phases, and
mechanics.

The exact evidence indicates two evaluator integration seams:

- non-quiescent recurrence was additionally required to set an observation-
  ceiling marker;
- positive re-execution was counted through the obsolete ordered `Deliver`
  event rather than WS0 wave-level Drive incidence.

These classifications do not authorize a repair. A separate preregistered
diagnostic/correction gate is required before any new RS2 execution. The
canonical runtime, authority, oracle, `arch.md`, and Academy curriculum remain
unchanged.
