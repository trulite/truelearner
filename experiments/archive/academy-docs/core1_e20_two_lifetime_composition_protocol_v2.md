# CORE1-E20 — Two-Lifetime Composition Protocol v2

## Status

Fixture-only correction after the first attempted P1 process stopped at the
arena capacity assertion and before it returned an E20 observation. Protocol
v1 remains binding for both candidate laws, both preflights, the stop rule, the
eight-seed gate, representation audit, rejection screen, and measurements.

No P1 pass/fail observation and no E20 evidence marker exists at this point.

## Corrected fixture contradiction

The frozen spatial body allocates only sixteen CELL identities beyond its
preassembled `6 cells x 4 motors x 1,024 contexts`. The preregistered
completion-rooted fresh variation cycle can lawfully propose additional local
contact CELLs, but its first attempted execution reached:

```text
resident arena has no free CELL slot
```

The arena therefore prohibited the exact bounded structural process that E20
v1 requires it to test. Capacity is resource headroom, not a route lifetime,
action list, alternative reservoir, eligibility rule, completion rule, or
learning rule.

## Sole correction

For the experimental `core1` spatial body, reserve bounded headroom of four
additional contact CELL identities and twelve additional ARROW identities per
context/motor pair. The ordinary base geometry remains byte-for-meaning
identical; unused capacity has no physical effect and is not serialized as
learned structure.

The bound is fixed from body dimensions before any seed runs. It does not
depend on seed, action order, usefulness, attempts, observed failures, or
runtime silence. Reference and Production receive identical capacities.

If the corrected fixture again exhausts either capacity, P1 stops negative. No
further capacity correction is authorized inside E20.

## Unchanged scientific contract

In particular:

- one delivered completion while OPEN earns exactly one fixed two-stage local
  variation cycle;
- retries still require returned completion;
- OPEN and USED-PENDING remain payload-free and independently cleared;
- existing PQLC remains unchanged;
- P1 seed 7 and P2 seed 0 must both pass before the frozen matrix can run;
- primary success remains `8/8`.
