# CR0 coupling-necessity handoff v2

Status: development-ready discriminator; non-authoritative.

## Result

```text
coupling
    = transmission efficacy

resistance
    = durable persistence

qualified consequence
    may need to alter both jobs
```

CR0 found an independently useful physical role for coupling plasticity. At
equal resistance, coupling 2 enabled one retained route to cross a physical
target threshold and emit outward activity; coupling 1 only kept the route
alive. Neutral thresholds and two-input topology controls prevent interpreting
coupling 2 as a universal success token.

All CPC0/CPC1/PQLC0/PQLC1/FD0/FD1 representative behavior passed in both arms,
with exact Reference/Production physical agreement and replay.

## Frozen lineage

- v1 protocol: `b79107c`, tag `cr0-coupling-necessity-protocol-v1`;
- v1 frozen evaluator: `5d23d34`, tag
  `cr0-coupling-necessity-frozen-v1`;
- v1 immutable measurement negative: `200076f`, tag
  `cr0-coupling-necessity-negative-v1`;
- v2 protocol: `414f953`, tag
  `cr0-coupling-necessity-v2-protocol-v1`;
- v2 frozen evaluator: `6fb360f`, tag
  `cr0-coupling-necessity-v2-frozen-v1`;
- v2 positive evidence: `9e72425`, tag
  `cr0-coupling-necessity-v2-evidence-positive-v1`;
- final result audit and handoff: current commit/tag.

## Current status

```text
CR0 coupling necessity          DEVELOPMENT POSITIVE

coupling-law integration        NEXT
FD2 v1                          IMMUTABLE NEGATIVE
FD2 v2                          BLOCKED ON INTEGRATION
ARC                             PAUSED
authority / oracle / arch.md    UNCHANGED
```

Do not restore the historical `coupling += 1` merely to satisfy CPC0. The next
gate must earn how efficacy change interacts with continuously graded local
participation and PQLC closure.
