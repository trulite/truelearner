# PQLC1 depth composition handoff v1

PQLC1 is development-positive and frozen. It has not advanced authority.

## Established

The exact PQLC0 local rule composed unchanged through contact depths
`1, 2, 4, 8, 16`:

```text
downstream Modulatory consequence
        ↓
local participation-qualified closure
        ↓
ordinary Modulatory transmission to adjacent contact
        ↓
the same local decision repeats
```

There is no runtime representation of depth or path. Complete chains produced
exactly `depth - 1` QLP traversals and linear PhysicalWork.

Structural absence, relaxed participation, and wrong branches each stopped
closure at the first locally unqualified contact. Genuine fan-out continued
along every physically qualified branch. Recurrent closure naturally
quiesced at every tested depth without a new damping law.

## Evidence

- variants: `39/39`;
- physical cases: `780/780`;
- mechanics rows: `1560/1560`;
- ordered Reference/Production agreement: `780/780`;
- exact same-mechanics reconstruction: `3120/3120`;
- recurrent maximum work: `1262/8192`;
- PQLC0 core hashes: unchanged;
- matrix executions: exactly one.

## Frozen lineage

- protocol: `ddcd4e7`, tag `pqlc1-depth-composition-protocol-v1`;
- evaluator/comparator: `226c180`, tag
  `pqlc1-depth-composition-frozen-v1`;
- evidence: `47406a9`, tag `pqlc1-depth-composition-positive-v1`.

## Status boundary

```text
CPC0   contact-compartment attribution          development positive
CPC1   continuous local participation           development positive
CPC2   adjacent arbitrary-depth closure          stopped negative
PQLC0  one-hop qualified local transduction
       plus cycle safety                         development positive
PQLC1  unchanged composition through depth 16   development positive

pressure de-supply                               now unblocked, not started
eligibility deletion                             paused
ARC A3-A5                                        paused
authority / oracle / arch.md                     unchanged
```

The next eligible scientific action is a fresh pressure-integration protocol.
It should ask whether the same path-local participation state can interact with
ordinary pressure and Modulation without an `if eligible { skip pressure }`
exception. PQLC1 itself authorizes no such change.
