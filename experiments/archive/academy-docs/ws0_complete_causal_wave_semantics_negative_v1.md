# WS0 complete causal-wave semantics immutable negative v1

Status: frozen technical negative. No WS0 physical row was published.

Protocol: `7f194d4` (`ws0-complete-causal-wave-semantics-protocol-v1`).

Frozen candidate: `7e84488`
(`ws0-complete-causal-wave-semantics-frozen-v1`).

One-shot E2B worker: `ivh1dykus42ttzhk52gp9`.

## Exact stop

The command compiled and began exactly once, then stopped while constructing
the first row's checkpoint continuation:

```text
PlasticSubstrate::from_live_checkpoint_with_mechanics(...)
-> Err(InvalidPhysicalBody)
```

No matrix/report artifact or WS0 predicate was published.

## Classification

The evaluator enabled `cv0j0` although WS0 creates no variation contacts and
asks no J0 lifetime question. J0 lawfully deallocated unused fixture junctions
as orphans. Its historical durable snapshot retains nonzero CELL resistance on
those dead records, while the checkpoint loader requires
`live == (resistance > 0)`. The resulting snapshot cannot be restored.

This is an evaluator-feature/fixture integration negative, not evidence about
Drive, Modulatory, PQLC, causal waves, identity renaming, insertion order, or
continuation divergence. The runtime candidate did not assert and the former
RS2 `Drive only` stop was not reproduced.

V1 is not repaired or rerun. A successor may change only the evaluator feature
surface from unnecessary `cv0j0` to the directly required retained
`ce0 + rs0/pqlc0 + si0` surface. The runtime candidate, 14 families, five
permutations, predicates, checkpoint contract, and stop rule must remain
unchanged.

SI0/PQLC/retained replay, RS2, CE1, FD2, ARC, authority, oracle status, and
`arch.md` remain unchanged.
