# RS2 learned inhibitory topology protocol v1

Status: frozen before any RS2 evaluator, world, or executable change.

Parent: RS1 positive result `5473276`.

## Question

Can ordinary variation plus consequence learning discover and retain the
inhibitory topology that RS1 showed is sufficient, without evaluator-supplied
inhibition?

## Gate A — proposal eligibility

Audit the accepted ordinary local proposal path first:

```text
new local opportunity
→ ordinary structural proposal
→ can the proposed ARROW have negative Drive coupling?
```

Gate A passes only if the already-accepted proposal physics can emit an
ordinary negative-Drive candidate without an RS2 edit, evaluator-supplied
ArrowSpec, preferred sign, candidate label, or hidden candidate injection.

If the accepted proposal path can emit only positive Drive, RS2 stops static
negative immediately. No negative proposal law, evaluator, learning world,
selection matrix, or rescue run may be added inside RS2.

## Gate B — consequence selection

Gate B is blocked unless Gate A passes. If eligible, it would permute opaque
candidate identities and physical locations and require ordinary participation
plus qualified consequence to retain only physically useful topology. It
would then freeze learning and probe recurrent stability with the full hard
controls specified by the user.

Gate B must never supply `STABILIZE`, `INHIBIT_HERE`, a cycle detector,
oscillation score, reward for quiescence, preferred negative sign, or semantic
candidate identity.

## Claims and exclusions

A positive RS2 could claim only that accepted variation and consequence
learning discover and retain ordinary inhibitory topology. A Gate-A negative
claims only that the accepted variation operator lacks the required candidate
support; it does not justify a particular replacement proposal law.

RS2 does not change learning, forgetting, pressure, CE0, FD2, ARC, authority,
the oracle, or `arch.md`.
