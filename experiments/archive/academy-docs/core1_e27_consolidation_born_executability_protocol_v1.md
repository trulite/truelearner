# CORE1-E27 — Consolidation-Born Executability Protocol v1

## Status

Preregistered before runtime evidence. E26 is frozen at `2d8141a` and is not
rerun as a baseline.

## Frozen result and hypothesis

E26 established, exactly across eight roots and both mechanics:

```text
successful PQLC       yes
re-entry topology     0|1|2|3|4
autonomous probes     none|none|none|none
```

Its re-entry arrow already uses the ordinary `SourceFires` trigger. Merely
marking that arrow as executable would therefore make no physical change.

E27 tests one stronger hypothesis:

> Successful PQLC on a complete used route must consolidate not only its
> survival and re-entry topology, but enough local signed efficacy for normal
> source activity to execute the route through each target's ordinary firing
> surface.

## Candidate law

The candidate is default-off **consolidation-born local threshold closure**.
When successful PQLC at a contact has live evidence of a complete used route,

```text
participating source -> contact
participating contact -> target
```

the same consolidation event:

1. creates the unchanged E26 durable source-to-contact re-entry edge;
2. marks that re-entry edge and the participating contact-to-target edge as
   the consolidated executable route;
3. makes both marked edges durable;
4. raises each edge's signed material magnitude only when necessary to equal
   its own target's positive threshold, preserving the existing sign.

Later execution remains entirely ordinary:

```text
external context incidence
-> source reaches its threshold and fires
-> SourceFires edge emits
-> contact reaches its threshold and fires
-> SourceFires edge emits
-> motor reaches its threshold and fires
```

There is no recall operation, route lookup, threshold bypass, injected motor
activity, action identity, context payload, reward payload, episode identity,
timeout, or evaluator-visible choice. Endpoints, sign, and target threshold are
supplied by the successful local physical event itself.

Negative routes retain negative sign. Threshold closure makes their magnitude
locally decisive; it does not reinterpret inhibition as positive firing.

## Frozen composition

Every E27 organism retains the exact E26 stack:

```text
E24 atomic route closure
+ E25 local signed gating
+ E25 motor integration window
+ E22 atomic participation-born credit return
+ E26 consolidation-born re-entry
+ E27 consolidation-born executability
```

No consequence, PQLC, E22 return, route-formation, gating, integration-window,
action-map, frame, recovery, or probe mechanic may change.

## Roots and mechanics

- roots: `94000000..94000007`;
- Reference;
- exact Reference replay;
- Production;
- frozen useful teaching actions: `1|4|2|3`;
- frozen autonomous probes: the same four learned frames, with no babble and no
  consequence.

## Gate 1 — executable consolidation

For each root, run the first two unchanged teaching turns and then recover a
clone through the ordinary E26 recovery path. Probe the first learned frame
with no babble.

Required:

```text
teaching actions             1|4
Modulatory                   0|>0
PQLC                         0|>0
E26 re-entry count           0|1
E27 executable-edge count    0|2
ordinary probe action        1
executable traversals        >=2
natural quiescence           true
Reference replay             exact
Reference/Production         exact
```

The two traversal events must arise from ordinary source firing: one on the
consolidated source-to-contact edge and one on the consolidated
contact-to-target edge. Gate 1 must pass `8/8`; otherwise the full regimen is
not run.

## Gates 2 and 3 — full credit and autonomy

If Gate 1 passes, run the unchanged five-turn E26 teaching/closing regimen.
Require, for every root and exact mechanics execution:

```text
teaching actions             1|4|2|3
later Modulatory             >0
later PQLC                   >0
E26 re-entry counts          0|1|2|3|4
E27 executable-edge counts   0|2|4|6|8
E22 return counts            1|1|1|1|0
USED-PENDING                 0 throughout
natural quiescence           true throughout
autonomous probe actions     1|4|2|3
executable traversals/probe  >=2 each
```

Full success is `8/8` only.

## Interpretation

- Gate 1 `0..7/8`: candidate fails to make consolidation physically
  executable; stop.
- Gate 1 `8/8`, full `0..7/8`: local threshold closure creates executable
  paths but is insufficient for autonomous ARC policy; falsified after
  executability.
- Gate 1 `8/8`, full `8/8`: E27 earns candidate CORE1 physics, subject to
  unchanged historical controls and later adoption testing.

No acceptance predicate is relaxed after evidence. No repair or rerun is
permitted.
