# SV0 symmetric sign variation handoff v1

Status: development stopped negative at Gate B; no authority promotion.

## Result

```text
ordinary local variation
→ weak Drive +1 and Drive -1 alternatives
→ symmetric lifetime and bounded proposal count                 PASS

both alternatives share one source→target contact opportunity
→ both traverse
→ both carry identical participation
→ one selected-sign consequence arrives at the shared contact
→ both mature                                                  FAIL
```

The proposal law explored both sides of zero correctly. Existing consequence
learning could not distinguish the alternatives because their physical
participation was co-located.

## Frozen lineage

- RS2 parent/result: `af570f7`, tag
  `rs2-learned-inhibitory-topology-gate-a-negative-v1`;
- protocol: `3d7dc13`, tag
  `sv0-symmetric-sign-variation-protocol-v1`;
- candidate/evaluator: `bb87658`;
- remote formatting: `3f7122c`;
- consequence admission binding: `fd2edb4`;
- initial freeze: `88970d1`, tag
  `sv0-symmetric-sign-variation-frozen-v1`;
- strict-Clippy correction: `09fb4f6`, tag
  `sv0-symmetric-sign-variation-frozen-v1-corrected`;
- evidence-eligible preflight: `b1dfdfa`, tag
  `sv0-symmetric-sign-variation-evidence-eligible-v1`;
- immutable raw negative: `9bf9313`, tag
  `sv0-symmetric-sign-variation-negative-raw-v1`;
- deterministic recovery: `d25c3b1`, tag
  `sv0-symmetric-sign-variation-matrix-recovery-v1`.

## E2B provenance

- reusable formatting/preflight/recovery worker: `igk8z307lfx74zbk20yeg`;
- sole fresh matrix worker: `inihr4kunadbpkcoxa8lk`;
- formatting, strict Clippy, static audit: PASS;
- corrected exact targeted core control: `1/1` PASS;
- matrix executions: exactly one;
- Reference/Production and replay: exact;
- no Rust or project executable ran locally.

## Boundary

Do not return to RS2 Gate B with the current co-located signed pair: it would
repeat the demonstrated cross-retention.

A successor may independently test whether ordinary variation can create
physically distinct contact compartments for alternative signed effects while
remaining symmetric, bounded, and consequence-agnostic. It must not introduce
sign-specific credit, semantic candidate identity, or evaluator-selected
gating as an SV0 repair.

FD2, ARC, authority, the oracle, and `arch.md` remain unchanged.
