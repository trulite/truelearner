# Academy A1 + A1-V Development Handoff v1

Status: development positive, pending human review. A2 and runtime R7 remain
unstarted.

## Claim earned

Academy can now generate a physical relation-recognition world, teach it by
ordinary TrueLearner participation and LR-C modulation, and issue a fresh
non-teaching probe without supplying an expected answer to the organism.

The taught joint route is reusable after two supported physical loops. Fresh
echo, distractor, wrong-context, modulation-without-eligibility, and adjacent
unsupported-repetition controls remain silent. The same result holds for text
and raster inputs, fresh generated identities, reflected geometry, and reversed
allocation order.

This is a narrow A1 result. It establishes generated relation recognition. It
does not yet establish arbitrary naming, retrieval, transfer, retention,
curriculum scheduling, mastery, or learning-to-learn.

## A1-V record and replay

Every completed A1 experience contains:

- the exact before and after live checkpoints;
- the admitted physical spike stream;
- outward crossings and physical work observations;
- organism and shared-world raster surfaces;
- the generated case, capability, seed, and probe family;
- exact selected replay evidence.

The Playground history filmstrip selects and replays the recorded experience
from its before checkpoint. Generic text, image, file, and drawing admission is
unscored: it carries neither an expected answer nor capability IDs.

## Durable publication

`academy-storage` publishes immutable content-addressed payloads before one
manifest. A bounded publisher worker runs outside the organism thread after the
physical result exists. Missing credentials, S3 failure, or publisher
backpressure cannot alter the organism result or destroy the local replayable
experience.

The private production bucket is
`s3://truelearner-academy-444123143883-ap-south-1/academy/v1`. Versioning,
SSE-S3, and public-access blocking are enabled. The AWS SDK uses the normal
credential chain; no credential is stored in this repository.

## Evidence

- `academy-core`: 9/9 tests passed in E2B.
- `academy-storage`: 5/5 tests passed in E2B.
- strict Clippy for the two headless crates passed with warnings denied.
- Playground typecheck passed locally against the native Dioxus/GTK stack.
- Playground canvas-coordinate tests passed 2/2 locally.
- The native Playground launched locally and emitted its ready marker.
- Exact A1 selected replay passed.
- Natural physical quiescence passed.
- TrueLearner physical-law source was not modified.

Relevant E2B workers:

- generated A1 text/raster physical worlds: `ij13s21qhu5yltosl7li6`;
- integrated Academy tests: `inommo3blfxik21lhhp1s`;
- strict headless validation before publisher completion:
  `iv1l9nphwzc64eimew00y`;
- storage publication and offline-causality tests: `idf8xovflbtjcjikqtbvz`.
- final formatter and warnings-denied headless validation:
  `itxgkm1ividlvkbz9w5r6`.

The first formatting-only follow-up worker, `i5bqj4yryr4ykq2fnhy5e`, stopped
before compilation and caused no scientific execution. Its sole finding was a
Rust formatter difference in the boxed publication event.

## Human review boundary

Stop here. Do not begin A2, R7, curriculum scheduling, retention scheduling,
shared-world drawing, transport, or runtime-law changes until this A1/A1-V
result and the Playground interaction are reviewed.
