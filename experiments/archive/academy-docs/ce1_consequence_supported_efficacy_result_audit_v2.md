# CE1 consequence-supported efficacy result audit v2

Status: immutable development negative.

Protocol: `97c0093` (`ce1-consequence-supported-efficacy-protocol-v2`).
Frozen candidate: `80c42f8`
(`ce1-consequence-supported-efficacy-frozen-v2`).
Fresh E2B worker: `ikdm71haq7x8yourpubqz`.

## Complete result

- cases: `200/200`;
- mechanics rows: `400/400`;
- same-mechanics replay: `400/400` exact;
- Reference/Production physical agreement: `400/400` exact;
- natural quiescence: `400/400`;
- frozen predicates: `360/400`;
- maximum PhysicalWork: `219`.

All nine retained CE0 non-recurrent families passed under both roots, all ten
clock origins, and both mechanics. Consequence-free use did not change
efficacy; prompt, graded, repeated, threshold, equal-persistence, and fan-out
efficacy behavior remained exact.

## Exact recurrent-family failure

All `40` recurrent rows failed identically. The anchor correction worked and
the full family serialized, but no recurrent coupling matured:

```text
selected generated stem       coupling +1, support 3,774,873,600
selected generated outgoing   coupling -1, support 4,026,531,840

four positive recurrence links
                              coupling +1, support below 2^32

required support quantum      4,294,967,296
Coupling events                0
```

The ordinary consequence consolidated resistance (`R1 -> R4` on the selected
generated relation and `R100000 -> R100003` on the recurrence links), but the
remaining continuously decayed participation did not complete a support
quantum. Consequently the positive recurrence path stayed subthreshold:

```text
probe A fires                 1
probe B fires                 0
learned negative contact      0
```

The intended executable recurrence and its learned inhibitory stabilization
therefore never occurred. The recurrent world also recorded `108` bounded
ordinary proposals rather than the preregistered six, exposing additional
local opportunity geometry around its close fixture cells.

This is not an observer, replay, identity, mechanics, or checkpoint mismatch.
The frozen CE1 efficacy/stability predicate was not established, so CE1 v2 is
scientifically negative even though its first nine families remain useful
diagnostic positives.

## Evidence

- matrix SHA-256:
  `db3020fd103f436590fbeb5d1420348389ef69a8b4bc013ec5470a13c9afd983`;
- report SHA-256:
  `06d388fc230e8f63d56f96712c319825f0fa6840390ca6b62d63692475e46b64`.

No repair or rerun occurred. FD2 v2 and frozen ARC A2 did not run.

