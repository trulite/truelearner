# CE1 consequence-supported efficacy immutable negative v1

Status: stopped before row 1; no CE1 scientific result.

Protocol: `b4d85ee` (`ce1-consequence-supported-efficacy-protocol-v1`).
Frozen candidate: `f6f2606`
(`ce1-consequence-supported-efficacy-frozen-v1`).
Fresh E2B worker: `i0nsdr0k92mdupnzzm30h`.

## Exact stop

The complete CE1 command executed exactly once. During construction of the
first recurrent-family case, the evaluator expected CV0/J0 to generate two
signed candidates and stopped at:

```text
CE1 variation must create two signed candidates: []
```

The evaluator had created `A` and `B` as initially orphan J0 junctions. J0's
ordinary topology-derived lifetime correctly deallocated those unanchored
fixture cells at the first physical moment, before external firing could
produce the intended signed contact opportunity. This is the same boundary-
fixture requirement previously established by CV0-E/J0: stable world-boundary
cells need ordinary pre-existing anchor topology.

No matrix row or report was published. The downloaded archive contains only
the empty result directory. The sign-preserving efficacy candidate therefore
received no scientific evidence, positive or negative.

## Classification

This is an evaluator/world-construction defect, not an organism counterexample.
The runtime did exactly what J0 specifies for an orphan junction. Nevertheless,
the frozen CE1 stop rule forbids repair or rerun inside v1.

No FD2 v2 or ARC A2 command ran. Authority, oracle status, `arch.md`, and the
Academy curriculum remain unchanged.

