# CORE1 E14 — Behavioral Frontier Closure v1

## Frozen conclusion

The original E14 behavioral frontier is closed.

Three statements must remain distinct:

| Claim | Status | Evidence |
|---|---|---|
| Mechanism evidence | **passed** | E22 delayed return `8/8`; E27 autonomous execution `8/8` |
| Behavioral frontier | **repaired** | full original E14 body produces `1|4|2|3` with consequence, PQLC, re-entry, and executable consolidation |
| Legacy E14 acceptance | **negative / stale** | expected one update, observed two updates on the two physical route halves |

The unchanged E14 contract is not rewritten. Its formal result remains
negative because its frozen predicate requires:

```text
updates = 0|1|1|1|1
```

The learned architecture now contains a two-arrow route:

```text
source -> contact -> motor
```

Later consequence physically reaches the contact and existing PQLC updates
both participating incident arrows. The observed result is therefore:

```text
updates = 0|2|2|2|2
```

That is not a behavioral failure. It is the expected physical multiplicity of
the learned topology that E24-E27 established.

## Closed causal chain

On the original full E14 body and frames:

```text
context
-> complete route becomes physically live
-> motor participates
-> useful outward action occurs
-> later consequence traverses E22 return topology
-> PQLC consolidates both used route halves
-> consolidation leaves executable learned topology
```

E27 separately establishes the final revisit transition under the validated
isomorphic fixture:

```text
later ordinary source activity
-> both consolidated route halves traverse
-> autonomous useful action
```

The compact E27 baseline reproduced E26's inert-topology failure `8/8`, so the
autonomous result cannot be attributed to making the fixture easier.

## Contract disposition

- preserve unchanged E14 and its negative result as historical evidence;
- do not repair physics to force one update from a two-arrow route;
- do not reinterpret or overwrite the one-shot result;
- future acceptance should state structural credit explicitly—every qualifying
  used route half updates, and unrelated topology does not—rather than freeze
  a topology-dependent scalar count;
- no E28 mechanism is motivated by this mismatch.

