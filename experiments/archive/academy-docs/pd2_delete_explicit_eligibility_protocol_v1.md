# PD2 delete explicit eligibility protocol v1

Status: frozen before PD2 runtime or evaluator edits.

Parent: PD1 development-positive handoff at
`f5a7675bb48ae4e5b19ed9e15504d9c7e7eb9442`.

## Question

Does the old explicit eligibility mechanism retain any future-causal role that
is not already supplied by continuously relaxing path-local participation,
PD1 pressure/load exchange, qualified Modulation, and PQLC?

PD2 is deletion-only. If accepted behavior requires a replacement law, timer,
gate, state variable, or semantic address, PD2 stops negative.

## Required deletion

The PD2 active runtime must contain no:

```text
eligible_until
LOCAL_WINDOW
eligibility predicate
eligibility frontier
eligibility event
eligibility checkpoint bytes
unsupported eligibility-expiry pressure
```

This includes causally inert accounting and execution-cost counters, not only
the ARROW field.

Historical source and evidence remain frozen at their tags. The successor core
promotes the already-positive CPC1, PD1, and PQLC physical quantities into its
active law; it does not preserve a legacy execution branch inside the active
organism.

## Modulation law guardrail

For every live outgoing local contact, Modulation always evaluates the existing
arithmetic response from its participation magnitude:

```text
support += participation
gain = ceil(min(participation, Q) * 3 / Q)
resistance += gain
```

At zero participation the arithmetic produces zero. The plasticity law may not
first branch on `participation != 0`, `participation > 0`, or an equivalent
Boolean susceptibility test. Observer accounting may report a durable update
only when durable state actually changes.

The already-earned PQLC trigger remains a separate physical coincidence:
Modulatory arrival plus local participating Drive structure. PD2 does not
redesign that affordance.

## Frozen successor corpus

### D0 — deletion and persistence

- active `Arrow`/SoA state has no deadline field;
- active runtime has no eligibility frontier/event/counter/accessor;
- live checkpoint has no eligibility bytes;
- checkpoint format version changes explicitly rather than silently reusing
  incompatible bytes;
- exact live continuation preserves the already-existing future-causal
  participation and pressure-load state used by the exercised non-PQLC worlds;
- canonical durable body remains independent of transient participation/load.

### D1 — PD1 pressure corpus

Replay all ten PD1 families across pressure phases `0..9`, two fresh identity
roots, Reference and Production mechanics, and exact replay. Remove only the
serialized eligibility field/event. Require identical causal outcomes,
participation, pressure load, support, resistance, liveness, clock, pressure
phase, body, and quiescence.

PhysicalWork may decrease only by the exactly enumerated deleted eligibility
bookkeeping. It may not increase. Reference and Production PD2 PhysicalWork
must match exactly.

### D2 — retained local-credit corpus

- CPC0 contact topology: old shared-contact ambiguity and separated-contact
  specificity, including fan-out honesty;
- CPC1 delay curve and same-path renewal controls;
- PQLC0 one-hop controls and cycle quiescence;
- PQLC1 depth `1,2,4,8,16`, breaks, branches, fan-out, and recurrent controls.

Frozen historical evaluators may be reused only when they do not mention the
deleted surface. Otherwise a successor adapter must change measurement only,
not world geometry, schedules, thresholds, or expected causal outcome.

### D3 — retained body/mechanics corpus

Run the permanent core tests plus R1-R5 differential and R6 partition
invariance against the successor. Their scientific worlds and ordering
comparators remain unchanged. Execution-cost columns deleted with the old
eligibility frontier are not causal observations.

### D4 — unchanged frozen ARC A2 replay

Only after D0-D3 pass, run the exact official ARC A2-only world:

```text
game       ls20
seed       205
curriculum [1,4,2,3]
phases     0,9
mechanics  Reference, Production
```

No curriculum, admission schedule, action map, babbling schedule, thresholds,
world code, expected output, or toolkit revision may change. Require four
coexisting contextual action-effect relations, changed rasters, update series
`[0,1,1,1,1]`, quiescence, exact replay, and mechanics equality after removing
only the mechanics label.

## Frozen comparison boundary

PD1 versus PD2 comparison excludes only the intentionally deleted deadline,
eligibility events/frontier counters, and their exact bookkeeping work. It
includes all causal deliveries, fires, QLP traversals, resistance changes,
proposals, deallocations, crossings, participation, support, pressure load,
durable body, clock/phase, pending activity, and subsequent behavior.

Reference versus Production comparison within PD2 remains exact on the full
PD2 physical observation and PhysicalWork.

No comparator repair is allowed after evidence begins.

## Static prohibitions

The active core may contain no token or state equivalent to:

- eligible, eligibility, deadline, expires, grace, window, remaining ticks;
- credit/reward/cause/path/route/parent/depth/hop semantics;
- a Boolean Modulation gate over participation;
- a new timer, countdown, task horizon, or pressure exception.

Archive documentation and immutable historical experiments are excluded from
the token audit. No ARC identity or curriculum fact may enter the core.

## Decision

- Need for replacement physics, any retained causal failure, checkpoint
  ambiguity, mechanics divergence, replay failure, or ARC change: immutable
  PD2 negative; stop.
- D0-D3 positive but unchanged ARC A2 negative: freeze PD2 positive and ARC
  replay negative separately; do not tune.
- D0-D4 positive: PD2 development-ready only.

PD2 does not advance authority/oracle status, start ARC A3-A5, or update
`arch.md`.
