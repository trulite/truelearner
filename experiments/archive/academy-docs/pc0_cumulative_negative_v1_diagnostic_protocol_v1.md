# PC0 cumulative-negative v1 diagnostic protocol v1

Status: frozen before diagnostic evaluator edits or execution.

Parent negative: `pc0-pressure-closure-compatibility-cumulative-negative-v1`.

## Question

Did cumulative PC0 v1 stop solely because PD1's old magnitude measurement
assumed every contact's pre-pressure participation was below one impulse `Q`,
or does the non-consuming PC0 law break a retained causal outcome?

## Frozen diagnostic

Mechanically derive one diagnostic evaluator from the frozen PC0 PD1-successor
adapter. Preserve all 200 worlds, identities, phases, schedules, mechanics,
same-mechanics replay, Reference/Production comparison, and every family
assertion except this exact stopped predicate:

```text
before_pressure participation < Q for every magnitude contact
```

Serialize the existing complete stage observations for all cases and add:

- number of magnitude physical cases with any contact `>= Q`;
- number of individual magnitude contacts `>= Q`;
- maximum pre-pressure participation;
- whether every other retained family/causal assertion passed.

The diagnostic may not change the core law, world, schedule, threshold,
pressure phase, or any other expected outcome. It is not a PC0 rerun and cannot
establish readiness.

## Classification

- If all other retained outcomes and both mechanics pass exactly, classify the
  v1 stop as a frozen evaluator/measurement mismatch: the old upper-bound
  predicate encoded sacrificial participation.
- If any other retained outcome fails, classify a scientific PC0
  counterexample and stop.

Any fresh PC0 cumulative attempt requires a separately frozen v2 protocol and
fresh evidence. ARC remains forbidden.
