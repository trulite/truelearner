# RS2 learned inhibitory topology protocol v5

Status: frozen before any v5 evaluator or executable change.

Parent: AH0 development-ready `6ab8a15b92200d63a0d2417fe2a56ada51fffb31`
(`ah0-handle-ordering-removal-development-ready-v1`).

## Question

Can the organism generate, select, retain, and later use inhibitory topology
without the architect supplying where inhibition belongs, now that AH0 has
removed arbitrary numeric-handle ordering from causal execution?

## Frozen science

RS2 v5 preserves the exact RS2 v4 training and probe worlds:

1. ordinary CV0/J0 variation generates separate signed `C+` and `C-` contact
   topology;
2. an ordinary qualified consequence selects the useful signed relation;
3. the selected relation consolidates and the unsupported relation decays;
4. learning is frozen;
5. a separate executable recurrent probe tests whether the learned inhibitory
   topology settles recurrence.

Training geometry, thresholds, delays, phases, consequence age, identity
namespaces, observation ceiling, roots, phase sweep, and all nine v4 families
remain unchanged. No organism law is added or modified by RS2 v5.

## Frozen predicates

1. Positive and negative sign selection follows physical consequence.
2. Identity permutation produces identical physics after logical renaming.
3. Insertion and observer-recording order are causally irrelevant.
4. No qualified consequence produces no consolidation.
5. An irrelevant negative candidate does not survive merely because its sign
   is negative.
6. A learned inhibitor disconnected from the recurrence cannot settle it.
7. A learned inhibitor that is present but not traversed cannot settle it.
8. An active learned inhibitor permits the intended traversal and the
   recurrent probe settles naturally.
9. Reference and Production are equal after wave-normalized physical
   observation.
10. Same-mechanics replay and natural-quiescence/ceiling classification are
    exact.

## Observation contract

RS2 v5 does not assign causal meaning to the recording order of transitions
that share physical tick and phase. Its observer preserves event kind,
payload, multiplicity, tick, and phase while canonically normalizing
same-time physical observations. SI0 causal-wave `DriveIncidence` records
remain explicit, so zero-delay effects in a later causal wave cannot collapse
into their cause. This changes the evaluator only; it does not change runtime
execution.

Numeric CELL/ARROW handles are translated to logical world names before
identity-permutation comparison. Generated contacts and links are named by
their physical role (selected/unsupported sign and topology), not by numeric
allocation order.

Raw ordered traces remain diagnostic evidence but are not an equivalence
predicate.

## Checkpoint contract

Raw live-checkpoint byte/hash equality is not required unless the
representation is independently canonical. The physically meaningful exact
checkpoint contract is:

- identical canonical durable body;
- identical physical clock;
- identical canonical pending physical activity and admitted loads;
- identical continuation under the same future physical inputs.

If native Reference/Production continuation differs in any physical event,
work, final durable body, clock, pending activity, or quiescence result, RS2 v5
stops negative. No comparator repair is permitted after evidence execution.

## Execution and stop rule

After targeted formatting/check/Clippy, the complete v5 matrix executes once
in a fresh E2B worker. Any scientific, continuation, replay, or normalized
representation failure freezes v5 negative without repair or rerun.

Only a complete RS2 v5 positive permits the already-planned prefix:

```text
RS2 v5 -> CE1 -> FD2 v2 -> unchanged frozen ARC A2
```

Each successor may run only if its immediate predecessor is positive. No
authority, oracle, or `arch.md` state changes in this gate.
