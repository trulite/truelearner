# CORE1-E13 — retained PQLC composition protocol v3

## Status

Integration-only continuation from the immutable E13 v2 stopped negative at
`b6cedfa`. This run may compose already-established CPC/PQLC and continuous
efficacy behavior. It may not add or alter organism physics, variation,
forgetting, participation, coupling, resistance, causal-wave, or boundary
laws. ARC remains out of scope.

## Observed integration break

The v2 action path was ordinary generated topology:

```text
context source -> contact -> motor
```

Both Drive links traversed, but Academy returned consequence only to the old
source-local relay. No `QualifiedLocalParticipation` traversal occurred, so
the retained PQLC law was present but disconnected from E13.

## Sole wiring correction

For every generated signed contact, Academy adds the same existing ordinary
topology, without sign, identity, output, or evaluator preference:

```text
return relay --Modulatory/SourceFires--> motor
motor        --Modulatory/QLP---------> contact
contact      --Modulatory/QLP---------> context source
```

The trigger/effect combinations and all local update laws already exist in the
retained substrate. `BoundaryRuntime::add_arrow_with_trigger` is only a public
delegate to the existing substrate operation.

The world consequence is admitted separately from the next sensory frame. It
uses the same returning CELL pulse and existing relay topology, but does not
present or age the next context before its own development begins.

## Fixed developmental count

CE1/E12 already established continuous efficacy maturation rather than
one-shot threshold learning. Every context therefore receives exactly two
supported executions, fixed before observation and never stopped when a probe
succeeds:

```text
history-resolved action -> consequence
one refractory recovery tick
history-resolved action -> consequence
ordinary 12-tick cleanup
```

The same count is used for every profile, context, identity, and mechanics.
Use without consequence, adaptive train-until-success, a chooser, RNG, winner,
sign preference, path address, target efficacy, or new learning law is
forbidden.

## Observation and prefix rules

For each profile A/B/C, record Reference, exact Reference replay, and
Production independently. A missing contact pair or a route absent before the
fixed second experience is a structured profile result, not a process panic;
other profiles continue as required by CORE1's frozen prefix discipline.

Success requires all four contexts to show:

- silent generation primes;
- the frozen negative-material control silent;
- the positive-material history emits the intended action;
- both fixed actions receive ordinary consequence with Modulatory deliveries
  and local material updates;
- the fresh context re-executes the intended action;
- natural quiescence;
- exact replay and Reference/Production equality.

The run must report, rather than repair, any profile whose existing material
cannot keep the generated topology available for this composition.
