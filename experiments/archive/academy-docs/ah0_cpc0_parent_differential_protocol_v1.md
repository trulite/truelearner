# AH0 CPC0 parent-differential protocol v1

Status: frozen before adapter implementation.

## Baseline conflict

The frozen CPC0 successor reaches every spatial-attribution assertion on the
current SI0 v2 parent, but then requires the historical coupling result
`1 -> 2`. The current cumulative body produces resistance consolidation while
coupling remains `1`. CR0 established that efficacy is independently useful,
but CE0/RS2 have not yet established the lawful cumulative efficacy mechanism.
AH0 cannot resolve that scientific dependency.

## AH0 differential

Copy the already-frozen CPC0 successor evaluator. Preserve all eleven worlds,
roots, phases, schedules, identity/order permutations, Reference/Production
comparisons, replay, quiescence, attribution predicates, resistance predicates,
and serialized coupling observations.

Remove only the two historical `coupling == 2` assertions. Do not replace them
with a new coupling claim.

Run this observer on the exact SI0 v2 parent and freeze its complete matrix.
The AH0 candidate must produce a byte-identical matrix and report while still
passing every retained spatial-attribution predicate.

This establishes no coupling result. It asks only whether removing handle
ordering changes the current parent's CPC0 physical history.
