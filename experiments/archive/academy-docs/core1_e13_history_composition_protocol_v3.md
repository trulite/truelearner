# CORE1-E13 — history composition protocol v3 amendment

## Status

Fixture-only continuation after immutable v1 partial negative `b4b475f`.

## Demonstrated v1 mismatch

CORE1-A creates contact alternatives in one ordinary variation opportunity.
CORE1-B/C generic variation first creates direct signed links and, on a later
ordinary opportunity, subdivides those existing links through fresh contact
junctions. The v1 fixture requested contact history after only one prime, so
the generic profiles correctly had no contact pair yet.

## Sole amendment

Every context now receives exactly two fixed ordinary no-babble primes before
the unchanged early/late history pair.

```text
prime 1  → direct generic alternatives may form
prime 2  → existing alternatives may acquire contact subdivisions
history 0 / history 1 unchanged
```

Both primes are used for all profiles, contexts, identities, and mechanics.
They are not conditional on inspection or output. Everything from protocols
v1/v2 remains unchanged, including the fixed local-forgetting gap, action map,
consequence law, comparisons, prohibitions, and no-ARC boundary.
