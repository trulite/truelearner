# CORE1-E13 retained PQLC composition — result v3

## Outcome

The integration repair is positive for **CORE1-B**, the primitive generic-edit
organism. No organism law changed.

```text
context source -> generated contact -> motor
       Drive             Drive

motor consequence
  -> local downstream-link consolidation
  -> QLP motor -> contact
  -> local contact incident-link consolidation
  -> QLP contact -> source
```

All four contextual relations `[1, 4, 2, 3]` re-execute after two fixed
supported experiences and the ordinary cleanup interval.

## Direct answers to the composition diagnostic

For the first CORE1-B context:

- `source -> contact` traversed and retained nonzero participation;
- `contact -> motor` traversed and retained nonzero participation;
- before consequence, the generated stem participation was `5,880,650,625`
  and the selected outgoing participation was `6,272,694,000`;
- consequence first met the participating downstream link at the motor;
- the existing QLP topology then continued `motor -> contact -> source`;
- after the two fixed supported experiences, the selected outgoing resistance
  was `84,837,313,562` and the generated stem resistance was
  `160,197,391,832`;
- after cleanup, both links remained live and the fresh context emitted the
  learned action.

Each successful CORE1-B context recorded `12` Modulatory deliveries and `40`
local material updates across the two fixed consequences. The maximum
per-context `PhysicalWork` was `8,546`.

The old E13 wiring had produced zero QLP traversals because consequence ended
at the old source-local relay. The repaired Academy topology uses only the
already-retained `SourceFires` and `QualifiedLocalParticipation` trigger/effect
combinations.

## Profile boundaries

| Profile | E13 | Replay | Reference/Production | First boundary |
|---|---:|---:|---:|---|
| CORE1-A | false | exact | exact | old weak preassembled contact route disappears during the first exploratory history, before consequence can close and before the fixed second experience |
| CORE1-B | true | exact | exact | complete four-context chain closes and re-executes |
| CORE1-C | false | exact | exact | ordinary-activity generic variation has no live signed contact pair at the first history point |

CORE1-A answers the “which link died?” question more strongly: both generated
contact links are absent already in the post-history snapshot. Its consequence
therefore cannot exercise PQLC; the route is gone before the world return.
CORE1-C stops even earlier, before an exercisable contact route exists.

These are deterministic material/lifetime boundaries, not scheduler,
identity, replay, or observer disagreements. They were recorded without
repair. CORE1's independent-profile prefix rule permits B's positive result
without relabeling A/C.

## Evidence

- protocol commit: `66a0721`
- frozen candidate: `f294e4c`
- evidence marker: `CORE1_E13_PQLC_COMPOSITION_V3_EVIDENCE_SPENT`
- process exit: `0`
- CSV data rows: `15` (`3` A + `12` B + `0` C)
- matrix SHA-256:
  `ad0110b52ddb1480b58ed8d0a7ddc7bd5af147fa72f86fa0ebe50a36655c4230`
- summary SHA-256:
  `13f7bb372f03d82fed7b0888930572c36f04be321e1602cf22bf7f7d5775043f`

Formatting, strict Clippy for the E13/core1 target, and the default Academy
compile passed. Broader inherited unit suites still contain checkpoint and
legacy Academy expectation failures outside this integration gate; they were
not repaired or reclassified here.

## Scientific boundary

```text
CORE1-B E0-E13       positive developmental prefix
CORE1-A E13          weak-route lifetime negative
CORE1-C E13          contact-availability negative
E14 ARC              not run
physics authority    unchanged
```

The result establishes the requested point: where the generated route remains
physically present, the existing CPC/PQLC/continuous-material pieces already
compose and keep both links. No new learning physics is required for CORE1-B's
contextual action learning.
