# CORE1-E13 — history composition protocol v4 amendment

## Status

Development-only fixture correction. No v2 definitive evidence has been
spent.

## Demonstrated sequential-history mismatch

The v3 generic preflight created both physically separated signed contacts.
Running history 0 and then history 1 on one weak body was nevertheless invalid:
ordinary time in the first history could remove the second weak alternative
before the second history began.

SSA2-P's established comparison starts every transient history from the same
complete physical state. E13 must preserve that contract.

## Sole amendment

After the two fixed primes, clone the complete Academy body once:

```text
same complete post-prime state
├─ history 0: negative contact early   evaluator control
└─ history 1: positive contact early   developmental continuation
```

The continuation is fixed by protocol before execution. It is not selected
from either branch's output. The control branch is discarded after its
ordinary propagation; no state is merged. This is the same deterministic
history comparison established by SSA2-P.

Everything else in v1-v3 remains unchanged. No chooser, RNG, feedback-based
branching, physics edit, learning-law edit, or ARC execution is permitted.
