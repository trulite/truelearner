# RS2 learned inhibitory topology protocol v4

Status: frozen before any v4 evaluator or executable change.

Parent: RS2 v3 evaluator-identity negative `bc3b691`.

## Question

Can the unchanged RS2 v3 training/probe experiment execute after making the
evaluator's physical identity allocation disjoint from organism-generated
contact identities?

This is evaluator hygiene only. It introduces no organism law and asks no new
scientific question.

## Sole permitted change

Replace the evaluator's single stale `next_physical` allocator with explicit,
non-overlapping fixture namespaces:

- boundary/initial fixtures remain in their existing low root-relative range;
- pre-generation auxiliary targets use a reserved fixture range;
- Modulatory source fixtures use a separate reserved range;
- probe relay/dummy fixtures use another separate reserved range;
- external-arrival origin identities use a final separate range.

Organism-generated CV0/J0 contact identities remain allocated exclusively by
the substrate from its own current physical-identity maximum. No later
evaluator fixture may allocate from or adjacent to that generated range.

The ranges must be mechanically disjoint for every frozen root and family.
Identity choice must remain causally inert: permutations still have to select
by physical consequence rather than ID or slot.

## Frozen experiment

The following remain unchanged from RS2 v3:

- threshold-2 subthreshold training geometry;
- consequence age and candidate exercise;
- signed CV0/J0 variation and selection;
- training/probe separation;
- executable recurrent probe geometry and phase order;
- nine frozen families;
- all scientific predicates, transition-history comparison, replay,
  quiescence, and observation ceiling.

The required positive outcome is:

1. the useful inhibitory branch alone consolidates during training;
2. the learned connected and traversed inhibitory topology settles the fresh
   recurrent probe;
3. no consequence means no consolidation;
4. sign, ID, slot, and location permutations follow physical consequence;
5. irrelevant negative topology does not survive merely because it is
   inhibitory;
6. disconnected or untraversed learned inhibition cannot settle recurrence;
7. useful positive topology remains selectable and executable;
8. Reference and Production histories, PhysicalWork, final state, replay, and
   quiescence/ceiling classification are exact.

## Stop rule

The v4 matrix executes once after targeted formatting/check/Clippy. Any
failure freezes v4 negative without repair or rerun. Only a complete positive
RS2 v4 result permits CE1. CE1 alone may permit FD2 v2, and FD2 v2 alone may
permit the exact frozen ARC A2 replay.

