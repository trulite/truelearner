# CJ0 Arm A coincidence-threshold CELL PROBE v3 result audit

Status: **FROZEN MATERIAL NEGATIVE; FIRST COLLAPSE P4; CJ-A COMPLETE**.

## Execution and atomic artifacts

The sole valid development PROBE ran from implementation commit
`3d94f3fd0ea2852e3f3d65fafde94a7b61b39575`, tag
`cj0-a-coincidence-threshold-probe-v3-implementation`. It emitted exactly one
`CJ0_A_COINCIDENCE_THRESHOLD_PROBE_V3_EVIDENCE_SPENT` marker, atomically
published both artifacts, emitted the frozen-negative marker, and exited `1`.
There was no rerun, regeneration, tuning, or rescue.

- CSV: `results/cj0_a_coincidence_threshold_probe_v3.csv`, SHA-256
  `c18b93a2d71a7fcc8a81b073260c0754135ca1c6e13be6b70d7d88b1ac62f0b9`,
  `1,208` bytes, `2` rows, `40` columns;
- report: `results/cj0_a_coincidence_threshold_probe_v3.md`, SHA-256
  `9fc0b8e4dd96c5a2788ab3cf6df3aea2de622f7e42e75df274c87afd43c716ff`,
  `853` bytes;
- both staging paths: absent.

V1 and v2 remain frozen invalid mechanical executions. Each emitted one
disclosed PROBE marker and stopped at the authoritative past-arrival guard;
neither published an artifact or scientific row. V3 corrected only staging,
not timestamps or physics.

## Mechanism and physical contract

No candidate law or persistent variable was added. The tested conjunction was
ordinary threshold integration over authoritative PX0--PX2 physics:

```text
fresh input:       coupling 1 + coupling 1 -> threshold-3 CELL transient
second overlap:    residual 1 + fresh 1 + fresh 1 -> CELL fires
ordinary return:   each participating input resistance +3, coupling 1 -> 2
mature singleton:  coupling 2; one shot remains subthreshold
```

The complete anonymous six-site field consumed external source-port SPIKEs,
ordinary ARROW traversal/proposal, CELL decay and threshold/refractory state,
four-tick eligibility, returned activity, coupling plasticity, ten-tick
pressure, generation invalidation, region crossing, and natural queue drain.
It produced only CELL firings, effect crossings, changed ARROW resistance/
coupling, deallocation generations, transient state, and ordinary work. There
is no Event, Pair, Group, member list, semantic key, evaluator-selected update,
or new organism-visible state.

## Positive boundary before first collapse

Both fresh replicas matched exactly in behavior under normal and mirrored/
reversed allocation, identity, placement, and insertion:

- route occurrences: `7|7|7|7`;
- source-port firings: `21|21|21|21`;
- training convergence/effect firings: `6|0|0|0|0|6`;
- A+B and C+D input support: `11|11` each;
- A+D and C+B input support: `0|0` each;
- held-out A+B/C+D effects: `1|1`;
- held-out A+D/C+B effects: `0|0`;
- one-shot A/B/C/D effects: `0|0|0|0`.

Thus the flat candidate passed matched individual frequency, activity,
traversal, timing, pressure, return/effect, and identity marginals and initially
distinguished learned from crossed use. Claims `P0--P3` passed in both rows.

## Exact first collapse: self-evidence

Two ordinary consecutive occurrences of A alone produced one old A+B
convergence/effect firing in each replica. B alone did the same. The A-side
old input support changed `11 -> 17` while the absent B-side stayed `11`.

The physical identity is decisive: a recurring fresh conjunction supplies
two coupling-`1` impulses per occurrence, while a recurring mature singleton
supplies one coupling-`2` impulse. Both present total impulse `2` to the same
threshold CELL at the same ticks. The authoritative transient state therefore
cannot bootstrap the former without also allowing the latter to fire old
organization. Old output plus A alone is not evidence that A and B currently
co-participated, yet it organizationally transmitted. Ordered claim `P4`
failed in both replicas and is the frozen first collapse.

## Reversal and controls

The later serialized reversal independently confirmed the failure mode:

- old A+B/C+D fired `2|2` times during A+D/C+B experience even though their
  contributors never contemporaneously co-participated;
- new A+D/C+B did bootstrap and fired `6|6` times;
- post-reversal old/new held-out effects were `0|0` / `1|1`;
- old residual supports were `0|1;1|0`, new supports `11|11;11|11`.

Changed organization can form and execute, and old held-out execution
eventually disappears, but old organization transmitted and refreshed from
separate singleton bursts during reversal. `P5` therefore also failed; it is
not the first collapse.

All independent controls passed: too-late B `0`, correlation without
traversal `0`, traversal without return `0`, absent opportunity `0`, stale
opportunity `0`, three-contributor ambiguity `3`, four-contributor activity
`6`, fresh alternative old/new `0/2`, zero autonomous source refiring,
natural quiescence, and exact duplicate complete-state replay.

Claim vectors were `11110011`, first failure `P4`, for both rows. Per-row work
was `53,879` and `53,862`, total `107,741` ledgered operations. Each final
primary matter used `6,752` persistent bytes. Final complete fingerprints were
`2693689831904342339` and `1789342953007162903`.

## Classification and stop

Classification:
`FROZEN_NEGATIVE_EXISTING_PHYSICS_SELF_EVIDENCE`.

Threshold `2` cannot discriminate a fresh crossed conjunction because
`1+1` fires directly. Threshold `3` is the smallest singleton-safe threshold,
but recurrence makes a weak pair and a mature singleton physically identical.
Higher thresholds only require longer recurrence of the same identical total
input. A seeded coupling-`2` construction is not rerun here; its inability to
bootstrap after coupling-`1` re-proposal remains the separate immutable PX3-R
shared-CELL negative.

CJ-A therefore has no surviving flat prototype. MICRO, GATE, recursion,
ordinary-convergence OR, and temporal-expressivity testing were not reached.
No definitive or authority surface was created or entered. Continuing would
require a scientifically distinct coactivity-gated law/state or semantic
representation, which is outside this arm and triggers the mandated stop.

## Validation and source isolation

- formatting, focused check, strict Clippy, and library tests (`1/1`): pass;
- refusal and no-CELL preflight: pass;
- frozen ancestry, source hashes, negative endpoints, and fresh namespaces:
  pass;
- organism-visible forbidden-token scan: pass;
- authoritative PX0--PX2 changed paths: none;
- diff from frozen start: fresh CJ-A sidecar/protocol/audit/result paths only;
- exact replay, CSV shape, staging absence, work/storage ledger: pass.
