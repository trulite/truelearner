# PX5 physical plasticity-allocation GATE v1 negative audit

Status: **FROZEN MECHANICAL NEGATIVE; GATE v2 ELIGIBLE; AUTHORITY ABSENT**.

## Immutable outcome

The one-shot GATE v1 emitted its evidence-spent marker once and failed with
`0/12` cells and `132/144` claims. Exactly `P8` failed in every cell; `P0--P7`
and `P9--P11` passed in every cell.

| frozen artifact | SHA-256 |
|---|---|
| GATE v1 protocol | `cba4b94c991283429138f5825bebdbdd1074a27afc9ed25bc1a8197cfd205e6f` |
| GATE v1 implementation | `5e267b344d698fd8ef9833754e2d99e9c2813bddfaff9f7d42a967a017b8af71` |
| GATE v1 CSV | `e50e9c88cc26e8b418b8264a3cb7cb71973e661d5001c76a6c05c28f4a79747c` |
| GATE v1 report | `db5e76774cf160d58e7050c4dcafa2ae484b80d531cf673e830972883c871299` |
| unchanged PX0--PX2 law | `3ee8b2bfc9c9ac2d4b9726d60d93759c66eaeec6cd2e61db7041bde753aad12d` |

These artifacts are immutable and must never be rerun, rescued, regenerated,
or overwritten.

## First collapse

The collapse is mechanically unique. The protocol withheld physical useful
source `0`, while the same frozen protocol assigned source `0` to the hot
recurrence half. Its ARROW entered the withholding phase at resistance `10`.
The v1 schedule ended at tick `90`, supplying only four scalar pressure units
after the tick-`62` return:

```text
tick 70: ordinary pressure + expired-use pressure, 10 -> 8
tick 80: ordinary pressure,                         8 -> 7
tick 90: ordinary pressure,                         7 -> 6
```

Therefore the edge correctly remained live. `withheld_dead`, `stale_dead`, and
`replacement_live` were false, while the preregistered activity still executed
one crossing through the retained old edge. Allocation order changed the
serialized generation number but not this physical result.

This is a protocol-horizon mismatch, not evidence for a missing allocator,
representation, or substrate law. The law behaved exactly as preregistered
pressure and recurrence predict.

## Preserved positive discriminator

All twelve cells independently passed:

- exact initial generic proposals under loads `32, 64, 128`;
- exact hot/warm useful return work (`18` or `36` updates);
- all useful routes live and `4/4` or `8/8` held-out crossings;
- zero live structure across all one-shot and repeatedly encountered
  return-free distractors;
- useful/distractor resistance separation;
- matched return-free, late-return, and outside-radius controls;
- evaluator-only allocation shuffle inertness;
- matched physical-return permutation: original useful routes `0`, actually
  returned distractor routes `4` or `8`;
- natural quiescence, exact duplicate replay, and work/storage accounting.

## Sole lawful continuation

A fresh GATE v2 may change only:

1. fresh namespaces/artifact identities; and
2. continue recurrence for every non-withheld useful source through tick `140`,
   then advance ordinary pressure to tick `150` before reacquisition.

That horizon is already mechanically bounded by the frozen resistance and the
positive MICRO withholding schedule. No physics, threshold, pressure,
eligibility, return, topology, representation, clause, load, or control may
change. If that exact correction fails, it is a new frozen negative.

No authority workflow is authorized or simulated. PX0--PX2 remain unchanged,
PX3--PX8 remain unadvanced, and no definitive matrix or authoritative ancestor
exists in this lane.
