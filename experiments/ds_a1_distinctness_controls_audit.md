# DS-A1 normalized distinctness, bridge, leak, and negative-control audit

## Primary inventory

MICRO seed 100 and every GATE seed 100..104 have the same primary inventory:

| candidates | consolidated templates | installed roots | structural unique | unique normalized effects | opaque handles |
|---:|---:|---:|---:|---:|---:|
| 2 | 3 | 2 | 2 | 2 | 2 |

Four route CELLs and two live route ARROWs are installed before bridge. The
organism-side normalized live-adjacency traces are distinct current-member
binding traces. The bridge copies both roots one-to-one and contains no rank or
effect. Independent handle executions from identical frozen temporary starts
each inject a root SPIKE, traverse live adjacency, and yield two nonempty,
pairwise-distinct E0-relative binding/activation effects.

## Corrected evaluator boundary

The amended same-effect control creates a third structurally unique route only
in a negative-control copy by adding parallel live adjacency. Structural dedup
therefore retains three roots, the bridge exposes all three handles, and all
three execute. Post-hoc normalization finds only two unique effects. The
mutation is mechanically diagnosed as a stage-6 failure condition; no root or
handle is filtered, replaced, selected, or altered. This proves the corrected
protocol boundary.

## Required controls

Every boolean below passed for every seed:

- disjoint fresh acquisition/support and target occurrences;
- bijective occurrence relabeling;
- allocation-order and memory-layout perturbation;
- proposal observation-order and route-installation-order shuffle;
- opaque handle permutation, with no rank;
- identical E0 event with local plasticity disabled gives no roots;
- two support presentations, below the threshold of three, give no roots;
- removing one underlying member relation reduces candidates, structural roots,
  handles, and unique effects by exactly one, with no replacement;
- duplicate proposal evidence leaves the full inventory unchanged;
- an allocated copy of one normalized live route is structurally deduplicated
  before bridge;
- the same-effect mutation remains fully bridged and forces the stage-6 failure
  diagnostic described above;
- one stale root generation removes exactly that affordance;
- the symmetric timing copy retains multiple unranked handles/effects;
- three distractor raw propagation relations do not become event affordances;
- cleanup leaves zero episode occurrences, cells, arrows, roots, handles,
  observations, or bindings;
- actual E0 export/binding provenance is exact; and
- all claimed zero source paths are mutation-sensitive.

Distinctness is unchanged by occurrence relabeling, allocation/layout,
observation/installation order, or handle permutation. Route allocation or
handle identity is never used as effect distinction.
