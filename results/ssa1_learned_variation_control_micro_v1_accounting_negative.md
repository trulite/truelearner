# SSA1 learned variation-control MICRO v1 accounting negative

Status: **frozen mechanical development negative; no SSA1 classification**.

The two-seed MICRO executed all five worlds deterministically, but the
post-execution audit rejected its online C/E accounting before scientific
classification.

Observed diagnostic:

```text
one physical affordance resolution
  -> four live supporter edges
  -> same downstream consequence applied four times
  -> false within-episode recurrence
```

In world E the repeated-history consequence cycled across four equal physical
timing shapes. Because each realized trajectory was counted four times, the
currently active shape acquired a margin of four within one episode, crossing
the frozen M6 margin threshold of two. The resulting route value inflated to
`638` even though the across-episode shape history was balanced.

That is not the preregistered causal path. One trajectory produces one
downstream consequence observation. The physical SSA0.3 trace identifies one
support arrival as the threshold-closing occurrence; frozen M6/M5 eligibility
must therefore attach once to that causally closing supporter for the online
episode.

The retry changes only this accounting/linker edge:

```text
one realized affordance
  -> one threshold-closing supporter eligibility
  -> one delayed physical consequence observation
```

Supporter identity rotates with the physical transient schedule so all live
structure can receive experience over time. No M4/M5/M6 threshold, evidence,
pressure, exploration, substrate state, world history, or result criterion
changes. The v1 outputs are not promoted to Classification C.
