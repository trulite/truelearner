# SSA0 spatiotemporal stochastic-affordance MICRO v1 result

Outcome: **CLASSIFICATION C — THE TESTED SUBSTRATE DOES NOT SUPPORT USEFUL
STOCHASTIC AFFORDANCE RESOLUTION**.

This is immutable developmental substrate-characterization evidence. It is not
a definitive execution, does not advance M6, does not create M7, and uses no
Lane A evidence or mechanism. Authoritative M6 remains
`aa4e22efd8a65b7694956a53cfaa970582695215` /
`core-autonomy-checkpoint-established`.

## Lineage and frozen fingerprints

- protocol: `ssa0-spatiotemporal-affordance-v1`;
- protocol commit/tag: `8ae8dc78bb6ecf6172f1d69cb41472beac327df4` /
  `ssa0-spatiotemporal-affordance-protocol-v1`;
- protocol SHA-256:
  `92ff3f758977a575e2f8ca651f7a45756e15241a6d4bf829012a266bae9489fc`;
- implementation commit/tag:
  `0f759047353e9afe00bd2883c4a8f42e94192129` /
  `ssa0-spatiotemporal-affordance-implementation-v1`;
- implementation source SHA-256:
  `180b24f6b682ec5d274e44b0c680062d10b1f68b6fddeb4d857ec599b32f6299`;
- runner SHA-256:
  `fb693aa098e45617deefd5ae9b9de1003528d4b7fbfa87078545fbda5e90fa7f`.

The frozen source hashes remained exact after execution:

| frozen source | SHA-256 |
|---|---|
| DS-A1 affordance multiplicity | `b0a1841af3f85e725f92490b92357ddafd65289717846b5c16b85a49261e5ba1` |
| M6 definitive authority | `c2a95199139828e360713320ad57c77a100fc0135ba06b9219624d4f16e1d18d` |
| research runtime | `e570b3cd0fcff759a02a38a685f22f33bc28de65e25b7beb34f77d138f3fd711` |

The pre-existing results-tree digest before this artifact was
`369e5b45a6fcaa26aea237b1388928569feb4f89a37515952db72e11ad3933aa`.

## Ordered outcome

| stage | outcome |
|---|---|
| SSA0-A exact physical replay | PASS |
| SSA0-B microscopic transient-history sensitivity | PASS |
| SSA0-C minimal explicit physical noise | NOT INVOKED, correctly skipped |
| SSA0.1 support-frequency signature | PASS |
| SSA0.2 spatiotemporal discrimination | **FAIL** |
| all ten controls | PASS |
| source/accounting audit | PASS |

The exact duplicate start, trace, and end fingerprints were respectively
`4a7de7a500da0f48`, `c23c488a7993c6f3`, and `6b8043d1e2447a3d`;
the exact run performed 718 ledgered operations.

SSA0-B held permanent structure fixed and realized both physical routes across
the 15 balanced MICRO histories: A 9 times and B 6 times. Every history was
duplicate-exact. Because physical transient history was already sufficient to
switch the realized route, SSA0-C was not permitted or needed; no RNG or dormant
noise implementation exists.

## SSA0.1 realization frequencies

| stored support A:B | A | B | favored share | outcome |
|---:|---:|---:|---:|:---:|
| 6:6 | 9 | 6 | 60% / 40% | PASS |
| 7:6 | 9 | 6 | 60% A | PASS |
| 8:6 | 12 | 3 | 80% A | PASS |
| 10:4 | 15 | 0 | 100% A | PASS |
| 6:7, physically permuted | 6 | 9 | 60% B | PASS |
| 6:8, physically permuted | 3 | 12 | 80% B | PASS |
| 4:10, physically permuted | 0 | 15 | 100% B | PASS |

This supports only the narrow developmental observation that local learned
structural support changed empirical realization frequency without an explicit
probability representation. It does not overcome the SSA0.2 collapse.

## SSA0.2 collapse

Seven independent factor pairs reversed under their mirrored physical
perturbations: path length, distance, spike timing, equal-tick arrival order,
recent local activation, competing local activity, and stored support. All four
required crossed cases also passed.

The first and only ordered collapse was **supporting-spike count**. With equal
stored support seven on each side, the preregistered active-spike perturbation
kept six versus seven live support paths. Both the first and mirrored arms
realized physical route 0 (`Some(0)` / `Some(0)`), rather than the required
route 1 / route 0 reversal. In this implementation the removed support path was
not among the first four arrivals that crossed the contender threshold, so the
count change did not affect the local race. This is a mechanistic negative, not
an evaluator or execution error.

No threshold, support level, path activation rule, acceptance band, or history
matrix was changed. GATE was not run because the deterministic factor collapse
would persist and the protocol requires preserving the failed experiment
without rescue. A different count intervention or substrate revision would
require a separate preregistration outside this stopped SSA0 lane.

## Controls and focused validation

All preregistered controls passed: no second route, two aliases/one physical
route, same observable effect, fresh identities, route permutation, allocation
and inert layout perturbation, stale/blocked route, exact duplicate,
independent route execution, and production-path source audit. The production
physics contains no RNG, probability, softmax, route scorer, semantic effect
inspection, or `noise + argmax` chooser.

Focused validation:

```text
cargo fmt --all -- --check                                      PASS
cargo test --release --bin ssa0_spatiotemporal_affordance       PASS (6/6)
cargo run --release --quiet --bin ssa0_spatiotemporal_affordance -- --micro
                                                                CLASSIFICATION C
cargo run --release --quiet --bin ssa0_spatiotemporal_affordance -- --definitive
                                                                REJECTED, exit 2
```

The literal strict-Clippy command first stopped on ten pre-existing warnings in
frozen M6/generated sources under Rust/Clippy 1.97. Those sources were not
modified. The isolated implementation then passed strict Clippy with only the
three frozen lint classes allowlisted:

```text
cargo clippy --release --bin ssa0_spatiotemporal_affordance -- \
  -D warnings \
  -A clippy::derivable_impls \
  -A clippy::manual_is_multiple_of \
  -A clippy::manual_div_ceil
```

No broad historical suite ran because shared and frozen code stayed
byte-identical.

## Stopping rule

The lane stops here with exactly classification **C**. There is no scientific
ambiguity requiring a decision inside this frozen protocol. SSA1, SSA2,
usefulness, creative diversity, adversarial unpredictability, generative
sequence worlds, definitive execution, and M6 advancement were not attempted.
